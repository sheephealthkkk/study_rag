## 第六章：向量检索工具封装 —— 让 Agent 自动"查知识库":o::o::o::o::o::o:

前五章我们构建了从文档解析到向量入库的完整链路。但在真实应用中，检索能力不是独立调用的一段代码——它需要被嵌入到一个更大的系统里，由 Agent 根据用户意图决定"要不要检索"、"检索哪个库"、"检索什么"、"检索到结果后做什么"。

**本章的核心命题：** 如何把前五章建好的向量数据库，封装成 Agent 能听懂、能调用的"工具"，让大模型通过自然语言自动驱动底层的向量检索逻辑。

### 6.1 为什么要把检索封装成工具：从"写死逻辑"到"Agent 自主决策"

#### 6.1.1 不封装的问题

```
═══════════════════════════════════════════════════════════════════════
        硬编码检索 vs Agent 工具化检索 —— 一个真实场景的对比
═══════════════════════════════════════════════════════════════════════

  场景: 用户问 "财务部2024年的报销标准是多少？"

  硬编码方式 (没有 Agent):
    def handle_query(user_query: str) -> str:
        # 你需要手写规则:
        if "财务" in user_query:           ← 规则 1: 部门判断
            filters = {"department": "Finance"}
        elif "HR" in user_query:
            filters = {"department": "HR"}
        
        if "2024" in user_query:           ← 规则 2: 年份判断
            filters["year"] = "2024"
        
        if "最新" in user_query:           ← 规则 3: 版本判断
            filters["status"] = "active"
        
        result = vector_db.search(user_query, filter=filters)
        return llm.summarize(result)
    
    问题:
      ✗ 每加一个条件 → 加一条 if 规则 → 规则很快失控
      ✗ 用户不说 "财务" 说 "财务部"、"财务部门"、"我是财务的" —— 规则全崩
      ✗ 组合条件爆炸: "HR+2024" vs "财务+最新+V3.0" —— N个if嵌套
      ✗ 无法处理"先说HR又说财务"的修正对话
      ✗ 多源检索 ("PDF库里没有就去数据库查") 几乎不可实现


  Agent 工具化方式:
    # 你定义工具:
    search_tool = VectorDBToolSpec(vector_db=index)
    
    # 创建 Agent:
    agent = ReActAgent(tools=[search_tool])
    
    # 用户发话，Agent 自己判断:
    agent.chat("财务部2024年的报销标准是多少？")
    
    Agent 内部推理过程:
      Thought: 用户想查财务部的报销标准
      Action: 调用 search_tool.auto_retrieve_fn
      Action Input: query="报销标准", department="Finance", year="2024"
      Observation: [检索到 3 个相关 Chunk...]
      Thought: 信息足够回答
      Answer: 根据《费用报销管理办法》，一线城市住宿标准不超过500元/天...

    优势:
      ✓ Agent 自己理解 "财务部" = department="Finance"
      ✓ Agent 自己判断条件组合
      ✓ 新增检索源 = 新增一个 Tool → Agent 自动学会用
      ✓ 多步推理: 第一次检索不充分 → Agent 自动重试
═══════════════════════════════════════════════════════════════════════
```

#### 6.1.2 三种场景的封装价值

| 场景 | 不封装的痛点 | 封装为Tool后 |
|------|------------|------------|
| **多知识源** | 每加一个数据源（Milvus/Pinecone/SQL）要改主流程代码 | 每个源封装为一个Tool → Agent自动选择哪个源 |
| **多步骤推理** | 手写"先查A→判断→不够再查B→汇总"逻辑 | Agent自动规划步骤、自动重试 |
| **权限与路由** | if user.department == "HR": ... 规则爆炸 | Agent从用户自然语言中提取意图、自动路由 |
| **与第三方集成** | 每个API/SQL都要在主流程里加特殊判断 | 每个API封装为一个FunctionTool → Agent统一调度 |

---

### 6.2 LlamaIndex 的工具封装体系全景

LlamaIndex 提供了四种把检索能力暴露给 Agent 的方式。它们从"你控制一切"到"Agent 控制一切"依次排列：

```
═══════════════════════════════════════════════════════════════════════
        四种工具封装方式的定位与层级
═══════════════════════════════════════════════════════════════════════

  你控制的程度  高 ←───────────────────────────────→ 低 (Agent自主)
                │          │            │            │
  封装方式       │ index.   │ Retriever  │ Query      │ VectorDB
                │ as_      │ Tool       │ Engine     │ ToolSpec
                │ retriever│            │ Tool       │ + Function
                │ ()       │            │            │ Tool
                │          │            │            │
  返回给Agent的  │ raw      │ 原始Node   │ 自然语言   │ NodeWithScore
  数据类型       │ scores   │ 列表       │ 答案       │ + metadata
                │ + IDs    │            │            │
                │          │            │            │
  谁做LLM汇总    │ 你       │ 你         │ Agent自动  │ 你
                │          │            │            │
  适用场景       │ 评估     │ 需要原始   │ 直接回答   │ 多源检索
                │ 调参     │ 片段加工   │ FAQ型Agent │ 路由+过滤
                │ 自建管线 │ 传给外部   │            │ 权限控制
═══════════════════════════════════════════════════════════════════════
```

---

### 6.3 四种封装方式逐一深度解析:o::o::o::o::o::o::o::o::o::o::o::o::o::o::o::o::o:

#### 6.3.1 index.as_retriever() —— 最底层、最灵活、不走 Agent

**是什么：** `VectorStoreIndex.as_retriever()` 返回一个 `Retriever` 对象。它不是 Tool——它是一个可直接调用的 Python 对象。你拿到它后，手动调用 `.retrieve(query)` 来获取检索结果。

**返回什么：** `List[NodeWithScore]` —— 每个元素包含：Node 文本 + 相似度分数（`node.score`）+ 完整 metadata

**内部做了什么事：**
```
  retriever = index.as_retriever(similarity_top_k=5)
  
  调用 retriever.retrieve("请假规定") 时:
    1. Query → Settings.embed_model → 向量
    2. 向量在 VectorStore 中做 ANN 检索
    3. 返回 Top-5 个 NodeWithScore (每个带 score 和 metadata)
    
  → 不调用 LLM！只做检索，不做汇总。
  → 你拿到的是"原始数据"，可以自己决定下一步。
```

**什么时候用它（而不是 Tool）：**

| 场景 | 为什么用 as_retriever |
|------|----------------------|
| 评估 Embedding 模型效果 | 需要 raw_scores 算 Recall@K / MRR@K |
| 调 chunk_size 参数 | 需要看不同参数下检索到的具体 Node 和分数 |
| 需要自定义重排逻辑 | 拿到 scores 后自己写 reranker |
| 自建管线（不用 Agent） | 你自己的业务代码直接调用检索 |
| 把检索结果传给外部系统 | 你需要 NodeWithScore 对象，而不是 Agent 加工后的文本 |

**参数详解：**

| 参数 | 默认值 | 作用 | 调优建议 |
|------|--------|------|----------|
| `similarity_top_k` | 2 | 返回多少个最相似的 Node | FAQ 场景用 3-5；需要 Rerank 的场景用 10-20（粗排后精排） |
| `filters` | None | metadata 过滤条件 | 生产环境几乎必加（status='active'等） |
| `alpha` | None | 混合检索权重（向量 vs BM25） | 0=纯BM25, 1=纯向量, 0.5=混合 |
| `node_postprocessors` | None | 检索后处理器列表 | 加 Reranker / MetadataReplacement 等 |

**代码示例：**

```python
# ── 获取底层 Retriever ────────────────────────────────────
retriever = index.as_retriever(
    similarity_top_k=5,
    filters=MetadataFilters(
        filters=[MetadataFilter(key="status", value="active")]
    ),
)

# ── 手动检索，拿到原始数据 ───────────────────────────────
nodes = retriever.retrieve("请假需要提前多久？")

# ── 你可以拿到每个 Node 的分数和完整 metadata ────────────
for node in nodes:
    print(f"score={node.score:.4f} | "
          f"page={node.metadata.get('page_number')} | "
          f"version={node.metadata.get('version')} | "
          f"text={node.text[:60]}...")

# ── 你可以自己决定下一步做什么 ────────────────────────────
# 比如: 只取 score > 0.7 的、按 version 排序、传给外部系统
```

---

#### 6.3.2 RetrieverTool —— 把 Retriever 封装成 Agent 能调用的工具:o:

**是什么：** `RetrieverTool` 是对 `Retriever` 的一层包装，让它变成 Agent 可以调用的 Tool。Agent 看到这个 Tool 的描述和参数定义后，就知道"可以用它来搜索知识库"。

**与 as_retriever() 的关系：** `RetrieverTool (外层) → Retriever (内层) → VectorStoreIndex (底层)`。RetrieverTool 不改变检索逻辑，只改变了调用方式（从你手动调用 → Agent 自动调用）。

**返回什么：** Agent 拿到的是 `List[Node]`（原始文本片段列表）。Agent 需要自己用 LLM 组织成自然语言回答。

**关键理解 —— RetrieverTool 和 VectorDBToolSpec 的本质区别：**

```
  RetrieverTool:
    你传 query → Agent 调 retrieve(query) → 返回 Node 列表
    → Agent 自己决定要不要过滤、怎么过滤

  VectorDBToolSpec:
    你传 query → Agent 调 auto_retrieve_fn(query) 
    → 内部先用 LLM 分析 query，自动提取 metadata filter
    → 然后执行过滤后的检索
    → 返回 NodeWithScore 列表

  核心区别: VectorDBToolSpec 内部多了一步 "LLM分析query→自动构造filter"。
           RetrieverTool 是纯粹的 "传什么query就检索什么"。
```

**什么时候用 RetrieverTool：**

- 不需要自动 metadata 过滤（你自己在 Agent 外部已经处理了过滤逻辑）:o:
- 检索逻辑简单，Agent 只需要"查一下"这个动作
- 与其他 Tool 组合时，作为"检索"环节的组件

**代码示例：**

```python
from llama_index.core.tools import RetrieverTool

# ── 创建底层 Retriever ────────────────────────────────────
retriever = index.as_retriever(similarity_top_k=3)

# ── 封装为 RetrieverTool ──────────────────────────────────
retriever_tool = RetrieverTool.from_defaults(
    retriever=retriever,
    name="company_policy_search",
    description=(
        "搜索公司内部政策和员工手册。"
        "当用户询问关于公司制度、政策、福利、规定时使用此工具。"
        "输入: 用户的自然语言问题。"
        "输出: 最相关的政策文档片段。"
    ),
)

# ── 创建 Agent ────────────────────────────────────────────
agent = ReActAgent.from_tools(
    [retriever_tool],
    llm=Settings.llm,
    verbose=True,
)

# ── Agent 自动调用 ────────────────────────────────────────
response = agent.chat("加班费怎么算的？")
# Agent 内部:
#   Thought: 用户问加班费 → 需要查公司政策
#   Action: company_policy_search
#   Action Input: "加班费计算标准"
#   Observation: [Node1: "工作日加班按1.5倍工资...", Node2: "休息日加班按2倍..."]
#   Thought: 信息充分
#   Answer: 根据公司政策，加班费...
```

---

#### 6.3.3 VectorDBToolSpec —— 多源知识库 + 自动元数据过滤 + 路由检索

**是什么：** `VectorDBToolSpec` 是最强大的检索工具封装。它的核心创新是 `auto_retrieve_fn`——这个函数内部用 LLM 分析用户查询，自动提取隐含的元数据过滤条件，然后执行过滤后的检索。

**它实际上创建了两个子工具：**

```
  VectorDBToolSpec(vector_db=index).to_tool_list() 返回:

  [0] auto_retrieve_fn  →  Tool(name="auto_retrieve")
      描述: "Automatically retrieve relevant data from the vector database"
      行为: 用 LLM 分析 query → 提取 metadata filter → 检索 → 返回 NodeWithScore

  [1] search_fn         →  Tool(name="search")
      描述: "Search the vector database with a specific query string"
      行为: 直接用 query 字符串检索 (不走 auto filter)，返回 NodeWithScore
```

**auto_retrieve_fn 的完整内部流程：**

```
  Agent 调用 auto_retrieve_fn(query="最新的HR请假政策")
          │
          ▼
  Step 1: LLM 分析 query (这是 VectorDBToolSpec 独有的!)
    Prompt: "用户说'最新的HR请假政策'。请提取以下信息:
             1. 查询字符串 (用户真正想搜的内容)
             2. metadata 过滤条件 (部门/版本/状态/日期等)"
    
    LLM 分析结果:
    {
      "query": "请假政策",
      "filters": {
        "department": "HR",
        "status": "active",
        "topic": "请假"
      },
      "order_by": "created_date DESC"
    }

  Step 2: 用提取的 filters 构造 MetadataFilters
    filters = MetadataFilters(filters=[
      MetadataFilter(key="department", value="HR"),
      MetadataFilter(key="status", value="active"),
      MetadataFilter(key="topic", value="请假"),
    ])

  Step 3: 在过滤后的子集中做向量检索
    retriever.retrieve("请假政策", filters=filters)

  Step 4: 返回 Top-K NodeWithScore (带完整 metadata)
          │
          ▼
    Agent 拿到原始的 NodeWithScore 列表
    → Agent 可以自己决定要不要再汇总、传给下一个工具
```

**什么时候用 VectorDBToolSpec（而不是 RetrieverTool）：**

| 场景 | 为什么必须用 VectorDBToolSpec |
|------|------|
| 多个知识库并存 | Agent 需要从多个向量库中选择"搜哪个" |
| 用户查询中隐含过滤条件 | "最新的"、"HR的"、"2024年的" → auto_retrieve_fn 自动提取 |
| 权限控制 | 根据用户部门自动限定检索范围 |
| 需要 metadata 辅助决策 | Agent 拿到 metadata 后可以判断"这个结果是否足够新" |

**关键参数：**

| 参数 | 类型 | 说明 |
|------|------|------|
| `vector_db` | `VectorStoreIndex` | 要封装的向量索引 |
| `llm` | `LLM` | 用于分析 query 提取 metadata filter（通常是 GPT-4o） |
| `tool_name` | `str` | 工具名称（Agent 描述用） |
| `tool_description` | `str` | 工具描述（Agent 判断何时调用） |

---

#### 6.3.4 QueryEngineTool —— 检索+LLM汇总一体化，Agent 直接拿答案:o::o::o:

**是什么：** `QueryEngineTool` 把整个 `QueryEngine`（检索 → 增强 → LLM 生成）封装为一个 Tool。Agent 调用它后，拿到的不是原始 Node，而是**已经写好的自然语言回答**。

**与 VectorDBToolSpec 的核心区别：**

```
  VectorDBToolSpec:
    Agent 调用 → 拿到 NodeWithScore 列表 → Agent 自己决定下一步
    (可能: 汇总、传给其他工具、判断是否需要二次检索)

  QueryEngineTool:
    Agent 调用 → QueryEngine 内部完成 "检索+LLM汇总" → Agent 拿到最终答案
    (Agent 不需要做任何额外的处理，答案已经写好了)
```

**什么时候用 QueryEngineTool：**

- FAQ 型 Agent —— 用户问知识库问题，直接返回答案
- Agent 不需要对检索结果做额外处理:o:
- 作为多工具组合中"直接回答"的兜底工具:o:

**代码示例：**

```python
from llama_index.core.tools import QueryEngineTool, ToolMetadata

# ── 创建 QueryEngine ──────────────────────────────────────
query_engine = index.as_query_engine(similarity_top_k=3)

# ── 封装为 QueryEngineTool ────────────────────────────────
query_tool = QueryEngineTool.from_defaults(
    query_engine=query_engine,
    name="ask_hr_policy",
    description=(
        "直接回答关于公司人事政策的问题。"
        "适用场景: 用户询问具体的政策规定时，可以直接用这个工具获取答案。"
    ),
)

# ── 加入 Agent ────────────────────────────────────────────
agent = ReActAgent.from_tools([query_tool], llm=Settings.llm)
response = agent.chat("公司给员工交哪些保险？")
# Agent 拿到的是: "公司依法为员工缴纳五险一金..." (已经是完整答案!)
```

---

#### 6.3.5 FunctionTool —— 任意自定义动作，最强灵活性

**是什么：** `FunctionTool` 可以把**任意 Python 函数**封装为 Agent 能调用的 Tool。这个函数可以调用 API、写入数据库、触发审批流程、发送通知——任何你想让 Agent 做的事情。

**与其他 Tool 的本质区别：** 前四种都是"检索"相关的 Tool。FunctionTool 不限于检索——它是 Agent 与"外部世界"交互的通用桥梁。

**关键概念：ToolSpec（工具规格描述）**

无论哪种 Tool，Agent 都需要知道：
- 这个工具叫什么名字？（`name`）
- 这个工具是干什么的？（`description`）
- 调用这个工具需要什么参数？（参数 Schema）
- 调用后返回什么？（返回类型）

`description` 是最关键的——Agent 完全靠它来判断"什么时候该用这个工具"。如果描述不清晰，Agent 可能永远不调用你的工具，或者在错误的时机调用。

```python
# ── 好的描述 ──
description = (
    "搜索公司内部政策和员工手册。"
    "当用户询问关于公司制度、政策、福利、加班、请假、报销等问题时使用此工具。"
    "输入参数 query: 用户的自然语言问题。"
)
# Agent 看到这个描述 → 清楚地知道"用户问政策时用这个"

# ── 差的描述 ──
description = "搜索一下"
# Agent 无法判断什么时候该调用 → 可能乱用或不用
```

---

### 6.4 六类封装方式全景对比

| 封装方式 | 返回类型 | LLM汇总？ | auto filter？ | Agent可用？ | 灵活度 | 最佳场景 |
|----------|----------|:---:|:---:|:---:|:---:|----------|
| `index.as_retriever()` | `List[NodeWithScore]` | ❌ | ❌ | ❌ (你手动调) | 极高 | 评估/调参/自建管线/拿raw scores |
| `RetrieverTool` | `List[Node]` (给Agent) | ❌ | ❌ | ✅ | 高 | 简单检索/多工具组合的检索环节 |
| `VectorDBToolSpec` | `List[NodeWithScore]` | ❌ | ✅ (auto) | ✅ | 高 | 多源知识库/自动metadata过滤/路由 |
| `QueryEngineTool` | `str` (自然语言) | ✅ (自动) | ❌ | ✅ | 低 | FAQ型Agent/直接拿答案/兜底工具 |
| `FunctionTool` | 你定义 | 你决定 | 你决定 | ✅ | 极高 | 调API/写DB/触发任务/任意自定义 |
| `RetrieverQueryEngine` | `Response` (含scores) | ✅ | ❌ | ❌ | 中 | 需要同时看scores和答案的评估场景 |

---

### 6.5 演示一：VectorDBToolSpec 完整实战

```python
"""
═══════════════════════════════════════════════════════════════════════════
  演示 1: VectorDBToolSpec —— 加载已有Chroma，封装为Agent工具
═══════════════════════════════════════════════════════════════════════════

  场景: 第五章已经在 Chroma 中建好了 employee_handbook 索引。
        现在用它创建 Agent，让用户自然语言查询，Agent 自动检索+回答。

  核心步骤:
    1. 加载已有 Chroma Collection (不需要重新 Embedding!)
    2. 从 Chroma 构建 VectorStoreIndex
    3. 用 VectorDBToolSpec 封装为 Agent 工具
    4. 创建 ReActAgent
    5. 测试: 标准查询 + 带隐含过滤条件的查询
"""
import chromadb
from llama_index.core import Settings, VectorStoreIndex, StorageContext
from llama_index.vector_stores.chroma import ChromaVectorStore
from llama_index.embeddings.openai import OpenAIEmbedding
from llama_index.llms.openai import OpenAI
from llama_index.core.agent import ReActAgent
from llama_index.core.tools.vector_db_tool import VectorDBToolSpec

# ── 全局配置 ──────────────────────────────────────────────────
Settings.embed_model = OpenAIEmbedding(
    model="text-embedding-3-small", dimensions=512
)
Settings.llm = OpenAI(model="gpt-4o", temperature=0)

print("=" * 70)
print("  演示 1: VectorDBToolSpec 完整实战")
print("=" * 70)

# ═══════════════════════════════════════════════════════════════
# Step 1: 加载已有的 Chroma 向量库
# ═══════════════════════════════════════════════════════════════
# 这里的数据是第五章入库的，现在直接加载——不需要重新 Embedding!
chroma_client = chromadb.PersistentClient(path="./chroma_db")
chroma_collection = chroma_client.get_collection("employee_handbook")

print(f"  [Chroma] 已连接: employee_handbook")
print(f"  [Chroma] 向量总数: {chroma_collection.count()}")
print(f"  [Chroma] 存储路径: ./chroma_db/")

# ═══════════════════════════════════════════════════════════════
# Step 2: 从 Chroma 构建 VectorStoreIndex
# ═══════════════════════════════════════════════════════════════
# from_vector_store() = 跳过 Embedding，直接从已有向量库读取
# vs from_documents() = 重新解析+切分+Embedding+入库 (从头的全流程)

vector_store = ChromaVectorStore(chroma_collection=chroma_collection)
index = VectorStoreIndex.from_vector_store(vector_store)

print(f"  [Index] 构建完成")
print(f"  [Index] Node 总数: {len(index.docstore.docs)}")
print(f"  [Index] 说明: 这些 Node 的数据来源是 Chroma 的 SQLite 文件")

# ═══════════════════════════════════════════════════════════════
# Step 3: 封装为 VectorDBToolSpec
# ═══════════════════════════════════════════════════════════════
# llm 参数的作用: auto_retrieve_fn 内部用它来分析用户查询
# 比如: 用户说 "最新的" → LLM 提取出 status="active"

tool_spec = VectorDBToolSpec(
    vector_db=index,
    llm=Settings.llm,   # GPT-4o 分析查询意图
)

# to_tool_list() 返回两个 Tool:
tools = tool_spec.to_tool_list()
print(f"\n  [ToolSpec] 生成了 {len(tools)} 个工具:")
for t in tools:
    print(f"    ├─ {t.metadata.name}")
    print(f"    │  描述: {t.metadata.description[:100]}...")
    # 打印参数 Schema
    fn_schema = t.metadata.get_fn_schema()
    if hasattr(fn_schema, 'model_json_schema'):
        schema = fn_schema.model_json_schema()
        props = schema.get('properties', {})
        for pname, pinfo in props.items():
            print(f"    │  参数: {pname} ({pinfo.get('type','?')}) - {pinfo.get('description','')[:60]}")
    print()

# ═══════════════════════════════════════════════════════════════
# Step 4: 创建 Agent
# ═══════════════════════════════════════════════════════════════
agent = ReActAgent.from_tools(
    tools,
    llm=Settings.llm,
    verbose=True,           # 打印 Agent 的思考→行动→观察 全流程
    max_iterations=5,       # 最多迭代 5 步 (防止无限循环)
)

print(f"  [Agent] ReActAgent 创建完成, 拥有 {len(tools)} 个工具\n")

# ═══════════════════════════════════════════════════════════════
# Step 5: 测试 —— 对比四种类型的查询
# ═══════════════════════════════════════════════════════════════

# 测试 A: 简单事实查询 (Agent 直接调用检索工具)
print("=" * 70)
print("  测试 A: 简单查询 —— '事假需要提前多久申请?'")
print("=" * 70)
response = agent.chat("事假需要提前多久申请？")
print(f"\n  最终回答: {response}\n")

# 测试 B: 多跳推理 (Agent 可能需要多次检索)
print("=" * 70)
print("  测试 B: 多跳推理 —— '请假和加班的政策有什么区别?'")
print("=" * 70)
response = agent.chat("请假和加班的政策有什么区别？")
print(f"\n  最终回答: {response}\n")
# Agent 可能的思考过程:
#   Thought: 需要分别查请假政策和加班政策
#   Action: auto_retrieve("请假政策") → 拿到结果
#   Thought: 还要查加班政策
#   Action: auto_retrieve("加班政策") → 拿到结果
#   Thought: 现在可以对比了
#   Answer: 请假需要提前1个工作日申请... 加班按1.5/2/3倍计算...

# 测试 C: 带有隐藏过滤条件的查询
print("=" * 70)
print("  测试 C: 隐含过滤 —— '最新版本的请假规定是什么?'")
print("=" * 70)
response = agent.chat("最新版本的请假规定是什么？")
print(f"\n  最终回答: {response}\n")
# Agent 内部 auto_retrieve_fn 会:
#   1. LLM 分析 "最新版本" → filters={status: "active"}
#   2. 在过滤后的子集中检索 "请假规定"
#   3. 返回结果给 Agent
```

---

### 6.6 演示二：自定义元数据函数 + FunctionTool + Agent 路由

```python
"""
═══════════════════════════════════════════════════════════════════════════
  演示 2: 自定义元数据过滤 + FunctionTool + Agent 智能路由
═══════════════════════════════════════════════════════════════════════════

  场景: 公司有 HR/财务/技术 三个部门的知识。
        不同部门的员工查询时，自动限定对应的检索范围。
        Agent 从用户自然语言中理解"我是HR部门的"，自动传入 department="HR"。

  实现方式:
    1. 写一个自定义的检索函数 (内部根据参数构造 metadata filter)
    2. 用 FunctionTool 封装 → Agent 自动调用
    3. Agent 自己理解用户意图 → 自己传参 → 自己汇总结果

  对比 VectorDBToolSpec:
    VectorDBToolSpec 的 auto_retrieve_fn 已经内置了自动 filter 提取。
    但它是通用的——它对 metadata 的理解是"自动猜测"。
    
    自定义 FunctionTool 的优势:
    - 你可以写死某些过滤条件 (如: 永远只查 status='active')
    - 你可以加入业务逻辑 (如: 总监可以跨部门查, 普通员工只能查自己部门)
    - 你可以做权限校验 (如: 在函数内部检查用户是否有权限查某类文档)
"""
from llama_index.core.tools import FunctionTool
from llama_index.core.vector_stores import (
    MetadataFilter, MetadataFilters, FilterOperator, FilterCondition,
)
from llama_index.core.agent import ReActAgent
from typing import Optional, List

# ═══════════════════════════════════════════════════════════════
# Step 1: 定义自定义检索函数
# ═══════════════════════════════════════════════════════════════

def search_policy_by_department(
    query: str,
    department: str,
    only_active: bool = True,
    topics: Optional[List[str]] = None,
    min_version: Optional[str] = None,
) -> str:
    """
    按部门权限检索公司政策。

    参数说明:
      query:        用户的问题 (Agent 从用户自然语言中提取)
      department:   部门: "HR" / "Finance" / "Engineering"
                    注意: 这个参数是 Agent 根据用户意图自动传入的!
                    用户说 "我是HR的" → Agent 自动传 department="HR"
      only_active:  只查生效中的政策 (默认 True，防止检索到旧版/废弃内容)
      topics:       限定主题列表，如 ["请假", "加班"]，None 表示不限
      min_version:  最低版本号，如 "V3.0" (用字符串比较: "V3.0" > "V2.0")
      
    返回:
      检索结果的文本摘要
    """

    # ── 构造 metadata 过滤条件 ─────────────────────────
    filter_list = [
        MetadataFilter(key="department", value=department),
    ]

    if only_active:
        filter_list.append(
            MetadataFilter(key="status", value="active")
        )

    if topics:
        # 主题可以是多个 (OR 关系: 请假 OR 加班)
        topic_filters = [
            MetadataFilter(key="topic", value=t)
            for t in topics
        ]
        filter_list.append(
            MetadataFilters(
                filters=topic_filters,
                condition=FilterCondition.OR,
            )
        )

    if min_version:
        # 版本号是字符串, 但 V3.0 > V2.0 在字典序下也是对的
        # 更严谨的做法: 用数字版本号 (如 3.0 > 2.0)
        filter_list.append(
            MetadataFilter(
                key="version",
                value=min_version,
                operator=FilterOperator.GTE,
            )
        )

    # 所有条件 AND 组合:
    # department='HR' AND status='active' AND (topic='请假' OR topic='加班') AND version>='V3.0'
    filters = MetadataFilters(
        filters=filter_list,
        condition=FilterCondition.AND,
    )

    # ── 执行检索 ──────────────────────────────────────
    query_engine = index.as_query_engine(
        similarity_top_k=3,
        filters=filters,
    )
    response = query_engine.query(query)

    # ── 构建返回文本 (Agent 会读这段文本) ─────────────
    filter_desc = f"部门={department}"
    if only_active:
        filter_desc += ", 仅active"
    if topics:
        filter_desc += f", 主题={topics}"
    if min_version:
        filter_desc += f", 版本>={min_version}"

    return (
        f"[检索范围: {filter_desc}]\n"
        f"检索到 {len(response.source_nodes)} 条相关结果:\n"
        f"{response}\n"
    )


# ═══════════════════════════════════════════════════════════════
# Step 2: 封装为 FunctionTool
# ═══════════════════════════════════════════════════════════════

dept_search_tool = FunctionTool.from_defaults(
    fn=search_policy_by_department,
    name="search_by_department",
    description=(
        "按部门搜索公司政策。"
        "适用场景: 用户明确提到自己的部门 (如'我是HR的'、'财务部门')，"
        "或询问某个部门相关的政策时使用。"
        "参数: query(必填,用户的问题), department(必填,HR/Finance/Engineering), "
        "only_active(可选,是否只查当前有效的政策,默认True), "
        "topics(可选,限定主题列表如['请假','加班']), "
        "min_version(可选,最低版本号如V3.0)"
    ),
)

# ═══════════════════════════════════════════════════════════════
# Step 3: 创建 Agent
# ═══════════════════════════════════════════════════════════════

agent_dept = ReActAgent.from_tools(
    [dept_search_tool],
    llm=Settings.llm,
    verbose=True,
)

print("\n" + "=" * 70)
print("  演示 2: 自定义元数据过滤 + Agent 路由")
print("=" * 70)

# ── 场景 A: 明确说出部门 ───────────────────
print("\n  --- 场景 A: 用户明确说 HR 部门 ---")
response = agent_dept.chat(
    "我是HR部门的，帮我查一下最新的请假规定"
)
print(f"\n  [Agent 回答] {response}")

# ── 场景 B: 隐含部门信息 ───────────────────
print("\n  --- 场景 B: 隐含部门信息 ---")
response = agent_dept.chat(
    "我负责财务工作，最近要出差，住宿标准是多少？"
)
print(f"\n  [Agent 回答] {response}")

# ── 场景 C: 跨部门查询 (Agent 可能多次调用) ──
print("\n  --- 场景 C: 跨部门对比 ---")
response = agent_dept.chat(
    "对比一下HR和财务两个部门关于报销的政策有什么不同"
)
print(f"\n  [Agent 回答] {response}")
```

---

### 6.7 演示三：多工具组合 —— 检索 + 评估 + 综合

```python
"""
═══════════════════════════════════════════════════════════════════════════
  演示 3: 多工具组合 —— 检索工具 + 评估工具 + 综合回答
═══════════════════════════════════════════════════════════════════════════

  场景: Agent 拥有两个工具:
    Tool A: 检索知识库 (VectorDBToolSpec)
    Tool B: 检索结果的可靠性评分 (自定义 FunctionTool)

  Agent 的工作流:
    1. 用户提问 → Agent 调用 Tool A 检索
    2. Agent 拿到检索结果 → 调用 Tool B 评估可靠性
    3. 如果可靠性低 → Agent 重新检索或告知用户"信息可能不完整"
    4. 汇总 → 返回带可靠性标注的回答
"""
from llama_index.core.tools import FunctionTool

# ── Tool B: 评估函数 ─────────────────────────────────────
def evaluate_result_quality(query: str, retrieved_texts: str) -> str:
    """
    评估检索结果与查询的相关性和可靠性。

    这是一个简化的评估逻辑。生产环境可以用 FaithfulnessEvaluator。
    """
    # 简单的启发式评估
    has_keywords = any(
        kw in retrieved_texts
        for kw in query.replace("？","").replace("?","").split()
    )
    has_numbers = any(c.isdigit() for c in retrieved_texts)
    length_ok = len(retrieved_texts) > 20

    score = 0
    if has_keywords: score += 0.4
    if has_numbers: score += 0.3
    if length_ok: score += 0.3

    if score > 0.7:
        return f"可靠性评分: {score:.1f}/1.0 —— 结果可靠 ✓"
    elif score > 0.4:
        return f"可靠性评分: {score:.1f}/1.0 —— 结果中等，建议核实"
    else:
        return f"可靠性评分: {score:.1f}/1.0 —— 结果不可靠，建议重新检索 ✗"

eval_tool = FunctionTool.from_defaults(
    fn=evaluate_result_quality,
    name="evaluate_reliability",
    description="评估检索结果的可靠性和相关性。在拿到检索结果后调用此工具来验证质量。"
)

# ── 多工具 Agent ────────────────────────────────────────
agent_multi = ReActAgent.from_tools(
    tools + [eval_tool],  # VectorDBToolSpec 的2个工具 + 评估工具
    llm=Settings.llm,
    verbose=True,
)

print("\n" + "=" * 70)
print("  演示 3: 多工具组合 Agent")
print("=" * 70)

response = agent_multi.chat("公司给员工的五险一金是怎么交的？")
print(f"\n  [Agent 回答] {response}")
```

---

### 6.8 选型速查

```
═══════════════════════════════════════════════════════════════════════
            向量检索 Tool 选型速查表
═══════════════════════════════════════════════════════════════════════

  你的需求                                   →  推荐用哪个

  评估 Embedding/切分 → 需要 raw_scores     →  index.as_retriever()
  拿到分数后手动分析或传给外部系统

  简单检索 → Agent 调一下就行                  →  RetrieverTool
  不需要自动过滤或路由

  多源知识库 → Agent 需要选"搜哪个库"         →  VectorDBToolSpec
  用户查询隐含过滤条件 ("最新的""HR的")

  直接问答 → Agent 调用后拿最终答案             →  QueryEngineTool
  FAQ 型、不需要对检索结果二次加工

  自定义业务逻辑 → 权限/路由/API/SQL           →  FunctionTool
  完全自由的控制

  多工具协同 → 检索+过滤+评估+回答             →  上述 Tool 的任意组合
  生产环境的常态
═══════════════════════════════════════════════════════════════════════
```


---

## 6.9 大厂面试题深度讲解

### 6.9.1 工具封装理念类

#### Q1：为什么要把 RAG 的检索能力封装成 Agent 的 Tool？硬编码检索和 Agent 工具化检索的本质区别是什么？（字节跳动 / 腾讯 高频）

**面试官考察点：** 这道题考察对"Agent 工具化"这一范式的理解——不是技术上能不能做，而是为什么这样做更好。

**回答框架（从一个具体场景的对比展开）：**

**硬编码方式的五个致命局限：**

```
场景：用户问 "财务部2024年的新政策中，关于报销有什么规定？"

硬编码方式必须手写所有规则：
  if "财务" in query → filters["department"] = "Finance"
  if "2024" in query → filters["year"] = "2024"
  if "新" in query or "最新" in query → filters["status"] = "active"

问题 1：规则爆炸——"财务部最新"、"HR2024年"、"技术部V3.0"…每个组合都要写 if
问题 2：自然语言脆弱——用户说"我是财务的"、"财务部门"、"管钱的"→ if 全崩
问题 3：组合条件爆炸——"HR+2024+请假+最新版"→ N 层 if 嵌套
问题 4：无法处理对话修正——"先查HR的…不对，查财务的"→ if 规则无法消解指代
问题 5：多源检索不可实现——"PDF库里没有就去数据库查"→ 需要手写回退逻辑
```

**Agent 工具化的本质——从"人写规则"到"模型自主决策"：**:o::o::o::o::o::o:

```
Agent 工具化方式：
  tools = [VectorDBToolSpec(vector_db=index)]
  agent = ReActAgent(tools=[...])
  agent.chat("财务部2024年的新政策中，关于报销有什么规定？")

Agent 内部自主推理：
  Thought: "用户想查财务部2024年的报销政策"
  Action: 调用 auto_retrieve_fn
  Action Input: query="报销规定", filters={department:"Finance", year:"2024", status:"active"}
  Observation: [检索到 3 个相关 Chunk]
  Thought: "信息充分"
  Answer: "根据《费用报销管理办法》V3.0，一线城市住宿标准不超过500元/天..."

关键差异：
  → 不需要你写任何 if 规则
  → Agent 自己理解"财务部"= department="Finance"
  → Agent 自己判断"新政策"= status="active"
  → 对话修正时 Agent 自己调整 filters
  → 多工具协同时 Agent 自己决定调用顺序
```

**面试官追问："Agent 工具化有什么代价？"**

"三个代价：:o::o::o::o::o::o::o::o::o::o::o::o:
1. 延迟增加——Agent 推理（Thought-Action-Observation 循环）比直接调用 API 慢 200-500ms
2. 不确定性——Agent 可能选错工具、传错参数、陷入循环（需设置 max_iterations 兜底）
3. Token 消耗——每一步推理都消耗 LLM token，复杂任务可能消耗数万 token

所以不是所有检索都需要 Agent 化。简单场景（单知识库、无过滤需求）直接用 QueryEngine 就够了。Agent 化适用于复杂场景——多知识源、隐含过滤条件、多步推理。"

---

#### Q2：LlamaIndex 提供了六种把检索暴露出去的方式（as_retriever / RetrieverTool / VectorDBToolSpec / QueryEngineTool / FunctionTool / RetrieverQueryEngine），它们之间的层级关系和控制权转移是怎样的？（阿里巴巴 / 腾讯 高频综合对比题）

**面试官考察点：** 是否能清楚地讲出六种方式的定位差异——核心是"控制权在谁手里"。

**回答框架（从控制权的转移讲）：**

**核心轴线——"你控制多少 vs Agent 控制多少"：**

```
控制权在你手里 ←──────────────────────────────────→ 控制权在 Agent 手里

index.as_retriever()     RetrieverTool      VectorDBToolSpec     QueryEngineTool
     │                        │                    │                   │
     ▼                        ▼                    ▼                   ▼
 你手动调 .retrieve()   Agent 调检索      Agent 调检索+自动过滤  Agent 直接拿答案
 拿到 raw NodeWithScore  Agent 自己汇总    Agent 拿到 raw 结果    内部完成了检索+LLM生成
 你自己做所有后续处理    你配 description   内部 LLM 分析 query    你不介入生成过程
                        来控制调用时机     自动提取 metadata filter  Agent 只收答案
```

**六种方式一句话定位：**

| 方式 | 一句话 | 返回什么 | LLM汇总？ | Agent可用？ |
|------|--------|----------|:---:|:---:|
| **as_retriever()** | 最底层的 Python API——你手动调检索 | `List[NodeWithScore]` | ✗ | ✗ |
| **RetrieverTool** | 把检索包装成 Agent 能调的工具 | `List[Node]` | ✗ | ✓ |
| **VectorDBToolSpec** | 自动 metadata 过滤 + 多源路由 | `List[NodeWithScore]` | ✗ | ✓ |
| **QueryEngineTool** | 检索+LLM汇总一体化 | `str`（完整回答） | ✓ | ✓ |
| **FunctionTool** | 任意 Python 函数→Agent工具 | 你定义 | 你决定 | ✓ |
| **RetrieverQueryEngine** | 检索+LLM汇总（非Agent） | `Response`(含scores) | ✓ | ✗ |

**面试官追问："VectorDBToolSpec 和 QueryEngineTool 都涉及检索，什么时候用哪个？"**

"关键区别在于——**Agent 是否需要参与检索结果的解读**：
- VectorDBToolSpec：Agent 拿到原始 NodeWithScore → Agent 自己判断结果质量 → 可能二次检索/调整参数/传给其他 Tool
- QueryEngineTool：Agent 直接拿到最终答案 → Agent 不做任何中间判断

用 VectorDBToolSpec 当：多步推理（先检索→判断够不够→不够再查→最后汇总）
用 QueryEngineTool 当：简单问答（问一个知识库问题→直接给答案），或作为多工具中'直接回答'的兜底"

---

### 6.9.2 VectorDBToolSpec 深度类

#### Q3：VectorDBToolSpec 的 auto_retrieve_fn 内部是如何工作的？LLM 是怎么从"最新的HR请假政策"中提取出 metadata filter 的？（字节跳动 / 百度 高频原理题）

**面试官考察点：** 理解"LLM 分析 query → 自动构造 filter"这一核心创新。

**回答框架（四步内部流程）：**

```
用户 query："最新的HR请假政策"

Step 1：LLM 分析 query（VectorDBToolSpec 独有的步骤）

  内部 Prompt（简化版）：
    "用户说'最新的HR请假政策'。请提取以下信息：
     1. query_string: 用户真正想搜的内容
     2. filters: metadata 过滤条件（部门/状态/主题/日期等）
     3. order_by: 排序方式"

  LLM 返回结构化结果：
    {
      "query_string": "请假政策",
      "filters": {
        "department": "HR",
        "status": "active",        ← "最新的" → 推断 status="active"
        "topic": "请假"             ← "请假政策" → 提取主题
      },
      "order_by": "created_date DESC"  ← "最新的" → 按日期倒序
    }

Step 2：构造 MetadataFilters
  → MetadataFilter(key="department", value="HR")
  → MetadataFilter(key="status", value="active")
  → MetadataFilter(key="topic", value="请假")
  → 全部 AND 组合

Step 3：在过滤后的子集中做向量检索
  → 先用 metadata 过滤（缩小候选集）
  → 再在子集中做 ANN 检索

Step 4：返回 Top-K NodeWithScore（带完整 metadata）
  → Agent 拿到结果 → 自己决定下一步
```

**这个机制的三个价值：**

1. **用户不需要知道 metadata 字段名**——用户说"HR的"而非 `department="HR"`，LLM 做翻译
2. **隐含条件自动提取**——"最新的"→ status="active"，"去年的"→ year="2024"，LLM 做推理
3. **组合条件零规则**——"HR部门2024年关于加班的V3.0以上版本"→ 四个 filter 自动组合

**面试官追问："LLM 提取 filter 会不会出错？比如'最新的'被理解成'最新创建的'而非'最新版本'？"**

"会出错。这是 auto_retrieve_fn 的主要风险。缓解措施：
1. **Tool description 中给出明确约束**——'最新=status=active，不是 created_date 最大'
2. **向量数据库的 metadata schema 设计简洁**——只有核心字段（department/status/topic），减少歧义
3. **对关键查询加后验证**——如果 filter 后的候选集为空 → 去掉 filter 重试 → 对比结果
4. **FunctionTool 兜底**——对高风险场景（合规/金融），用 FunctionTool 写死过滤逻辑，不让 LLM 猜测"

---

#### Q4：VectorDBToolSpec 生成了两个子工具（auto_retrieve 和 search），它们分别用于什么场景？Agent 怎么知道该调哪个？（腾讯 / 美团）

**面试官考察点：** 理解两个子工具的分工，以及 Agent 的工具选择机制。

**回答思路：**

**两个子工具的分工：**

```
auto_retrieve（"聪明检索"）：
  行为：LLM 分析 query → 提取 metadata filter → 过滤后检索
  适用场景：
    ✓ 用户 query 中有隐含过滤条件（"最新的"、"HR的"、"2024年的"）
    ✓ 需要缩小检索范围提高精度
    ✓ 多部门/多类别知识库，需要路由到正确的子集
  
  例子："财务部最新的报销标准"
    → auto_retrieve 提取：query="报销标准", department="Finance", status="active"

search（"直搜"）：
  行为：直接用 query 字符串做向量检索（不走 auto filter）
  适用场景：
    ✓ 用户 query 中没有任何隐含过滤条件（"五险一金是什么？"）
    ✓ 需要全库搜索不需要限定范围
    ✓ 已知 query 本身已足够精确
  
  例子："什么是五险一金？"
    → search 直接用 query 检索，不需要提取 filter
```

**Agent 如何选择——靠 Tool Description：**

```
auto_retrieve 的 description：
  "Automatically retrieve relevant data from the vector database.
   Use this when user query contains implicit filters like department,
   date range, status, or other metadata conditions."
  → Agent 看到 "最新的"、"HR的" → 判断需要 auto filter → 调 auto_retrieve

search 的 description：
  "Search the vector database with a specific query string.
   Use this for direct semantic search without metadata filtering."
  → Agent 看到纯粹的内容查询 → 调 search

Agent 判断的准确性取决于 description 的质量。
description 里必须写清楚"什么场景用这个工具"。
```

**面试官追问："如果 Agent 选错了工具怎么办？比如该用 auto_retrieve 却用了 search？"**

"三种防护：
1. **max_iterations 限制**——Agent 用 search 检索结果不满意 → 下一轮发现结果不够精准 → 尝试 auto_retrieve → 最多 5 轮
2. **Tool description 优化**——在 auto_retrieve 的 description 中列举典型触发词（'最新的'、'HR的'、'某部门的'），帮助 Agent 判断
3. **下游容错**——用 Rerank 精排兜底，即使 search 返回了较多噪声，Rerank 也能过滤掉大部分"

---

### 6.9.3 工程实践类

#### Q5：FunctionTool 和 VectorDBToolSpec 都是 Agent 工具，在什么情况下应该选择 FunctionTool 而不是 VectorDBToolSpec？（华为 / 拼多多 工程决策题）

**面试官考察点：** 决定"自建"还是"用现成"的判断力。

**回答框架——FunctionTool 的四个不可替代场景：**

**场景一——需要写死的业务规则（不让 LLM 猜测）**

```python
# 场景：总监可以跨部门查，普通员工只能查自己部门
# VectorDBToolSpec 的 auto_retrieve 可能让 LLM 误判部门范围

def search_with_permission(query, user_department, user_level):
    if user_level == "director":
        filters = {}  # 总监无限制
    else:
        filters = {"department": user_department}  # 限定本部门
    
    # 权限逻辑是写死的，不能被 LLM "猜"
    return query_engine.query(query, filters=filters)

# → FunctionTool：权限规则在代码里，不会出错
# → VectorDBToolSpec：权限规则依赖 LLM 判断，可能被绕过
```

**场景二——需要调用外部 API/数据库（不仅限于向量检索）**

```python
# 场景：先查 SQL 数据库获取员工部门 → 再查向量库

def query_employee_policy(employee_name):
    # Step 1: SQL 查员工部门
    dept = db.query("SELECT department FROM employees WHERE name=?", employee_name)
    # Step 2: 向量库查该部门政策
    result = index.as_query_engine(filters={"department": dept}).query(...)
    return result

# → FunctionTool：编排多步、跨系统逻辑
# → VectorDBToolSpec：只能做向量检索，无法编排跨系统调用
```

**场景三——需要对检索结果做后处理**

```python
# 场景：检索后要过滤掉过期内容、统计数字摘要、按优先级排序

def search_and_analyze(query):
    raw = index.as_retriever(similarity_top_k=20).retrieve(query)
    # 自定义后处理：按 source 去重、提取数字、生成摘要
    deduped = deduplicate_by_source(raw)
    stats = extract_key_numbers(deduped)
    return format_response(deduped, stats)

# → FunctionTool：完全控制后处理逻辑
# → VectorDBToolSpec：返回的是原始 NodeWithScore，无法插入后处理
```

**场景四——需要精确的错误处理和审计**

```python
# 场景：金融合规场景，每次检索都要记录审计日志

def audited_search(query, user_id):
    start = time.time()
    result = index.as_query_engine(...).query(query)
    
    audit_log.insert({
        "user": user_id, "query": query,
        "result_count": len(result.source_nodes),
        "latency_ms": (time.time()-start)*1000,
        "timestamp": now()
    })
    return result

# → FunctionTool：每一步都可控、可审计
```

**决策原则：** "当检索逻辑是'标准'的（选一个库→搜一下→给出结果），用 VectorDBToolSpec。当检索逻辑需要'定制'的（权限/跨系统/后处理/审计），用 FunctionTool。生产环境通常是两者混合——标准检索用 VectorDBToolSpec，定制逻辑用 FunctionTool 做一层包装。"

---

#### Q6：Agent 的 Tool Description 怎么写才好？如果 Agent 总是不调用你的检索工具或乱调怎么办？（字节跳动 / 腾讯 实战调试题）

**面试官考察点：** 工程调优经验。Tool Description 质量直接决定 Agent 的行为。

**回答框架——Description 设计的五条黄金法则：**

**法则一——明确触发条件（"什么时候用"）**

```
❌ 差: "搜索一下"
   → Agent 不知道什么时候该调用

✓ 好: "搜索公司内部政策和员工手册。当用户询问公司制度、政策、福利、
      加班、请假、报销、考勤等问题时使用。不要用于聊日常话题。"
   → Agent 清楚知道"用户问政策→用这个、用户闲聊→不用"
```

**法则二——列举正向和负向示例**

```
✓ 最佳:
  "使用场景（正向）:
   - 用户问'年假怎么算？'→ 用此工具
   - 用户问'加班费标准？'→ 用此工具
   不使用场景（负向）:
   - 用户问'今天天气怎么样？'→ 不用
   - 用户问'你是谁？'→ 不用
   - 用户说'谢谢'→ 不用"
```

**法则三——描述参数的具体含义和格式**

```
❌ 差: "参数: query (用户的问题)"

✓ 好: "参数:
       query (必填, str): 从用户自然语言中提取的搜索关键词。
                          例如用户说'HR最新的年假规定'→ query='年假规定'
       department (必填, str): 部门代码，只能是 HR/Finance/Engineering。
                               例如'HR部门'→'HR'，'财务'→'Finance'"
```

**法则四——描述返回值的格式和含义**

```
✓ "返回: 包含以下内容的文本——
       [检索范围: 部门=HR, 仅active]
       检索到 3 条相关结果:
       1. (0.92) 年假天数按工龄计算...
       2. (0.85) 年假最小请假单位为半天...
       注意: 返回的文本中已经包含了相似度分数和来源页码。"
```

**法则五——写清楚与其他工具的边界**

```
✓ "此工具 vs search_policy 的区别:
   - 此工具: 直接搜索，不限定部门，返回全公司通用政策
   - search_policy: 限定部门搜索，需要传入 department 参数
   
   如果用户明确提到部门，优先使用 search_policy。"
```

**Agent 乱调/不调的排查步骤：**

```
Step 1——检查 description 是否出现在 Agent 的 system prompt 中
  → 在 verbose=True 时查看 tool 列表 → 确认你的 tool 在里面

Step 2——检查触发条件描述是否足够具体
  → "当用户询问..." 换成 "当用户询问[具体话题列表]时..."
  → 每多加一个关键词，命中率可能提升 10-20%

Step 3——检查是否有"工具选择歧义"
  → 两个 Tool 的 description 太相似 → Agent 不知道该选谁 → 随机选
  → 每个 Tool 加"此工具 vs X工具的区别"

Step 4——检查是否有"负向示例"
  → 明确告诉 Agent "不要用"的场景 → 减少误调

Step 5——用具体的 bad case 测试
  → 拿实际出错的 query → 手动构造同样的 Agent 调用 → 看 Agent 的 Thought
  → 根据 Thought 调整 description → 重新测试 → 直到修好
```

---

### 6.9.4 面试高频知识点速查

#### 一句话答案系列

| 问题 | 一句话答案 |
|------|-----------|
| 为什么要把检索封装成 Tool？ | 从"人写规则"到"Agent 自主决策"——Agent 自己判断要不要搜、搜什么、怎么过滤 |
| as_retriever 和 RetrieverTool 的区别？ | as_retriever 是你手动调 Python API，RetrieverTool 是封装后给 Agent 自动调 |
| VectorDBToolSpec 的核心创新？ | auto_retrieve_fn——内部用 LLM 分析 query，自动提取 metadata filter，再执行过滤后检索 |
| auto_retrieve 和 search 的区别？ | auto_retrieve 先分析 query 提取 filter 再检索，search 直接用 query 字符串检索 |
| QueryEngineTool 什么时候用？ | FAQ 型 Agent——Agent 直接拿答案，不需要对检索结果做中间处理 |
| FunctionTool 什么时候用？ | 需要写死业务规则（权限/审计）、跨系统调用（SQL+向量）、自定义后处理 |
| Tool Description 最重要的要素？ | 触发条件（什么时候用/不用）+ 参数格式 + 返回值格式 + 与其他工具的边界 |
| Agent 不调你的工具怎么排查？ | 查 description 是否具体→查 trigger 词覆盖→查工具选择歧义→拿 bad case 调 |

#### 六种封装方式速查表

| 方式 | 控制权 | Agent？ | 返回 | LLM汇总？ | 最佳场景 |
|------|:---:|:---:|------|:---:|------|
| **as_retriever()** | 完全你控制 | ✗ | NodeWithScore | ✗ | 评估/调参/自建管线 |
| **RetrieverTool** | Agent 调检索 | ✓ | Node 列表 | ✗ | 简单检索/多工具组件 |
| **VectorDBToolSpec** | Agent 自主过滤 | ✓ | NodeWithScore | ✗ | 多源/自动过滤/路由 |
| **QueryEngineTool** | Agent 直拿答案 | ✓ | str(完整答案) | ✓(自动) | FAQ型/兜底工具 |
| **FunctionTool** | 你定义一切 | ✓ | 你定义 | 你决定 | 权限/API/SQL/后处理 |
| **RetrieverQueryEngine** | 你手动调 | ✗ | Response+scores | ✓ | 需要同时看分数和答案 |

---

