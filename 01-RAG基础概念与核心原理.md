## 一、大模型的四大局限性

### 1.1 幻觉问题（Hallucination）

**具体表现：**
模型会自信地生成看似合理但完全虚构的内容，包括编造人名、日期、数据、论文引用、API 名称等，且表达方式与真实知识无异，难以凭阅读分辨。

**为什么会有幻觉：**
- 大模型的本质是**概率预测下一个 token**，而非"理解事实"。它学到的是语言模式而非真实世界的因果逻辑。
- 训练数据中存在矛盾信息，模型在低概率区间会"平滑"地编造合理内容。
- 推理时温度采样机制引入随机性，放大了不确定区间的编造倾向。

**行业应对策略：**
| 策略 | 思路 |
|------|------|
| RAG | 检索外部知识作为生成依据，从源头约束输出 |
| 事实核查管线 | 生成后用 NLI 模型 / 搜索引擎交叉验证 |
| 降低温度 + 明确指令 | 要求"不知道就说不知道"，禁止编造 |
| 思维链验证（Self-Consistency） | 多次采样，投票选择最一致的答案 |

---

### 1.2 知识截止（Knowledge Cutoff）

**具体表现：**
模型的训练有一个截止日期，此日期之后发生的事情、发布的论文、更新的法律法规、新出现的工具/框架，模型一概不知。例如一个 2024 年训练的模型无法回答 2025 年的新闻事件。

**为什么存在：**
- 训练一轮大模型耗时数月、耗资数千万美元，"实时更新"在成本和工程上不现实。
- 训练数据需要清洗、去重、配比，无法像搜索引擎一样持续增量更新。

**行业应对策略：**
| 策略 | 思路 |
|------|------|
| RAG | 用外部知识库弥补时效差，新文档入库即可被检索 |
| 搜索增强 | 实时调用搜索引擎，将搜索结果注入 prompt |
| 持续预训练（CPT） | 用新数据做小规模增量预训练，逐步刷新知识 |
| 插件 / Function Calling | 模型调用外部 API 获取实时数据 |

---

### 1.3 领域盲区（Domain Blindness）

**具体表现：**
通用大模型在垂直领域（医疗、法律、金融、企业内部系统）表现不佳——不理解专业术语、不知道内部业务流程、抓不住领域特定的约束条件。

**为什么存在：**
- 预训练语料以公开互联网文本为主，垂直领域的高质量数据占比极低。
- 企业内部数据出于安全与合规原因不会进入公开训练集。
- 领域知识有隐性知识（tacit knowledge），不存在于任何文本中，模型无从学习。

**行业应对策略：**
| 策略 | 思路 |
|------|------|
| RAG | 将企业内部文档/行业标准纳入知识库，按需检索 |
| 领域微调（Domain Fine-tuning） | 用领域语料继续训练模型，内化领域知识 |
| 混合架构 | 通用模型 + 领域小模型 + 规则引擎组合使用 |
| Human-in-the-loop | 关键决策保留人工审核环节 |

---

### 1.4 上下文窗口限制（Context Window Limitation）

**具体表现：**
虽然上下文窗口在不断增长（128K → 1M → 2M tokens），但仍有硬上限。当需要处理大量文档时（如分析一整年的财报、阅读整个代码仓库），无法将所有内容一次性放入上下文。

**为什么存在：**
- Transformer 的注意力机制复杂度为 O(n²)，窗口扩大导致计算量和显存呈平方级增长。
- 即使技术上支持超长上下文，"大海捞针"实验表明模型对长文本中间位置的信息利用率显著下降（Lost in the Middle 现象）。
- 长上下文带来的推理延迟和成本也是工程上的现实约束。

**行业应对策略：**
| 策略 | 思路 |
|------|------|
| RAG | 只检索最相关的片段，不让模型处理全部文档 |
| 上下文压缩 / 摘要 | 先用小模型压缩长文本再送入大模型 |
| 分块处理 | Map-Reduce / ReAct 模式分批处理 |
| KV-Cache 优化 | 工程层面优化长上下文的显存占用与推理速度 |

---

## 二、RAG 如何解决幻觉问题

RAG 解决幻觉的核心逻辑是：**不给模型"编"的机会，而是给它"抄"的材料。**

在没有 RAG 时，模型面对它不知道或不确定的问题，会基于训练时学到的语言分布模式去"生成一个看起来合理的答案"——这就是幻觉的根源。

RAG 的做法是：
1. 先将可靠的、可溯源的知识整理入库；
2. 用户提问时，先检索出与问题最相关的知识；
3. 将检索到的知识作为 prompt 的一部分交给模型，要求模型"根据以下参考资料作答"。

**这样做的效果：** 模型从"闭卷考试"变成了"开卷考试"，它的任务从"记住并复现知识"变为"阅读理解并整合给定材料"。只要知识库质量高、检索结果准确，幻觉就会被大幅压制。

**两个关键前提：**
- **知识库质量要高：** 垃圾进、垃圾出。如果知识库本身有错，RAG 就是在给模型喂错误材料，幻觉问题不会消失，只是从"模型编的"变成"文档写的"。
- **检索要准确：** 检索漏了相关文档，模型得不到必要信息；检索多了无关文档，模型受到噪声干扰。两者都会重新触发模型"编造"的行为。

---

## 三、三种知识注入方案对比

### 3.1 核心思路

| 方案 | 核心思路 |
|------|----------|
| **提示词工程** | 在 prompt 中直接写入知识/规则，不改变模型本身 |
| **微调（Fine-tuning）** | 用领域数据训练模型，将知识内化到模型参数中 |
| **RAG** | 知识留在外部知识库，推理时动态检索注入 |

三种方案的本质区别在于**知识存储的位置**：提示词工程和 RAG 将知识放在模型外部（prompt 中），微调将知识放在模型内部（参数中）。

### 3.2 多维对比

| 维度 | 提示词工程 | RAG | 微调（Fine-tuning） |
|------|-----------|-----|---------------------|
| **知识更新速度** | 即时（改 prompt 文本即可） | 准实时（新文档入库即生效） | 慢（需重新训练，数小时到数天） |
| **成本** | 几乎为零 | 中等（向量数据库 + embedding + 检索） | 高（GPU 算力 + 标注数据 + 人才） |
| **知识容量** | 极小（受上下文窗口限制） | 极大（知识库可无限扩展，仅受存储成本约束） | 有限（模型参数是有限的，知识之间互相竞争容量） |
| **幻觉控制** | 弱（全靠 prompt 约束，模型可能不听） | 强（有明确参照文本约束） | 中等（内化了知识但仍可能编造） |
| **推理延迟** | 无额外延迟 | 有检索延迟（通常 100-500ms） | 无额外延迟 |
| **适用场景** | 少量规则/角色设定/格式约束 | 大量动态知识、需要溯源、快速迭代 | 稳定领域知识、对话风格、特定格式输出 |

### 3.3 为什么会有这些差异

**更新速度的差异：** 提示词和 RAG 操作的是外部的文本，修改文本即生效，路径最短。微调则需要构建数据集 → 训练 → 评估 → 部署，链路上每个环节都有时间成本。

**成本的差异：** 微调需要大量 GPU 算力做反向传播，且高质量标注数据稀缺昂贵。RAG 的成本主要是 embedding 计算和向量检索，这是推理而非训练，算力需求低一个数量级。提示词工程零边际成本。

**知识容量的差异：** 模型参数是固定的（如 7B、70B），知识编码进参数必然面临上限——新知识会"覆盖"旧知识（灾难性遗忘）。外部知识库可以通过加机器横向扩展，理论上无限。

**幻觉控制的差异：** RAG 把知识作为"参考答案"明示给模型，模型做的是阅读理解而非记忆回想，幻觉概率显著降低。微调将知识融入参数分布，模型在低置信度区域仍有编造倾向。提示词工程仅靠文本指令约束，约束力最弱。

### 3.4 经验法则：先 RAG 后微调

**实践中最经济的路径是：**

```
RAG 验证可行性 → 满足需求？ → 是 → 持续优化 RAG，不用微调
                              → 否 → 分析瓶颈 → 引入微调
```

**为什么是这个顺序：**
1. **RAG 试错成本极低。** 加文档、改 chunk 策略、调检索参数都是分钟级操作。微调一次从数据准备到训练完成往往需要数天，且每次调整都要重新训练。
2. **RAG 帮助发现真正需要微调的地方。** 在搭建 RAG 的过程中，你会积累用户真实提问与检索命中情况的日志，这些数据恰好是微调所需的高质量训练样本。
3. **大多数企业场景 RAG + 提示词工程已经够用。** 除非模型对知识的理解程度不够（如医学影像报告生成需要深入的领域推理）、或者输出格式高度定制化（如特定 JSON schema 的 API 生成），否则微调的增量收益往往不如预期。

### 3.5 什么时候应该直接微调

以下情况下 RAG 的收益有限，应该直接考虑微调：
- 需要模型**以特定风格/语气写作**（如某位作家的文风）
- 需要模型**遵循极其复杂的指令格式**，prompt 本身已很长且不稳定
- 领域知识需要**深层理解**而非事实查找（如临床诊断推理）
- **推理延迟**是硬指标，不能接受检索环节

---

## 四、微调与 RAG 的详细对比

### 4.1 更新周期

| RAG | 微调 |
|-----|------|
| 新知识入库后**秒级生效**，上传文档 → 向量化 → 可检索 | 新知识需要**数天到数周**：收集数据 → 标注 → 训练 → 评估 → 部署 |
| 适合快速变化的知识（如政策法规、产品文档） | 适合相对稳定的知识（如医学基础理论） |

### 4.2 人力成本

| RAG | 微调 |
|-----|------|
| 主要成本在**工程搭建**：向量数据库、检索管线、Rerank 模型 | 主要成本在**数据标注**：需要领域专家构建高质量的（指令，回复）对 |
| 一个人 + 开源生态 1-2 周可搭建基础版 | 通常需要标注团队 + 算力资源，持续投入 |

### 4.3 覆盖范围

| RAG | 微调 |
|-----|------|
| 知识是外挂的，**有边界**——知识库里有什么才能回答什么。未入库的知识不可能被用到 | 知识是内化的，**无明确边界**——模型会对未见过的相关知识做泛化和推理，但可能编造 |
| 覆盖面可控，加了就有，没加就没有 | 覆盖面不可控，记了什么、忘了什么难以精确验证 |

### 4.4 能否回答溯源

| RAG | 微调 |
|-----|------|
| **天然支持溯源**：检索到的文档就是答案依据，可以直接展示给用户（"本回答参考了《XX 手册》第 3 章"） | **难以溯源**：知识溶解在数十亿参数中，无法追溯"模型为什么这么回答"；可解释性差 |
| 在医疗、法律、金融等合规要求高的行业，溯源是刚需 | 在需要"模型自己得出结论"的场景，溯源不是重点 |

### 4.5 错误纠正

| RAG | 微调 |
|-----|------|
| 发现知识错误时，**直接修改对应文档**，下次检索即自动纠正。不涉及模型变更 | 发现错误时，需要构建纠正样本 → 重新训练 → 重新部署。代价高、周期长 |
| 可以做到**分钟级修正** | 通常**天级甚至周级修正** |

---

## 五、RAG 的核心原理

### 5.1 核心思想

> **模型不需要记住所有知识，只需要在需要时查阅相关知识。**

类比人类的认知过程：一个医生面对罕见病例时，不是凭记忆背诵所有医学文献，而是查阅病例相关的诊疗指南和最新论文，综合判断后给出诊断。RAG 赋予大模型同样的能力——**把"记忆"与"推理"解耦**：记忆由外部知识库负责（可以无限扩展、随时修改），推理由大模型负责（理解问题、整合信息、生成回答）。

### 5.2 三个核心步骤

```
用户提问 → [1. 检索 Retrieve] → [2. 增强 Augment] → [3. 生成 Generate] → 回答
              ↑                       ↑                      ↑
         从知识库中找到          将检索结果与原始       LLM 综合生成
         最相关的文档           问题拼接成完整prompt     最终回答
```

#### 步骤一：检索（Retrieval）

**做什么：** 将用户的问题转化为查询，在知识库中找到语义最相关的文档片段。

**具体过程：**
1. 用户问题向量化（embedding），得到问题向量
2. 在向量数据库中用相似度检索（如余弦相似度）找到 Top-K 个最相关的文档块
3. 可选：用 Rerank 模型对 Top-K 做精排，进一步筛选真正相关的

**优化角度：**
| 环节 | 优化方向 |
|------|----------|
| **文档切块（Chunking）** | 块太大 → 检索精度下降、引入噪声；块太小 → 丢失上下文、语义不完整。常用 512-1024 tokens，按语义边界切分而非硬截断 |
| **Embedding 模型选型** | 通用模型 vs 领域模型，多语言场景需选支持对应语言的模型 |
| **检索策略** | 稀疏检索（BM25）+ 稠密检索（向量）混合，互补长短 |
| **Rerank** | 用更强的交叉编码器模型做精排，提升 Top-N 精度 |
| **查询改写** | 将用户简短/模糊的问题扩展为更精确的检索查询（Query Expansion / HyDE） |
| **多路召回** | 并行检索+关键词检索+知识图谱检索，多路结果融合 |

#### 步骤二：增强（Augmentation）

**做什么：** 将检索到的文档片段与原始用户问题组合，构建一份包含"参考资料"的完整 prompt。

**具体过程：**
1. 将检索到的多个文档片段排序、去重
2. 按照模板组织 prompt 结构，典型格式：
   ```
   请根据以下参考资料回答用户问题。
   如果参考资料中没有相关信息，请明确说明。

   ## 参考资料
   [文档1] ...
   [文档2] ...

   ## 用户问题
   {用户原始问题}
   ```
3. 控制拼接后的总长度不超出模型的上下文窗口

**优化角度：**
| 环节 | 优化方向 |
|------|----------|
| **上下文编排** | 重要文档放最前/最后（利用首尾优先效应），避免"Lost in the Middle" |
| **元数据注入** | 附上文档来源、日期、章节等元信息，帮助模型判断可靠性 |
| **长度控制** | 动态调整注入的文档数量，确保总 token 数在窗口安全范围内 |
| **指令设计** | 明确告诉模型"参考资料不足时不要编造"，设置行为边界 |
| **引用标注** | 对文档片段编号，要求模型回答时标注引用来源 |

#### 步骤三：生成（Generation）

**做什么：** LLM 阅读增强后的 prompt，综合参考资料中的信息，生成最终的回答。

**具体过程：**
1. LLM 理解用户问题，解析参考资料中的相关内容
2. 对多个来源的信息进行综合、对比、推理
3. 生成结构化的自然语言回答，可附带引用标注

**优化角度：**
| 环节 | 优化方向 |
|------|----------|
| **模型选型** | 更强的模型在信息整合和遵循指令方面表现更好，在复杂 RAG 场景差异明显 |
| **流式输出** | 开启 streaming 降低用户感知延迟 |
| **校验机制** | 对生成结果做后处理校验（如事实一致性检查、敏感词过滤） |
| **引用还原** | 将回答中的引用标记映射回原始文档的具体位置 |
| **兜底策略** | "知识库中未找到相关信息"时的降级方案（如转人工、转搜索） |

### 5.3 RAG 架构全景图

```
                         ┌──────────────────────┐
                         │   ① 文档预处理（离线）  │
                         │  PDF/Word/网页 → 解析  │
                         │  → 切块 → Embedding   │
                         │  → 存入向量数据库      │
                         └──────────┬───────────┘
                                    │
                                    ▼
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ 用户提问  │ ──▶ │ ② 检索    │ ──▶ │ ③ 增强    │ ──▶ │ ④ 生成    │ ──▶ 回答
│          │     │ Query→向量 │     │ Prompt    │     │ LLM推理  │
│          │     │ 向量检索   │     │ 拼接组织  │     │ 引用溯源  │
└──────────┘     │ Rerank精排│     └──────────┘     └──────────┘
                 └──────────┘
```

---

## 六、企业 RAG 的核心应用场景

### 6.1 智能客服 / 帮助中心

**痛点：** 客服团队需要记忆大量产品文档和政策，培训成本高、人员流动大。

**RAG 方案：** 将产品手册、FAQ、工单历史纳入知识库，用户提问时实时检索，模型生成准确的、有据可查的回复。新功能上线只需更新文档，无需重新培训。

**代表性指标：** 首次解决率（FCR）、平均处理时间（AHT）

---

### 6.2 企业知识库问答

**痛点：** 大型企业有海量内部文档（制度、流程、技术方案、会议纪要），员工查找效率低。

**RAG 方案：** 将 Confluence、Notion、 SharePoint 等平台的全部文档向量化入库，员工用自然语言提问即可获得整合后的答案并附上原文链接。

**代表性指标：** 知识查找时间、答案准确率

---

### 6.3 法律 / 合规审查

**痛点：** 律师和法务需要翻阅大量法律文书、判例、合同条款，低效且容易遗漏关键信息。

**RAG 方案：** 将法律法规库、历史判例库、企业合同库向量化，审查时快速检索相关条款和先例，生成分析报告并精确引用条文编号。

**代表性指标：** 审查覆盖率、引用准确率

---

### 6.4 医疗辅助诊断

**痛点：** 医生面对罕见病案例时查阅文献效率低，且最新研究论文持续涌现。

**RAG 方案：** 将医学论文、诊疗指南、药品说明书纳入知识库，辅助医生基于最新证据制定诊疗方案，并溯源至具体文献。

**代表性指标：** 循证支持率、诊断建议一致性

---

### 6.5 代码仓库问答 / 开发者文档

**痛点：** 新人入职需要熟悉庞大的代码库，API 文档分散、示例不完整。

**RAG 方案：** 将代码仓库、API 文档、技术博客统一入库。开发者用自然语言提问（"这个接口为什么返回 500？"），模型检索相关代码和文档后给出分析和修复建议。

**代表性指标：** 问题解决率、onboarding 效率提升

---

### 6.6 电商 / 内容推荐

**痛点：** 商品库和内容库庞大，用户难以通过关键词精准定位。

**RAG 方案：** 商品描述和用户评价向量化，用户用自然语言描述需求（"适合送给女朋友的生日礼物，预算 500 以内"），系统检索匹配商品并生成个性化推荐理由。

**代表性指标：** 点击率（CTR）、转化率（CVR）

---

## 七、总结

| 要点 | 一句话概括 |
|------|-----------|
| 幻觉 | 模型不是故意撒谎，而是在它不确定的地方"概率平权"地编造 |
| RAG 核心思想 | 让模型从"闭卷默写"变成"开卷阅读理解" |
| 先 RAG 后微调 | RAG 试错成本低，先把业务闭环跑通，瓶颈明确了再上微调 |
| 检索是关键 | RAG 的上限由知识库质量决定，下限由检索精度决定 |
| 三大步骤 | 检索（找到对的文档）→ 增强（把文档和问题拼好）→ 生成（综合信息产出回答） |
| 企业落地 | 客服、知识库、法律、医疗、代码、电商——有"大量文档 + 需要精准答案"的地方就有 RAG 的用武之地 |

---

