## 目录
- [1 大模型的四大局限性](#1-大模型的四大局限性)
- [2 RAG 如何解决幻觉问题](#2-RAG-如何解决幻觉问题)
- [3 三种知识注入方案对比](#3-三种知识注入方案对比)
- [4 微调与 RAG 的详细对比](#4-微调与-RAG-的详细对比)
- [5 RAG 的核心原理](#5-RAG-的核心原理)
- [6 企业 RAG 的核心应用场景](#6-企业-RAG-的核心应用场景)
- [7 总结](#7-总结)


## 1 大模型的四大局限性

### 1.1 幻觉问题（Hallucination）

**具体表现：**
模型会自信地生成看似合理但完全虚构的内容，包括编造人名、日期、数据、论文引用、API 名称等，且表达方式与真实知识无异，难以凭阅读分辨。

**为什么会有幻觉：**
- 大模型的本质是**概率预测下一个 token**，而非"理解事实"。它学到的是语言模式而非真实世界的因果逻辑。
- 训练数据中存在矛盾信息，模型在低概率区间会"平滑"地编造合理内容。
- 推理时温度采样机制引入随机性，放大了不确定区间的编造倾向。

**行业应对策略：**
| 策略 | 思路 |
|------|------|
| RAG | 检索外部知识作为生成依据，从源头约束输出 |
| 事实核查管线 | 生成后用 NLI 模型 / 搜索引擎交叉验证 |
| 降低温度 + 明确指令 | 要求"不知道就说不知道"，禁止编造 |
| 思维链验证（Self-Consistency） | 多次采样，投票选择最一致的答案 |

---

### 1.2 知识截止（Knowledge Cutoff）

**具体表现：**
模型的训练有一个截止日期，此日期之后发生的事情、发布的论文、更新的法律法规、新出现的工具/框架，模型一概不知。例如一个 2024 年训练的模型无法回答 2025 年的新闻事件。

**为什么存在：**
- 训练一轮大模型耗时数月、耗资数千万美元，"实时更新"在成本和工程上不现实。
- 训练数据需要清洗、去重、配比，无法像搜索引擎一样持续增量更新。

**行业应对策略：**
| 策略 | 思路 |
|------|------|
| RAG | 用外部知识库弥补时效差，新文档入库即可被检索 |
| 搜索增强 | 实时调用搜索引擎，将搜索结果注入 prompt |
| 持续预训练（CPT） | 用新数据做小规模增量预训练，逐步刷新知识 |
| 插件 / Function Calling | 模型调用外部 API 获取实时数据 |

---

### 1.3 领域盲区（Domain Blindness）

**具体表现：**
通用大模型在垂直领域（医疗、法律、金融、企业内部系统）表现不佳——不理解专业术语、不知道内部业务流程、抓不住领域特定的约束条件。

**为什么存在：**
- 预训练语料以公开互联网文本为主，垂直领域的高质量数据占比极低。
- 企业内部数据出于安全与合规原因不会进入公开训练集。
- 领域知识有隐性知识（tacit knowledge），不存在于任何文本中，模型无从学习。

**行业应对策略：**
| 策略 | 思路 |
|------|------|
| RAG | 将企业内部文档/行业标准纳入知识库，按需检索 |
| 领域微调（Domain Fine-tuning） | 用领域语料继续训练模型，内化领域知识 |
| 混合架构 | 通用模型 + 领域小模型 + 规则引擎组合使用 |
| Human-in-the-loop | 关键决策保留人工审核环节 |

---

### 1.4 上下文窗口限制（Context Window Limitation）

**具体表现：**
虽然上下文窗口在不断增长（128K → 1M → 2M tokens），但仍有硬上限。当需要处理大量文档时（如分析一整年的财报、阅读整个代码仓库），无法将所有内容一次性放入上下文。

**为什么存在：**
- Transformer 的注意力机制复杂度为 O(n²)，窗口扩大导致计算量和显存呈平方级增长。
- 即使技术上支持超长上下文，"大海捞针"实验表明模型对长文本中间位置的信息利用率显著下降（Lost in the Middle 现象）。
- 长上下文带来的推理延迟和成本也是工程上的现实约束。

**行业应对策略：**
| 策略 | 思路 |
|------|------|
| RAG | 只检索最相关的片段，不让模型处理全部文档 |
| 上下文压缩 / 摘要 | 先用小模型压缩长文本再送入大模型 |
| 分块处理 | Map-Reduce / ReAct 模式分批处理 |
| KV-Cache 优化 | 工程层面优化长上下文的显存占用与推理速度 |

---

## 2 RAG 如何解决幻觉问题

RAG 解决幻觉的核心逻辑是：**不给模型"编"的机会，而是给它"抄"的材料。**

在没有 RAG 时，模型面对它不知道或不确定的问题，会基于训练时学到的语言分布模式去"生成一个看起来合理的答案"——这就是幻觉的根源。

RAG 的做法是：
1. 先将可靠的、可溯源的知识整理入库；
2. 用户提问时，先检索出与问题最相关的知识；
3. 将检索到的知识作为 prompt 的一部分交给模型，要求模型"根据以下参考资料作答"。

**这样做的效果：** 模型从"闭卷考试"变成了"开卷考试"，它的任务从"记住并复现知识"变为"阅读理解并整合给定材料"。只要知识库质量高、检索结果准确，幻觉就会被大幅压制。

**两个关键前提：**:o:

- **知识库质量要高：** 垃圾进、垃圾出。如果知识库本身有错，RAG 就是在给模型喂错误材料，幻觉问题不会消失，只是从"模型编的"变成"文档写的"。
- **检索要准确：** 检索漏了相关文档，模型得不到必要信息；检索多了无关文档，模型受到噪声干扰。两者都会重新触发模型"编造"的行为。

---

## 3 三种知识注入方案对比

### 3.1 核心思路

| 方案 | 核心思路 |
|------|----------|
| **提示词工程** | 在 prompt 中直接写入知识/规则，不改变模型本身 |
| **微调（Fine-tuning）** | 用领域数据训练模型，将知识内化到模型参数中 |
| **RAG** | 知识留在外部知识库，推理时动态检索注入 |

三种方案的本质区别在于**知识存储的位置**：提示词工程和 RAG 将知识放在模型外部（prompt 中），微调将知识放在模型内部（参数中）。

### 3.2 多维对比

| 维度 | 提示词工程 | RAG | 微调（Fine-tuning） |
|------|-----------|-----|---------------------|
| **知识更新速度** | 即时（改 prompt 文本即可） | 准实时（新文档入库即生效） | 慢（需重新训练，数小时到数天） |
| **成本** | 几乎为零 | 中等（向量数据库 + embedding + 检索） | 高（GPU 算力 + 标注数据 + 人才） |
| **知识容量** | 极小（受上下文窗口限制） | 极大（知识库可无限扩展，仅受存储成本约束） | 有限（模型参数是有限的，知识之间互相竞争容量） |
| **幻觉控制** | 弱（全靠 prompt 约束，模型可能不听） | 强（有明确参照文本约束） | 中等（内化了知识但仍可能编造） |
| **推理延迟** | 无额外延迟 | 有检索延迟（通常 100-500ms）:o: | 无额外延迟 |
| **适用场景** | 少量规则/角色设定/格式约束 | 大量动态知识、需要溯源、快速迭代 | 稳定领域知识、对话风格、特定格式输出 |

### 3.3 为什么会有这些差异

**更新速度的差异：** 提示词和 RAG 操作的是外部的文本，修改文本即生效，路径最短。微调则需要构建数据集 → 训练 → 评估 → 部署，链路上每个环节都有时间成本。

**成本的差异：** 微调需要大量 GPU 算力做反向传播，且高质量标注数据稀缺昂贵。RAG 的成本主要是 embedding 计算和向量检索，这是推理而非训练，算力需求低一个数量级。提示词工程零边际成本。

**知识容量的差异：** 模型参数是固定的（如 7B、70B），知识编码进参数必然面临上限——新知识会"覆盖"旧知识（灾难性遗忘）。外部知识库可以通过加机器横向扩展，理论上无限。

**幻觉控制的差异：** RAG 把知识作为"参考答案"明示给模型，模型做的是阅读理解而非记忆回想，幻觉概率显著降低。微调将知识融入参数分布，模型在低置信度区域仍有编造倾向。提示词工程仅靠文本指令约束，约束力最弱。

### 3.4 经验法则：先 RAG 后微调

**实践中最经济的路径是：**

```
RAG 验证可行性 → 满足需求？ → 是 → 持续优化 RAG，不用微调
                              → 否 → 分析瓶颈 → 引入微调
```

**为什么是这个顺序：**
1. **RAG 试错成本极低。** 加文档、改 chunk 策略、调检索参数都是分钟级操作。微调一次从数据准备到训练完成往往需要数天，且每次调整都要重新训练。
2. **RAG 帮助发现真正需要微调的地方。** 在搭建 RAG 的过程中，你会积累用户真实提问与检索命中情况的日志，这些数据恰好是微调所需的高质量训练样本。
3. **大多数企业场景 RAG + 提示词工程已经够用。** 除非模型对知识的理解程度不够（如医学影像报告生成需要深入的领域推理）、或者输出格式高度定制化（如特定 JSON schema 的 API 生成），否则微调的增量收益往往不如预期。

### 3.5 什么时候应该直接微调

以下情况下 RAG 的收益有限，应该直接考虑微调：
- 需要模型**以特定风格/语气写作**（如某位作家的文风）
- 需要模型**遵循极其复杂的指令格式**，prompt 本身已很长且不稳定
- 领域知识需要**深层理解**而非事实查找（如临床诊断推理）
- **推理延迟**是硬指标，不能接受检索环节

---

## 5 RAG 的核心原理

### 5.1 核心思想

> **模型不需要记住所有知识，只需要在需要时查阅相关知识。**

类比人类的认知过程：一个医生面对罕见病例时，不是凭记忆背诵所有医学文献，而是查阅病例相关的诊疗指南和最新论文，综合判断后给出诊断。RAG 赋予大模型同样的能力——**把"记忆"与"推理"解耦**：记忆由外部知识库负责（可以无限扩展、随时修改），推理由大模型负责（理解问题、整合信息、生成回答）。

### 5.2 三个核心步骤:o:

```
用户提问 → [1. 检索 Retrieve] → [2. 增强 Augment] → [3. 生成 Generate] → 回答
              ↑                       ↑                      ↑
         从知识库中找到          将检索结果与原始       LLM 综合生成
         最相关的文档           问题拼接成完整prompt     最终回答
```

#### 5.2.1 步骤一：检索（Retrieval）:o:

**做什么：** 将用户的问题转化为查询，在知识库中找到语义最相关的文档片段。

**具体过程：**
1. 用户问题向量化（embedding），得到问题向量
2. 在向量数据库中用相似度检索（如余弦相似度）找到 Top-K 个最相关的文档块
3. 可选：用 Rerank 模型对 Top-K 做精排，进一步筛选真正相关的

**优化角度：**
| 环节 | 优化方向 |
|------|----------|
| **文档切块（Chunking）** | 块太大 → 检索精度下降、引入噪声；块太小 → 丢失上下文、语义不完整。常用 512-1024 tokens，按语义边界切分而非硬截断 |
| **Embedding 模型选型** | 通用模型 vs 领域模型，多语言场景需选支持对应语言的模型 |
| **检索策略** | 稀疏检索（BM25）+ 稠密检索（向量）混合，互补长短 |
| **Rerank** | 用更强的交叉编码器模型做精排，提升 Top-N 精度 |
| **查询改写** | 将用户简短/模糊的问题扩展为更精确的检索查询（Query Expansion / HyDE） |
| **多路召回** | 并行检索+关键词检索+知识图谱检索，多路结果融合 |

#### 5.2.2 步骤二：增强（Augmentation）:o::o:

**做什么：** 将检索到的文档片段与原始用户问题组合，构建一份包含"参考资料"的完整 prompt。

**具体过程：**

1. 将检索到的多个文档片段排序、去重
2. 按照模板组织 prompt 结构，典型格式：
   ```
   请根据以下参考资料回答用户问题。
   如果参考资料中没有相关信息，请明确说明。
   
   ## 参考资料
   [文档1] ...
   [文档2] ...
   
   ## 用户问题
   {用户原始问题}
   ```
3. 控制拼接后的总长度不超出模型的上下文窗口

**优化角度：**
| 环节 | 优化方向 |
|------|----------|
| **上下文编排** | 重要文档放最前/最后（利用首尾优先效应），避免"Lost in the Middle" |
| **元数据注入** | 附上文档来源、日期、章节等元信息，帮助模型判断可靠性:o: |
| **长度控制** | 动态调整注入的文档数量，确保总 token 数在窗口安全范围内 |
| **指令设计** | 明确告诉模型"参考资料不足时不要编造"，设置行为边界:o: |
| **引用标注** | 对文档片段编号，要求模型回答时标注引用来源:o: |

#### 5.2.3 步骤三：生成（Generation）

**做什么：** LLM 阅读增强后的 prompt，综合参考资料中的信息，生成最终的回答。

**具体过程：**
1. LLM 理解用户问题，解析参考资料中的相关内容
2. 对多个来源的信息进行综合、对比、推理
3. 生成结构化的自然语言回答，可附带引用标注

**优化角度：**
| 环节 | 优化方向 |
|------|----------|
| **模型选型** | 更强的模型在信息整合和遵循指令方面表现更好，在复杂 RAG 场景差异明显 |
| **流式输出** | 开启 streaming 降低用户感知延迟 |
| **校验机制** | 对生成结果做后处理校验（如事实一致性检查、敏感词过滤）:o: |
| **引用还原** | 将回答中的引用标记映射回原始文档的具体位置 |
| **兜底策略** | "知识库中未找到相关信息"时的降级方案（如转人工、转搜索） |

### 5.3 RAG 架构全景图

```
                         ┌──────────────────────┐
                         │   ① 文档预处理（离线）  │
                         │  PDF/Word/网页 → 解析  │
                         │  → 切块 → Embedding   │
                         │  → 存入向量数据库      │
                         └──────────┬───────────┘
                                    │
                                    ▼
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│ 用户提问  │ ──▶ │ ② 检索    │ ──▶ │ ③ 增强    │ ──▶ │ ④ 生成    │ ──▶ 回答
│          │     │ Query→向量 │     │ Prompt    │     │ LLM推理  │
│          │     │ 向量检索   │     │ 拼接组织  │     │ 引用溯源  │
└──────────┘     │ Rerank精排│     └──────────┘     └──────────┘
                 └──────────┘
```

---

## 6 企业 RAG 的核心应用场景

### 6.1 智能客服 / 帮助中心

**痛点：** 客服团队需要记忆大量产品文档和政策，培训成本高、人员流动大。

**RAG 方案：** 将产品手册、FAQ、工单历史纳入知识库，用户提问时实时检索，模型生成准确的、有据可查的回复。新功能上线只需更新文档，无需重新培训。

**代表性指标：** 首次解决率（FCR）、平均处理时间（AHT）

---

### 6.2 企业知识库问答

**痛点：** 大型企业有海量内部文档（制度、流程、技术方案、会议纪要），员工查找效率低。

**RAG 方案：** 将 Confluence、Notion、 SharePoint 等平台的全部文档向量化入库，员工用自然语言提问即可获得整合后的答案并附上原文链接。

**代表性指标：** 知识查找时间、答案准确率

---

### 6.3 法律 / 合规审查

**痛点：** 律师和法务需要翻阅大量法律文书、判例、合同条款，低效且容易遗漏关键信息。

**RAG 方案：** 将法律法规库、历史判例库、企业合同库向量化，审查时快速检索相关条款和先例，生成分析报告并精确引用条文编号。

**代表性指标：** 审查覆盖率、引用准确率

---

### 6.4 医疗辅助诊断

**痛点：** 医生面对罕见病案例时查阅文献效率低，且最新研究论文持续涌现。

**RAG 方案：** 将医学论文、诊疗指南、药品说明书纳入知识库，辅助医生基于最新证据制定诊疗方案，并溯源至具体文献。

**代表性指标：** 循证支持率、诊断建议一致性

---

### 6.5 代码仓库问答 / 开发者文档

**痛点：** 新人入职需要熟悉庞大的代码库，API 文档分散、示例不完整。

**RAG 方案：** 将代码仓库、API 文档、技术博客统一入库。开发者用自然语言提问（"这个接口为什么返回 500？"），模型检索相关代码和文档后给出分析和修复建议。

**代表性指标：** 问题解决率、onboarding 效率提升

---

### 6.6 电商 / 内容推荐

**痛点：** 商品库和内容库庞大，用户难以通过关键词精准定位。

**RAG 方案：** 商品描述和用户评价向量化，用户用自然语言描述需求（"适合送给女朋友的生日礼物，预算 500 以内"），系统检索匹配商品并生成个性化推荐理由。

**代表性指标：** 点击率（CTR）、转化率（CVR）

---

## 7 总结

| 要点 | 一句话概括 |
|------|-----------|
| 幻觉 | 模型不是故意撒谎，而是在它不确定的地方"概率平权"地编造 |
| RAG 核心思想 | 让模型从"闭卷默写"变成"开卷阅读理解" |
| 先 RAG 后微调 | RAG 试错成本低，先把业务闭环跑通，瓶颈明确了再上微调 |
| 检索是关键 | RAG 的上限由知识库质量决定，下限由检索精度决定 |
| 三大步骤 | 检索（找到对的文档）→ 增强（把文档和问题拼好）→ 生成（综合信息产出回答） |
| 企业落地 | 客服、知识库、法律、医疗、代码、电商——有"大量文档 + 需要精准答案"的地方就有 RAG 的用武之地 |

---

## 8 大厂面试题深度讲解

### 8.1 基础概念与原理类

#### Q1：用自己的话解释什么是 RAG，它解决了什么问题？（字节跳动 / 腾讯 高频）

**面试官考察点：** 是否真正理解 RAG 的动机和本质，而非背诵定义。

**回答思路（三层递进）：**

**第一层——定义：** RAG（Retrieval-Augmented Generation）是一种将"信息检索"与"文本生成"结合的架构。在 LLM 生成回答之前，先从外部知识库中检索与用户问题相关的文档，:o:将这些文档作为"参考资料"注入 prompt:o:，让模型基于参考资料作答。

**第二层——解决的四个核心问题：**
1. **幻觉（Hallucination）：** 从"闭卷默写"变为"开卷阅读理解"，有据可依，编造概率大幅下降。
2. **知识时效性（Knowledge Cutoff）：** 新文档入库即生效，无需重新训练模型。
3. **领域盲区（Domain Blindness）：** 企业内部文档、行业标准可纳入知识库，覆盖垂直领域。
4. **上下文窗口限制：** 只检索最相关的片段注入 prompt，"大海捞针"式的全量灌入。

**第三层——本质理解（加分项）：** RAG 的本质是**将"记忆"与"推理"解耦**。记忆由外部知识库负责（可扩展、可修改），推理由 LLM 负责（理解、整合、生成）。这是一个架构哲学——不是让模型更强，而是给模型更准确的信息。

---

#### Q2：RAG 和微调（Fine-tuning）的本质区别是什么？什么时候用 RAG，什么时候用微调？（阿里 / 美团 高频）

**面试官考察点：** 能否在实际业务中做出正确的技术选型，理解两种方案的 trade-off。

**回答思路：**

**本质区别——知识存储位置：**
- RAG：知识在**模型外部**（向量数据库），推理时动态检索注入。修改知识 = 修改文档，秒级生效。
- 微调：知识在**模型内部**（参数中），训练时编码进权重。修改知识 = 重新训练，天/周级生效。

**选型决策矩阵：**

| 判断维度 | 选 RAG | 选微调 |
|----------|--------|--------|
| 知识更新频率 | 高（周/天级变化） | 低（月/年级稳定） |
| 知识量级 | 海量、持续增长 | 有限、稳定 |
| 溯源需求 | 强（医疗/法律/金融合规） | 弱 |
| 推理延迟要求 | 可接受 100-500ms 检索 | 必须零额外延迟 |
| 输出风格控制 | 不关心 | 需要特定风格/语气 |
| 深层推理需求 | 事实查找型 | 临床诊断/数学证明型 |
| 试错预算 | 低 | 高 |

**关键论点（加分项）：** 实践中 80% 的企业场景，**RAG + 提示词工程已经够用**。建议先 RAG 跑通闭环，积累 bad case 作为微调数据，瓶颈明确了再上微调。这样路径最经济、试错成本最低。

---

#### Q3：为什么 RAG 能缓解幻觉？RAG 能彻底消除幻觉吗？（百度 / 字节跳动）

**面试官考察点：** 对幻觉成因的理解深度，以及是否有批判性思维——不夸大 RAG 的能力。

**回答思路：**

**为什么能缓解：** 幻觉的根源在于模型在不确定时会"概率平权"地编造合理内容。RAG 把任务从"记忆 + 生成"变成"阅读理解 + 生成"，模型只需根据给定材料作答，不需要凭空回忆。这是从"闭卷考试"到"开卷考试"的转变。

**为什么不能彻底消除——四种残留幻觉：**:o:

1. **检索失败导致的幻觉：** 相关知识未命中，模型没有参考材料，仍可能编造。这是 RAG 中最常见的幻觉来源。
2. **知识库错误导致的幻觉：** "垃圾进、垃圾出"。如果知识库文档本身有事实错误，RAG 是在给模型喂错误材料，幻觉从"模型编的"变成了"文档写的"。
3. **模型"不听话"导致的幻觉：** :o:即使给了正确参考资料，模型有时也会忽略 prompt 指令，自由发挥。这在复杂推理任务中尤为常见。
4. **信息整合错误导致的幻觉：** 模型在综合多篇文档信息时，可能进行错误的逻辑拼接——每篇文档本身正确，但模型推导出文档中没有的结论。:o:

**面试官追问："那你怎么应对这些残留幻觉？"**

- 检索层：混合检索 + Rerank + 查询改写，提升命中率。
- Prompt 层：强化指令——"如果参考资料中没有相关信息，请明确说不知道，严禁编造"。
- 后处理层：事实一致性校验（用 NLI 模型比对回答与参考资料的一致性）。
- 工程层：引用溯源 + 高置信度兜底策略（检索分数低于阈值 → 转人工/转搜索）。

---

### 8.2 检索技术深度类

#### Q4：RAG 中的 Embedding 模型是怎么工作的？为什么用向量相似度而不是关键词匹配？（腾讯 / 小红书 高频）

**面试官考察点：** 是否理解语义检索 vs 关键词检索的本质区别。

**回答思路：**

**Embedding 工作原理：**
Embedding 模型将文本映射到高维向量空间（通常 768 或 1024 维），使得**语义相近的文本在向量空间中距离近，语义无关的文本距离远**。具体步骤：:o:

1. 文本经过 tokenizer 切分为 token
2. Token 经过 Transformer 编码器（通常是 BERT 类模型的 encoder），通过自注意力机制融合上下文信息
3. 取 [CLS] token 的最后一层隐状态或对所有 token 做 mean pooling，得到固定维度的稠密向量

**为什么不用纯关键词匹配（BM25）：**

| 问题 | 关键词匹配 | 向量语义匹配 |
|------|-----------|-------------|
| 同义词 | "汽车"搜不到"轿车" | 能匹配 |
| 跨语言 | "machine learning"搜不到"机器学习" | 多语言模型能跨语言 |
| 表述差异 | "怎么删除订单"搜不到"如何取消购买" | 语义相近能匹配 |
| 上下文消歧 | "苹果好吃吗"可能匹配到苹果公司文档 | 通过上下文语义区分 |

**最佳实践（加分项）：** 生产环境建议**混合检索**——BM25 做关键词精准匹配（专有名词、缩写、代码），向量检索做语义泛化。两路结果通过 RRF（Reciprocal Rank Fusion）融合，优势互补。

---

#### Q5：什么是"Lost in the Middle"现象？如何在 RAG 中应对？（字节跳动）

**面试官考察点：** 是否关注最新的 LLM 研究成果，能否将学术发现落地到工程实践。

**回答思路：**

**现象定义：** 这是斯坦福等机构在 2023 年论文《Lost in the Middle: How Language Models Use Long Contexts》中发现的现象——当 LLM 需要在长上下文中定位信息时，对**开头和结尾位置**的信息利用最好，对**中间位置**的信息利用率显著下降。即使模型宣称支持 128K 上下文，中间部分的"可见度"仍然很差。

**核心原因（目前学界共识）：**
1. Transformer 的注意力机制中，中间位置的 token 收到的注意力权重被头尾稀释。:o:
2. 预训练数据中，关键信息通常在开头（导语）和结尾（总结），模型学到了这个偏差。
3. RoPE 等位置编码对中间位置的区分度相对较低。

**RAG 中的应对策略：**:o::o::o:

1. **上下文编排（最直接）：** 将 Rerank 后得分最高的文档放在开头和结尾，中间放得分较低的文档。利用"首尾优先效应"提升关键信息的利用率。
2. **减少注入文档数量：** 不是越多越好。严格控制 Top-K，宁缺毋滥。经过 Rerank 精排后，K=3~5 通常比 K=10+ 效果更好。
3. **文档摘要压缩：** 对长文档先生成摘要，注入摘要而非全文，减少总长度。
4. **分块递归处理（Map-Reduce）：** 将多篇文档分批，每批独立让模型回答，再综合各批的回答。
5. **重排序策略：** 不只按相似度排序，可以按"问题-文档"相关性、文档权威性、时效性等多维排序。

---

#### Q6：为什么要用 Rerank 模型？它和 Embedding 检索有什么不同？（阿里巴巴）

**面试官考察点：** 是否理解两阶段检索的必要性和各自的原理。

**回答思路：**

**为什么需要 Rerank：**
Embedding 检索（一阶段召回）用的是**双塔模型**——query 和 document 分别独立编码为向量，检索时只做向量内积/余弦相似度。这种"离线编码、在线匹配"的设计牺牲了精度换取了速度——query 和 document 之间没有 token 级别的交叉注意力:o:，无法捕捉精细的语义匹配关系。:o:

Rerank（二阶段精排）用的是**交叉编码器（Cross-Encoder）**——将 query 和 document **拼接后一起输入模型**，通过全自注意力机制让每个 token 互相 attend，得到的是逐 token 级别的语义匹配分数:o::o:。精度远高于双塔，但计算成本高，不能对全库做。

**两阶段架构的设计哲学：**

```
全量文档（百万级）
    │
    ▼  Embedding 检索（双塔，速度快，召回率高）
    │   从百万 → Top-100
    │
    ▼  Rerank 精排（交叉编码器，精度高，速度慢）
    │   从 Top-100 → Top-5
    │
    ▼  注入 LLM
```

**关键数据（加分项）：** 一阶段向量召回通常能保证召回率（Recall@100 > 95%），但精度低（Precision@5 可能只有 60-70%）。Rerank 能把 Precision@5 提升到 90%+，大幅减少注入 LLM 的噪声。

---

#### Q7：文档切块（Chunking）有哪些策略？怎么选择？（美团 / 百度）

**面试官考察点：** 工程落地经验——切块策略直接影响 RAG 效果，是面试中的高频考点。

**回答思路（从原理到实践）：**

**切块的核心矛盾：**
- 块太大 → 包含的语义信息多但噪声也多，检索精度下降（一个块可能涉及多个话题）
- 块太小 → 语义不完整、上下文丢失，检索可能命中但模型看到的信息不足以回答问题

**五种常见策略：**

| 策略 | 做法 | 适用场景 |
|------|------|----------|
| **固定长度切分** | 按 token 数硬截断（如 512 tokens） | 简单场景，快速原型 |
| **句子边界切分** | 按句号/换行等自然边界切分，尽量不被断句 | 通用文本 |
| **语义切分** | 用 Embedding 计算相邻句子的相似度，在语义"断层"处切分 | 多主题混合文档 |
| **结构切分** | 按 Markdown 标题层级 / HTML 标签 / 表格行 切分 | 结构化文档（技术文档、API文档） |
| **小块 + 上下文窗口（Sentence Window）** | 检索时用小粒度块提高精度，生成时取该块前后各 N 句扩展上下文 | 兼顾检索精度和生成质量（LlamaIndex 推荐方案） |

**面试官追问："句子窗口（Sentence Window）具体怎么做，为什么要这样设计？"**:o:

这是一个巧妙的解耦设计：**检索用小粒度，生成用大粒度**。具体做法：
1. 文档按句子切分为小块（如每块 1-2 句）
2. Embedding 时对这些小块做向量化（检索精度高）
3. 检索到某个小块后，生成时取出该块前后各 K 句一并送入 prompt（保证语义完整）
4. 索引中只存小块向量，上下文的扩展在生成阶段动态拼接

**工程实践建议（加分项）：**
- 通用文本：512 tokens 是一个经过大量验证的"甜点"值。
- 技术文档/代码：建议结构切分，按函数/类边界拆分。
- 一定要保留 **Overlap（重叠窗口）**——相邻块之间重叠 10-20%，防止关键信息恰好在切分边界被截断。
- 元数据保持：chunk 时附上文档标题、章节、页码等元信息，方便生成阶段引用溯源。

---

### 8.3 架构设计与系统设计类

#### Q8：设计一个企业级的 RAG 系统，你会考虑哪些模块？（字节跳动 / 腾讯 系统设计面试）

**面试官考察点：** 系统设计能力，能否从工程视角做端到端的设计。

**回答框架（从离线到在线、从数据到展示）：**

```
┌─────────────────────────────────────────────────────────┐
│                     离线数据处理管线                       │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌────────┐ │
│  │ 文档解析   │──▶│ 文档切块   │──▶│ Embedding │──▶│向量数据库│ │
│  │ PDF/Word  │   │ Chunking │   │ 向量化    │   │  存储   │ │
│  │ HTML/MD   │   │ +元数据   │   │          │   │        │ │
│  └──────────┘   └──────────┘   └──────────┘   └────────┘ │
└─────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────┐
│                     在线推理管线                          │
│                                                         │
│  用户问题                                               │
│     │                                                   │
│     ▼                                                   │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐            │
│  │ 查询改写   │──▶│ 混合检索   │──▶│ Rerank   │            │
│  │ 意图识别   │   │向量+关键词 │   │ 精排     │            │
│  │ 多轮改写   │   │  +过滤    │   │          │            │
│  └──────────┘   └──────────┘   └──────────┘            │
│                                       │                 │
│                                       ▼                 │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐            │
│  │ Prompt    │◀──│ 上下文    │◀──│ 过滤/去重 │            │
│  │ 构建      │   │ 编排      │   │          │            │
│  └──────────┘   └──────────┘   └──────────┘            │
│        │                                                │
│        ▼                                                │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐            │
│  │ LLM 生成  │──▶│ 后处理    │──▶│ 返回用户   │            │
│  │ Streaming│   │ 事实校验  │   │ +引用溯源  │            │
│  └──────────┘   └──────────┘   └──────────┘            │
└─────────────────────────────────────────────────────────┘
```

**各模块的设计要点：**

**1. 文档解析层：**
- 支持多格式：PDF（含扫描版 OCR）、Word、HTML、Markdown、图片（多模态场景）
- 难点：PDF 表格、列表、分栏布局的正确解析；扫描件 OCR 质量
- 工具选型：PyMuPDF、Unstructured、LlamaParse

**2. 切块与 Embedding 层：**
- 切块策略按文档类型自适应（结构化文档用结构切分，非结构化用语义切分）
- Embedding 模型选型考虑语言（中英文）、领域（通用 vs 垂类）、维度（trade-off 精度和成本）
- 多向量索引：一个文档块可生成多个向量（摘要向量、关键词向量、原文向量）

**3. 存储层——向量数据库选型：**

| 数据库 | 特点 | 适用规模 |
|--------|------|----------|
| Milvus | 分布式、高可用、支持万亿级 | 大型企业 |
| Qdrant | Rust 实现、性能高、过滤能力强 | 中等规模 |
| Weaviate | 自带向量化 + 混合检索 | 快速原型 |
| Chroma | 轻量、Python 原生 | 开发测试 |
| Elasticsearch | 支持向量 + 全文搜索一体 | 已有 ES 基础设施的团队 |

**4. 检索层——核心优化点：**
- **查询改写（Query Rewriting）：** 用户问题通常简短、口语化，需要 LLM 将其改写为标准检索查询。这是很多团队忽视但效果显著的优化点。
- ***多轮对话上下文：** 用户说"它和 X 比呢？"，需要把上一轮的实体代入，生成完整的检索查询*。
- **混合检索 + RRF 融合：** BM25 抓专有名词/缩写，向量检索抓语义，融合后去重。
- **元数据过滤：** 利用文档标签（日期、部门、类型）做前置过滤，缩小检索范围。

**5. 生成层——关键设计：**

- Prompt 模板化管理，支持针对不同场景（客服/法务/代码）使用不同的 system prompt
- 引用标注：要求模型用 [1] [2] 标注引用来源，后续映射回原始文档
- 兜底策略：检索分数低 → 回复"未找到相关信息，建议联系 XX"
- 流式输出（SSE）：降低用户感知延迟

**6. 评估与监控（加分项，必问）：**
- 离线评估：RAGAS（忠实度、答案相关性、上下文精度、上下文召回）
- 在线监控：检索延迟 P50/P99、检索命中率、用户点赞/点踩率
- 反馈闭环：用户点踩的 case 自动进入 bad case 库，用于迭代优化

---

#### Q9：多轮对话场景下，RAG 怎么处理？（腾讯 / 快手）:o::o::o:

**面试官考察点：** 实际业务中多轮对话是标配，单轮 RAG 直接上多轮会有明显问题。

**回答思路：**

**多轮 RAG 的三个核心挑战：**

**挑战 1——指代消解（Coreference Resolution）：**
用户说"它的性能和 X 比怎么样？"，"它"指代上一轮提到的某个产品/方案。单轮 RAG 直接把这个原问题做检索，必然失败。

**解决方案：** 查询改写（Query Rewriting / Contextualized Query）

- 将历史对话上下文 + 当前问题发给 LLM，让 LLM 生成一个**独立的、完整的检索查询**。
- 示例：
  - 用户第一轮："介绍一下 Milvus 向量数据库"
  - 用户第二轮："它和 Qdrant 比呢？"
  - 改写后："Milvus 向量数据库和 Qdrant 向量数据库的对比"
- 查询改写本身是轻量操作（通常用 GPT-3.5 级别即可），延迟增加 < 200ms。

**挑战 2——上下文窗口抢占：**
多轮对话的历史消息 + 每轮检索的文档，总 token 数迅速膨胀。如果不加控制，几轮后就会超出上下文窗口，或者因"Lost in the Middle"导致信息利用率下降。

**解决方案：**

- 历史对话摘要：每隔 K 轮对历史做一次摘要压缩，保留关键实体和结论，丢弃冗余。
- 滑动窗口：只保留最近 N 轮完整对话，更早的用摘要替代。
- 每轮动态控制注入文档数：随着对话深入，问题越来越聚焦，可以逐渐减少 Top-K。

**挑战 3——检索策略的阶段性差异：**
多轮对话中，用户首轮通常是探索式提问（"我想了解 XX"），后续轮次是聚焦追问（"XX 的具体参数是多少？"）。首轮需要高召回（宁可多召回不可漏），后续轮需要高精度（精准定位细节）。

**解决方案：** 根据对话轮次动态调整检索参数——首轮 Top-K 较大、Rerank 宽松；后续轮次 Top-K 减小、Rerank 严格，甚至可以加入关键词权重提升。

---

### 8.4 高级话题与前沿类

#### Q10：什么是 Self-RAG？它和传统 RAG 有什么区别？（百度 / 阿里 前沿考察）:o::o:

**面试官考察点：** 是否跟踪前沿论文，能否理解新范式的动机和设计思路。

**回答思路：**

**传统 RAG 的问题：** "不管需不需要检索都一律检索，不管检索到什么都无条件注入"。这带来三个问题：

1. 有些简单问题（"你好"、"1+1=?"）不需要检索，检索反而引入噪声和延迟。
2. 检索结果可能不相关，但传统 RAG 仍会注入，导致模型被误导。
3. 没有对检索质量的自检机制。

**Self-RAG 的核心思想：** 让 LLM 通过特殊的**反思 token（Reflection Tokens）**，在生成过程中自主决定：

- **Retrieve：** 是否需要检索（是/否）
- **ISREL：** 检索到的文档是否与问题相关（相关/不相关）
- **ISSUP：** 生成的内容是否有检索文档作为支撑（有支撑/无支撑）
- **ISUSE：** 生成的回答整体是否有用（有用/无用）

**训练方式：** 用 GPT-4 生成带有反思 token 的训练数据，然后用这些数据微调 Llama 等开源模型，让模型学会在不同的解码步骤中输出反思 token。

**回答话术：** "Self-RAG 的核心贡献是把'是否需要检索'和'检索结果是否有用'这两个决策也交给模型自主判断，而非在外部规则中写死。这让 RAG 从'强制检索'进化为'按需检索 + 自检'，尤其适合需要多步推理的复杂 QA 场景。如果你面试的是搜索/问答团队，Self-RAG 是一个很好的深度话题。"

---

#### Q11：Graph RAG 的原理是什么？它解决了什么问题？（微软 / 阿里 高频前沿题）:o:

**面试官考察点：** 2024 年下半年最热的 RAG 话题，对前沿方案的掌握程度。

**回答思路：**

**传统 RAG 的致命缺陷（引出动机）：** 传统 RAG 处理的是**向量相似度 = 主题相关性**。但很多问题需要的是**多跳推理（Multi-hop Reasoning）**——信息分散在多篇文档中，需要通过实体关系串联才能回答。例如"写过《深度学习》的作者还在哪所大学任教？"，传统 RAG 可能分别检索到"《深度学习》的作者是 A"和"A 在 B 大学任教"两段信息，但无法将它们串联起来。

**Graph RAG 的核心思路：**

1. **知识图谱构建（离线）：** 用 LLM 从文档中抽取实体和关系，构建知识图谱。例如从文本"张三于 2020 年加入阿里巴巴"中抽取三元组 (张三, 任职于, 阿里巴巴, 2020)。
2. **社区检测（Graph RAG 特有，核心创新）：** 用 Leiden 算法对知识图谱做社区检测，将紧密关联的实体聚合成"社区"。每个社区内的实体和关系构成一个"主题单元"。
3. **社区摘要（关键步骤）：** 对每个社区，用 LLM 生成摘要描述。摘要本质是将图结构信息"文本化"——把实体间的复杂关系用自然语言概括出来。
4. **检索 + 生成：** 用户提问时，检索相关社区摘要，将摘要作为上下文注入 LLM 生成回答。

**Graph RAG vs 传统 RAG：**

| 维度 | 传统 RAG | Graph RAG |
|------|---------|-----------|
| 知识组织 | 扁平向量空间 | 图结构（实体 + 关系） |
| 适用问题 | 单跳事实查询 | 多跳推理、全局总结 |
| 计算成本 | 低（embedding + 检索） | 高（图构建 + 社区检测 + 摘要生成） |
| 索引构建 | 分钟级 | 小时级（需 LLM 大量调用） |
| 代表框架 | LangChain / LlamaIndex | 微软 GraphRAG |

**面试官追问："Graph RAG 的局限性是什么？"**
1. **构建成本极高：** 实体抽取、关系抽取、社区摘要都依赖 LLM 调用，百万级文档的图构建成本可能达到数万美元。
2. **增量更新困难：** 新文档入库需要重新做实体链接、可能触发社区重划分、需要重生成摘要。传统 RAG 只需对新文档做 embedding。
3. **中文实体抽取难度高：** 中文分词和 NER 本身就有歧义，实体对齐（同一个实体在不同文档中的不同称呼）是额外挑战。
4. **不是所有场景都需要：** 如果业务主要是单跳事实查询（如客服 FAQ），Graph RAG 的投资回报率很低。

**适用场景：** "数据集的全局理解"类问题（"这份财报的核心主题有哪些？"、"这家公司的业务板块如何划分？"），多跳推理类问题，以及需要探索实体间隐蔽关联的场景。

---

#### Q12：RAG 系统的常见 Failure Mode 有哪些？分别如何应对？（综合考察，所有大厂通用）

**面试官考察点：** 是否有生产环境实战经验。能说出 failure mode 说明真正上手做过。

**七大 Failure Mode 与应对策略：**

| # | Failure Mode | 表现 | 根因 | 应对策略 |
|---|-------------|------|------|----------|
| 1 | **检索漏召** | 知识库有答案但没检索到 | 问题短、口语化、与文档表述差异大 | 查询改写 + HyDE + 多路召回 + 同义词扩展 |
| 2 | **检索误召** | 检索到文档但不相关 | Embedding 模型在特定领域区分度不足 | Rerank 精排 + 相似度阈值过滤 + 领域 Embedding |
| 3 | **上下文噪声** | 注入太多"看似相关实则无用"的文档 | Top-K 太大、去重不足 | 减小 Top-K + Rerank + 文档去重（MinHash/LSH） |
| 4 | **切分破坏语义** | 答案恰好被切在 chunk 边界，信息分裂 | Chunk 策略粗放、无 overlap | 增加 overlap + 句子窗口 + 语义切分替代固定切分 |
| 5 | **模型忽略参考** | 给了正确文档但模型按自己"记忆"回答 | Prompt 约束力不足、模型先验过强 | 强化 Prompt 指令 + 用更强的模型 + 事实一致性校验 |
| 6 | **幻觉引用** | 回答中标了引用号但原文没有对应信息 | 模型"迎合"引用格式要求而编造 | 后处理做引用还原校验、NLI 交叉验证 |
| 7 | **多模态缺失** | 文档有表格/图片中的关键信息，但被当纯文本处理 | 解析层只处理文字 | 多模态解析（表格识别、OCR、图像描述）+ 多向量索引 |

**面试官追问（高阶）："你有遇到过一个让你印象深刻的线上 RAG 问题吗？你怎么排查和解决的？"**

这是一个行为面试题，考察排查能力。回答结构建议：STAR 法则（Situation → Task → Action → Result）。

**参考回答框架：**
"Situation：我们有一个企业文档问答系统，用户反馈某个问题明明手册里有答案，系统却返回'未找到'。Task：需要排查是检索问题还是生成问题。Action：我在日志中发现检索阶段该文档的相似度分数为 0.72，排名 Top-8，但我们设置的注入阈值是 Top-5，所以被截断了。进一步分析发现，用户问题用的是口语化表述（'那个报销要什么材料'），而文档标题是'差旅费用报销申请流程与附件清单'，两者语义距离较远。于是我做了三个优化——1）引入查询改写，将口语问题标准化；2）将 Top-K 从 5 调至 10 并加 Rerank 做精排；3）在 Embedding 阶段同时用文档标题和内容的拼接作为 embedding 输入。优化后该类型问题的命中率从 72% 提升到 94%。Result：用户满意度提升，工单量下降。"

---

#### Q13：RAG 系统中如何处理表格和图片？（多模态 RAG）（字节跳动 / 阿里）

**面试官考察点：** 多模态 RAG 是 2025 年重点方向，考察对非纯文本场景的处理能力。

**回答思路：**

**表格处理：**
- 问题：PDF/HTML 中的表格被当纯文本抽取后结构丢失，语义关系被破坏。

  :o::o::o::o::o:

- 方案一（轻量）：使用支持表格解析的文档加载器（如 Unstructured、LlamaParse），保留行列结构，将表格转为 Markdown 表格或 CSV 格式再 embedding。

- 方案二（精准）：每个表格作为一个独立的 chunk，单独做 embedding，检索时表格和文本独立召回，再统一注入 prompt。

- 方案三（表格 QA 专用）：使用 Table Transformer 类模型（如 TAPAS），让模型直接在表格上做推理，而非将表格转为文本。

**图片处理：**
- 问题：技术文档中的架构图、产品手册中的截图含有关键信息，纯文本 RAG 完全忽略。
- 方案一（Caption 方案）：用多模态模型（如 GPT-4V、Qwen-VL）对每张图生成文本描述，将描述做 embedding 加入索引。检索时命中描述，生成时将图片 URL 和描述一起注入 prompt。
- 方案二（多向量方案）：一个文档块同时存储文本向量和图片向量，两路独立检索后融合。
- 方案三（端到端多模态）：使用支持图片输入的 VLM 做生成——检索到的图片直接作为图片 URL 注入 prompt（而非转为文本描述）。模型直接"看图说话"，信息损失最小。

**工程挑战：**
- 图片描述生成成本高（每张图一次 VLM 调用），大规模文档库需要预计算。
- 多模态 Embedding 模型（如 CLIP、Chinese-CLIP）的中文支持仍在快速迭代中。
- 检索融合策略：文本和图片的相似度分数不在同一分布空间，直接比较无意义，需要归一化或分别排序后融合。

---

### 8.5 评估与优化类

#### Q14：如何评估一个 RAG 系统的好坏？（所有大厂通用）

**面试官考察点：** 评估是工程落地的重要环节，必须有量化指标才能做迭代优化。

**回答框架（三层评估体系）：**

**第一层——检索评估（离线可计算、无需 LLM）：**:o::o::o::o::o:

| 指标 | 定义 | 说明 |
|------|------|------|
| Recall@K | Top-K 结果中包含正确答案的比例 | 评价召回能力，K 通常取 5/10/20 |
| Precision@K | Top-K 结果中相关文档的比例 | 评价精度 |
| MRR（Mean Reciprocal Rank） | 第一个相关文档排名的倒数均值 | 评价排序质量，第一个正确答案排得越前越好 |
| NDCG@K | 归一化折损累计增益 | 评价排序质量，考虑相关度分级 |

**第二层——生成评估（需要 LLM 或人工标注）：**:o:

| 指标 | 定义 | 评估方式 |
|------|------|----------|
| **忠实度（Faithfulness）** | 回答中的每句话是否都可以从检索文档中推导 | RAGAS 框架，用 LLM 将回答拆解为原子命题，逐一验证 |
| **答案相关性（Answer Relevance）** | 回答是否紧扣用户问题 | LLM 根据回答反向生成问题，计算与原始问题的相似度 |
| **上下文精度（Context Precision）** | 检索到的文档中，相关文档的排序质量 | RAGAS 框架 |
| **上下文召回（Context Recall）** | 参考答案所需的信息在检索结果中的覆盖程度 | 需要标注 ground truth |

**第三层——线上业务指标（最终金标准）：**

| 指标 | 说明 |
|------|------|
| 用户满意度（点赞率/点踩率） | 直接反馈 |
| 问题解决率 | 用户是否因同一问题再次提问 |
| 首次响应时间 / 完整回答时间 | 延迟体验 |
| 转人工率 | RAG 无法回答时的兜底 |
| A/B 测试指标 | 对比有无 RAG 的业务指标差异（如客服场景的 AHT） |

**评估框架工具：**
- RAGAS（最主流）：自动评估忠实度、答案相关性、上下文精度、上下文召回
- TruLens：提供 RAG 应用的评估和追踪
- LangSmith（LangChain 生态）：全链路追踪 + 评估
- DeepEval：类似 RAGAS，支持指标更丰富

**面试官追问："如果没有 ground truth 标注数据，怎么评估？"**
这是一个非常现实的工程问题。答法：

1. 先用 LLM 自动生成 QA 对作为"伪 ground truth"——把文档按段落发给 LLM，让它生成（问题，答案）对。用这些自动生成的 QA 做初步评估。
2. 从线上日志抽样，让人工标注 100-200 条作为评估集。量不需要大，但要有代表性。
3. 用 RAGAS 的 LLM-as-judge 方式做无标注评估——让 GPT-4 充当裁判，判断回答的忠实度和相关性。其局限是 LLM 作为裁判本身有偏差，需要人工抽检校准。

---

#### Q15：如何优化 RAG 系统的延迟？（字节跳动 / 拼多多 性能优化题）

**面试官考察点：** 工程优化能力，延迟直接影响用户体验。

**延迟分解与优化策略：**

```
总延迟 ≈ T_query_rewriting + T_embedding + T_vector_search + T_rerank + T_llm_generation
         ~200ms            ~50ms        ~10-50ms          ~100-300ms   ~1-5s
```

**各环节优化：**

| 环节 | 优化策略 | 典型收益 |
|------|----------|----------|
| **查询改写** | 用轻量模型（如 GPT-3.5/Qwen-7B）而非 GPT-4；热点问题缓存改写结果 | -50% |
| **Embedding** | 批量推理替代单条推理；用 ONNX/TensorRT 加速；选更小维度的模型（384 vs 1024） | -30~50% |
| **向量检索** | 索引分片（Partition）+ 近似最近邻（ANN）而非精确搜索；合适的索引类型（HNSW 精度高、IVF 速度快） | 从百毫秒到 < 10ms |
| **Rerank** | 用轻量级交叉编码器（如 bge-reranker-base 而非 large）；减少 Rerank 的候选数（Top-20 而非 Top-100） | -50% |
| **LLM 生成** | 流式输出感知优化（首个 token 出现即开始展示）；KV-Cache 复用；模型量化（INT8/INT4） | 感知延迟 -50% |
| **工程架构** | 查询改写与 Embedding 并行；预加载热点文档的向量；结果缓存（Redis，TTL 按知识更新频率设置） | 热点问题 0ms 检索 |

**关键论点：** 延迟优化的核心哲学是**"各环节并行化 + 用更轻的模型做前置环节"**。另外，首 token 延迟（TTFT）比完整回答延迟（TPOT）对用户体验影响更大——优先优化 TTFT。

---

### 8.6 场景分析实战题

#### Q16：如果让你从零搭建一个"公司内部制度问答"RAG 系统，你会怎么做？（华为 / 腾讯 综合性开放题）

**面试官考察点：** 端到端的工程落地思维，从调研到上线的完整链路。

**回答框架（分阶段阐述）：**

**Phase 1——需求调研与数据盘点（1-2 天）：**
- 盘点文档：制度文件、流程手册、FAQ、历史工单，各多少篇、什么格式、更新频率如何
- 明确用户画像：全员使用 or 特定部门？日活预期？典型问题长什么样？
- 确定验收标准：准确率目标（如 > 85%）、延迟目标（如 < 3s）

**Phase 2——MVP 搭建（1 周）：**
- 文档解析 + 切块 + Embedding + 向量库入库（选 Chroma 或 Qdrant 快速启动）
- 基础检索管线：向量检索 Top-10 + 简单 prompt 模板 + LLM 生成
- 内部灰度测试 5-10 人，收集反馈

**Phase 3——迭代优化（2-4 周）：**
- Bad Case 驱动优化（最重要）：
  - 检索失败的 → 加查询改写、调 chunk 策略
  - 答案不准确的 → 加 Rerank、优化 prompt 模板
  - 不知道的乱编 → 加强化"不知道就说不"的指令 + 兜底策略
- 评估体系建立：RAGAS 离线评估 + 用户反馈在线收集
- 多轮对话支持：查询改写 + 历史缓存

**Phase 4——生产化（1-2 周）：**
- 权限控制：不同部门只能检索自己部门的文档（元数据过滤）
- 监控告警：检索延迟、LLM 错误率、空结果率
- 文档更新管线：文档变更自动触发重新 embedding 入库
- A/B 测试框架：对比新旧策略的效果差异

**关键论点（加分项）：** 强调"从业务出发而非技术炫技"——"我会先用最简单的方案跑通全流程，然后根据实际 bad case 逐一定位瓶颈。大部分场景一个简单的向量检索 + 基础 prompt 就能解决 70% 的问题。剩下的 30% 才是真正需要花功夫的地方。"

---

## 9 面试高频知识点速查

### 9.1 一句话回答系列（用于面试热身环节）

| 问题 | 一句话答案 |
|------|-----------|
| RAG 是什么？ | 检索增强生成——先从知识库检索相关信息，再让 LLM 基于检索结果生成回答 |
| RAG 的三个步骤？ | 检索（Retrieve）→ 增强（Augment）→ 生成（Generate） |
| 为什么需要 RAG？ | 解决 LLM 的幻觉、知识截止、领域盲区、上下文窗口限制四大问题 |
| RAG 和微调选哪个？ | 80% 场景先 RAG，验证瓶颈后再考虑微调 |
| Embedding 的作用？ | 将文本映射为向量，使得语义相近的文本在向量空间中距离近 |
| 为什么需要 Rerank？ | Embedding 检索是粗略召回（快但精度低），Rerank 做精细排序（慢但精度高） |
| Chunk Size 怎么选？ | 通用场景 512 tokens，具体需按文档类型和任务调整 |
| 向量数据库用什么？ | 大厂用 Milvus，中小团队用 Qdrant，快速原型用 Chroma |
| RAG 能彻底消除幻觉吗？ | 不能。检索失败、知识库错误、模型不听话、信息整合错误都会导致残留幻觉 |
| 多轮对话 RAG 的关键挑战？ | 指代消解、上下文窗口抢占、检索策略阶段性差异 |

### 9.2 必须知道的关键技术名词

| 名词 | 一句话解释 | 出现在哪 |
|------|-----------|----------|
| **HyDE** | 假设文档嵌入——先让 LLM 生成一个假设答案，再拿假设答案的向量去检索 | 查询改写 |
| **RRF** | 倒数排序融合——不依赖绝对分数，只用排名做多路结果融合 | 混合检索 |
| **Self-RAG** | 模型通过反思 token 自主决定是否需要检索、检索结果是否相关 | 前沿架构 |
| **Graph RAG** | 将文档转为知识图谱，按社区检测组织知识，回答需多跳推理的问题 | 前沿架构 |
| **Lost in the Middle** | LLM 对长文本中间位置的信息利用率远低于开头和结尾 | 上下文编排 |
| **Sentence Window** | 检索时用小粒度块提高精度，生成时取该块前后扩展保证语义完整 | Chunking |
| **RAGAS** | RAG 系统自动评估框架，评估忠实度、相关性、上下文精度和召回 | 评估 |
| **Reranker / Cross-Encoder** | 将 query 和 doc 拼接后共同编码的模型，精度远超双塔但速度慢 | 精排 |
| **Streaming / SSE** | 流式输出，LLM 逐 token 返回而非全量生成完再返回 | 延迟优化 |

---

