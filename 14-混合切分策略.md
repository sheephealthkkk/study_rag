## 二十一、混合切分策略 —— 取各家长处

前一章我们逐个讲解了 9 种切分器。在实际生产中，**单一策略几乎无法胜任复杂场景**。混合策略才是生产环境的标准做法——结合结构切分的层级感知、语义切分的话题识别、以及元数据的附加信息，构建多粒度、可溯源、可过滤的索引体系。

### 21.1 为什么需要混合策略

每种切分器解决了特定的问题，也带来了特定的局限：

| 单一策略 | 解决了什么问题 | 带来了什么局限 |
|----------|-------------|-------------|
| MarkdownNodeParser（层级） | 按标题切分，保持文档层级 | 依赖格式规范；某章节过长时一个大 chunk 包含太多子话题，检索时语义稀释 |
| SemanticSplitter（语义） | 按话题切换切分，不依赖格式 | 没有文档标题层级信息；不知道 chunk A 和 chunk B 属于同一章节 |
| SentenceSplitter（句子） | 按句子边界切分，通用性强 | 没有层级结构；没有话题感知——两个完全不同的章节可能被拼到同一个 chunk |
| sentence-window（窗口） | 检索精度极高 | Node 数量爆炸；没有章节归属信息 |

**混合策略的核心思想：让每种切分器做它最擅长的事，然后组合结果。**

---

### 21.2 混合策略一：层级 + 语义双层切分

这是生产环境中最成熟、效果最稳定的组合方案。核心逻辑是 **"层级保证结构完整性，语义保证话题精准度"**。

#### 21.2.1 整体架构

```
═══════════════════════════════════════════════════════════════════════════
              层级 + 语义双层混合切分架构
═══════════════════════════════════════════════════════════════════════════

  原始 Markdown 文档:
  ┌─────────────────────────────────────────────────────────────┐
  │ # 员工手册                                                    │
  │                                                             │
  │ ## 第一章 考勤制度                                             │
  │ ### 1.1 工作时间                                             │
  │ 公司实行弹性工作制。核心工作时间10:00-17:00。                     │
  │ ### 1.2 打卡规定                                             │
  │ 使用企业微信打卡，每月补卡不超过3次。                            │
  │ ### 1.3 迟到与旷工                                           │
  │ 迟到30分钟内不扣工资。超过1小时按旷工处理。                       │
  │                                                             │
  │ ## 第二章 请假制度                                             │
  │ ### 2.1 事假                                                 │
  │ 事假需提前1个工作日申请。期间不发放工资。                          │
  │ ### 2.2 病假                                                 │
  │ 病假应在8:30前通知。连续超2天需医院证明。                         │
  │                                                             │
  │ ## 第三章 费用报销                                             │
  │ ### 3.1 住宿标准                                             │
  │ 一线城市不超过500元/天，二线城市不超过350元/天。                   │
  │ ### 3.2 餐饮补贴                                             │
  │ 出差期间每日餐饮补贴80元。                                      │
  └─────────────────────────────────────────────────────────────┘

  ═══════════════════  第一层：层级切分  ═══════════════════
  (MarkdownNodeParser 或类似的按#标题层级切分的策略)

  目的：保留文档的结构骨架。每个 ## 标题作为一个"父 Node"。
  
  第 1 层输出（父 Node）:
  ┌─────────────────────────────────────────────────────────┐
  │ ParentNode[0]: ## 第一章 考勤制度                        │
  │   metadata: {heading_level: 2, md_path: "员工手册/第一章"}│
  │   text: "### 1.1 工作时间\n...\n### 1.2 打卡规定\n..."  │
  │                                                         │
  │ ParentNode[1]: ## 第二章 请假制度                        │
  │   metadata: {heading_level: 2, md_path: "员工手册/第二章"}│
  │   text: "### 2.1 事假\n...\n### 2.2 病假\n..."          │
  │                                                         │
  │ ParentNode[2]: ## 第三章 费用报销                        │
  │   metadata: {heading_level: 2, md_path: "员工手册/第三章"}│
  │   text: "### 3.1 住宿标准\n...\n### 3.2 餐饮补贴\n..."  │
  └─────────────────────────────────────────────────────────┘

  ═══════════════════  第二层：语义切分  ═══════════════════
  (对每个父 Node 的内容用 SemanticSplitter 按话题边界再切)

  目的：在结构的基础上，进一步按语义话题细分。

  以 ParentNode[0] 为例:
  ┌─────────────────────────────────────────────────────────┐
  │ 计算相邻句子的 Embedding 相似度：                          │
  │   "弹性工作制。核心时间10:00-17:00。" → cos=0.94 → "打卡规定"  │
  │   (同属 1.1 内部，不切)                                    │
  │                                                         │
  │   "每月补卡不超过3次。" → cos=0.54 → "迟到30分钟内不扣工资"    │
  │   (1.2→1.3 话题从"打卡"切换到"迟到"，相似度下降)            │
  │                                    ↑ 在此处切分！          │
  │                                                         │
  │ 第 2 层输出:                                              │
  │   ChildNode[0-0]: "### 1.1 工作时间\n弹性工作制，核心..."  │
  │     metadata: {heading_level: 3, parent_id: ParentNode[0]}│
  │   ChildNode[0-1]: "### 1.2 打卡规定\n使用企业微信打卡..."  │
  │     metadata: {heading_level: 3, parent_id: ParentNode[0]}│
  │   ChildNode[0-2]: "### 1.3 迟到与旷工\n迟到30分钟内不扣..." │
  │     metadata: {heading_level: 3, parent_id: ParentNode[0]}│
  └─────────────────────────────────────────────────────────┘

  ═══════════════════  检索时的工作流  ═══════════════════

  Step 1: 用子 Node（细粒度）做向量检索
  Step 2: 检索到 ChildNode[0-2] "迟到30分钟内不扣..."
             → 通过 PARENT 关系追溯到 ParentNode[0] "第一章 考勤制度"
             → 通过 CHILD 关系拿到 ParentNode[0] 所有子 Node
             → 同时提交 ParentNode[0] 的完整内容给 LLM
  Step 3: LLM 看到的不仅是"迟到30分钟不扣工资"，还有
            "弹性工作制""打卡规定""旷工半日"等上下文

═══════════════════════════════════════════════════════════════════════
```

#### 21.2.2 完整代码实现

```python
"""
═══════════════════════════════════════════════════════════════════════════
  混合策略一：层级切分 + 语义切分 + 父子关系保留
═══════════════════════════════════════════════════════════════════════════

  核心思想:
    第一层: MarkdownNodeParser 按 ## 标题切分 → 得到章节级父 Node
    第二层: 对每个父 Node，用 SentenceSplitter 按语义边界再切分 → 得到段落级子 Node
    索引:   对子 Node 建向量索引（精准检索）
    生成:   检索到子 Node 后 → 追溯父 Node → 拿到完整章节上下文

  最终效果:
    检索精度 ↑ (子 Node 粒度细，单一话题，语义信号强)
    上下文完整 ↑ (父 Node 粒度粗，完整章节，LLM 理解不受限)
    可溯源 ↑ (父子关系链 + metadata 层级路径)
"""
from llama_index.core import Document, VectorStoreIndex, Settings
from llama_index.core.node_parser import (
    MarkdownNodeParser,
    SentenceSplitter,
)
from llama_index.core.schema import NodeRelationship, TextNode
from llama_index.embeddings.openai import OpenAIEmbedding
from llama_index.llms.openai import OpenAI
from typing import List

Settings.embed_model = OpenAIEmbedding(model="text-embedding-3-small")
Settings.llm = OpenAI(model="gpt-4o-mini", temperature=0.3)


# ═══════════════════════════════════════════════════════════════
# Step 1: 第一层切分 —— 按 Markdown 标题层级
# ═══════════════════════════════════════════════════════════════

md_text = """# 员工手册

## 第一章 考勤制度

### 1.1 工作时间
公司实行弹性工作制。核心工作时间为上午10:00至下午17:00。
弹性时段为上午8:00-10:00及下午17:00-20:00。每日工作时长不少于8小时，
含午休1.5小时（12:00至13:30）。

### 1.2 打卡规定
公司使用企业微信进行打卡。每日需上班打卡一次、下班打卡一次。
忘记打卡需在当日内在企业微信中提交补卡申请，由部门主管审批。
每月补卡次数不超过3次。

### 1.3 迟到与旷工
核心工作时间内迟到30分钟以内，不扣工资。
迟到30分钟至1小时，按1小时事假处理。
迟到超过1小时，按旷工半日处理。
累计旷工3日或连续旷工2日，视为严重违纪，公司有权解除劳动合同。

## 第二章 请假制度

### 2.1 事假
事假需提前1个工作日向部门主管申请。事假期间不发放工资。
每月事假累计不超过3天。紧急情况可在当日8:30前电话通知部门主管，
事后补办手续。

### 2.2 病假
病假需在当日8:30前通知部门主管。
连续病假超过2天须提供二级及以上医院的病假证明。
病假期间工资按当地最低工资标准的80%发放。
因公负伤按国家工伤保险条例执行。

### 2.3 年假
年假天数按工龄计算：入职1-5年员工享有5天年假，
5-10年员工享有10天年假，10年以上员工享有15天年假。
年假以自然年为单位计算，次年3月31日前未休完的自动清零。
年假最小请假单位为半天。

## 第三章 费用报销

### 3.1 住宿标准
出差住宿标准：一线城市（北上广深）不超过500元/天，
二线城市（成都、杭州等）不超过350元/天，其他城市不超过200元/天。

### 3.2 餐饮与交通
出差期间每日餐饮补贴80元。商务宴请需提前报备，实报实销。
市内交通实报实销。高铁/动车二等座，飞机经济舱。
"""

doc = Document(
    text=md_text,
    metadata={
        "title": "员工手册",
        "author": "人力资源部",
        "version": "V3.0",
        "category": "人事政策",
    }
)

# 第一层: MarkdownNodeParser —— 按 ## 标题切分
md_parser = MarkdownNodeParser()
parent_nodes = md_parser.get_nodes_from_documents([doc])

print("=" * 70)
print("  第一层：层级切分 (MarkdownNodeParser)")
print("=" * 70)
print(f"  {len(parent_nodes)} 个父 Node (章节级)\n")
for i, pnode in enumerate(parent_nodes):
    path = pnode.metadata.get("md_path", "?")
    first_line = pnode.text.split("\n")[0]
    char_count = len(pnode.text)
    print(f"  ParentNode[{i}]: {first_line}")
    print(f"    标题路径: {path}")
    print(f"    字符数: {char_count}")


# ═══════════════════════════════════════════════════════════════
# Step 2: 第二层切分 —— 对每个父 Node 做语义切分
# ═══════════════════════════════════════════════════════════════

# 这里用 SentenceSplitter 在句子边界处切分，同时保持 chunk 大小
sentence_splitter = SentenceSplitter(
    chunk_size=300,        # 段落级切分的 chunk 大小
    chunk_overlap=30,      # 重叠
    paragraph_separator="\n\n",
)

all_child_nodes: List[TextNode] = []
for pnode in parent_nodes:
    # 对父 Node 的文本做二级切分
    child_texts = sentence_splitter.split_text(pnode.text)

    for j, child_text in enumerate(child_texts):
        # 创建子 Node
        child_node = TextNode(
            text=child_text,
            # 继承父 Document 的全局 metadata
            metadata={
                **pnode.metadata,               # 继承: title, author, version...
                "parent_md_path": pnode.metadata.get("md_path", ""),
                "chunk_level": 2,               # 标记这是第2层
                "child_index": j,               # 在父 Node 内的子序号
            },
        )
        # 建立 PARENT 关系
        child_node.relationships[NodeRelationship.PARENT] = pnode.as_related_node_info()
        # 建立 SOURCE 关系(回到原始 Document)
        child_node.relationships[NodeRelationship.SOURCE] = pnode.relationships.get(
            NodeRelationship.SOURCE
        )
        all_child_nodes.append(child_node)

print("\n" + "=" * 70)
print("  第二层：语义切分 (SentenceSplitter)")
print("=" * 70)
print(f"  {len(all_child_nodes)} 个子 Node (段落级)\n")
for i, cnode in enumerate(all_child_nodes):
    parent_path = cnode.metadata.get("parent_md_path", "?")
    child_idx = cnode.metadata.get("child_index", "?")
    first_line = cnode.text.split("\n")[0][:60]
    print(f"  ChildNode[{i}] (child_idx={child_idx}): {first_line}...")
    print(f"    → 父章节: {parent_path}")
    # 验证 PARENT 关系是否存在
    has_parent = NodeRelationship.PARENT in cnode.relationships
    print(f"    → PARENT 关系: {'已建立' if has_parent else '缺失!'}")
    print()


# ═══════════════════════════════════════════════════════════════
# Step 3: 对子 Node 建索引 + 检索验证
# ═══════════════════════════════════════════════════════════════

print("=" * 70)
print("  建索引 + 检索验证 (展示父子关系追溯)")
print("=" * 70)

index = VectorStoreIndex(all_child_nodes)

# 查询: "迟到怎么处理？"
# 预期: 命中 "1.3 迟到与旷工" 相关的子 Node
query_engine = index.as_query_engine(similarity_top_k=3)
response = query_engine.query("迟到怎么处理？")

print(f"\n  查询: 迟到怎么处理？")
print(f"  回答: {response}")
print(f"\n  检索到的源 Node:")
for src_node in response.source_nodes:
    parent_path = src_node.metadata.get("parent_md_path", "?")
    print(f"    [{src_node.score:.3f}] {src_node.text[:60]}...")
    print(f"    所属章节: {parent_path}")
    print(f"    文档版本: {src_node.metadata.get('version')}  ← 继承自 Document")
    print(f"    分类: {src_node.metadata.get('category')}  ← 继承自 Document")
    print()


# ═══════════════════════════════════════════════════════════════
# Step 4: 查询时可选的"上下文展开"
# ═══════════════════════════════════════════════════════════════

print("=" * 70)
print("  上下文展开：从子 Node 追溯到父 Node")
print("=" * 70)

# 只取第一个命中结果做演示
top_node = response.source_nodes[0]
top_child = top_node.node  # 这是实际检索到的子 Node

# 追溯父 Node
parent_rel = top_child.relationships.get(NodeRelationship.PARENT)
if parent_rel:
    # 在父 Node 列表中查找匹配的父 Node
    parent = next(
        (p for p in parent_nodes if p.node_id == parent_rel.node_id),
        None
    )
    if parent:
        print(f"  命中的子 Node: {top_child.text[:80]}...")
        print(f"  追溯到的父 Node (完整章节):")
        print(f"  {'─' * 50}")
        for line in parent.text.split("\n")[:8]:
            print(f"  │ {line}")
        print(f"  {'─' * 50}")
        print(f"\n  → LLM 可以用这个完整章节作为上下文生成回答")


# ═══════════════════════════════════════════════════════════════
# 总结
# ═══════════════════════════════════════════════════════════════

print("\n" + "=" * 70)
print("  混合策略一总结: 层级 + 语义双层切分")
print("=" * 70)
print("""
  三层价值:
    检索层: 子 Node (300 字符, 单一话题) → 精准匹配, 高 Recall
    上下文层: 父 Node (整章内容) → LLM 看到完整上下文, 高理解质量
    元数据层: 从 Document → 父 Node → 子 Node 全链路继承
             → 按 category/version/author 过滤
             → 溯源: "来自《员工手册》V3.0 第一章第3节"

  关键点:
    - 父 Node 和子 Node 都带有完整的 metadata 继承链
    - PARENT 关系让检索后的"上下文展开"成为可能
    - 两层切分各用不同的 chunk_size: 父 Node 大(完整章节), 子 Node 小(段落级)
""")
```

---

### 21.3 混合策略二：Unstructured chunk_by_title —— 利用元素类型切分

#### 21.3.1 什么是 chunk_by_title

Unstructured 的 `chunking_strategy="by_title"` 是一种**基于文档元素类型**的智能切分策略。它不像字符切分器那样"数到 N 个字符就切一刀"，而是利用 Unstructured 解析出的 Element 类型标签来判断"哪里是更好的切分点"。

```
═══════════════════════════════════════════════════════════════════════════
          chunk_by_title 与 basic 切分的本质区别
═══════════════════════════════════════════════════════════════════════════

  Unstructured 解析文档后产出的 Elements:
  ┌─────────────────────────────────────────────────────────┐
  │ [Title]       "第一章 考勤制度"                          │
  │ [Title]       "1.1 工作时间"                             │
  │ [NarrativeText] "公司实行弹性工作制。核心工作时间为..."   │
  │ [Title]       "1.2 打卡规定"                             │
  │ [NarrativeText] "公司使用企业微信进行打卡。每月补卡不..."  │
  │ [Title]       "1.3 迟到与旷工"                           │
  │ [NarrativeText] "核心工作时间内迟到30分钟以内不扣工资..."  │
  │ [Title]       "第二章 请假制度"                           │
  │ [Title]       "2.1 事假"                                 │
  │ [NarrativeText] "事假需提前1个工作日向部门主管申请..."    │
  │ [Title]       "2.2 病假"                                 │
  │ [NarrativeText] "病假需在当日8:30前通知部门主管..."      │
  └─────────────────────────────────────────────────────────┘

  chunking_strategy="basic":
    把 Elements 当纯文本，按字符数硬切:
    ┌──────────────────────┐ ┌──────────────────────┐
    │ Chunk 0 (500字符)     │ │ Chunk 1 (500字符)     │
    │ 第一章...弹性工作制...│ │ ...打卡...迟到...     │
    │  (包含1.1+1.2的碎片)  │ │ (包含1.2碎片+1.3)    │
    └──────────────────────┘ └──────────────────────┘
    → "1.2 打卡规定" 被拦腰切成两半！

  chunking_strategy="by_title":
    识别到 Title Element → 开始一个新 Chunk:
    ┌──────────────────────┐ ┌──────────────────────┐
    │ Chunk 0               │ │ Chunk 1               │
    │ "1.1 工作时间"        │ │ "1.2 打卡规定"        │
    │ + 下面的 NarrativeText│ │ + 下面的 NarrativeText│
    │ (完整的标题+内容)      │ │ (完整的标题+内容)      │
    └──────────────────────┘ └──────────────────────┘
    → 每個 chunk = 1 个标题 + 1 段正文，语义单元完整！

═══════════════════════════════════════════════════════════════════════
```

#### 21.3.2 chunk_by_title 的工作流程

```
  Step 1: Unstructured 解析文档 → 得到 Elements 列表
    每个 Element 有 type 属性: Title / NarrativeText / ListItem / Table / ...

  Step 2: 扫描 Elements 列表，找到所有的 Title 元素
    每遇到一个 Title → 标记为新 Chunk 的起点

  Step 3: 从 Title 开始，合并后续的 Element（NarrativeText、ListItem 等）
    直到遇到下一个 Title 或达到 max_characters 限制

  Step 4: 如果合并后的内容超过 max_characters:
    → 在当前 Element 内用 separators 做二级切分（回退到字符切分兜底）

  Step 5: 应用 overlap（在 Chunk 之间建立重叠区域）
```

#### 21.3.3 关键参数详解

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `chunking_strategy` | `str` | — | `"basic"` / `"by_title"` / `"by_page"` |
| `max_characters` | `int` | 500 | 每个 chunk 的最大字符数 |
| `overlap` | `int` | 0 | chunk 之间重叠的字符数 |
| `multipage_sections` | `bool` | True | 是否允许 chunk 跨页（by_title 时通常为 True） |
| `combine_text_under_n_chars` | `int` | 500 | 比这个短的 Element 强制合并到前一个 chunk |
| `new_after_n_chars` | `int` | 1500 | 超过此长度就强制切分（软上限） |

#### 21.3.4 代码演示 + 数据变化

```python
from unstructured.partition.auto import partition

elements = partition(
    filename="星辰科技员工手册.pdf",
    strategy="auto",
    languages=["chi_sim", "eng"],

    # ── chunk_by_title 核心参数 ─────────────────────────
    chunking_strategy="by_title",
    max_characters=800,            # 每个 chunk 上限 800 字符
    overlap=60,                    # chunk 间重叠 60 字符
    multipage_sections=True,       # 允许跨页 chunk
    combine_text_under_n_chars=100, # <100 字符的 Element 强制合并
    new_after_n_chars=1200,        # 软上限: 超过 1200 字符强制切分
)

# 注意: 当设置了 chunking_strategy 后，
# partition() 返回的不是 Element，而是已经分好块的 Chunk 对象
# 每个 Chunk 类似于 Element，但 text 已经是合并后的完整内容

print(f"Chunk 数量: {len(elements)}")
for i, chunk in enumerate(elements):
    char_count = len(chunk.text)
    # chunk.metadata 里包含来源信息
    source = chunk.metadata.filename
    page = chunk.metadata.page_number
    first_line = chunk.text.split("\n")[0][:80]
    print(f"\n  Chunk[{i}] ({char_count}字符, 第{page}页)")
    print(f"  首行: {first_line}")
    # 前 3 行
    for line in chunk.text.split("\n")[1:4]:
        print(f"    {line[:80]}")
```

**数据变化直观展示（模拟员工手册的输出）：**

```
Chunk 数量: 12

  Chunk[0] (450字符, 第1页)
  首行: # 星辰科技有限公司 — 员工手册
    ## 第一章 公司简介
    ### 1.1 公司概况
    星辰科技有限公司成立于2018年，总部位于北京市海淀区...

  Chunk[1] (380字符, 第1页)
  首行: ## 第二章 入职与试用期
    ### 2.1 入职材料
    新员工入职时需提交以下材料：1. 身份证原件及复印件...

  Chunk[2] (520字符, 第2页)
  首行: ### 2.2 试用期规定
    劳动合同期限三年以上的，试用期不超过六个月。
    一般情况下，公司标准试用期为3个月...

  Chunk[3] (720字符, 第3页)
  首行: ## 第三章 考勤与工时
    ### 3.1 工作时间
    公司实行弹性工作制。核心工作时间为上午10:00至12:00...

  Chunk[4] (340字符, 第3页)
  首行: ### 3.2 打卡规定
    公司使用企业微信进行打卡...

  观察关键点:
    ✓ 每个 ## 章节 = 一个独立的 Chunk（不是按字符数硬切）
    ✓ Chunk 的起点始终是 Title 元素，保持了"标题+内容"的语义完整性
    ✓ "1.1 公司概况"和"2.1 入职材料"在不同的 Chunk —— 因为它们是不同的 ## 章节
    ✓ Chunk 大小因内容而异: 720 字符 vs 340 字符 —— 由内容决定，不是固定值
```

#### 21.3.5 chunk_by_title vs chunk_by_page vs basic 对比

| 维度 | basic | by_title | by_page |
|------|-------|----------|---------|
| **切分边界** | 字符数 | Title Element | 页面边界 |
| **语义完整性** | 低——可能在句中截断 | 高——标题+内容不分离 | 中——页面边界不是语义边界 |
| **chunk 大小** | 均匀 | 不均匀(由内容决定) | 不均匀(由页内容决定) |
| **表格处理** | 可能被切散 | Title 后的 Table 保持完整 | 受页面边界限制 |
| **适用场景** | 完全无结构的纯文本 | 有标题结构的文档 | 需要精确页码溯源的场景 |
| **关键参数** | max_characters | max_characters + combine_text_under_n_chars | max_characters |

#### 21.3.6 combine_text_under_n_chars 的精确作用

```
  combine_text_under_n_chars = 100 的实际效果:

  场景: 一个很短的 Title 后面跟着一个很短的段落

  Elements:
    [Title]           "1.1 工作时间"       ← 只有 7 个字符 (< 100)
    [NarrativeText]   "公司实行弹性工作制" ← 只有 10 个字符 (< 100)
    [Title]           "1.2 打卡规定"       ← 只有 7 个字符 (< 100)
    [NarrativeText]   "使用企业微信打卡..." ← 150 个字符

  没有 combine_text_under_n_chars:
    → "1.1 工作时间" 自己成为一个 Chunk (太小!!)
    → "公司实行弹性工作制" 自己成为一个 Chunk (太小!!)

  有 combine_text_under_n_chars=100:
    → "1.1 工作时间" 没有达到 100 字符 → 强制与后面的 "公司实行弹性工作制" 合并
    → "1.2 打卡规定" 没有达到 100 字符 → 强制与后面的 "使用企业微信打卡..." 合并
    → 最终: 2 个语义完整的 Chunk (标题+内容)，而非 4 个碎片 Chunk
```

---

### 21.4 混合策略三：元数据增强 —— 给 Chunk 注入"索引之外的信息"

#### 21.4.1 什么是元数据增强

元数据增强的核心思想是：**在 Chunk 创建时注入比 Chunk 文本本身更多的上下文信息，使得检索和生成阶段可以利用这些信息做更精准的判断。**

```
═══════════════════════════════════════════════════════════════════════════
              三类元数据增强
═══════════════════════════════════════════════════════════════════════════

  类型 1: 层级元数据 (Hierarchical Metadata)
  ┌─────────────────────────────────────────────────────────┐
  │  每个 Chunk 的 metadata 包含它在文档层级中的位置:          │
  │                                                         │
  │  {                                                      │
  │    "document_title": "员工手册",                          │
  │    "chapter": "第三章 考勤与工时",                         │
  │    "section": "3.1 工作时间",                             │
  │    "heading_level": 2,                                   │
  │    "md_path": "员工手册/第三章/3.1 工作时间",              │
  │    "parent_node_id": "abc123..."                         │
  │  }                                                      │
  │                                                         │
  │  用途: 前端显示"当前位置"、层级过滤、上下文扩展            │
  └─────────────────────────────────────────────────────────┘

  类型 2: 来源元数据 (Provenance Metadata)
  ┌─────────────────────────────────────────────────────────┐
  │  每个 Chunk 记录它的来源信息:                               │
  │                                                         │
  │  {                                                      │
  │    "source_file": "考勤管理制度.pdf",                      │
  │    "page_number": 3,                                    │
  │    "coordinates": {"x": 72, "y": 340, "w": 468, "h": 18},│
  │    "author": "人力资源部",                                 │
  │    "created_date": "2024-03-15",                        │
  │    "version": "V3.0",                                   │
  │    "category": "人事政策",                                 │
  │    "department": "HR",                                  │
  │    "last_modified": "2025-01-20"                        │
  │  }                                                      │
  │                                                         │
  │  用途: 来源标注、按类别/日期过滤、前端PDF高亮定位          │
  └─────────────────────────────────────────────────────────┘

  类型 3: 内容元数据 (Content Metadata)
  ┌─────────────────────────────────────────────────────────┐
  │  每个 Chunk 记录其内容特征:                                  │
  │                                                         │
  │  {                                                      │
  │    "content_type": "NarrativeText",  ← Table/List/Code   │
  │    "language": "chi_sim",                               │
  │    "contains_table": false,                             │
  │    "contains_numbers": true,                            │
  │    "emphasis_keywords": ["1个工作日", "提前"],             │
  │    "token_count": 85,                                   │
  │    "readability_level": "medium"                        │
  │  }                                                      │
  │                                                         │
  │  用途: 检索时加权（含数字的chunk更可能是政策条款）、       │
  │        路由（表格chunk走表格处理管线）                     │
  └─────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════════
```

#### 21.4.2 元数据在检索中的应用：MetadataFilter

```python
from llama_index.core import VectorStoreIndex
from llama_index.core.vector_stores import (
    MetadataFilter,
    MetadataFilters,
    FilterOperator,
    FilterCondition,
)

# ── 场景 1: 单条件过滤 ──────────────────────────────────
# 只检索 "人事政策" 类别下的内容
filter_hr = MetadataFilters(
    filters=[
        MetadataFilter(key="category", value="人事政策")
    ]
)

# ── 场景 2: 多条件 AND ──────────────────────────────────
# 只检索 "人事政策" 且版本为 "V3.0" 且 2024 年之后的内容
filter_complex = MetadataFilters(
    filters=[
        MetadataFilter(key="category", value="人事政策"),
        MetadataFilter(key="version", value="V3.0"),
        MetadataFilter(
            key="created_date",
            value="2024-01-01",
            operator=FilterOperator.GT,  # Greater Than
        ),
    ],
    condition=FilterCondition.AND,  # 所有条件都要满足
)

# ── 场景 3: 多条件 OR ───────────────────────────────────
# 检索 "人事政策" 或 "财务制度" 的内容
filter_or = MetadataFilters(
    filters=[
        MetadataFilter(key="category", value="人事政策"),
        MetadataFilter(key="category", value="财务制度"),
    ],
    condition=FilterCondition.OR,
)

# ── 使用过滤的查询 ─────────────────────────────────────
query_engine = index.as_query_engine(
    similarity_top_k=3,
    filters=filter_complex,  # 传入过滤条件
)
response = query_engine.query("加班费怎么算的？")
```

#### 21.4.3 元数据在生成中的应用：MetadataReplacementPostProcessor

这是 LlamaIndex 的一个内置后处理器，核心逻辑是：**检索时用子 Node 做匹配（细粒度、高精度），但在送入 LLM 之前，把子 Node 替换为其父 Node 的内容（粗粒度、完整上下文）。**

```python
from llama_index.core.postprocessor import MetadataReplacementPostProcessor

# 创建替换后处理器
# target_metadata_key: 子 Node 的 metadata 中存储父 Node 内容的字段名
postprocessor = MetadataReplacementPostProcessor(
    target_metadata_key="window",  # 默认值
)

query_engine = index.as_query_engine(
    similarity_top_k=3,
    node_postprocessors=[postprocessor],  # 检索后自动替换
)

# 工作流程:
#   1. Query 向量化 → 在子 Node 中检索 → 找到最相关的子 Node
#   2. MetadataReplacementPostProcessor 处理:
#      读取子 Node.metadata["window"] → 取出父 Node 的完整内容
#      将子 Node 替换为父 Node
#   3. 替换后的 Node 送到 LLM 生成
```

#### 21.4.4 完整代码：元数据增强的全链路演示

```python
"""
═══════════════════════════════════════════════════════════════════════════
  元数据增强完整流程:
    1. 文档加载 → 注入全局 metadata (category, author, version, ...)
    2. 切分时 → 子 Node 自动继承 metadata + 补充层级 metadata
    3. 检索时 → 用 metadata 过滤 + 用 metadata 加权
    4. 生成时 → 用 MetadataReplacementPostProcessor 展开上下文
    5. 溯源时 → 展示完整的 metadata 链 (source→page→chapter→section)
═══════════════════════════════════════════════════════════════════════════
"""
from llama_index.core import Document, VectorStoreIndex, Settings
from llama_index.core.node_parser import SentenceWindowNodeParser
from llama_index.core.postprocessor import MetadataReplacementPostProcessor
from llama_index.embeddings.openai import OpenAIEmbedding
from llama_index.llms.openai import OpenAI

Settings.embed_model = OpenAIEmbedding(model="text-embedding-3-small")
Settings.llm = OpenAI(model="gpt-4o-mini", temperature=0.3)

# ── 创建带完整 metadata 的 Document ─────────────────────
doc = Document(
    text="""## 第三章 考勤与工时

### 3.1 工作时间
公司实行弹性工作制。核心工作时间为上午10:00至12:00，下午14:00至17:00。
弹性时段为上午8:00-10:00及下午17:00-20:00。

### 3.2 打卡规定
公司使用企业微信进行打卡。每日需上班打卡一次、下班打卡一次。
忘记打卡需在当日内在企业微信中提交补卡申请，由部门主管审批。

### 3.3 迟到与旷工
核心工作时间内迟到30分钟以内，不扣工资。
迟到30分钟至1小时，按1小时事假处理。
迟到超过1小时，按旷工半日处理。""",
    metadata={
        "title": "员工手册",
        "chapter": "第三章 考勤与工时",
        "author": "人力资源部",
        "version": "V3.0",
        "category": "人事政策",
        "department": "HR",
        "created_date": "2024-03-15",
        "last_modified": "2025-01-20",
    }
)

# ── 用 SentenceWindowNodeParser (保留窗口元数据) ─────────
parser = SentenceWindowNodeParser.from_defaults(
    window_size=2,
    window_metadata_key="window",
    original_text_metadata_key="original_text",
)

nodes = parser.get_nodes_from_documents([doc])

# 查看增强后的 Node
print("增强后的 Node 的 metadata 内容:")
for i, node in enumerate(nodes[:3]):
    print(f"\n  Node[{i}]: {node.text[:60]}...")
    print(f"  metadata keys: {list(node.metadata.keys())}")
    print(f"    title: {node.metadata.get('title')}")
    print(f"    chapter: {node.metadata.get('chapter')}")
    print(f"    category: {node.metadata.get('category')}")
    print(f"    version: {node.metadata.get('version')}")
    print(f"    has window: {'window' in node.metadata}")

# ── 建索引 + 设置 MetadataReplacement ─────────────────────
index = VectorStoreIndex(nodes)
postprocessor = MetadataReplacementPostProcessor(
    target_metadata_key="window"
)

query_engine = index.as_query_engine(
    similarity_top_k=3,
    node_postprocessors=[postprocessor],
)

response = query_engine.query("迟到了怎么处理？")
print(f"\n回答: {response}")
print(f"\n来源:")
for src in response.source_nodes:
    print(f"  [{src.score:.3f}] {src.metadata.get('title')} "
          f"- {src.metadata.get('chapter')}")
    print(f"    版本: {src.metadata.get('version')}")
```

---

### 21.5 混合策略总结

| 策略 | 核心思想 | 检索精度 | 上下文质量 | 实现复杂度 | 最适合场景 |
|------|---------|:---:|:---:|:---:|----------|
| **层级+语义双层** | 父Node保结构 + 子Node保精度 | 高 | 极高 | 中 | 有层级结构的文档(手册/论文/制度) |
| **chunk_by_title** | 利用Unstructured的Title元素做边界 | 高 | 高 | 低 | PDF/Word解析后的结构化文档 |
| **元数据增强** | metadata注入层级/来源/内容三类信息 | 极高(可过滤) | 极高(可替换) | 中 | 需要多维度过滤和精确溯源的生产环境 |
| **三层组合(最佳)** | 上述三种策略叠加使用 | 极高 | 极高 | 高 | 大型企业RAG系统 |

**三层组合的推荐顺序：**

```
  1. Unstructured 解析 (chunk_by_title)
     → 获取结构化的 Elements，保留 Title/NarrativeText/Table 等类型信息

  2. 层级切分 (MarkdownNodeParser / HierarchicalNodeParser)
     → 建立 Document → 父 Node → 子 Node 的层级关系

  3. 元数据增强
     → 在 Document 级别注入全局 metadata
     → 在 Node 级别补充层级 metadata (md_path, heading_level, parent_id)
     → 在检索时利用 metadata 做过滤和加权
     → 在生成时利用 MetadataReplacement 展开上下文

  最终效果:
    检索 → 子 Node (精准) → metadata 过滤 → parent Node (完整上下文) → LLM
    溯源 → metadata 链: source → chapter → section → page → coordinates
```


---

