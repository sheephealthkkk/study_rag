## 第五章：向量入库实战 —— 从内存到云端全链路演示

前四章讲了向量数据库选型、Embedding 模型、索引算法和元数据建模。本章把这些知识串联起来，用代码演示从文档加载到向量入库的完整流程。我们会覆盖六种存储方式：内存、本地 Chroma、云端 Pinecone、Milvus、FAISS，以及 MongoDB 多索引组合。

### 为什么需要掌握多种入库方式

RAG 项目通常经历"原型 → 内部测试 → 上线 → 规模化"四个阶段。每个阶段对存储的需求完全不同：

| 阶段 | 数据量 | 核心需求 | 推荐方案 |
|------|--------|----------|----------|
| 原型验证 | < 1000条 | 快速验证想法 | 内存 (SimpleVectorStore) |
| 内部测试 | 千~万条 | 数据不丢失、多人共享 | Chroma 本地持久化 |
| 生产上线 | 万~百万条 | 高性能、元数据过滤 | Chroma / Qdrant / pgvector |
| 规模化 | 千万~亿条 | 分布式、高可用 | Milvus / Pinecone |

**本章的核心目标：** 让你学会根据项目阶段选择合适的存储方案，理解每种方案的数据流和适用边界。

---

### 5.0 前期准备：文档加载与切分

在进入数据库演示之前，先完成所有存储方式都需要的"硬菜"——加载文档并切分为 Node。

#### 数据变化全流程

这一步虽然简单，但数据变化是理解后续所有演示的"锚点"：

```
  📄 星辰科技员工手册.md (4860 字符)
      │  FlatReader (底层调用 Unstructured)
      │  作用: 将 Markdown 源码解析为 LlamaIndex 的 Document 对象
      │        Document = {text: 全文字符串, metadata: {文件名, 路径等}}
      ▼
  📦 1 个 Document 对象
      │  此时 text 是完整的全文，一个 Document 太大不能直接入库
      │  metadata 只有基本的文件信息
      │
      │  手动注入 metadata:
      │    category="人事政策", version="V3.0", status="active" ...
      │    这些字段将在后续的元数据过滤中发挥关键作用
      │
      │  SentenceSplitter(chunk_size=400, chunk_overlap=50)
      │  作用: 将长文本切分为语义完整的小块
      │        每块约 400 字符 (中文约 250-300 字)
      │        重叠 50 字符防止信息在边界丢失
      │        自动继承父 Document 的 metadata
      ▼
  🧩 ~15 个 Node 对象
      │  每个 Node = 一段 ~400 字符的文本 + 继承的 metadata
      │  此时: Node 之间已经建立了 PREVIOUS/NEXT 关系链
      │  但: Node 还是"文本"，需要向量化才能被检索
      │
      │  Embedding (text-embedding-3-small, 512维)
      │  作用: 将每段文本转换为机器可计算的向量
      │        语义相近的文本 → 向量方向也相近
      ▼
  🔢 ~15 个 512维 float32 向量
      │  每向量 = 512 × 4 = 2048 bytes
      │  此时: 数据已准备好入库
      ▼
  🗄️ 存入向量数据库 (接下来 6 种方式任选)
```

#### 代码实现

```python
from llama_index.core import Settings
from llama_index.core.node_parser import SentenceSplitter
from llama_index.embeddings.openai import OpenAIEmbedding
from llama_index.readers.file import FlatReader
from pathlib import Path

# ── 全局 Embedding: 一次设置，后续所有索引和查询自动使用 ──
Settings.embed_model = OpenAIEmbedding(
    model="text-embedding-3-small", dimensions=512
)

# ── Step 1: 加载文档 ───────────────────────────────────────
# FlatReader 底层用的是 Unstructured，自动识别 .md 格式
reader = FlatReader()
documents = reader.load_data(Path("星辰科技员工手册.md"))

doc = documents[0]  # 只有一个 .md 文件
print(f"Document text: {len(doc.text)} 字符")

# ── Step 2: 注入元数据 (检索时用于过滤) ──────────────────
doc.metadata.update({
    "category": "人事政策",
    "department": "HR",
    "version": "V3.0",
    "status": "active",
    "created_date": "2024-03-15",
})

# ── Step 3: 切分为 Node ────────────────────────────────────
splitter = SentenceSplitter(chunk_size=400, chunk_overlap=50)
nodes = splitter.get_nodes_from_documents([doc])
print(f"1 个 Document → {len(nodes)} 个 Node")

# 👉 nodes 就是后续六大演示的统一输入
```

**到这里，数据准备完毕。** 下面用六种方式把 `nodes` 存入不同的后端。每种方式的核心差异在于：**数据存在哪里、怎么持久化、怎么检索。**

---

### 5.1 方式一：内存存储 —— 原型验证的"秒开"方案

#### 方案定位

这是最简的入门方式。所有数据（向量 + 文本 + 元数据）存在于 Python 进程的内存中。关闭程序 = 数据消失。

#### StorageContext：LlamaIndex 的"存储调度中心"

理解内存存储的关键是理解 `StorageContext`。它是 LlamaIndex 中**统一管理所有存储后端的抽象层**，内部持有三样东西：

```
  StorageContext
  ├── vector_store   →  存向量 (用于 ANN 检索)
  ├── docstore       →  存 Node 文本 + metadata (用于溯源、展示)
  └── index_store    →  存索引元数据 (node_id 映射、索引配置)

  这三种存储在物理上可以放在不同地方。
  例如: vector_store=Chroma, docstore=MongoDB, index_store=MongoDB
  这就是 5.6 节要讲的 "多索引组合" 模式。
```

VectorStoreIndex 工作时，**写入阶段**（建索引）会自动把向量写入 vector_store、文本写入 docstore、元数据写入 index_store；**查询阶段**（检索）从 vector_store 找最近向量 → 通过 ID 从 docstore 取回原文 → 返回。

#### 代码实现

```python
from llama_index.core import VectorStoreIndex, StorageContext
from llama_index.core.storage.docstore import SimpleDocumentStore
from llama_index.core.storage.index_store import SimpleIndexStore
from llama_index.core.vector_stores import SimpleVectorStore

# 显式创建内存中的三分存储
storage_context = StorageContext.from_defaults(
    vector_store=SimpleVectorStore(),     # 向量 → Python dict
    docstore=SimpleDocumentStore(),       # 文本 → Python dict
    index_store=SimpleIndexStore(),       # 元数据 → Python dict
)

# 建索引: 这一步会调用 Embedding API 并写入三分存储
index = VectorStoreIndex(nodes, storage_context=storage_context)

# 查询
query_engine = index.as_query_engine(similarity_top_k=3)
response = query_engine.query("事假需要提前多久申请？")
```

#### 数据在内存中的实际存储形态

```
  Python 进程内存中:

  ┌─ SimpleVectorStore ──────────────────────────┐
  │  {node_id_0: [0.023, -0.147, ...512维],     │
  │   node_id_1: [-0.089, 0.341, ...512维],     │
  │   ...                                        │
  │   node_id_14: [0.056, -0.231, ...512维]}     │
  │  存储: Python dict, key=node_id, value=向量   │
  ├─ SimpleDocumentStore ────────────────────────┤
  │  {node_id_0: Document(text="...", metadata=  │
  │              {category:"人事政策",...}),      │
  │   ...}                                       │
  │  存储: Python dict, key=node_id, value=Doc   │
  └─ SimpleIndexStore ───────────────────────────┘
     {index_id: {nodes: [node_id_0, node_id_1,...]}}
     存储: Python dict, 索引结构元数据

  ⚠ 进程结束 → 以上全部消失 → 需要重建索引
```

#### 适用场景与局限

| 方面 | 评价 |
|------|------|
| 速度 | 极快（无网络、无磁盘 IO） |
| 配置 | 零配置 |
| 持久化 | 无，程序退出即消失 |
| 共享 | 不支持，另一个进程无法访问 |
| 推荐场景 | 验证想法、跑通 Pipeline、调试参数 |
| 不推荐 | 任何需要保存数据的场景 |

---

### 5.2 方式二：Chroma 本地持久化 —— 从"内存"到"磁盘"的第一步

#### 方案定位

原型跑通后，你希望重启程序后数据还在，但又不想部署复杂的数据库服务。Chroma 恰好满足这个需求：它是一个**嵌入式向量数据库**，所有数据存到一个 SQLite 文件里，部署方式就是一个 `pip install`。

#### Chroma 内部是怎么存储的

```
  ./chroma_db/ 目录 (你指定的 persist_path):
  ┌──────────────────────────────────────────────────────┐
  │  chroma.sqlite3                                      │
  │  ├── embeddings 表     → HNSW 索引的向量数据          │
  │  ├── embedding_metadata 表 → 每个向量的元数据 (JSON)   │
  │  ├── collections 表     → Collection 列表             │
  │  └── segments 表       → HNSW 图的持久化分片           │
  │                                                      │
  │  HNSW 索引在内存中工作 (快速检索)，持久化到 SQLite     │
  │  写入: 自动写入磁盘 (数据库级持久化)                     │
  │  读取: 启动时从 SQLite 加载 HNSW 图到内存               │
  └──────────────────────────────────────────────────────┘
```

**Chroma 的 Collection 概念：** 类似于关系数据库中的"表"。一个 Chroma 实例可以创建多个 Collection，每个 Collection 存储一个独立的向量集合。例如你可以用 `handbook_v3` 和 `handbook_v4` 两个 Collection 分别存不同版本的员工手册。

#### 代码实现

```python
import chromadb
from llama_index.vector_stores.chroma import ChromaVectorStore
from llama_index.core import StorageContext, VectorStoreIndex

# ── Step 1: 创建 Chroma 客户端 ───────────────────────────
# PersistentClient = 持久化到磁盘 (数据不会丢)
# EphemeralClient  = 只在内存 (进程结束即消失，类似5.1)
chroma_client = chromadb.PersistentClient(path="./chroma_db")

# ── Step 2: 创建或获取 Collection ────────────────────────
# Collection = Chroma 中的"表"
collection = chroma_client.get_or_create_collection(
    name="employee_handbook",
    metadata={"description": "星辰科技员工手册向量索引"}
)

# ── Step 3: 封装为 LlamaIndex 的 VectorStore ─────────────
vector_store = ChromaVectorStore(chroma_collection=collection)

# ── Step 4: StorageContext + 建索引 ──────────────────────
storage_context = StorageContext.from_defaults(
    vector_store=vector_store,
    # docstore 和 index_store 不传 → LlamaIndex 自动用内存存储
    # Chroma 自身已经持久化了向量 + 元数据 + 文本
)
index_chroma = VectorStoreIndex(nodes, storage_context=storage_context)

print(f"Collection 中的向量数: {collection.count()}")
```

#### 持久化验证：模拟进程重启

```python
# 假设这是"第二天重新打开程序"
chroma_client2 = chromadb.PersistentClient(path="./chroma_db")
collection2 = chroma_client2.get_collection("employee_handbook")
print(f"向量数: {collection2.count()}  ← 数据还在！无需重建索引")

# 直接从已有的向量库加载索引(不重新 Embedding)
vector_store2 = ChromaVectorStore(chroma_collection=collection2)
index2 = VectorStoreIndex.from_vector_store(vector_store2)
# 👆 from_vector_store: 跳过建索引，直接读取已有数据
#    对比 from_documents: 重新解析+切分+Embedding+入库(从头来)

query_engine = index2.as_query_engine(similarity_top_k=3)
response = query_engine.query("加班费怎么算？")
```

#### 关键区别：from_vector_store vs from_documents

| 方法 | 做什么 | 何时用 |
|------|--------|--------|
| `from_documents(docs)` | 重新 Embedding + 全部入库 | 数据源更新了、重新构建 |
| `from_vector_store(vs)` | 跳过 Embedding，直接读已有数据 | 数据已经在库里了、进程重启后 |

#### 元数据过滤查询

```python
from llama_index.core.vector_stores import MetadataFilter, MetadataFilters

# 只查 status='active' 且 version='V3.0' 的内容
filters = MetadataFilters(filters=[
    MetadataFilter(key="status", value="active"),
    MetadataFilter(key="version", value="V3.0"),
])
engine = index2.as_query_engine(similarity_top_k=3, filters=filters)
response = engine.query("请假规定")
```

#### 适用场景与局限

| 方面 | 评价 |
|------|------|
| 持久化 | 自动持久化到 SQLite，数据安全 |
| 部署 | 零外部依赖，pip install 即用 |
| 规模 | 百万级以下表现良好，超过百万性能下降 |
| 分布式 | 不支持（单机） |
| 推荐场景 | 本地开发、小团队内部使用、单机部署 |
| 不适合 | 十亿级数据、需要多副本高可用 |

---

### 5.3 方式三：Pinecone 云端存储 —— 零运维的 SaaS 方案

#### 方案定位

当你不想管理任何数据库服务器——不想装 Docker、不想调 K8s、不想做备份——Pinecone 就是答案。它是一个纯托管的向量数据库 SaaS，你只需要一个 API Key。

#### Pinecone 的三层数据组织

理解 Pinecone 的数据模型是正确使用它的关键：

```
  Pinecone 数据层级:

  Index (索引)
  │  相当于一个"数据库实例"
  │  创建时指定: 名称、维度(512)、度量(cosine)
  │  计费单元: 按 Pod 或 Serverless 按用量
  │
  ├── Namespace (命名空间) "V3.0"
  │   │  相当于 Index 内的"逻辑分区"
  │   │  不同 namespace 的数据互相隔离
  │   │  检索时指定 namespace → 只搜该分区
  │   │  不指定 → 只搜默认 namespace ("")
  │   │
  │   ├── Vector(id="chunk_0", values=[0.23, -0.45,...], metadata={...})
  │   ├── Vector(id="chunk_1", values=[...], metadata={...})
  │   └── ...
  │
  ├── Namespace "V4.0"     ← 新版本放新 namespace
  │   └── ...
  │
  └── Namespace "Finance"  ← 按部门分 namespace
      └── ...

  关键理解:
    · 不同 namespace 的向量从不混合——检索 V3.0 不会碰 V4.0 的数据
    · 一个 Index 只有一个维度(创建时确定)——不能改
    · namespace 是轻量逻辑隔离(非物理隔离)——不收费、无限制
```

**namespace 的实战价值：** 假设你的员工手册每年更新，你可以把 2024 版放 namespace="V3.0"，2025 版放 namespace="V4.0"。用户查询时根据"版本"选对应的 namespace。这比"建两个 Index"更经济——共享同一个计费实例。

#### 代码实现

```python
import os
from pinecone import Pinecone, ServerlessSpec
from llama_index.vector_stores.pinecone import PineconeVectorStore
from llama_index.core import StorageContext, VectorStoreIndex

# ── Step 1: 初始化 Pinecone ──────────────────────────────
pc = Pinecone(api_key=os.environ["PINECONE_API_KEY"])
INDEX_NAME = "employee-handbook"

# ── Step 2: 创建 Index (如果还不存在) ────────────────────
if INDEX_NAME not in pc.list_indexes().names():
    pc.create_index(
        name=INDEX_NAME,
        dimension=512,                    # 必须匹配 Embedding 维度
        metric="cosine",                  # cosine / euclidean / dotproduct
        spec=ServerlessSpec(              # Serverless = 按用量计费
            cloud="aws", region="us-east-1"
        ),
    )

pinecone_index = pc.Index(INDEX_NAME)

# ── Step 3: 用 namespace 隔离不同版本 ────────────────────
vector_store_v3 = PineconeVectorStore(
    pinecone_index=pinecone_index,
    namespace="V3.0",                    # ← 关键! 版本隔离
)

storage_context = StorageContext.from_defaults(
    vector_store=vector_store_v3
)

index_v3 = VectorStoreIndex(nodes, storage_context=storage_context)
print("V3.0 入库完成")

# 查询: 自动只在 namespace="V3.0" 的范围内检索
engine = index_v3.as_query_engine(similarity_top_k=3)
response = engine.query("年假有多少天？")
```

#### namespace 隔离验证

```python
# 查看当前 Index 下有哪些 namespace
stats = pinecone_index.describe_index_stats()
print(stats.get("namespaces", {}))
# 输出: {"V3.0": {"vector_count": 15}}
#       ↑ 只有 V3.0 的数据, 其他版本的数据被完全隔离

# 如果后续新增 V4.0:
vector_store_v4 = PineconeVectorStore(
    pinecone_index=pinecone_index, namespace="V4.0"
)
# 建 V4.0 的 Index → V4.0 的数据不会和 V3.0 混合
```

#### 适用场景与局限

| 方面 | 评价 |
|------|------|
| 运维 | 零——不需要管理任何服务器 |
| 扩展 | 自动——Serverless 按用量伸缩 |
| 数据位置 | 云端——有网络延迟(~50-200ms)，不适合延迟敏感场景 |
| 成本 | 小规模便宜，大规模按量付费可能较贵 |
| 推荐场景 | 不想运维、快速上线、全球多区域 |
| 不适合 | 数据不能出内网、超低延迟需求 |

---

### 5.4 方式四：Milvus —— 大规模分布式生产方案

#### 方案定位

Milvus 是功能最全的开源向量数据库——支持 HNSW/IVF/DiskANN/PQ 等十几种索引，支持分布式部署，支持十亿级向量。它是企业级大规模 RAG 系统的首选。

#### Milvus 的内部架构

```
  Milvus 分布式部署 (简化版):

  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
  │  Query Node  │  │  Query Node  │  │  Query Node  │   ← 处理查询请求
  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘
         │                 │                 │
  ┌──────┴─────────────────┴─────────────────┴──────┐
  │                   Data Node                      │   ← 存储向量数据
  │              (可分片, 水平扩展)                   │
  └──────────────────────┬──────────────────────────┘
                         │
  ┌──────────────────────┴──────────────────────────┐
  │               Object Storage (MinIO/S3)          │   ← 持久化存储
  │          + etcd (元数据协调, 选主)                │
  └─────────────────────────────────────────────────┘

  你可以从单机 Docker 开始(零分布式),
  数据量增长后添加 Data Node 做水平扩展,
  不需要改任何代码。
```

#### 为什么选 Milvus 而不是 Chroma

Chroma 的可扩展上限约 100 万向量。如果你预估一年后用户增长到需要处理千万级别的文档，从第一天就用 Milvus 可以避免后期迁移的麻烦。Milvus 还支持：

- **多种索引自由切换：** 低延迟场景用 HNSW，海量数据场景用 IVF_PQ
- **混合检索：** 向量 + 标量过滤 + 全文检索
- **多租户：** Partition Key 物理隔离

#### 代码实现

```python
from pymilvus import connections
from llama_index.vector_stores.milvus import MilvusVectorStore
from llama_index.core import StorageContext, VectorStoreIndex

# ── 连接 Milvus ──────────────────────────────────────────
connections.connect(host="localhost", port="19530")

# ── 创建 MilvusVectorStore ───────────────────────────────
vector_store_milvus = MilvusVectorStore(
    uri="http://localhost:19530",
    collection_name="employee_handbook",
    dim=512,
    index_config={
        "index_type": "HNSW",     # 可选: HNSW/IVF_FLAT/IVF_PQ/DISKANN
        "metric_type": "COSINE",
        "params": {"M": 16, "efConstruction": 200}
    },
    overwrite=True,
)

storage_context = StorageContext.from_defaults(
    vector_store=vector_store_milvus
)

index_milvus = VectorStoreIndex(nodes, storage_context=storage_context)
```

#### 索引类型切换指南

| 你的需求 | 选什么 index_type | 为什么 |
|----------|-------------------|--------|
| 低延迟、高召回 | `HNSW` | 查询最快(1-5ms)，召回率>95% |
| 十亿级、内存有限 | `DISKANN` | 用 SSD 存索引，内存放导航节点 |
| 千万~亿级、成本敏感 | `IVF_PQ` | 向量压缩(PQ)大幅减少内存 |
| 刚起步不确定 | `HNSW`(默认) | 最通用，后续可迁移 |

#### 适用场景与局限

| 方面 | 评价 |
|------|------|
| 规模 | 十亿级，支持水平扩展 |
| 索引 | 最全（HNSW/IVF/DiskANN/PQ...） |
| 部署 | 需要 Docker/K8s，复杂度最高 |
| 推荐场景 | 大规模生产、需要极致性能 |
| 不适合 | 原型验证、不想花时间运维 |

---

### 5.5 方式五：FAISS —— 回归最纯粹的向量检索

#### 方案定位

FAISS 不是数据库——它是 Meta 开源的**向量检索算法库**。没有 SQL、没有持久化、没有服务端。它只做一件事：给你一个向量，返回最相似的 Top-K。

它适合两种人：
1. **手写 RAG 管线的开发者**——想要完全掌控检索逻辑，不引入任何数据库依赖
2. **需要做异构集成的架构师**——FAISS 作为检索算法引擎，外面包一层 LlamaIndex 或自定义服务

#### FAISS 在 LlamaIndex 中的位置

```
  你的代码
    ↓
  LlamaIndex VectorStoreIndex
    ↓
  FaissVectorStore (适配层)
    ↓
  FAISS C++ 核心 (IndexFlatIP / IndexHNSW / IndexIVFPQ)
```

FAISS 负责"向量检索算法"，LlamaIndex 负责"文本管理(调用 Embedding API 等) + 元数据管理 + 查询引擎"。FAISS 本身不知道"文本"是什么——它只知道"浮点数数组"。

#### 代码实现

```python
import faiss
from llama_index.vector_stores.faiss import FaissVectorStore
from llama_index.core import StorageContext, VectorStoreIndex

# ── Step 1: 创建 FAISS 索引 ──────────────────────────────
# 归一化后的向量 → IndexFlatIP (内积 = 余弦相似度)
faiss_index = faiss.IndexFlatIP(512)

# ── Step 2: 封装为 LlamaIndex VectorStore ────────────────
vector_store_faiss = FaissVectorStore(faiss_index=faiss_index)

storage_context = StorageContext.from_defaults(
    vector_store=vector_store_faiss
)

index_faiss = VectorStoreIndex(nodes, storage_context=storage_context)
print(f"FAISS 向量数: {faiss_index.ntotal}")

# ── 手动持久化 ───────────────────────────────────────────
faiss.write_index(faiss_index, "./faiss_index.bin")
# 注意: 这只是向量索引! 文本和元数据还需额外保存
```

#### 重要的陷阱

```python
# ❌ 错误理解:
# "我把 FAISS 索引保存了，数据就安全了"
faiss.write_index(faiss_index, "./faiss_index.bin")

# ✓ 正确理解:
# FAISS .bin 文件只包含向量和索引结构。
# 文本内容、metadata 都在 LlamaIndex 的 docstore 中 (内存里!)。
# 如果你没有单独持久化 docstore，重启后你只有向量，不知道每个向量对应什么文本。

# 正确的完整持久化:
storage_context.persist("./storage")  # 保存 docstore + index_store
faiss.write_index(faiss_index, "./faiss_index.bin")  # 保存向量索引
```

#### 适用场景与局限

| 方面 | 评价 |
|------|------|
| 速度 | 极快（算法库直调，无网络开销） |
| 灵活性 | 最高（完全控制索引类型和参数） |
| 持久化 | 需手动处理（且文本和向量分开管理） |
| 元数据过滤 | 需自己实现（FAISS 不支持） |
| 推荐场景 | 手写管线、嵌入式部署、需要完全控制 |
| 不适合 | 需要开箱即用的元数据过滤、分布式 |

---

### 5.6 方式六：MongoDB + Chroma 多索引组合 —— 生产级架构

#### 方案定位

这是生产环境中先进但确实有效的模式：**用不同的数据库存储不同类型的数据，各取所长。**

#### 为什么要"三分存储"

核心问题：向量数据库擅长向量检索，但不擅长复杂的文档查询。反过来，MongoDB 擅长文档查询，但不支持 ANN 索引。

```
  一种常见的数据分裂:

  ┌─────────────────────────────────────────────────────────┐
  │  你需要做的查询:                                          │
  │                                                         │
  │  1. "找到和这个 Query 语义最相似的 Top-5 个 Chunk"        │
  │     → 需要 ANN 向量检索 → Chroma 最擅长                   │
  │                                                         │
  │  2. "找到 2024 年之后 HR 部门发布的所有 active 政策"       │
  │     → 需要复杂的文档查询 + 过滤 → MongoDB 最擅长           │
  │                                                         │
  │  3. "管理多个索引的元数据 (哪些 Node 属于哪个索引)"       │
  │     → 需要结构化存储 → MongoDB 合适                       │
  └─────────────────────────────────────────────────────────┘

  StorageContext 的 from_defaults() 让你可以把这三种存储
  分别指定到不同的后端:

    storage_context = StorageContext.from_defaults(
        vector_store=ChromaVectorStore(...),       # 擅长向量检索
        docstore=MongoDocumentStore(...),          # 擅长文档查询
        index_store=MongoIndexStore(...),          # 擅长元数据管理
    )
```

#### 多索引共享的好处

如果你的系统有多个索引（比如按部门分了 HR 索引、财务索引、技术索引），它们可以**共享同一个 MongoDB docstore**——同一份文档不会在多个索引中重复存储。Chroma 的 Collection 只管向量检索，文档实体存在 MongoDB 中。

#### 代码实现

```python
import chromadb
from pymongo import MongoClient
from llama_index.vector_stores.chroma import ChromaVectorStore
from llama_index.storage.docstore.mongodb import MongoDocumentStore
from llama_index.storage.index_store.mongodb import MongoIndexStore
from llama_index.core import StorageContext, VectorStoreIndex

# ── Chroma: 向量检索 ─────────────────────────────────────
chroma_client = chromadb.PersistentClient(path="./chroma_db_multi")
chroma_collection = chroma_client.get_or_create_collection("handbook_vectors")
vector_store = ChromaVectorStore(chroma_collection=chroma_collection)

# ── MongoDB: 文档 + 元数据 ────────────────────────────────
mongo_client = MongoClient("mongodb://localhost:27017")
docstore = MongoDocumentStore.from_uri(
    uri="mongodb://localhost:27017",
    db_name="rag_db",
    collection_name="documents",       # Node 文本 + metadata
)
index_store = MongoIndexStore.from_uri(
    uri="mongodb://localhost:27017",
    db_name="rag_db",
    collection_name="index_meta",      # 索引结构元数据
)

# ── 三合一 ───────────────────────────────────────────────
storage_context = StorageContext.from_defaults(
    vector_store=vector_store,
    docstore=docstore,
    index_store=index_store,
)

index_multi = VectorStoreIndex(nodes, storage_context=storage_context)

# ── 多索引共享 (创建第二个索引) ─────────────────────────
chroma_collection2 = chroma_client.get_or_create_collection("handbook_v4")
vector_store2 = ChromaVectorStore(chroma_collection=chroma_collection2)

storage_context2 = StorageContext.from_defaults(
    vector_store=vector_store2,    # 新的向量 Collection
    docstore=docstore,             # ← 共享同一个 MongoDB!
    index_store=index_store,       # ← 共享同一个 MongoDB!
)
# 文档实体不重复存储，只增加向量索引
```

#### 适用场景与局限

| 方面 | 评价 |
|------|------|
| 灵活性 | 最高——每个存储层独立选型 |
| 查询能力 | 向量+文档双重查询 |
| 复杂度 | 最高——需管理多个数据库服务 |
| 推荐场景 | 大型企业 RAG 系统、需要复杂文档查询 |
| 不适合 | 小项目（过度设计） |

---

### 5.7 补充：五种索引类型的适用场景

前面所有演示用的都是 `VectorStoreIndex`（向量索引）。LlamaIndex 还提供了其他索引类型，各有适合的场景：

| 索引类型 | 核心机制 | 检索方式 | 适用场景 | 典型查询 |
|----------|----------|----------|----------|----------|
| **VectorStoreIndex** | Embedding → 余弦相似度 | Top-K ANN | **通用 RAG（90% 场景）** | "事假需要提前多久申请？" |
| **SummaryIndex** | 所有 Node → LLM 迭代摘要 | 遍历合成 | 文档概览、摘要生成 | "这份文档的主要内容是什么？" |
| **TreeIndex** | 自顶向下树形结构 | 逐层下钻 | 长篇文档的分层问答 | "第三章具体讲了什么？" |
| **KeywordTableIndex** | 关键词 → Node 映射表 | 关键词匹配 | 精确术语查找 | "五险一金包括哪些？" |
| **KnowledgeGraphIndex** | 实体+关系三元组 | 图遍历+向量 | 需要推理实体间关系 | "哪些政策是HR发布的且2024年更新过？" |

**选择策略：**
- 90% 的 RAG 场景用 `VectorStoreIndex` 就够了
- 需要"概述整份文档"时用 `SummaryIndex`
- 需要按章节层级导航时用 `TreeIndex`
- 需要精确关键词查找时用 `KeywordTableIndex`（通常是混合检索的补充）

---

### 5.8 两种持久化策略深度对比

#### 数据库级持久化

**Chroma、Milvus、Pinecone、Qdrant、pgvector** 都是这一类别。

```
  工作原理:
    每次 write 操作 → 数据库自动写入磁盘
    Chroma → SQLite WAL (Write-Ahead Log)
    Milvus → MinIO/S3 + etcd
    Pinecone → 云端的分布式存储

  优点:
    ✅ 实时持久化 —— 写入即持久，不需要手动 save()
    ✅ 数据一致性高 —— 数据库保障 ACID 或最终一致性
    ✅ 崩溃恢复 —— 数据库自身有 WAL/checkpoint/snapshot 机制
    ✅ 并发安全 —— 多进程/多线程同时读写不会损坏数据

  缺点:
    ⚠ 频繁 IO —— 每次写入触发磁盘操作，建索引时 IO 是瓶颈
    ⚠ 资源消耗 —— 需要独立数据库进程 (Chroma 除外，它是嵌入式的)
    ⚠ 不能"回滚" —— 写错了就写错了，不像文件可以 git checkout
```

#### 框架级持久化

**LlamaIndex 的 storage_context.persist()、FAISS 的 write_index()** 都是这一类别。

```
  工作原理:
    手动调用 persist()/save() → 框架将内存数据序列化到磁盘文件
    LlamaIndex: ./storage/docstore.json + index_store.json + vector_store.json
    FAISS: ./faiss_index.bin

  优点:
    ✅ 可控次数 —— 只在需要时保存，批量处理后一次性写入
    ✅ 调试灵活 —— JSON 文件可以直接查看和编辑
    ✅ 可以版本控制 —— .json 文件可以 git commit，方便回滚
    ✅ 无额外进程 —— 不需要独立数据库服务

  缺点:
    ⚠ 可能丢数据 —— 如果在 persist() 前崩溃，未保存的数据全部丢失
    ⚠ 无并发控制 —— 多进程同时 persist() 可能覆盖对方的数据
    ⚠ 不可共享 —— JSON 文件不能多机器同时访问
    ⚠ 大规模不可用 —— JSON 文件超过数 GB 时读写性能急剧下降
```

#### 选择策略

```
  生产 vs 开发的选择:

  原型/开发阶段:
    → 框架级持久化
    理由: 快速迭代，每天可能改很多次索引结构
          JSON 文件方便调试，随时可以看了再改
          数据丢了也不怕——重新跑一遍 Embedding
    实践: LLamaIndex 的 storage_context.persist("./storage")

  生产环境:
    → 数据库级持久化
    理由: 自动实时持久化，不会丢数据
          有崩溃恢复机制
          多副本、高可用
    实践: Chroma / Milvus / Pinecone

  混合方案 (最稳健):
    → 数据库级持久化 (主要) + 框架级定期导出 (备份)
    → 即使数据库出了问题，还有一份 .json 文件可以恢复
    → 生产环境的"防弹"策略
```

---

### 5.9 六种方式全景速查

| 方式 | 存储位置 | 持久化 | 规模上限 | 复杂度 | 何时选用 |
|------|----------|:---:|------|:---:|----------|
| 1. 内存 | Python 进程 | ❌ | <10万 | 极低 | 跑通流程、调参数 |
| 2. Chroma | 本地 SQLite | ✅ 自动 | <100万 | 低 | 本地开发、小规模生产 |
| 3. Pinecone | 云端 SaaS | ✅ 自动 | 不限 | 极低 | 不想运维、快速上线 |
| 4. Milvus | 自建服务 | ✅ 自动 | 十亿+ | 高 | 大规模生产、极致性能 |
| 5. FAISS | 本地文件 | ⚠ 手动 | <千万 | 中 | 手写管线、嵌入式 |
| 6. MongoDB+Chroma | 混合 | ✅ 自动 | <100万(向量) | 中高 | 需要复杂文档查询+向量 |

**选择流程图：**

```
  你的项目处于什么阶段?

  原型验证 (写代码试想法)
    → 方式 1 (内存) 或 方式 2 (Chroma)

  内部测试 (给同事试用)
    → 方式 2 (Chroma) —— 数据需要保留

  生产上线 (对外服务)
    ├─ 不想运维 → 方式 3 (Pinecone)
    ├─ 数据量大 (千万+) → 方式 4 (Milvus)
    └─ 小规模 (<百万) → 方式 2 (Chroma) 或 pgvector

  完全自定义 (不用框架)
    → 方式 5 (FAISS) —— 自己搭全套
```


---

## 第六章：向量检索工具封装 —— 让 Agent 能"查知识库"

前五章我们构建了从文档到向量数据库的完整链路。但在真实应用中，检索能力通常不是独立运行的——它是 Agent 手中的一个"工具"，在被需要时才被调用。

### 6.1 为什么要把检索封装成工具


---

