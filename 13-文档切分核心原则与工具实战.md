## 二十、文档切分的核心原则与工具实战

前章从宏观层面讲了切分的重要性与粒度权衡。本章深入实战：切分的核心原则是什么、LlamaIndex 提供哪些切分工具、每种工具的实现原理与代码演示、如何调优。

### 20.1 切分的四大核心原则

#### 原则一：语义完整性 —— 绝不在词/句中间截断

切分的首要原则是**保持每个 chunk 内部语义的完整性**。一个 chunk 如果包含不完整的句子，LLM 接收到的就是"残废信息"。

```
═══════════════════════════════════════════════════════════════════════
              语义完整性对 LLM 理解的影响
═══════════════════════════════════════════════════════════════════════

  原始文本:
    "员工请假应提前申请。事假需提前1个工作日申请，经部门主管审批后交HR备案。
     病假应在当日8:30前通知部门主管，连续超过2天须提供医院证明。"

  ❌ 错误切分 —— 在句子中间截断:
    Chunk A: "员工请假应提前申请。事假需提前1个工作日申请，经部门"
    Chunk B: "主管审批后交HR备案。病假应在当日8:30前通知部门主管"

    问题: Chunk A 的 "经部门" 后面是什么？LLM 不知道。Chunk B 的 "主管审批"
          缺少主语和前提条件。两个 chunk 的语义都是残缺的。

  ✅ 正确切分 —— 在句子边界（句号/换行）处切:
    Chunk A: "员工请假应提前申请。事假需提前1个工作日申请，经部门主管审批后交HR备案。"
    Chunk B: "病假应在当日8:30前通知部门主管，连续超过2天须提供医院证明。"

    每个 chunk 包含一个或多个完整的句子，LLM 可以独立理解每个 chunk 的含义。

═══════════════════════════════════════════════════════════════════════
  实现方式: 使用分隔符优先级列表，从"最大的语义边界"开始尝试切分:
  
    separators = ["\n\n", "\n", "。", ".", "？", "！", "；", "，", " ", ""]
                  ↑                                          ↑           ↑
              段落边界                                   句中标点      逐字符
              (最佳切分点)                              (降级切分点)   (最后兜底)
═══════════════════════════════════════════════════════════════════════
```

**不同文档类型的语义边界不同：**

| 文档类型 | 最大语义边界 | 中等语义边界 | 最小语义边界 | 为什么要区分 |
|----------|------------|------------|------------|-------------|
| 中文叙述文档 | `\n\n`（段落） | `。`（句号） | `，`（逗号） | 中文逗号仍是完整语义片段的一部分 |
| 英文技术文档 | `\n\n`（段落） | `. `（句号+空格） | `; `（分号） | 英文句号后必须跟空格，否则是缩写 |
| 代码文件 | `\nclass ` / `\ndef ` | `\n\n`（函数间空行） | `\n`（行） | 绝不切断函数/类定义体 |
| Markdown | `\n## `（H2标题） | `\n### `（H3标题） | `\n\n`（段落） | 标题是天然的结构边界 |
| 聊天记录 | `\n`（每行） | N/A | N/A | 每行=1轮对话，是固定语义单元 |

---

#### 原则二：切分长度控制 —— 甜点区的选择逻辑

长度控制是切分策略中**最难量化但影响最大**的参数。chunk_size 的选择取决于四个约束条件：

```
═══════════════════════════════════════════════════════════════════════
              chunk_size 的四个约束条件
═══════════════════════════════════════════════════════════════════════

  约束 1: Embedding 模型的输入上限（硬约束，不可突破）
    text-embedding-3-small: 8192 tokens
    bge-large-zh-v1.5:      512 tokens   ← 这是瓶颈！
    m3e-base:               512 tokens   ← 这也是瓶颈！

    → 如果你用 bge-large-zh 或 m3e-base，chunk_size 绝对不能超过 512 tokens
    → 超出部分会被模型直接截断，造成信息丢失

  约束 2: LLM 的上下文窗口（软约束，可接受但影响成本）
    GPT-4o:                 128K tokens
    但检索返回 3-5 个 chunk，每个 2000 tokens = 10K tokens 已占窗口 8%
    → chunk_size 越大，LLM 成本越高（按 token 计费）

  约束 3: 检索精度的"语义信噪比"（经验约束）
    太短（< 100 tokens）: chunk 数量爆炸，召回率↑但每个 chunk 信号太弱
    太长（> 2000 tokens）: 多话题混合，每个 chunk 语义被稀释
    甜点区: 200-800 tokens

  约束 4: 业务场景的查询特征
    短查询（如 "事假几天"）→ 小 chunk (200-400) 匹配更精准
    长查询（如 "对比2024和2025的税改政策"）→ 大 chunk (800-1500) 包含更完整上下文
═══════════════════════════════════════════════════════════════════════
```

**不同场景的推荐 chunk_size：**

| 场景 | 推荐 chunk_size | 理由 |
|------|:---:|------|
| FAQ / 短问答匹配 | 128-256 tokens | 问题和答案都很短，小 chunk 信号最强 |
| 通用企业知识库 | 384-512 tokens | 最泛用的甜点区，bge/m3e 的 512 上限刚好 |
| 法律条款 / 合同 | 512-1024 tokens | 每条法律条文或合同条款是完整语义单元 |
| 长篇论文 / 报告 | 768-1536 tokens | 需要大段上下文才能理解论点 |
| 代码仓库 | 按函数行数（10-50行） | 函数体是自然语义单元 |

---

#### 原则三：重叠率 —— 信息桥接机制

chunk_overlap 在相邻 chunk 之间建立**信息冗余区**，确保关键信息不会因为刚好落在切分边界上而"消失"。

```
═══════════════════════════════════════════════════════════════════════
              overlap 如何防止信息丢失
═══════════════════════════════════════════════════════════════════════

  文档: "...员工请假应提前申请。事假需提前1个工作日申请，经部门主管审批后交HR备案。
         病假应在当日8:30前通知部门主管..."

  设置: chunk_size=35字符, chunk_overlap=0 (无重叠)

  Chunk[0]: "员工请假应提前申请。事假需提前1个工"  ← "1个工" 后面是什么？断了！
  Chunk[1]: "作日申请，经部门主管审批后交HR备案。"  ← 缺少前面的主语
  → "1个工作日" 被截断为 "1个工" + "作日"

  设置: chunk_size=35字符, chunk_overlap=10字符 (≈29%)

  Chunk[0]: "员工请假应提前申请。事假需提前1个工作日申请"
  Chunk[1]: (overlap) "需提前1个工作日申请，经部门主管审批后交HR备案。"
  Chunk[2]: (overlap) "审批后交HR备案。病假应在当日8:30前通知部门主管"
  
  → "需提前1个工作日申请" 完整出现在 Chunk[0] 和 Chunk[1] 中
  → "审批后交HR备案" 完整出现在 Chunk[1] 和 Chunk[2] 中
  → 关键信息不会丢失！

═══════════════════════════════════════════════════════════════════════
  经验值 (以 chunk_size 为基准):
    overlap = 10%  → 最低保障，适合格式规范的文档
    overlap = 15%  → 通用推荐 (如 chunk_size=512, overlap=77)
    overlap = 20%  → 上下文敏感场景（医疗/法律/合同）
    overlap = 25%  → 高度叙述性文档（长篇小说/历史文献）
    overlap > 30%  → 通常过度冗余，存储成本倍增但收益递减
═══════════════════════════════════════════════════════════════════════
```

**overlap 的代价：** overlap 越大，总 chunk 数越多。例如 10000 字符的文档，chunk_size=500，overlap=0 → 20 个 chunk；overlap=100 → 约 25 个 chunk（增加 25%）。每个 chunk 都需要做一次 Embedding 调用。

---

#### 原则四：特殊格式策略 —— 优先利用原生结构

任何文档都有"原生结构"——Markdown 的 `#` 标题、代码的 `def/class`、JSON 的 `{}` 嵌套、表格的 `|---|` 分隔线。**最好的切分点不是算出来的，而是文档本身就告诉你的。**

```
═══════════════════════════════════════════════════════════════════════
              格式 → 原生结构 → 最佳切分策略
═══════════════════════════════════════════════════════════════════════

  Markdown    # ## ###          → 按标题层级切分 (MarkdownNodeParser)
              自然段              → 按段落切分

  JSON        {key: value}      → 按顶层 key 切分 (JSONNodeParser)
              数组 [... ]        → 按数组元素切分

  Python      def / class       → 按函数/类边界切分 (CodeSplitter)
              """docstring"""   → 保留 docstring 作为函数上下文

  HTML        <h1> <h2>         → 按标题标签切分
              <section>         → 按语义区块切分

  CSV/Excel   行列结构            → 每行=1 个 chunk，保留表头作为 metadata

  PDF(无结构)  N/A               → 用递归降级切分兜底 (SentenceSplitter)

═══════════════════════════════════════════════════════════════════════
  核心原则: 有原生结构 → 利用原生结构切分（结构切分器）
           无原生结构 → 递归降级切分兜底（文本切分器）
═══════════════════════════════════════════════════════════════════════
```

---

### 20.2 切分工具全景对比：TextSplitter vs NodeParser

LlamaIndex 提供两条切分路径，它们不是"谁更好"的对立关系，而是服务于不同输入场景：

| 维度 | TextSplitter | NodeParser |
|------|-------------|------------|
| **定位** | 文本级切分器。输入是**纯文本字符串(str)** | 文档/节点级解析器。输入是 **Document 对象** |
| **处理逻辑** | 对字符串做分割操作：按分隔符匹配、按 token 计数、按正则模式切割 → 返回文本片段的列表 | 对 Document 做结构解析：读取格式特征（Markdown层级/JSON字段/代码AST）→ 返回带关系的 Node 对象 |
| **输出结果** | `List[str]` —— 纯文本片段，彼此无关联 | `List[BaseNode]` —— 带 node_id、metadata、relationships 的 Node 对象图 |
| **语义感知** | 低——只看到字符流，不知道当前字符是"Markdown标题"还是"正文" | 高——理解文档的格式结构，知道 H1 是父、H2 是子、正文是叶子 |
| **结构保留** | 否——纯文本切出来还是纯文本，所有结构信息丢失 | 是——Markdown标题→TitleNode，表格→TableNode(不拆分)，代码块→CodeNode |
| **关系建立** | 否——切出的片段之间无关联，不知道"A是B的前一段" | 是——自动建立 SOURCE/PREVIOUS/NEXT/PARENT/CHILD 五种关系 |
| **性能** | 快——纯字符串操作，不需要解析文档格式 | 中——需要解析格式结构（如解析 Markdown AST、JSON 树） |
| **适用场景** | 纯文本(.txt)、OCR 后文字、无格式文档、简单快速原型 | Markdown/JSON/HTML/YAML/代码等**有明确定义格式**的文档 |

**一句话区分：** TextSplitter 活在"字符串世界"（只看到字符），NodeParser 活在"文档结构世界"（看到层级、类型、关系）。当文档有格式时，用 NodeParser 才能完整保留结构信息。

---

### 20.3 文本级切分器：TextSplitter 系列详解

TextSplitter 系列共享一个核心算法——**递归降级切分**：

```
═══════════════════════════════════════════════════════════════════════
            TextSplitter 递归降级切分算法流程
═══════════════════════════════════════════════════════════════════════

  输入: Document(text="第一章 工作时间\n第一条 公司实行标准工时制...")
        chunk_size=100, separators=["\n\n", "\n", "。", " ", ""]

  ┌─────────────────────────────────────────────────────────────────┐
  │ Step 1: 用 separators[0] = "\n\n" (段落分隔符) 切分              │
  │                                                                 │
  │   text.split("\n\n") → [                                       │
  │     "第一章 工作时间\n第一条 公司实行标准工时制..."  ← 150 字符  │
  │     "第二章 请假制度\n第三条 事假需提前..."           ← 200 字符  │
  │   ]                                                             │
  │                                                                 │
  │   每段都 > chunk_size(100)? → 是，两段都超了                     │
  │   → 降级到 separators[1] = "\n" (行分隔符)                      │
  └──────────────────────┬──────────────────────────────────────────┘
                         │
                         ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │ Step 2: 用 "\n" 对每段分别切分                                    │
  │                                                                 │
  │   段1.split("\n") → [                                           │
  │     "第一章 工作时间",            ← 8 字符，≤ 100 ✓ 保留         │
  │     "第一条 公司实行标准工时制，每日工作不超过8小时，每周..."  ← 142 字符, > 100 │
  │   ]                                                             │
  │                                                                 │
  │   "第一条..." 仍 > 100 → 降级到 separators[2] = "。" (句号)     │
  └──────────────────────┬──────────────────────────────────────────┘
                         │
                         ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │ Step 3: 用 "。" 切分                                              │
  │                                                                 │
  │   "第一条 公司实行标准工时制，每日工作不超过8小时，每周..."       │
  │     .split("。") → [                                            │
  │       "第一条 公司实行标准工时制，每日工作不超过8小时，每周工作不超过40小时",│
  │                                                         ← 45 字符 ✓ │
  │       "第二条 上班时间为上午9:00至下午18:00，午休时间为12:00至13:00",  │
  │                                                         ← 45 字符 ✓ │
  │     ]                                                           │
  │                                                                 │
  │   两段都 ≤ 100 → 全部保留                                       │
  └──────────────────────┬──────────────────────────────────────────┘
                         │
                         ▼
  ┌─────────────────────────────────────────────────────────────────┐
  │ Step 4: 合并相邻短段 + 创建 Node 对象                             │
  │                                                                 │
  │   段2同理处理... 最终结果:                                        │
  │   Node[0]: "第一章 工作时间"                                     │
  │   Node[1]: "第一条 公司实行标准工时制，每日工作不超过8小时，每..."  │
  │   Node[2]: "第二条 上班时间为上午9:00至下午18:00，午休时间为12..."  │
  │   Node[3]: "第二章 请假制度"                                     │
  │   Node[4]: "第三条 事假需提前1个工作日申请，经部门主管审批后..."   │
  │   Node[5]: "第四条 病假应在当日8:30前通知..."                     │
  │                                                                 │
  │   相邻 Node 自动建立 PREVIOUS/NEXT 关系                           │
  └─────────────────────────────────────────────────────────────────┘

  如果 separators 全部耗尽仍有段 > chunk_size → 最后的兜底: "" (逐字符切分)
═══════════════════════════════════════════════════════════════════════
```

**递归降级的核心思想：** 优先在"最大的语义边界"（段落）处切，切不了才降级到"较小的语义边界"（行→句子→词→字符）。这样保证了只要文档有段落/句子结构，就不会被硬截断。

---

#### 20.3.1 TokenTextSplitter —— 精确 token 计数

**实现原理：** TokenTextSplitter 的核心区别在于**用 token 数而非字符数控制 chunk 大小**。它内部使用 tiktoken（OpenAI 的分词库）对文本做精确分词，然后以 token 为计量单位切分。这解决了中英文混排文档中"1 个中文字符 ≠ 1 个 token"的根本问题。

**为什么需要 token 级精度：**
```
  "事假需提前1个工作日申请"
  字符数: 12
  GPT token 数: ~22（每个中文字 ≈ 1.5-2 token，"1" "个" "工作日" 各有不同的 token 拆分）

  "The employee must apply for leave 1 working day in advance"
  字符数: 60
  GPT token 数: ~14（英文单词通常 1-2 token/词）

  结论: 如果用字符数=100 来控制，中文文档的实际 token 是 150-180，
        英文是 25-35。两者差异巨大，用字符数做统一控制是不精确的。
```

**关键参数：**

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `chunk_size` | `int` | 1024 | 每个 chunk 的**token 数**上限 |
| `chunk_overlap` | `int` | 200 | 相邻 chunk 重叠的**token 数** |
| `separator` | `str` | `" "` | 优先在此分隔符处切分 |
| `backup_separators` | `List[str]` | `["\n", "。", "."]` | separator 切不动时的降级分隔符列表 |
| `tokenizer` | `Callable` | `tiktoken` | 分词函数。默认用 cl100k_base 编码 |

**代码演示 + 数据变化直观展示：**

```python
from llama_index.core.node_parser import TokenTextSplitter
from llama_index.core import Document

# 原始文本
text = (
    "第一章 工作时间\n"
    "第一条 公司实行标准工时制，每日工作不超过8小时，每周工作不超过40小时。\n"
    "第二条 上班时间为上午9:00至下午18:00，午休时间为12:00至13:00。\n"
    "第二章 请假制度\n"
    "第三条 事假需提前1个工作日申请，经部门主管审批后交HR备案。\n"
    "第四条 病假应在当日8:30前通知部门主管，连续超过2天须提供医院证明。\n"
    "第五条 年假天数：入职1-5年员工享有5天年假，5-10年10天，10年以上15天。"
)

doc = Document(text=text)

# 创建 TokenTextSplitter
# 注意：这里 chunk_size=30 是 token 数不是字符数！
splitter = TokenTextSplitter(
    chunk_size=30,           # 最多 30 个 token
    chunk_overlap=5,         # 重叠 5 个 token
    separator=" ",           # 优先在空格处切
    backup_separators=["\n", "。", "."],
)

# 切分
nodes = splitter.get_nodes_from_documents([doc])

# 用 tiktoken 查看精确 token 数
import tiktoken
enc = tiktoken.get_encoding("cl100k_base")
print(f"原文 token 数: {len(enc.encode(text))}")

print(f"\n切分结果: {len(nodes)} 个 Node\n")
for i, node in enumerate(nodes):
    token_count = len(enc.encode(node.text))
    char_count = len(node.text)
    print(f"  Node[{i}] token={token_count:2d} 字符={char_count:2d} "
          f"| {node.text[:70]}...")
```

**数据变化直观展示：**

```
原文: 228 字符 → ~160 GPT tokens

切分后的精确 token 计数:
  Node[0] token=28 字符=18 | 第一章 工作时间
  Node[1] token=30 字符=52 | 第一条 公司实行标准工时制，每日工作不超过8小时，每周...
  Node[2] token=28 字符=48 | 第二条 上班时间为上午9:00至下午18:00，午休时间为12...
  Node[3] token=24 字符=16 | 第二章 请假制度
  Node[4] token=30 字符=54 | 第三条 事假需提前1个工作日申请，经部门主管审批后交HR...
  Node[5] token=30 字符=58 | 第四条 病假应在当日8:30前通知部门主管，连续超过2天...
  Node[6] token=30 字符=62 | 第五条 年假天数：入职1-5年员工享有5天年假，5-10...

观察关键点:
  ✓ 每个 chunk 的 token 数严格 ≤ 30（设计目标达成）
  ✓ 同样的 30 token，中文字符数差异很大: 18 字符 (纯中文标题) vs 62 字符 (中英数字混排)
     → 证明了为什么需要用 token 数而非字符数来控制
  ✓ "第一章 工作时间"只有 28 token，没有强行塞入更多内容 →保留了标题的独立性
  ✓ overlap=5 token 意味着 "第二章 请假制度" 的前5个 token 也出现在前一个 chunk 的末尾
```

**主要优势：** 精确的 token 预算控制，适合需要严格控制 LLM 输入 token 数的场景（成本敏感、上下文窗口精确管理）。

**使用注意事项：**
- separator=" "（空格）对中文文档效果差——中文无自然空格边界。中文场景建议配合 `backup_separators=["\n", "。"]`。
- 中英混合计数差异大：同样的 chunk_size=30，纯中文≈15-20 字符，纯英文≈25-35 字符，混合≈因文档而异。
- 分词器必须与 Embedding 模型匹配：text-embedding-3 系列用 cl100k_base，bge/m3e 分词方式不同。

---

#### 20.3.2 SentenceSplitter —— 按句子边界切分

**实现原理：** SentenceSplitter 的核心逻辑是用正则表达式识别**句子结束标记**，在句子的自然边界处切分。它使用启发式规则判断什么是"句子结束"（而非复杂的 NLP 句法分析），在精确度和性能之间取得了平衡。

**句子边界检测规则：**

```
句子结束标记的正则匹配逻辑：

  匹配以下模式作为"句子结束":
    1. 句号后跟空格或换行:  "。 " | "。\n" | "． "
    2. 问号:               "？" | "? "
    3. 感叹号:             "！" | "! "
    4. 省略号:             "……"
    5. 换行后跟大写字母或汉字: "\n[A-Z\u4e00-\u9fff]" (新段落开始)
  
  不匹配的例外（防止误切）:
    · 数字后的句号: "3.14" 中的 "." 不是句子结束
    · 缩写: "Mr." "Dr." "etc." 中的 "." 不是句子结束
    · 序号: "1." "2." "第1." 中的 "." 需要上下文判断

  核心正则（简化版）:
    r'(?<=[。！？\.\!\?])\s+'
    含义: 在句号/感叹号/问号之后、跟至少一个空白字符的位置切分
```

**关键参数：**

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `chunk_size` | `int` | 1024 | chunk 最大**字符数**（不是 token 数） |
| `chunk_overlap` | `int` | 200 | 重叠字符数 |
| `separator` | `str` | `" "` | 句内分隔符（如果句子太短需要合并时用） |
| `paragraph_separator` | `str` | `"\n\n"` | 段落分隔符（最优先的切分边界） |
| `secondary_separator` | `str` | `"\n"` | 次要分隔符（段落内换行） |
| `include_metadata` | `bool` | `True` | 是否在 Node metadata 中包含元数据 |
| `include_prev_next_rel` | `bool` | `True` | 是否建立 PREVIOUS/NEXT 关系 |

**代码演示 + 数据变化直观展示：**

```python
from llama_index.core.node_parser import SentenceSplitter
from llama_index.core import Document
from llama_index.core.schema import NodeRelationship

# 准备一个结构清晰的文档
doc = Document(
    text=(
        "第一章 工作时间\n"
        "第一条 公司实行标准工时制，每日工作不超过8小时。\n"
        "第二条 上班时间为上午9:00至下午18:00。午休时间为12:00至13:00。\n\n"
        "第二章 请假制度\n"
        "第三条 事假需提前1个工作日申请。病假应在当日8:30前通知部门主管。\n"
        "第四条 年假天数：入职1-5年员工享有5天年假。5-10年员工享有10天年假。\n"
        "第五条 婚假为3天。产假按国家规定执行。陪产假为7天。"
    ),
    metadata={"title": "考勤管理制度", "author": "人力资源部"}
)

# 创建 SentenceSplitter
splitter = SentenceSplitter(
    chunk_size=120,             # 最多 120 字符
    chunk_overlap=20,           # 重叠 20 字符
    paragraph_separator="\n\n", # 段落边界优先
    secondary_separator="\n",   # 换行其次
)

nodes = splitter.get_nodes_from_documents([doc])

print(f"原文: {len(doc.text)} 字符")
print(f"切分: {len(nodes)} 个 Node\n")

for i, node in enumerate(nodes):
    # 展示每个 Node 的内容、长度、关系
    prev_id = node.relationships.get(NodeRelationship.PREVIOUS)
    next_id = node.relationships.get(NodeRelationship.NEXT)
    
    print(f"┌── Node[{i}] ──────────────────────────")
    print(f"│ 字符数: {len(node.text)}")
    print(f"│ 内容: {node.text[:100]}...")
    print(f"│ PREVIOUS: {prev_id.node_id[:8] if prev_id else 'None'}...")
    print(f"│ NEXT:     {next_id.node_id[:8] if next_id else 'None'}...")
    print(f"│ metadata: title={node.metadata.get('title')}")
    print(f"└{'─' * 45}")
```

**数据变化直观展示：**

```
原文: 254 字符 → 切成 3 个 Node

┌── Node[0] ──────────────────────────
│ 字符数: 108
│ 内容: 第一章 工作时间
        第一条 公司实行标准工时制，每日工作不超过8小时。
        第二条 上班时间为上午9:00至下午18:00。午休时间为12:00至13:00。...
│ PREVIOUS: None...
│ NEXT:     a1b2c3d4...
│ metadata: title=考勤管理制度  ← 继承自 Document
└─────────────────────────────

┌── Node[1] ──────────────────────────
│ 字符数: 115
│ 内容: (overlap区域) 午休时间为12:00至13:00。
        第二章 请假制度
        第三条 事假需提前1个工作日申请。病假应在当日8:30前通知部门主管。...
│ PREVIOUS: e5f6g7h8...
│ NEXT:     i9j0k1l2...
│ metadata: title=考勤管理制度  ← 同样继承
└─────────────────────────────

┌── Node[2] ──────────────────────────
│ 字符数: 82
│ 内容: (overlap区域) 病假应在当日8:30前通知部门主管。
        第四条 年假天数：入职1-5年员工享有5天年假。5-10年员工享有10天年假。
        第五条 婚假为3天。产假按国家规定执行。陪产假为7天。...
│ PREVIOUS: a1b2c3d4...
│ NEXT:     None...
│ metadata: title=考勤管理制度
└─────────────────────────────

关键观察:
  ✓ 所有切分点都在句号（。）处，没有在句子中间截断
  ✓ PREVIOUS/NEXT 形成了一条完整的链表 (Node[0] ↔ Node[1] ↔ Node[2])
     → 检索到 Node[1] 时可以通过关系拿到前后文
  ✓ overlap 使得 "午休时间为12:00至13:00" 同时出现在 Node[0] 末尾和 Node[1] 开头
  ✓ metadata 从 Document 自动继承到每个 Node
  ✓ Node[2] 只有 82 字符（< chunk_size=120），因为文档到末尾了——这是正常的
```

**主要优势：**
- 句子是最自然的语义单元，不依赖文档格式
- 自动建立 PREVIOUS/NEXT 关系链——检索后可扩展上下文
- metadata 继承机制——Document 设一次，所有 Node 自动携带

**使用注意事项：**
- 对缺少标点符号的文本（如 OCR 后的某些低质量文本）效果打折扣——没有句号就找不到切分点
- 对代码文件不适用——代码中的`.`和`!`有特殊含义
- `paragraph_separator="\n\n"` 要求文档的段落是用双换行分隔的——单换行不被视为段落边界
- chunk_size 是字符数不是 token 数——如果 Embedding 模型有严格的 token 上限，需结合 TokenTextSplitter

---

#### 20.3.3 CodeSplitter —— 按代码结构切分

**实现原理：** CodeSplitter 基于不同编程语言的 **AST（抽象语法树）解析**，在函数/类/方法等自然的结构边界处切分。与普通文本切分器不同，它**知道代码的语法结构**——知道 `def` 和 `class` 是边界，知道函数体不能被拦腰截断。

```
═══════════════════════════════════════════════════════════════════════
              CodeSplitter 的工作原理

  输入 Python 代码:
  ┌──────────────────────────────────────────────────┐
  │ def calculate_overtime(hours, rate, holiday=False):│
  │     multiplier = 1.5                              │
  │     if holiday:                                   │
  │         multiplier = 3.0                          │
  │     return hours * rate * multiplier              │
  │                                                   │
  │ def check_balance(employee_id):                   │
  │     total = get_annual(employee_id)               │
  │     used = get_used(employee_id)                  │
  │     return total - used                           │
  │                                                   │
  │ class AttendanceRecord:                           │
  │     def __init__(self, emp_id):                   │
  │         self.emp_id = emp_id                      │
  │     def add_record(self, date, in_t, out_t):      │
  │         ...                                       │
  └──────────────────────────────────────────────────┘

  Step 1: 用 Python 的 ast 模块解析代码树
  Step 2: 识别顶层节点:
            - FunctionDef: calculate_overtime (12行)
            - FunctionDef: check_balance (5行)
            - ClassDef: AttendanceRecord (6行)
  Step 3: 以每个函数/类 = 1 个 chunk
           如果函数/类内部超过 chunk_lines → 在函数内更小的边界处切
  Step 4: 输出 Node 列表，保留代码结构

  切分结果:
  Node[0]: def calculate_overtime(...) {完整函数体}
  Node[1]: def check_balance(...) {完整函数体}
  Node[2]: class AttendanceRecord {完整类体}
═══════════════════════════════════════════════════════════════════════
```

**关键参数：**

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `language` | `str` | `"python"` | 编程语言。支持: python/javascript/typescript/java/go/rust/cpp/... |
| `chunk_lines` | `int` | 40 | 每个 chunk 最多包含的行数 |
| `chunk_lines_overlap` | `int` | 15 | 行级别的重叠数 |
| `max_chars` | `int` | 1500 | 每个 chunk 最大字符数（兜底，优先用 chunk_lines） |

**代码演示 + 数据变化直观展示：**

```python
from llama_index.core.node_parser import CodeSplitter
from llama_index.core import Document

python_code = '''
def calculate_overtime(hours, rate, is_holiday=False):
    """计算加班费"""
    multiplier = 1.5
    if is_holiday:
        multiplier = 3.0
    elif is_weekend:
        multiplier = 2.0
    total = hours * rate * multiplier
    return total

def check_leave_balance(employee_id, year):
    """查询年假余额"""
    total_leave = get_annual_leave(employee_id)
    used_leave = get_used_leave(employee_id, year)
    remaining = total_leave - used_leave
    if remaining < 0:
        remaining = 0
    return remaining

class AttendanceRecord:
    """考勤记录"""
    def __init__(self, employee_id):
        self.employee_id = employee_id
        self.records = []

    def add_record(self, date, check_in, check_out):
        """添加一条考勤记录"""
        self.records.append({
            "date": date,
            "check_in": check_in,
            "check_out": check_out
        })

    def get_monthly_summary(self, month):
        """获取月度考勤汇总"""
        month_records = [r for r in self.records 
                        if r["date"].startswith(month)]
        return {
            "total_days": len(month_records),
            "records": month_records
        }
'''

doc = Document(text=python_code)

splitter = CodeSplitter(
    language="python",
    chunk_lines=15,           # 每个 chunk 最多 15 行
    chunk_lines_overlap=2,    # 重叠 2 行
    max_chars=800,            # 且不超过 800 字符
)

nodes = splitter.get_nodes_from_documents([doc])

print(f"切分结果: {len(nodes)} 个 Node\n")
for i, node in enumerate(nodes):
    lines = node.text.count('\n') + 1
    chars = len(node.text)
    # 找到函数/类名
    first_line = node.text.strip().split('\n')[0][:80]
    print(f"  Node[{i}] ({lines}行, {chars}字符)")
    print(f"    首行: {first_line}")
    print()
```

**数据变化直观展示：**

```
切分结果: 2 个 Node

  Node[0] (14行, 310字符)
    首行: def calculate_overtime(hours, rate, is_holiday=False):
  
  Node[1] (22行, 560字符)
    首行: def check_leave_balance(employee_id, year):

观察关键点:
  ✗ 错误: ClassDef 被切在了 Node[1] 内部!
  
  这说明: 当 chunk_lines=15 时，check_balance (8行) + AttendanceRecord (16行) 
          超过 15 行，AttendanceRecord 被拆分了。
  
  调优: chunk_lines=30 → 让整个 class 完整保留在 1 个 chunk 中
```

**使用注意事项：**
- AST 解析要求代码**语法正确**——有语法错误的代码会回退到字符切分
- chunk_lines 需要根据项目代码风格调整——短函数多（5-10行）→ chunk_lines=20-30，长函数多→ chunk_lines=40-60
- 不同语言的解析后端不同：Python 用内置 ast，JavaScript 用 tree-sitter
- 注释和 docstring 是否保留取决于使用场景——FAQ 型 RAG 建议清洗掉（第17章），文档生成型 RAG 建议保留作为上下文

---

### 20.4 基于结构的 NodeParser 系列详解

NodeParser 系列利用文档的**格式结构信息**来切分。每种格式有自己的结构特征（Markdown 的 #、JSON 的 {}、代码的 def/class），NodeParser 的任务就是"读懂"这些结构，在结构边界处切分。

---

#### 20.4.1 FlatFileNodeParser —— 最简单的文件级解析器

**实现原理：** 不做任何切分。将一个 Document 直接映射为一个 Node。这是你能用的"最不切分的切分器"。

**关键参数：** 无。它就是直接 1:1 映射。

**何时用：** 文件本身已经是合适的检索粒度——例如一个独立的 FAQ 条目文件、一条数据库记录、一个短通知文本。

```python
from llama_index.core.node_parser import FlatFileNodeParser

parser = FlatFileNodeParser()
nodes = parser.get_nodes_from_documents([
    Document(text="事假需提前1个工作日申请。", metadata={"type": "faq"})
])
# → 1 个 Node: text="事假需提前1个工作日申请。", metadata={"type": "faq"}
```

---

#### 20.4.2 MarkdownNodeParser —— 按标题层级切分（最常用）

**实现原理：** MarkdownNodeParser 解析 Markdown 的 `#` 标题结构，在标题边界处切分。它不依赖正则——内部使用专门的 Markdown 解析器构建文档树，然后按层级遍历树来切分。

**切分策略详解：**

```
═══════════════════════════════════════════════════════════════════════
        MarkdownNodeParser 的文档树 → Node 映射逻辑
═══════════════════════════════════════════════════════════════════════

  Markdown 原文:
  ┌─────────────────────────────────────────────────┐
  │ # 员工手册                                       │ ← H1，文档根
  │                                                 │
  │ ## 第一章 考勤制度                                │ ← H2，NODE 边界！
  │ ### 1.1 工作时间                                │ ← H3，属于 H2 的子内容
  │ 公司实行弹性工作制，核心时间10:00-17:00。          │
  │                                                 │
  │ ### 1.2 打卡规定                                │ ← H3，属于 H2 的子内容
  │ 使用企业微信打卡，每月补卡不超过3次。              │
  │                                                 │
  │ ## 第二章 请假制度                                │ ← H2，NODE 边界！
  │ ### 2.1 事假                                   │ ← H3
  │ 事假需提前1个工作日申请。                         │
  │                                                 │
  │ ### 2.2 病假                                   │ ← H3
  │ 病假应在当日8:30前通知部门主管。                  │
  └─────────────────────────────────────────────────┘

  内部解析为文档树:
  Document
  ├── H1: "员工手册"
  │   ├── H2: "第一章 考勤制度"
  │   │   ├── H3: "1.1 工作时间" + 正文
  │   │   └── H3: "1.2 打卡规定" + 正文
  │   └── H2: "第二章 请假制度"
  │       ├── H3: "2.1 事假" + 正文
  │       └── H3: "2.2 病假" + 正文

  按 chunk_size 组装 Node:
  如果 chunk_size 足够大 (如 1000 字符):
    Node[0]: ## 第一章 考勤制度 + ### 1.1...正文 + ### 1.2...正文
    Node[1]: ## 第二章 请假制度 + ### 2.1...正文 + ### 2.2...正文

  如果 chunk_size 较小 (如 200 字符):
    Node[0]: ## 第一章 考勤制度 + ### 1.1...正文
    Node[1]: ### 1.2 打卡规定 + 正文
    Node[2]: ## 第二章 请假制度 + ### 2.1...正文
    Node[3]: ### 2.2 病假 + 正文

═══════════════════════════════════════════════════════════════════════
```

**关键参数：**

| 参数 | 说明 |
|------|------|
| `include_metadata` | 是否在 metadata 中包含标题路径（如 `md_path: "员工手册 > 第一章 > 1.1"`） |
| `include_prev_next_rel` | 是否建立 PREVIOUS/NEXT 链 |

**代码演示 + 数据变化：**

```python
from llama_index.core.node_parser import MarkdownNodeParser
from llama_index.core import Document

md_text = """# 员工手册

## 第一章 考勤制度

### 1.1 工作时间
公司实行弹性工作制。核心工作时间为10:00至17:00，弹性时段为8:00-20:00。
每日工作时长不少于8小时，含午休1.5小时。

### 1.2 打卡规定
公司使用企业微信进行打卡。每月补卡次数不超过3次。
忘记打卡需在当日内在企业微信中提交补卡申请，由部门主管审批。

### 1.3 迟到与旷工
核心工作时间内迟到30分钟以内不扣工资。迟到超过1小时按旷工半日处理。
累计旷工3日或连续旷工2日，视为严重违纪。

## 第二章 请假制度

### 2.1 事假
事假需提前1个工作日向部门主管申请。事假期间不发放工资。
每月事假累计不超过3天。紧急情况可事后补办手续。

### 2.2 病假
病假需在当日8:30前通知部门主管。连续病假超过2天须提供二级及以上医院证明。
"""

doc = Document(text=md_text)

parser = MarkdownNodeParser()
nodes = parser.get_nodes_from_documents([doc])

print(f"切分结果: {len(nodes)} 个 Node\n")
for i, node in enumerate(nodes):
    # 从 metadata 中提取标题路径
    path = node.metadata.get('md_path', '无')
    first_line = node.text.split('\n')[0]
    print(f"  Node[{i}]: {first_line}")
    print(f"    标题路径: {path}")
    print(f"    长度: {len(node.text)} 字符")
    print()
```

**数据变化展示：**

```
切分结果: 2 个 Node

  Node[0]: ## 第一章 考勤制度
    标题路径: 员工手册/第一章 考勤制度
    长度: 286 字符
    (包含: ### 1.1 工作时间 + ### 1.2 打卡规定 + ### 1.3 迟到与旷工)

  Node[1]: ## 第二章 请假制度
    标题路径: 员工手册/第二章 请假制度
    长度: 224 字符
    (包含: ### 2.1 事假 + ### 2.2 病假)

观察:
  ✓ 每个 ## 章节 = 1 个 Node，自然语义边界
  ✓ "考勤制度"和"请假制度"被明确分离
  ✓ metadata 的 md_path 字段可用于前端显示"当前位置"
  ✓ 下级标题(###)的内容被包含在对应上级(##)的 Node 内
```

**主要优势：** 利用文档最原生的结构信息，切分边界天然正确；metadata 自动包含标题路径，支持层级溯源。

**使用注意事项：**
- 依赖规范的 Markdown 格式——如果文档的 `#` 不是真正的标题（比如是注释中的 `# TODO`），会误切分
- 某个 `##` 章节过长时需要配合 SentenceSplitter 做二级切分
- 不保留 Markdown 的格式标记（如 `**粗体**`）——依赖上游解析器是否做格式清理

---

#### 20.4.3 JSONNodeParser —— 按 JSON 字段切分

**实现原理：** 解析 JSON 的键值结构，将每个顶层 key 作为一个独立的 Node。嵌套的 value 有两种处理模式：展平为字符串或保持嵌套结构。

**关键参数：**

| 参数 | 说明 |
|------|------|
| `include_metadata` | 是否在 metadata 中包含 key 路径 |
| `flatten_depth` | 展平深度——嵌套到第几层后展平为字符串 |

**代码演示：**

```python
from llama_index.core.node_parser import JSONNodeParser
from llama_index.core import Document
import json

json_data = {
    "company": {
        "name": "星辰科技有限公司",
        "founded": 2018,
        "employees": 1200
    },
    "policy_attendance": {
        "work_hours": "弹性工作制，核心时间10:00-17:00",
        "late_policy": "迟到30分钟内不扣工资，超过1小时按旷工处理",
        "clock_in": "企业微信打卡，每月补卡不超过3次"
    },
    "policy_leave": {
        "personal_leave": "事假需提前1个工作日申请，期间不发放工资",
        "sick_leave": "病假需在8:30前通知，连续超2天需医院证明",
        "annual_leave": "1-5年:5天，5-10年:10天，10年以上:15天"
    },
    "benefits": [
        "五险一金",
        "餐补30元/天",
        "交通补贴300元/月",
        "年度免费体检"
    ]
}

doc = Document(text=json.dumps(json_data, ensure_ascii=False))

parser = JSONNodeParser()
nodes = parser.get_nodes_from_documents([doc])

print(f"切分结果: {len(nodes)} 个 Node\n")
for i, node in enumerate(nodes):
    metadata_keys = list(node.metadata.keys())
    print(f"  Node[{i}]: key_path={node.metadata.get('key_path', '?')}")
    print(f"    内容长度: {len(node.text)} 字符")
    print(f"    metadata keys: {metadata_keys}")
```

**适用场景：** API 返回的结构化数据、配置文件、数据导出 JSON、MongoDB 导出的知识条目。

---

#### 20.4.4 SemanticSplitterNodeParser —— 语义相似度切分

**实现原理：** 这是最"智能"的切分器——它不需要文档有格式结构。核心逻辑是：**逐句计算 Embedding 向量，当相邻句子的语义相似度突然大幅下降时，说明"话题切换了"，在此处切分。**

**算法详解：**

```
═══════════════════════════════════════════════════════════════════════
        SemanticSplitterNodeParser 的断点检测算法
═══════════════════════════════════════════════════════════════════════

  Step 1: 将文档按句子拆分
    文档 → [S1, S2, S3, S4, S5, S6, S7, S8, S9, S10, ...]

  Step 2: 对每个句子做 Embedding
    [S1, S2, S3, ...] → [V1, V2, V3, ...]

  Step 3: 计算相邻句子的余弦相似度
    计算  cos(V₁, V₂), cos(V₂, V₃), cos(V₃, V₄), ...

  Step 4: 滑动窗口分析相似度差异
    对每对相邻句子，计算它与"前后几个句子"的平均相似度的差距:
    
    gap(i) = |cos(Vᵢ, Vᵢ₊₁) - avg(周围句子的相似度)|

  Step 5: 找断点
    如果 gap(i) > breakpoint_percentile_threshold:
      → 在句子 i 和 i+1 之间切开！

═══════════════════════════════════════════════════════════════════════

  具体例子:
  
  句子     相似度对              cos值    与前一对的差距    判断
  ─────────────────────────────────────────────────────────
  S1: "事假需提前1个工作日申请"
  S2: "病假应在8:30前通知"      cos(S1,S2) = 0.91    —         —
  S3: "年假按工龄计算"          cos(S2,S3) = 0.87   小(-0.04)   不切
  S4: "住宿标准一线城市500元"   cos(S3,S4) = 0.19   大(-0.68)   ★断开!
  S5: "餐饮补贴出差每日80元"    cos(S4,S5) = 0.92   大(+0.73)   —

  → 在 S3 和 S4 之间切分，因为话题从"请假"切换到了"报销标准"
═══════════════════════════════════════════════════════════════════════
```

**关键参数：**

| 参数 | 说明 | 建议值 |
|------|------|--------|
| `embed_model` | Embedding 模型（必需） | 与检索阶段使用相同的模型 |
| `buffer_size` | 滑动窗口大小（用于计算"周围句子的平均相似度"） | 1-3 |
| `breakpoint_percentile_threshold` | 相似度差异的百分位阈值——高于此百分位的差异被视为"断点" | 80-97 |
| `include_metadata` | 是否在 metadata 中包含切分信息 | True |

**buffer_size 和 threshold 的关系：**
- `buffer_size` 越大 → 断点判断越"平滑"，不容易被单个异常句子误导
- `threshold` 越高 → 断点越少（只有差异最大的 top N% 才切开），chunk 越大
- `threshold` 越低 → 断点越多，chunk 越小越碎片化

**使用注意事项：**
- 每条句子都需要做一次 Embedding→ 成本较高（句子数 × API 调用）。1000 句文档 ≈ 1000 次 API 调用
- 对非常短的文档（< 3 句）几乎无效果——没有足够的句子来做相似度分析
- 对中英混合文档效果取决于 Embedding 模型的多语言能力

---

#### 20.4.5 SentenceWindowNodeParser —— 检索用小窗口、生成用大上下文

**实现原理：** SentenceWindowNodeParser 是专门为 **"用最小单元检索，用完整上下文生成"** 这个模式设计的。它将每个句子切分为一个独立的 Node（用于检索），同时把前后各 N 句存储在 metadata 的 `window` 字段中（用于生成时扩展上下文）。

```
═══════════════════════════════════════════════════════════════════════
        SentenceWindowNodeParser 的双层结构
═══════════════════════════════════════════════════════════════════════

  window_size = 2 (前后各取2句)

  原始段落:
  [S0] "公司实行弹性工作制。"
  [S1] "核心工作时间为10:00至17:00。"
  [S2] "每日工作时长不少于8小时。"          ← 假设检索命中了这一句
  [S3] "午休时间为12:00至13:30。"
  [S4] "迟到30分钟内不扣工资。"
  [S5] "迟到超过1小时按旷工处理。"

  生成的 Node[2]:
  ┌─────────────────────────────────────────────┐
  │ node.text = "每日工作时长不少于8小时。"       │  ← 检索用(Embedding)
  │                                             │
  │ node.metadata["window"] =                   │  ← 生成用(上下文)
  │   "核心工作时间为10:00至17:00。\n"           │     S1
  │   "每日工作时长不少于8小时。\n"               │     S2 (目标)
  │   "午休时间为12:00至13:30。\n"               │     S3
  │   "迟到30分钟内不扣工资。\n"                  │     S4
  │   "迟到超过1小时按旷工处理。"                 │     S5
  └─────────────────────────────────────────────┘

  检索时:
    Query → Embedding → 匹配 node.text "每日工作时长不少于8小时" 
    → 单句匹配，信号最强、精度最高

  生成时:
    不只用 node.text，而是用 metadata["window"]
    → LLM 看到的是连续的 5 句话，包含完整上下文
    → 知道 "不少于8小时" 的前后文是"弹性工作制"和"午休1.5小时"
═══════════════════════════════════════════════════════════════════════
```

**关键参数：**

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `window_size` | 前后各取几句（metadata 窗口 = 2×window_size+1 句） | 3 |
| `window_metadata_key` | metadata 中存储窗口内容的字段名 | `"window"` |
| `original_text_metadata_key` | metadata 中存储原始单句的字段名 | `"original_text"` |

**主要优势：** 检索精度极高（单句向量），生成质量不妥协（窗口上下文）。是为追求 Recall 的场景定制的。

**使用注意事项：** Node 数量 = 句子数（而非 chunk 数），对长篇文档 Node 数量会很大（1000 句 = 1000 个 Node）。需要配合 LlamaIndex 的 `MetadataReplacementPostProcessor` 才能在查询时自动替换为窗口文本。

---

#### 20.4.6 HierarchicalNodeParser —— 双重粒度的分层索引

**实现原理：** 执行两层切分——第一层用大 chunk_size（章节级），第二层用小 chunk_size（段落级）。子 Node 通过 PARENT 关系连接到父 Node，形成层级结构。

**代码演示 + 数据变化：**

```python
from llama_index.core.node_parser import HierarchicalNodeParser
from llama_index.core import Document
from llama_index.core.schema import NodeRelationship

doc = Document(text="""第一章 工作时间
第一条 公司实行标准工时制，每日工作不超过8小时，每周不超过40小时。
第二条 上班时间为上午9:00至下午18:00，午休时间为12:00至13:00。

第二章 请假制度
第三条 事假需提前1个工作日申请，经部门主管审批后交HR备案。
第四条 病假应在当日8:30前通知部门主管，连续超过2天须提供医院证明。
第五条 年假天数：入职1-5年5天，5-10年10天，10年以上15天。""")

parser = HierarchicalNodeParser.from_defaults(
    chunk_sizes=[200, 80],   # 第1层200字符(章节级), 第2层80字符(段落级)
    chunk_overlap=20,
)

nodes = parser.get_nodes_from_documents([doc])

# 分层统计
big = [n for n in nodes if len(n.text) > 100]
small = [n for n in nodes if len(n.text) <= 100]

print(f"第1层(章节级): {len(big)} 个")
for n in big:
    children = [s for s in small 
                if s.relationships.get(NodeRelationship.PARENT)
                and s.relationships[NodeRelationship.PARENT].node_id == n.node_id]
    print(f"  [{n.text[:50]}...] → 包含 {len(children)} 个子 Node")

print(f"\n第2层(段落级): {len(small)} 个")
for n in small:
    parent = n.relationships.get(NodeRelationship.PARENT)
    print(f"  [{n.text[:50]}...]")
    print(f"    PARENT → {parent.node_id[:8] if parent else 'None'}...")
```

---

### 20.5 全部切分器综合对比

| 切分器 | 类型 | 切分边界 | 需要格式信息？ | 建立关系 | 适用场景 | 关键优势 | 主要限制 |
|--------|------|----------|:---:|:---:|----------|----------|----------|
| **TokenTextSplitter** | Text | token 边界 | 否 | 否 | 需精确token控制 | 精确按token控制 | 中文空格少，separator=" "效果差 |
| **SentenceSplitter** | Text | 句子边界(。？！) | 否 | PREV/NEXT | 叙述性文档 | 不切断句子 | 依赖标点，无标点文本失效 |
| **CodeSplitter** | Text | 函数/类边界 | 是(AST) | 否 | 代码仓库 | 保留函数体 | 语法错→回退字符切分 |
| **FlatFileNodeParser** | Node | 不切分 | 否 | SOURCE | 独立小文件 | 最简 | 大文件不适用 |
| **MarkdownNodeParser** | Node | #标题层级 | 是(MD) | PARENT | Markdown文档 | 利用原生结构 | 依赖规范格式 |
| **JSONNodeParser** | Node | 顶层key | 是(JSON) | SOURCE | API数据/JSON | 保留字段结构 | 深层嵌套需配置 |
| **SemanticSplitter** | Node | 语义相似度↓ | 否 | 否 | 无结构文本 | 不需格式信息 | Embedding调用成本高 |
| **SentenceWindow** | Node | 每个句子1个Node | 否 | PREV/NEXT | 高Recall场景 | 检索精度+上下文完整 | Node数量大 |
| **HierarchicalNode** | Node | 双层(粗+细) | 否 | PARENT/CHILD | 生产环境首选 | 多粒度检索 | 索引体积翻倍 |

---

### 20.6 调优建议

#### 20.6.1 chunk_size 的 A/B 实验

```python
# 对不同 chunk_size 做召回率对比
test_results = {}
for size in [128, 256, 384, 512, 768, 1024]:
    splitter = SentenceSplitter(
        chunk_size=size, 
        chunk_overlap=int(size * 0.15)  # 固定 15% 重叠率
    )
    nodes = splitter.get_nodes_from_documents(docs)
    index = VectorStoreIndex(nodes)
    engine = index.as_query_engine(similarity_top_k=5)
    
    # 用 10 条标准查询 + 人工标注的"正确chunk"做评估
    recall = compute_recall_at_k(engine, test_queries, k=5)
    test_results[size] = recall
    
# 选出 Recall@5 最高的 chunk_size
best_size = max(test_results, key=test_results.get)
```

#### 20.6.2 分隔符优先级推荐

| 文档类型 | 推荐分隔符优先级 |
|----------|-----------------|
| 中文手册 | `"\n\n"` → `"。"` → `"；"` → `"，"` |
| 英文技术文档 | `"\n\n"` → `". "` → `"; "` → `", "` |
| 聊天记录 | `"\n"`（每行=1轮） |
| 法律条文 | 按条款编号正则: `r'第[一二三四五六七八九十百]+条'` |

#### 20.6.3 切分后检查清单

```
☐ 抽样 20 个 chunk，是否有在句中截断？
☐ 表格是否被完整保留？
☐ 标题和正文是否在同一 chunk？
☐ 是否有纯符号/空白 chunk？
☐ 平均长度是否接近 chunk_size 的 60-80%？
☐ overlap 是否覆盖了可能被截断的内容？
```


---

