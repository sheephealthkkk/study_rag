## 九、RAG 的四代演进

```
                        复杂度
                          │
                          │                    ┌─────────────────┐
                          │                    │  Gen 4: Agentic │
                          │                    │     RAG         │
                          │                    │  Agent自主决策    │
                          │                    │  多路径动态规划   │
                          │         ┌──────────┤  自我纠错评估    │
                          │         │ Gen 3:   │                 │
                          │         │ Modular  │                 │
                          │         │   RAG    │                 │
                          │    ┌────┤ 可插拔   │                 │
                          │    │    │ 模块化   │                 │
                          │    │    └──────────┘                 │
                          │    │                                 │
                          │ ┌──┤                                 │
                          │ │  │ Gen 2: Advanced RAG             │
                          │ │  │ 检索前优化 + 检索后精排           │
                     ─────┼─┼──┤ 混合检索 + 查询改写 + Rerank     │
                     Gen 1│ │  └─────────────────────────────────┘
                     Naive│ │
                      RAG │
                          │
                          └──────────────────────────────────────────▶ 时间
                          2020        2022        2023        2024+
```

---

### 9.1 第一代：Naive RAG（原生 RAG）

Naive RAG 是 RAG 最原始、最基础的形式。它定义了"检索 → 增强 → 生成"这一经典范式，所有后续演进都建立在此基础之上。

#### 核心流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        Naive RAG 完整流程                                │
│                                                                         │
│  ═══════════════════════  离线阶段  ═══════════════════════              │
│                                                                         │
│  [原始文档]                                                              │
│      │                                                                  │
│      ▼                                                                  │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐          │
│  │ ① 文档   │───▶│ ② 文本   │───▶│ ③ 向量化 │───▶│ ④ 存入   │          │
│  │   加载   │    │   切分   │    │ Embedding│    │ 向量数据库│          │
│  │ PDF/Word │    │ 固定大小 │    │ 单模型   │    │ 单索引   │          │
│  │ → Document│   │ 固定重叠 │    │ → 向量   │    │          │          │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘          │
│                                                                         │
│  ═══════════════════════  在线阶段  ═══════════════════════              │
│                                                                         │
│  [用户提问]                                                              │
│      │                                                                  │
│      ▼                                                                  │
│  ┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐          │
│  │ ⑤ Query │───▶│ ⑥ 向量   │───▶│ ⑦ Prompt │───▶│ ⑧ LLM   │          │
│  │  向量化  │    │   检索   │    │   拼接   │    │   生成   │          │
│  │ 同模型   │    │ Top-K    │    │ 直接拼接 │    │ 直接回答 │          │
│  │ → 向量   │    │ → K个片段│    │ → Prompt │    │ → 答案   │          │
│  └──────────┘    └──────────┘    └──────────┘    └──────────┘          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

#### Naive RAG 的特点

**优点：**
- 架构简单，一个工程师 + 开源工具几天即可搭建
- 作为基线系统，快速验证 RAG 在业务场景中的可行性
- 便于理解和调试（每个环节输入输出清晰）

**局限：**

| 局限 | 具体表现 | 原因 |
|------|----------|------|
| **检索质量不稳定** | 用户提法稍有变化，检索结果差异大；模糊问题命中率低 | Query 直接向量化，不做任何预处理 |
| **噪声干扰** | Top-K 中混入不相关文档，干扰 LLM 判断 | 无 Rerank 精排，粗排结果直接喂给 LLM |
| **上下文浪费** | 检索片段可能重复或冗余，占用宝贵的上下文窗口 | 无去重、无压缩 |
| **缺乏验证** | 生成的答案没有校验环节，错误直接返回给用户 | 无后处理校验 |
| **一刀切流程** | 所有查询走同一流程，简单问题和复杂问题同等对待 | 无自适应路由 |

**代表框架：** 最早的 LangChain 示例、LlamaIndex 基础教程中的实现、各云厂商 RAG 基础版。

---

### 9.2 第二代：Advanced RAG（增强型 RAG）

Advanced RAG 在 Naive RAG 的基础上，在**检索前**和**检索后**分别引入优化环节，显著提升检索精度和生成质量。

#### 核心改进全景图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         Advanced RAG 流程                                    │
│                                                                             │
│  [用户原始提问]                                                              │
│       │                                                                     │
│       ▼                                                                     │
│  ╔══════════════════════════════════════════════════════════════╗           │
│  ║              检索前优化（Pre-Retrieval）                      ║           │
│  ║                                                              ║           │
│  ║  ┌──────────┐   ┌──────────────┐   ┌──────────────────┐    ║           │
│  ║  │ 查询改写  │   │  查询扩展     │   │   查询分解        │    ║           │
│  ║  │ Rewriting│   │  Expansion   │   │  Decomposition   │    ║           │
│  ║  │          │   │              │   │                  │    ║           │
│  ║  │ 模糊→精确 │   │ 短Query→多个 │   │ 复杂问题→子问题   │    ║           │
│  ║  │ "那个政策" │   │  变体Query   │   │ "对比A和B"→拆成  │    ║           │
│  ║  │ →"2025年  │   │ +关键词查询  │   │ "查A"+"查B"     │    ║           │
│  ║  │  假期政策" │   │              │   │                  │    ║           │
│  ║  └──────────┘   └──────────────┘   └──────────────────┘    ║           │
│  ║                                                              ║           │
│  ╚══════════════════════════════════════════════════════════════╝           │
│       │                                                                     │
│       ▼                                                                     │
│  ┌──────────────────────────────────────────────────────────────────┐      │
│  │                    混合检索（Hybrid Retrieval）                    │      │
│  │                                                                   │      │
│  │   优化后的 Query ──┬──▶ 向量检索（Dense）──▶ Top-K₁               │      │
│  │                    │                                              │      │
│  │                    ├──▶ BM25 检索（Sparse）──▶ Top-K₂              │      │
│  │                    │                                              │      │
│  │                    └──▶ (可选) 知识图谱检索 ──▶ Top-K₃             │      │
│  │                                   │                               │      │
│  │                                   ▼                               │      │
│  │                          RRF 融合排序 ──▶ 合并后的 Top-K           │      │
│  └──────────────────────────────────────────────────────────────────┘      │
│       │                                                                     │
│       ▼                                                                     │
│  ╔══════════════════════════════════════════════════════════════╗           │
│  ║              检索后优化（Post-Retrieval）                      ║           │
│  ║                                                              ║           │
│  ║  ┌──────────┐   ┌──────────────┐   ┌──────────────────┐    ║           │
│  ║  │ Reranker │   │  过滤/去重    │   │   上下文压缩      │    ║           │
│  ║  │   重排序  │   │  Filtering   │   │  Compression     │    ║           │
│  ║  │          │   │              │   │                  │    ║           │
│  ║  │ 粗排Top-N│   │ 相似度阈值   │   │ 长文档→摘要      │    ║           │
│  ║  │→精排Top-K│   │ 去除冗余文档 │   │ 节省Token        │    ║           │
│  ║  │ Cross-   │   │ 去重相似内容 │   │ 保留关键信息     │    ║           │
│  ║  │ Encoder  │   │              │   │                  │    ║           │
│  ║  └──────────┘   └──────────────┘   └──────────────────┘    ║           │
│  ║                                                              ║           │
│  ╚══════════════════════════════════════════════════════════════╝           │
│       │                                                                     │
│       ▼                                                                     │
│  ┌──────────┐    ┌──────────┐                                              │
│  │ Prompt   │───▶│ LLM 生成 │───▶ 最终回答                                  │
│  │ 拼接     │    │ (附引用) │                                              │
│  └──────────┘    └──────────┘                                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 检索前优化详解

**① 查询改写（Query Rewriting）**

用户的口语化提问往往简短、模糊、缺少上下文，直接用原话检索效果差。查询改写用一个轻量 LLM 将用户表述转化为"更适合检索"的标准化查询。

```
用户输入："那个请假的政策是什么来着？"（口语化、指代模糊、缺上下文）
      │
      ▼
  查询改写（LLM 改写）
      │
      ▼
改写输出："公司员工请假政策的规定内容和申请流程"
（去除口语化表达、消解指代、补充隐含信息、转换为检索友好的关键词形式）
```

**② 查询扩展（Query Expansion）**

生成用户查询的多个语义等价变体，多路检索后合并结果，减少单次检索的遗漏。

```
方法一：用 LLM 生成多个等价表述
  原始："如何提高免疫力"
  扩展："增强免疫力的方法" / "免疫力提升建议" / "改善免疫系统的饮食和运动"

方法二：HyDE (Hypothetical Document Embeddings)
  先用 LLM → 生成一个假设的理想答案文档
  再用这个"假设文档"的向量去检索（而非用问题的向量）
  原理：假设文档与实际文档的语义匹配度往往高于问题与文档的匹配度
  
  示例：
    用户问题："哺乳期可以吃布洛芬吗？"
    假设文档："哺乳期妇女服用布洛芬的安全性研究表明，布洛芬在母乳中分
             泌量极低，通常被认为是安全的止痛药选择..."（LLM生成）
    用这段"假设文档"的向量 → 去检索真实医学文献 → 匹配度显著提升
```

**③ 查询分解（Query Decomposition）**

对复杂/多跳问题，将其拆解为多个子问题，逐一检索，再综合生成答案。

```
用户输入："对比 2024 年和 2025 年的个人所得税起征点变化，以及对月薪 2 万
          员工的实际影响"
      │
      ▼
  查询分解
      │
      ├──▶ 子问题1："2024年个人所得税起征点是多少" ──▶ 检索1 ──▶ 答案1
      │
      ├──▶ 子问题2："2025年个人所得税起征点是多少" ──▶ 检索2 ──▶ 答案2
      │
      └──▶ 子问题3："月薪2万在新旧起征点下税负差多少" ──▶ 检索3 ──▶ 答案3
                                                                    │
                                                                    ▼
                                                     LLM 综合三个子答案生成最终回答
```

在模糊问题时，子问题分解可以让每个子查询更精准，每一步都能定位到具体知识。

#### 检索后优化详解

**① Reranker 重排序**

粗排（向量检索 / BM25）返回 Top-N 个候选文档（如 N=50），Reranker 对这些候选做精细排序，选出最相关的 Top-K（如 K=5）交给 LLM。

```
为什么需要 Reranker：
  Embedding 模型：双编码器，Query 和 Doc 分别向量化 → 速度快，但无交互
  Reranker：交叉编码器（Cross-Encoder），[Query + Doc] 一起送入 → 有交互
  
  双编码器（Bi-Encoder）：
    Query ──▶ 编码器 ──▶ Vec_Q  ─┐
                                  ├─ 独立编码后再比较（快但粗糙）
    Doc   ──▶ 编码器 ──▶ Vec_D ─┘
  
  交叉编码器（Cross-Encoder）：
    [Query, Doc] ──▶ 编码器 ──▶ 相似度分数（0~1）
    两个文本同时输入，注意力机制在两者之间充分交叉 ← 慢但精准

实际策略（两阶段检索）：
  阶段1（粗排）：Embedding/BM25 → 从 100万 中捞 Top-50（毫秒级）
  阶段2（精排）：Cross-Encoder Reranker → 50 中精排 Top-5（毫秒-百毫秒级）
  
常用 Reranker 模型：
  - Cohere Rerank v3（API）
  - bge-reranker-v2-m3（开源，BAAI）
  - Jina Reranker v2（开源）
  - cross-encoder/ms-marco-MiniLM-L-6-v2
```

**② 过滤与去重**

```
相似度阈值过滤：丢弃相似度分数 < 某个阈值（如 < 0.7）的文档
文档去重：相邻 chunk 可能来自同一文档，内容高度重复，去重后保留一个
元数据过滤：按日期、来源、分类等 metadata 字段过滤
```

**③ 上下文压缩**

当一个检索到的文档太长（如 chunk_size 设置为 2048 的文档），但只有其中一小段与问题相关时，用 LLM 将长文档压缩为精华摘要，节省上下文窗口。

#### 混合检索的具体实现

混合检索已在 8.7 节详细讲解。在 Advanced RAG 中，它配合检索前优化（改写后的 Query 同时送入向量和 BM25 两路）和检索后优化（Reranker 对融合结果再精排），构成一个完整的检索优化链路。

---

### 9.3 第三代：Modular RAG（模块化 RAG）

Modular RAG 将 RAG 中的各环节抽象为独立、可替换、可编排的功能模块，不再是一条固定的流水线，而是**根据场景灵活组合**。

#### 核心理念：从"管道"到"乐高"

```
Naive / Advanced RAG = 固定管道：
  检索 → 增强 → 生成
  （所有查询走同一条路径，不可变）

Modular RAG = 可插拔模块：
  
  ┌──────────┐  ┌──────────┐  ┌──────────┐
  │ 向量检索  │  │ 关键词检索│  │ 知识图谱  │  ← 可选的检索模块
  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘
        │              │              │
        └──────────────┼──────────────┘
                       │
                  ┌────┴────┐
                  │ 结果融合  │  ← 可插拔的融合模块
                  └────┬────┘
                       │
                  ┌────┴────┐
                  │  Rerank  │  ← 可选的精排模块
                  └────┬────┘
                       │
            ┌──────────┼──────────┐
            │          │          │
       ┌────┴────┐ ┌──┴──┐ ┌────┴────┐
       │ 直接生成 │ │摘要  │ │ 记忆    │  ← 可选的后处理模块
       └─────────┘ └─────┘ └─────────┘
```

#### Modular RAG 的关键模块

| 模块 | 功能 | 可替换方案举例 |
|------|------|---------------|
| **检索器** | 从知识库中找到相关文档 | 向量检索 / BM25 / 知识图谱 / Web Search |
| **路由器** | 决定当前查询走哪条检索路径 | LLM 路由决策 / 规则路由 / 意图分类 |
| **融合器** | 合并多路检索结果 | RRF / 加权融合 / 学习型融合 |
| **精排器** | 对粗排结果细排 | Cross-Encoder / LLM-as-Reranker |
| **阅读器** | 从检索结果中提取答案 | Extractive / Abstractive / 混合 |
| **记忆模块** | 跨轮次记住上下文 | 对话历史 / 向量化的对话记忆 |
| **评估器** | 评估检索/生成质量 | 忠实度评估 / 相关性评估 / 事实一致性 |
| **调度器** | 协调各模块的执行顺序 | 固定 DAG / 动态 DAG / Agent |

#### 自适应检索（Adaptive Retrieval）

Modular RAG 的一个关键特性是**根据问题类型动态选择检索策略**：

```
用户提问
    │
    ▼
┌──────────────┐
│  路由器（Router）│ ← LLM 判断问题类型
└──────┬───────┘
       │
  ┌────┼────────────────────┐
  │    │                    │
  ▼    ▼                    ▼
简单事实   多跳推理           对比分析
  │    │                    │
  ▼    ▼                    ▼
单路向量   子问题分解         并行多路
  检索   + 多轮检索          检索+融合
  │    │                    │
  └────┼────────────────────┘
       │
       ▼
   统一输出
```

#### 代表框架

- **LangChain**（最早提出模块化理念）：通过 Chain 和 LCEL（LangChain Expression Language）编排各模块
- **LlamaIndex**：强调数据索引的模块化，提供丰富的 retrieval 和 post-processing 模块
- **Haystack**（deepset）：以 Pipeline 为核心，YAML 配置编排各组件

---

### 9.4 第四代：Agentic RAG（智能体 RAG）

Agentic RAG 是 RAG 的最新演进方向。它不再依赖人工预设的检索流程，而是由一个 **Agent（智能体）自主规划、决策、执行和纠错**。

#### 核心区别：谁来决定"下一步做什么"

```
前三代 RAG：                                      Agentic RAG：
═══════════                                      ════════════
人设计流程                                          Agent 自己决定流程
  │                                                   │
  ▼                                                   ▼
固定的 DAG（有向无环图）                              动态的决策树
检索 → 增强 → 生成                                    Agent 思考："用户问的是什么？"
每一步是人规定的                                     → "我需要先查A知识库"
                                                     → "结果不够，再搜B数据库"
                                                     → "还需要计算一下"
                                                     → "综合以上，回答用户"
                                                     → "这个回答对吗？验证一下"
```

#### Agent 的核心能力

```
                    ┌──────────────────────────┐
                    │     Agentic RAG Agent     │
                    │                          │
                    │  1. 规划（Planning）      │
                    │     ├─ 理解用户意图       │
                    │     ├─ 拆解任务步骤       │
                    │     └─ 选择执行路径       │
                    │                          │
                    │  2. 工具调用（Tool Use）   │
                    │     ├─ 向量检索           │
                    │     ├─ BM25检索           │
                    │     ├─ Web搜索            │
                    │     ├─ 数据库查询(SQL)    │
                    │     ├─ 计算器/代码执行     │
                    │     └─ 知识图谱查询       │
                    │                          │
                    │  3. 记忆（Memory）        │
                    │     ├─ 短期：当前对话上下文│
                    │     ├─ 长期：历史交互总结 │
                    │     └─ 工作记忆：中间结果  │
                    │                          │
                    │  4. 反思（Reflection）    │
                    │     ├─ 检索结果是否充分？  │
                    │     ├─ 生成答案是否一致？  │
                    │     ├─ 需要补充检索吗？    │
                    │     └─ 需要纠正错误吗？    │
                    └──────────────────────────┘
```

#### Agentic RAG 的执行流程示例

```
用户提问："我们公司去年Q3在华东区的销售额是多少？与竞争对手A相比差距在哪？"
                                                          │
                                                          ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Agent 自主执行过程                                  │
│                                                                             │
│  Step 1  【规划】                                                            │
│     Agent 思考："这个问题需要内部销售数据和对竞品A的分析。我需要：              │
│               ① 查内部数据库获取Q3华东区销售数据                               │
│               ② 查行业报告库获取竞品A的销售数据                                │
│               ③ 计算差距                                                     │
│               ④ 综合给出分析"                                                 │
│                                                                             │
│  Step 2  【工具调用 → SQL 查询】                                              │
│     → 查询内部数据库："SELECT SUM(amount) FROM sales                           │
│                        WHERE region='华东' AND quarter='2024Q3'"              │
│     → 结果：1.2亿元                                                          │
│                                                                             │
│  Step 3  【工具调用 → 向量检索】                                              │
│     → 查询行业报告库："竞品A 华东区 2024 Q3 销售额"                            │
│     → 检索到一份行业分析报告片段                                              │
│                                                                             │
│  Step 4  【反思】                                                            │
│     Agent 思考："行业报告只给了竞品A全国的数据，没有细分到华东区。               │
│               需要再搜索。"                                                   │
│                                                                             │
│  Step 5  【工具调用 → Web 搜索】                                              │
│     → 搜索："竞品A 2024年Q3 华东区 业绩"                                      │
│     → 找到一篇财经新闻提及华东区数据                                          │
│                                                                             │
│  Step 6  【工具调用 → 计算器】                                                │
│     → 计算：1.2亿 - 0.8亿 = 0.4亿（领先）                                    │
│                                                                             │
│  Step 7  【生成 + 自我评估】                                                  │
│     Agent 生成回答，并自我检查：                                               │
│     "数据有出处吗？✓ SQL / 财经新闻                                           │
│      计算正确吗？✓ 已验证                                                      │
│      推理是否完整？✓ 分析了差距原因"                                           │
│                                                                             │
│  Step 8  【输出最终回答】（附完整引用）                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### Agentic RAG 的关键技术

| 技术 | 作用 | 典型实现 |
|------|------|----------|
| **ReAct** | Reasoning + Acting 交替：思考一步、执行一步、观察结果、再思考 | LangChain Agent, LlamaIndex Agent |
| **Plan-and-Execute** | 先完整规划，再按计划逐步执行 | LlamaIndex QueryPipeline |
| **Self-RAG** | 生成时自我评判是否需要检索、检索结果是否相关、生成是否一致 | 特殊训练的 LLM（如 Self-RAG 论文实现） |
| **Corrective RAG (CRAG)** | 评估检索质量，不够好时自动触发补充检索或 Web 搜索 | 定制 Agent 流程 |
| **Multi-Agent RAG** | 多个 Agent 分工协作（检索 Agent / 分析 Agent / 写作 Agent） | CrewAI, AutoGen, LangGraph |

#### 代表框架

| 框架 | 特点 |
|------|------|
| **LangGraph** (LangChain) | 有状态的多步骤 Agent 编排，支持循环和条件分支 |
| **LlamaIndex Agent** | 内置 QueryEngineTool，将检索能力封装为 Agent 可调用的工具 |
| **CrewAI** | 多 Agent 协作框架，每个 Agent 有特定角色和工具 |
| **AutoGen** (Microsoft) | 多 Agent 对话框架，Agent 之间通过对话协调完成任务 |
| **Dify** | 可视化编排平台，支持 Agent 模式的 RAG 应用搭建 |

---

### 9.5 四代 RAG 总结对比

| 维度 | Gen 1: Naive | Gen 2: Advanced | Gen 3: Modular | Gen 4: Agentic |
|------|-------------|-----------------|----------------|----------------|
| **检索方式** | 单一向量检索 | 混合检索（向量+BM25） | 多模块可插拔 | Agent 动态选择工具 |
| **检索前处理** | 无 | 查询改写/扩展/分解 | 路由 + 自适应选择 | Agent 自主规划 |
| **检索后处理** | 无 | Rerank + 过滤 + 压缩 | 模块化组合 | Agent 反思 + 自动补检 |
| **流程控制** | 固定线性 | 固定线性（增加了前后处理） | 可配置 DAG | Agent 自主决策 |
| **纠错机制** | 无 | 无 | 评估模块（可选） | 自我评估 + 自动纠错 |
| **适合的查询** | 简单事实型 | 中等复杂、模糊查询 | 多类型混合查询 | 复杂多步推理 |
| **实现复杂度** | 低 | 中 | 中高 | 高 |
| **代表工具** | 基础 LangChain/LlamaIndex | RAGFlow, Dify | LlamaIndex, Haystack | LangGraph, CrewAI |

---

## 十、LangChain vs LlamaIndex 对比

这两个是 RAG 领域最主流的开源框架，定位有重叠但侧重点不同。

| 维度 | LangChain | LlamaIndex |
|------|-----------|------------|
| **核心定位** | 通用 LLM 应用框架 | 数据索引与检索框架 |
| **设计哲学** | 一切皆 Chain，组件可串联 | 一切皆 Index，数据可被"索引化" |
| **RAG 侧重点** | Retrieval 是其中一个模块，不强 | 专为 RAG 设计，检索和索引是最核心的能力 |
| **Agent 能力** | LangGraph 提供强大的 Agent 编排 | 内置 Agent，但编排能力弱于 LangGraph |
| **数据加载** | 丰富的 Document Loader 生态 | 同样丰富，LlamaHub 有海量 loader |
| **索引导入** | 基础 | 极其丰富：向量索引、树索引、关键词索引、知识图谱索引 |
| **检索策略** | 需手动组合 | 丰富的内置检索器：递归检索、路由检索、混合检索 |
| **生产部署** | LangServe + LangSmith（监控） | LlamaCloud（托管服务） |
| **学习曲线** | 中等（概念多：Chain/Agent/Tool/Memory） | 较低（围绕文档和索引的核心概念） |
| **社区生态** | 更大（GitHub Stars 更多，教程更多） | 略小但增长迅速 |

**选型建议：**

```
你的应用核心是 RAG？
  ├─ 是 → 用 LlamaIndex
  │        - 检索、索引、RAG 体系更专业
  │        - 开箱即用的高级 RAG 功能（递归检索、子问题分解等）
  │        - 上手更快，概念更聚焦
  │
  └─ 否 / 需要复杂的 Agent 编排 →
           用 LangChain + LangGraph
           - Agent 编排能力业界领先
           - 生态更大，集成更多第三方服务
           - 如果你的 RAG 只是应用的一部分

也可以混用：
  LlamaIndex 负责数据加载和索引 + LangChain 负责 Agent 调度
  两者有官方集成，LlamaIndex 的 QueryEngine 可以封装为 LangChain 的 Tool
```



---

