## 二十二、切分策略选择、参数调优、效果评估与最佳实践

前两章我们深入学习了各种切分器和混合策略。本章是切分篇的收尾——当你面对一个具体的 RAG 项目时，如何做出正确的策略选择、如何调优参数、如何评估切分效果。

### 22.1 场景驱动的策略选择

**不存在"最好的切分器"，只有"最适合你场景的切分器"。** 选择切分策略的核心依据是：你的文档是什么类型的？你的查询特征是什么？你的精度要求和成本预算是多少？

#### 22.1.1 按文档类型选择

| 文档类型 | 子类型 | 推荐策略 | 推荐工具 | 原因 |
|----------|--------|---------|---------|------|
| **结构化文档** | Markdown (带 # 标题) | 按标题层级切分 + 二级语义切分 | MarkdownNodeParser + SentenceSplitter | 标题是天然的语义边界，层级结构提供了 SOURCE/PARENT 关系的基础 |
| | PDF (数字原生) | Unstructured chunk_by_title + 递归切分兜底 | UnstructuredReader + SentenceSplitter | Unstructured 能解析出 Title Element 替代 Markdown 的 # 语义；递归切分处理无法识别标题的纯文本区 |
| | HTML 网页 | 按 `<h1>`~`<h6>` 标签 + `<section>` 标签切分 | HTMLNodeParser / MarkdownNodeParser (先转MD) | HTML 的语义标签是天然的结构锚点 |
| **代码文件** | Python / JS / Java / Go / ... | 按 def/class/function 边界切分 | CodeSplitter | 函数和类是代码最自然的检索单元，绝不能按字符硬切(会切断函数体) |
| **纯文本** | OCR 后的文字 / 聊天记录 / 访谈转录 | 句子切分 + 语义切分兜底 | SentenceSplitter + SemanticSplitterNodeParser | 无格式信息可用，句子是最可靠的切分边界；如果话题切换频繁，语义切分能识别换话题点 |
| **混合文档** | 文本 + 表格 + 图像混排 | 先按元素类型分类 → 逐类专用策略 | Unstructured chunk_by_title（Table 不切，Text 按标题切） | 表格一旦被切散就无法还原行列关系；图像需要单独走 OCR/多模态管线 |
| **长文本** | 书籍、论文、长篇报告 | 层级双层切分(章→节→段) | HierarchicalNodeParser(chunk_sizes=[1024, 512, 256]) | 长文本的"章"是大语义单元，只用一层切分会丢失层级导航能力 |
| **短文本** | FAQ、对话轮次、产品条目 | 不切分(1条目=1 Node) | FlatFileNodeParser / SentenceSplitter(chunk_size=999999) | 本身就是最小语义单元，强行切分反而不利于检索 |

#### 22.1.2 关键参数设置指南

**Markdown 文档（chunk_by_title + 二级切分）：**

| 参数 | 推荐值 | 为什么 |
|------|--------|--------|
| `chunk_size` (父Node) | 不设上限 / 4096 | 父 Node 保留完整章节，不做切分——LLM 上下文窗口够大 |
| `chunk_size` (子Node) | 256-512 tokens | 精准检索粒度，配合 Embedding 模型上限(bge=512) |
| `chunk_overlap` | chunk_size × 15% | 子 Node 之间保留信息桥接 |
| `combine_text_under_n_chars` | 100 | 合并过短的标题或段落，避免碎片化 |

**PDF / 无结构文档（递归切分）：**

| 参数 | 推荐值 | 为什么 |
|------|--------|--------|
| `chunk_size` | 512 字符 | 通用甜点区，兼顾检索精度和上下文完整性 |
| `chunk_overlap` | 64-128 字符 | 15%-25% 的重叠率，PDF 布局复杂需要更多重叠 |
| `separators` | `["\n\n", "\n", "。", ".", " ", ""]` | 中文文档——句号前不应有空格 |

**代码文件：**

| 参数 | 推荐值 | 为什么 |
|------|--------|--------|
| `chunk_lines` | 30-50 行 | 覆盖大多数函数/类的行数范围 |
| `chunk_lines_overlap` | 2-5 行 | 少量重叠防止 import/装饰器被孤立 |
| `max_chars` | 2000 | 兜底——如果有超长函数，强制切分 |

#### 22.1.3 通用场景 vs 高精度场景

| 场景 | 策略 | 代价 | 何时用 |
|------|------|------|--------|
| **通用场景** (快速原型/中小规模) | SentenceSplitter(chunk_size=512, overlap=64) | 精度中等、无层级信息 | 不确定文档特征时的安全起步策略 |
| **高精度场景** (生产环境/大规模) | 层级双层 + 元数据增强 + MetadataReplacement | 实现复杂度高、索引体积大 | 企业知识库、合规审计、需要精确溯源 |
| **极高 Recall** (宁可多找不能漏) | SentenceWindowNodeParser(window_size=3) + MetadataReplacement | Node 数量爆炸（1句=1Node） | 法律检索、医疗文献检索 |
| **成本敏感** (大量文档/预算有限) | TokenTextSplitter + 小维度 Embedding | 检索精度下降 | 百万级文档、Embedding API 费用是瓶颈 |

---

### 22.2 参数调优：三大关键参数的影响与选择

#### 22.2.1 chunk_size 的三档效果

| chunk_size 档位 | 数值范围 | 效果 | 适用条件 |
|:---:|------|------|------|
| **小 (细粒度)** | 100-256 tokens | Recall↑ 检索精度↑ 上下文完整性↓ chunk数量↑ 成本↑ | 短查询为主、FAQ 型问答、极高召回要求 |
| **中 (标准)** | 384-768 tokens | 平衡——Recall/Precision/上下文/成本 都是中等水平 | 通用知识库、起步配置 |
| **大 (粗粒度)** | 1024-2048 tokens | Recall↓ Precision↓ 上下文完整性↑ chunk数量↓ 成本↓ | 长查询、法律法规、需要大段完整上下文的领域 |

**chunk_size 的选择逻辑：**

```
  决策树:

  你的 Embedding 模型最大输入是多少？
    ├─ bge-large-zh / m3e-base (512 tokens)
    │   → chunk_size 必须 ≤ 480 tokens (留余量给 overlap)
    │
    ├─ text-embedding-3-small / large (8192 tokens)
    │   → 你的查询大多是什么类型的？
    │       ├─ 短查询 (3-8 词) → chunk_size=256-384
    │       ├─ 中查询 (1-2 句) → chunk_size=512-768
    │       └─ 长查询 (详细描述) → chunk_size=1024-2048
    │
    └─ 大规模生产 (成本敏感)
        → 用大 chunk 减少 Embedding API 调用次数
        → 配合 Reranker 精排弥补粗排精度下降
```

#### 22.2.2 chunk_overlap 的精确调优

chunk_overlap 的核心作用是**降低"关键信息恰好落在边界上"的概率**。它的最优值取决于文档的"信息密度"——信息越密集，需要的 overlap 越大。

```
  chunk_overlap 的三档效果:

  小 overlap (5%-10%): | 中 overlap (15%-20%): | 大 overlap (25%-30%):
                       |                       |
  成本节省 ↑           | 平衡点 ★              | 保险系数最高 ↑
  信息丢失风险 ↑       | 大多数场景适用         | 存储成本翻倍 ↑
                       |                       |
  适用: 格式规范的     | 适用: 通用文档         | 适用: 法律/医疗/合同
       结构化文档      |       (默认推荐)       |      需要极高安全边际
       表格数据        |                       |      信息连续性强
```

**具体数值映射：**

| chunk_size | overlap 10% | overlap 15% | overlap 20% | overlap 25% |
|:---:|:---:|:---:|:---:|:---:|
| 256 | 26 | 38 | 51 | 64 |
| 384 | 38 | 58 | 77 | 96 |
| 512 | 51 | 77 | 102 | 128 |
| 768 | 77 | 115 | 154 | 192 |
| 1024 | 102 | 154 | 205 | 256 |

#### 22.2.3 语义断点阈值 (breakpoint_percentile_threshold)

这个参数仅适用于 SemanticSplitterNodeParser，控制"相似度下降到什么程度才算话题切换"。

| 阈值 | 效果 | 适用场景 |
|:---:|------|------|
| **80-85** | 断点多、chunk 小、话题分离极细 | 话题频繁切换的聚合文档(如 FAQ 合集、论坛帖子汇总) |
| **90-95** | 平衡——大多数话题切换被正确识别 | 通用语义切分场景 |
| **95-97** | 断点少、chunk 大、只有大幅度的语义跳跃才切 | 叙述性长文档(小说/报告)、话题渐变而非突变 |
| **97-99** | 几乎不切——只在大篇章边界处断开 | 需要完整篇章上下文的学术场景 |

```
  breakpoint_percentile_threshold 的选择逻辑:

  你的文档中"话题切换"是突变还是渐变?
    ├─ 突变型: FAQ (每条问答话题不同) → threshold=80-85
    │          "如何请假" → "报销标准" → 明显的话题跳跃
    │
    ├─ 渐变型: 技术文档 (小节之间有关联) → threshold=90-95
    │          "安装步骤" → "配置说明" → 有一定关联的话题过渡
    │
    └─ 连续型: 学术论文 (全篇围绕一个主题) → threshold=95-99
               "引言" → "方法" → "实验" → 话题是逐步演进的
```

---

### 22.3 切分效果评估：三维指标体系

切分策略好不好，不是凭感觉判断的。你需要用**数据**来验证。评估分为三个维度：

#### 22.3.1 检索指标：召回率、精确率、F1

这三个指标回答的问题是：**检索系统能不能找到该找的内容？找到的内容是不是都是有关的？**

```
═══════════════════════════════════════════════════════════════════════
              检索三指标：Recall / Precision / F1
═══════════════════════════════════════════════════════════════════════

  前提: 你有 N 条测试查询 (如 20 条)，每条查询你手动标记了
        "应该返回哪些 Chunk (正确答案集合)"。

  混淆矩阵:
                      实际相关              实际不相关
                  ┌────────────────┐  ┌────────────────┐
    检索到的      │       TP       │  │       FP       │
   (系统返回的)   │   True Positive │  │  False Positive │
                  ├────────────────┤  ├────────────────┤
    未检索到的    │       FN       │  │       TN       │
   (系统未返回)   │  False Negative │  │  True Negative  │
                  └────────────────┘  └────────────────┘

  ═══════════════════════════════════════════════════════════

  召回率 (Recall) = TP / (TP + FN)
  ┌─────────────────────────────────────────────────────────┐
  │ 含义: 所有真正相关的 chunk 中，有多少被检索到了？         │
  │                                                         │
  │ Recall = 0.90 → "10 个相关 chunk 找到了 9 个，漏了 1 个" │
  │ Recall = 0.50 → "10 个相关 chunk 只找到 5 个，漏了一半"  │
  │                                                         │
  │ 高 Recall 意味着: "该找到的都找到了" (查全率)              │
  │ 低 Recall 意味着: "漏掉了重要信息"                        │
  │                                                         │
  │ Recall 偏低的原因:                                        │
  │   · chunk_size 太大 → 语义稀释, 匹配信号弱                │
  │   · chunk_size 太小 → 相关信息被切到其他 chunk            │
  │   · Embedding 模型不合适                                 │
  │   · 切分边界在关键信息的中间                              │
  └─────────────────────────────────────────────────────────┘

  精确率 (Precision) = TP / (TP + FP)
  ┌─────────────────────────────────────────────────────────┐
  │ 含义: 检索返回的结果中，有多少是真正相关的？               │
  │                                                         │
  │ Precision = 0.90 → "返回 10 个, 9 个相关, 1 个噪声"      │
  │ Precision = 0.30 → "返回 10 个, 只有 3 个相关"           │
  │                                                         │
  │ 高 Precision 意味着: "返回的都是相关的" (查准率)           │
  │ 低 Precision 意味着: "混入了大量无关内容"                  │
  │                                                         │
  │ Precision 偏低的原因:                                     │
  │   · chunk_size 太大 → 一个 chunk 含多个话题                │
  │   · overlap 太大 → 重复内容多, 挤占 Top-K                 │
  │   · 检索阈值太低 → 不相关的内容也被返回                    │
  └─────────────────────────────────────────────────────────┘

  F1 分数 = 2 × (Precision × Recall) / (Precision + Recall)
  ┌─────────────────────────────────────────────────────────┐
  │ 含义: Precision 和 Recall 的调和平均数                    │
  │       两个指标同等重要时用 F1 作为综合衡量                   │
  │                                                         │
  │ F1 = 0.90  → 精确率和召回率都很高 (理想)                  │
  │ F1 = 0.50  → 其中一项或两项都很低                          │
  │ F1 = 0     → 完全没有命中正确的                            │
  │                                                         │
  │ 什么时候用 F1:                                             │
  │   需要权衡 Precision 和 Recall 的场景                     │
  │   对比不同切分策略的整体表现                                │
  │   对两者权重相同 (如果有偏重, 用 F-beta)                    │
  └─────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
  三者的关系:
    · Recall 和 Precision 通常互为矛盾——提高一个会降低另一个
    · 减小 chunk_size → Recall↑ (容易匹配) 但 Precision↓ (碎片化)
    · 增大 chunk_size → Precision↑ (话题完整) 但 Recall↓ (语义稀释)
    · F1 在两者之间找到平衡点
    · 如果你的场景"漏检代价高"(医疗/法律), 优先提高 Recall
    · 如果你的场景"噪声代价高"(用户不满无关结果), 优先提高 Precision
═══════════════════════════════════════════════════════════════════════
```

#### 22.3.2 生成质量指标：相关性、准确性、完整性

检索到的 Chunk 最终要交给 LLM 生成回答。即使检索指标很好，如果 Chunk 内容是碎片化的，LLM 生成的回答质量也会很差。这三个指标衡量的是**最终输出对用户的价值**。

```
═══════════════════════════════════════════════════════════════════════
            生成质量三指标：Relevance / Accuracy / Completeness
═══════════════════════════════════════════════════════════════════════

  相关性 (Answer Relevance)
  ┌─────────────────────────────────────────────────────────┐
  │ 含义: LLM 生成的回答是否直接回应了用户的问题？              │
  │                                                         │
  │  高相关性: 用户问"事假提前多久", 回答"需提前1个工作日"     │
  │  低相关性: 用户问"事假提前多久", 回答"公司提供五险一金"    │
  │                                                         │
  │ 为什么切分会影响相关性:                                    │
  │   如果检索到的 chunk 包含无关信息 → LLM 被噪声干扰          │
  │   → 回答跑偏, 没有直接回答问题                             │
  │                                                         │
  │ 衡量方式:                                                │
  │   用另一个 LLM 做"裁判": 给定 Query + Answer, 问:          │
  │   "这个回答是否直接回应了问题？" 输出 0-1 分数。            │
  │   LlamaIndex 内置: RelevancyEvaluator                    │
  └─────────────────────────────────────────────────────────┘

  准确性 (Faithfulness / Factual Accuracy)
  ┌─────────────────────────────────────────────────────────┐
  │ 含义: LLM 生成的回答中, 有多少内容可以在检索到的 Chunk     │
  │       中找到直接依据？(不被幻觉影响)                        │
  │                                                         │
  │  高准确性: "事假需提前1个工作日申请" ← Chunk 中确实有这句话  │
  │  低准确性: "事假需提前3个工作日申请" ← Chunk 中是 1, LLM 编了 3│
  │                                                         │
  │ 为什么切分会影响准确性:                                    │
  │   Chunk 太小 → LLM 看到的是碎片, 没有足够信息做准确推理     │
  │   → 为了"填充"回答, LLM 开始编造                           │
  │   Chunk 太大 → LLM 可能被多个话题混淆, 张冠李戴            │
  │                                                         │
  │ 衡量方式:                                                │
  │   逐个拆解回答中的声明 → 在检索到的 Chunk 中搜索证据        │
  │   有证据的声明 / 总声明数 = Faithfulness 分数              │
  │   LlamaIndex 内置: FaithfulnessEvaluator                 │
  └─────────────────────────────────────────────────────────┘

  完整性 (Answer Completeness / Context Sufficiency)
  ┌─────────────────────────────────────────────────────────┐
  │ 含义: 检索到的 Chunk 是否包含了回答这个问题所需的全部信息？ │
  │                                                         │
  │  高完整性: 用户问"怎么请假+需要什么材料"→ Chunk 包含了     │
  │            [提前几天+向谁申请+需要交什么材料] 全部信息       │
  │  低完整性: Chunk 只说了"提前1个工作日"→ 没有"向谁申请"     │
  │           "需要什么材料"的信息 → LLM 无法全面回答           │
  │                                                         │
  │ 为什么切分会影响完整性:                                    │
  │   Chunk 太小 → 无法容纳完整的回答所需步骤/条款              │
  │   overlap 不够 → 相关信息分散在不同 Chunk, 难以关联         │
  │                                                         │
  │ 衡量方式:                                                │
  │   人工评估: 给定 Query + Retrieved Chunks, 判断信息是否完整 │
  │   自动评估: 用 LLM 判断 "Chunk 中是否包含了回答所需的全部信息"│
  └─────────────────────────────────────────────────────────┘
```

#### 22.3.3 效率指标：检索时间、生成时间、资源消耗

这些指标决定了你的 RAG 系统能不能上线——再好的切分策略，如果每次查询要等 10 秒，用户体验就是不可接受的。

```
═══════════════════════════════════════════════════════════════════════
              效率三指标：Latency / Throughput / Resource
═══════════════════════════════════════════════════════════════════════

  检索时间 (Retrieval Latency)
  ┌─────────────────────────────────────────────────────────┐
  │ 含义: 从用户发送 Query 到系统返回 Top-K Chunk 列表的时间    │
  │       包含: Query向量化 + 向量检索 + Rerank(如果启用)       │
  │                                                         │
  │ 参考值:                                                  │
  │   < 100ms   → 优秀 (本地 FAISS, 小索引)                    │
  │   100-500ms → 正常 (云向量库, 中等索引)                    │
  │   500ms-2s  → 可接受 (需要 Rerank 或混合检索)               │
  │   > 2s      → 需优化                                     │
  │                                                         │
  │ 切分策略如何影响检索时间:                                   │
  │   chunk 越小 → Node 越多 → 索引越大 → 检索越慢              │
  │   例如: 1000 页文档                                          │
  │     chunk_size=256 → ~4000 个 Node → 检索约 20ms           │
  │     chunk_size=64  → ~16000 个 Node → 检索约 80ms (4倍!)   │
  │                                                         │
  │ 这就是 SentenceWindowNodeParser(每句1个Node)的代价         │
  └─────────────────────────────────────────────────────────┘

  生成时间 (Generation Latency)
  ┌─────────────────────────────────────────────────────────┐
  │ 含义: LLM 从收到 Prompt 到输出完整回答的时间                 │
  │       主要取决于: Prompt token 数 + LLM 推理速度 + 输出长度  │
  │                                                         │
  │ 切分策略如何影响生成时间:                                   │
  │   chunk 越大 → Prompt 的 token 数越多 → LLM 推理越慢       │
  │   (上下文窗口处理成本与输入 token 数成正比)                   │
  │                                                         │
  │   3 个 chunk × 500 token  = 1500 token → 约 1.5s 生成     │
  │   3 个 chunk × 2000 token = 6000 token → 约 4s 生成       │
  │                                                         │
  │   同时: token 越多 → API 费用越高                          │
  └─────────────────────────────────────────────────────────┘

  资源消耗 (Resource Consumption)
  ┌─────────────────────────────────────────────────────────┐
  │ 含义: 整个 RAG 系统占用的存储空间 + 计算资源                │
  │                                                         │
  │  存储: 向量数据库的磁盘使用量                               │
  │  内存: 索引加载到内存的大小                                 │
  │  API 费用: Embedding 调用次数 × 单价                      │
  │                                                         │
  │  切分策略如何影响资源消耗:                                   │
  │    Node 数量 ↑ → 向量存储 ↑ → 磁盘/内存 ↑                   │
  │    Node 数量 ↑ → Embedding 调用次数 ↑ → API 费用 ↑          │
  │                                                         │
  │  1000 个 Node × 512 维 × float32(4B) = 2MB (极小)         │
  │  100万 Node × 512 维 × float32(4B) = 2GB (中等)           │
  │  100万 Node × 3072维 × float32(4B) = 12GB (较大)          │
  │                                                         │
  │  关键经验: 在大规模场景中, chunk_size 的选择                  │
  │           直接决定了基础设施成本                              │
  └─────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
```

---

### 22.4 LlamaIndex 评估器实战

LlamaIndex 提供了内置的评估模块，可以用"LLM 做裁判"来评估检索和生成质量。核心理念是：**用更强的 LLM 来评判 RAG 系统的输出质量**。

#### 22.4.1 完整代码演示

```python
"""
═══════════════════════════════════════════════════════════════════════════
  LlamaIndex 切分效果评估 —— 完整实战
═══════════════════════════════════════════════════════════════════════════

  评估流程:
    1. 准备测试数据集: 每条 = (问题, 参考答案)
    2. 用不同的切分策略构建索引
    3. 每种策略下运行所有测试查询
    4. 用评估器自动打分: 忠实度 + 相关性
    5. 对比得分 → 选择最佳切分策略

  依赖: pip install llama-index-core llama-index-embeddings-openai
"""
from llama_index.core import Document, VectorStoreIndex, Settings
from llama_index.core.node_parser import (
    SentenceSplitter,
    TokenTextSplitter,
    SentenceWindowNodeParser,
)
from llama_index.core.evaluation import (
    FaithfulnessEvaluator,       # 忠实度评估: 回答是否基于检索到的内容
    RelevancyEvaluator,          # 相关性评估: 检索到的内容是否与问题相关
    EvaluationResult,
)
from llama_index.embeddings.openai import OpenAIEmbedding
from llama_index.llms.openai import OpenAI
from llama_index.core.postprocessor import MetadataReplacementPostProcessor
from typing import List, Dict, Tuple
import json
import time

# ── 全局配置 ──────────────────────────────────────────────────
Settings.embed_model = OpenAIEmbedding(model="text-embedding-3-small")
# 评估器通常用更强的模型做裁判
Settings.llm = OpenAI(model="gpt-4o", temperature=0)

# 裁判 LLM: 用 GPT-4o 做评估, 比 gpt-4o-mini 更可靠
eval_llm = OpenAI(model="gpt-4o", temperature=0)


# ═══════════════════════════════════════════════════════════════
# Step 1: 准备测试数据集
# ═══════════════════════════════════════════════════════════════

# 每条测试数据: (问题, 参考答案)
# 参考答案由领域专家手动编写, 作为评估的"标准答案"
test_dataset = [
    {
        "query": "事假需要提前多久申请？",
        "reference": "事假需提前1个工作日向部门主管申请。",
    },
    {
        "query": "病假超过2天需要提供什么？",
        "reference": "连续病假超过2天须提供二级及以上医院的病假证明。",
    },
    {
        "query": "年假天数是怎么计算的？",
        "reference": "年假天数按工龄计算：入职1-5年5天，5-10年10天，10年以上15天。年假以自然年为单位，次年3月31日前未休完的自动清零。",
    },
    {
        "query": "公司一共给员工缴纳哪些保险？",
        "reference": "公司依法缴纳五险一金：养老保险、医疗保险、失业保险、工伤保险、生育保险和住房公积金。",
    },
    {
        "query": "加班费的计算标准是什么？",
        "reference": "工作日加班按1.5倍工资计算，休息日加班按2倍工资计算，法定节假日加班按3倍工资计算。",
    },
]

# ── 准备 Document (用员工手册内容) ─────────────────────────
handbook_text = open("星辰科技员工手册.md", "r", encoding="utf-8").read()
doc = Document(
    text=handbook_text,
    metadata={"title": "员工手册", "category": "人事政策"}
)


# ═══════════════════════════════════════════════════════════════
# Step 2: 创建不同切分策略的索引
# ═══════════════════════════════════════════════════════════════

def build_index_with_strategy(
    doc: Document,
    strategy_name: str,
    **splitter_kwargs
) -> Tuple[str, VectorStoreIndex, List]:
    """
    用指定的切分策略构建索引。

    返回: (策略名称, 索引对象, Node 列表)
    """
    print(f"\n  构建索引: {strategy_name}")

    if strategy_name == "SentenceSplitter_256":
        splitter = SentenceSplitter(
            chunk_size=256, chunk_overlap=32,
            paragraph_separator="\n\n"
        )
    elif strategy_name == "SentenceSplitter_512":
        splitter = SentenceSplitter(
            chunk_size=512, chunk_overlap=64,
            paragraph_separator="\n\n"
        )
    elif strategy_name == "SentenceSplitter_1024":
        splitter = SentenceSplitter(
            chunk_size=1024, chunk_overlap=128,
            paragraph_separator="\n\n"
        )
    elif strategy_name == "TokenTextSplitter_256":
        splitter = TokenTextSplitter(
            chunk_size=256, chunk_overlap=32,
            separator=" ", backup_separators=["\n", "。"]
        )
    elif strategy_name == "SentenceWindow_3":
        splitter = SentenceWindowNodeParser.from_defaults(
            window_size=3, window_metadata_key="window"
        )
        nodes = splitter.get_nodes_from_documents([doc])
        index = VectorStoreIndex(nodes)
        return strategy_name, index, nodes
    else:
        raise ValueError(f"未知策略: {strategy_name}")

    nodes = splitter.get_nodes_from_documents([doc])
    index = VectorStoreIndex(nodes)
    return strategy_name, index, nodes


# ═══════════════════════════════════════════════════════════════
# Step 3: 运行评估
# ═══════════════════════════════════════════════════════════════

def evaluate_strategy(
    strategy_name: str,
    index: VectorStoreIndex,
    nodes: List,
    test_dataset: List[Dict],
    top_k: int = 3,
) -> Dict:
    """
    对一种切分策略运行完整的评估。

    评估维度:
      1. 忠实度 (Faithfulness): 回答是否基于检索到的内容
      2. 相关性 (Relevancy): 检索到的内容是否与问题相关
      3. 检索时间: 平均每次查询的检索耗时
      4. Node 数量: 索引中的 Node 总数 (反映存储成本)

    返回: 评估结果字典
    """
    print(f"\n{'='*60}")
    print(f"  评估策略: {strategy_name}")
    print(f"{'='*60}")

    # 创建评估器
    faithfulness_evaluator = FaithfulnessEvaluator(llm=eval_llm)
    relevancy_evaluator = RelevancyEvaluator(llm=eval_llm)

    query_engine = index.as_query_engine(
        similarity_top_k=top_k,
        # SentenceWindow 需要 MetadataReplacement
        node_postprocessors=(
            [MetadataReplacementPostProcessor(target_metadata_key="window")]
            if strategy_name.startswith("SentenceWindow")
            else []
        ),
    )

    results = []
    total_retrieval_time = 0.0

    for i, test_item in enumerate(test_dataset):
        query = test_item["query"]
        reference = test_item["reference"]

        # ── 计时检索 ───────────────────────────────────────
        t0 = time.time()
        response = query_engine.query(query)
        retrieval_time = time.time() - t0
        total_retrieval_time += retrieval_time

        # ── 评估忠实度 ─────────────────────────────────────
        # "回答的内容是否都可以在检索到的 Chunk 中找到证据？"
        faith_result = faithfulness_evaluator.evaluate_response(
            response=response
        )

        # ── 评估相关性 ─────────────────────────────────────
        # "检索到的 Chunk 是否与用户问题直接相关？"
        relevancy_result = relevancy_evaluator.evaluate_response(
            query=query, response=response
        )

        # 收集分数
        faith_score = (
            1.0 if faith_result.passing else 0.0
            if faith_result.score is None
            else faith_result.score
        )
        relevancy_score = (
            1.0 if relevancy_result.passing else 0.0
            if relevancy_result.score is None
            else relevancy_result.score
        )

        results.append({
            "query": query,
            "reference": reference,
            "generated": str(response),
            "faithfulness": faith_score,
            "relevancy": relevancy_score,
            "retrieval_time_ms": retrieval_time * 1000,
        })

        print(f"\n  Query[{i}]: {query}")
        print(f"    忠实度: {faith_score:.2f} {'✓' if faith_result.passing else '✗'}")
        print(f"    相关性: {relevancy_score:.2f} {'✓' if relevancy_result.passing else '✗'}")
        print(f"    检索耗时: {retrieval_time*1000:.0f}ms")

    # ── 汇总统计 ────────────────────────────────────────────
    avg_faith = sum(r["faithfulness"] for r in results) / len(results)
    avg_relevancy = sum(r["relevancy"] for r in results) / len(results)
    avg_retrieval = total_retrieval_time / len(results)

    summary = {
        "strategy": strategy_name,
        "node_count": len(nodes),
        "avg_faithfulness": round(avg_faith, 3),
        "avg_relevancy": round(avg_relevancy, 3),
        "avg_retrieval_time_ms": round(avg_retrieval * 1000, 1),
        "total_retrieval_time_s": round(total_retrieval_time, 2),
        "per_query_results": results,
    }

    print(f"\n  ── 汇总 [{strategy_name}] ──")
    print(f"  Node 数量:     {len(nodes)}")
    print(f"  平均忠实度:    {avg_faith:.3f}")
    print(f"  平均相关性:    {avg_relevancy:.3f}")
    print(f"  平均检索耗时:  {avg_retrieval*1000:.0f}ms")

    return summary


# ═══════════════════════════════════════════════════════════════
# Step 4: 对比所有策略
# ═══════════════════════════════════════════════════════════════

# 测试三种不同的 chunk_size
strategies_to_test = [
    ("SentenceSplitter_256",   {"chunk_size": 256,  "chunk_overlap": 32}),
    ("SentenceSplitter_512",   {"chunk_size": 512,  "chunk_overlap": 64}),
    ("SentenceSplitter_1024",  {"chunk_size": 1024, "chunk_overlap": 128}),
]

all_summaries = []
for name, kwargs in strategies_to_test:
    strategy_name, index, nodes = build_index_with_strategy(doc, name, **kwargs)
    summary = evaluate_strategy(strategy_name, index, nodes, test_dataset)
    all_summaries.append(summary)


# ═══════════════════════════════════════════════════════════════
# Step 5: 输出对比报告
# ═══════════════════════════════════════════════════════════════

print("\n\n" + "=" * 80)
print("  切分策略对比报告")
print("=" * 80)

print(f"\n  {'策略':<25} {'Node数':<8} {'忠实度':<10} {'相关性':<10} {'检索耗时'}")
print(f"  {'─'*70}")
for s in all_summaries:
    print(f"  {s['strategy']:<25} {s['node_count']:<8} "
          f"{s['avg_faithfulness']:<10.3f} {s['avg_relevancy']:<10.3f} "
          f"{s['avg_retrieval_time_ms']:.0f}ms")

# ── 找出最佳策略 ────────────────────────────────────────────
# 复合得分: 忠实度 + 相关性 的平均值
for s in all_summaries:
    s["composite_score"] = (s["avg_faithfulness"] + s["avg_relevancy"]) / 2

best = max(all_summaries, key=lambda s: s["composite_score"])
print(f"\n  ★ 最佳策略: {best['strategy']}")
print(f"    复合得分: {best['composite_score']:.3f}")
print(f"    忠实度: {best['avg_faithfulness']:.3f}")
print(f"    相关性: {best['avg_relevancy']:.3f}")
print(f"    检索耗时: {best['avg_retrieval_time_ms']:.0f}ms")


# ═══════════════════════════════════════════════════════════════
# Step 6: 深入分析 —— 哪个查询表现最差？
# ═══════════════════════════════════════════════════════════════

print("\n\n" + "=" * 80)
print("  深入分析: 每条查询的详细得分 (最佳策略)")
print("=" * 80)

best_results = best["per_query_results"]
for i, r in enumerate(best_results):
    print(f"\n  Query[{i}]: {r['query']}")
    print(f"    参考答案: {r['reference'][:80]}...")
    print(f"    生成回答: {r['generated'][:120]}...")
    print(f"    忠实度: {r['faithfulness']:.2f}  相关性: {r['relevancy']:.2f}")
    # 标记需要关注的查询
    if r['faithfulness'] < 0.7 or r['relevancy'] < 0.7:
        print(f"    ⚠ 需要关注: ", end="")
        if r['faithfulness'] < 0.7:
            print("忠实度偏低(可能幻觉) ", end="")
        if r['relevancy'] < 0.7:
            print("相关性偏低(检索不准) ", end="")
        print()


# ═══════════════════════════════════════════════════════════════
# 总结
# ═══════════════════════════════════════════════════════════════

print("\n" + "=" * 80)
print("  评估总结")
print("=" * 80)
print("""
  评估的价值:
    1. 用数据而非直觉选择切分策略
    2. 忠实度低 → 检查 chunk 是否太小(碎片化)或太大(噪声多)
    3. 相关性低 → 检查检索/Embedding 是否匹配查询类型
    4. 检索耗时高 → 检查 Node 数量是否过大 → 增大 chunk_size
    5. 找出"问题查询" → 针对性优化(补充文档/调整切分/改进 Prompt)

  后续迭代:
    跑一轮评估 → 发现瓶颈 → 调整策略 → 再评估 → 直到满意
    这不是一次性的工作，而是持续优化的循环。
""")
```

---

### 22.5 最佳实践总结

#### 五步落地流程

```
═══════════════════════════════════════════════════════════════════════
              切分策略五步落地法
═══════════════════════════════════════════════════════════════════════

  Step 1: 了解你的数据
  ┌─────────────────────────────────────────────────────────┐
  │ 在写任何代码之前，先回答这些问题:                           │
  │                                                         │
  │  ☐ 文档是什么格式？(Markdown / PDF / HTML / 纯文本 / ...)│
  │  ☐ 文档有多大？(1页 / 100页 / 1000页+)                   │
  │  ☐ 文档有标题结构吗？(有 # / ## → MarkdownNodeParser)     │
  │  ☐ 文档里有表格吗？(有 → 不能按字符硬切)                   │
  │  ☐ 文档是扫描件吗？(是 → 先 OCR + 清洗)                   │
  │  ☐ 用户的典型查询是什么类型？(短词 / 长句 / 多步推理)      │
  │  ☐ 抽样看 5-10 个文档，手动标注"理想的chunk"应该长什么样   │
  └─────────────────────────────────────────────────────────┘

  Step 2: 选择合适的策略
  ┌─────────────────────────────────────────────────────────┐
  │ 根据 Step 1 的答案，从 22.1 的策略表中选择:               │
  │                                                         │
  │  Markdown → MarkdownNodeParser + 二级 SentenceSplitter   │
  │  PDF     → Unstructured chunk_by_title + 递归兜底       │
  │  代码    → CodeSplitter                                 │
  │  纯文本  → SentenceSplitter(chunk_size=512)              │
  │  混合    → 按文档类型分别选择 → 统一索引                   │
  │                                                         │
  │  如果是多文档类型混合的项目:                                │
  │    → 写一个分发函数(detect_doc_type → 选择对应切分器)      │
  └─────────────────────────────────────────────────────────┘

  Step 3: 调整参数
  ┌─────────────────────────────────────────────────────────┐
  │  从默认值开始，根据实际情况微调:                            │
  │                                                         │
  │  默认起点: chunk_size=512, overlap=64                     │
  │                                                         │
  │  如果检索 Recall 低 → 减小 chunk_size (256-384)           │
  │  如果检索噪声多 → 增大 chunk_size (768-1024)              │
  │  如果上下文不完整 → 增大 overlap 或启用双层切分            │
  │  如果 Node 数量过多 → 增大 chunk_size 降低存储成本         │
  │                                                         │
  │  每次只改一个参数，对比前后效果 (A/B 实验)                  │
  └─────────────────────────────────────────────────────────┘

  Step 4: 评估效果
  ┌─────────────────────────────────────────────────────────┐
  │  用 22.3-22.4 的方法评估:                                  │
  │                                                         │
  │  ☐ 准备 10-20 条测试查询 + 参考答案                        │
  │  ☐ 跑检索评估: Recall@5, Precision@5, F1                  │
  │  ☐ 跑生成评估: Faithfulness, Relevancy                    │
  │  ☐ 跑效率评估: 平均检索时间, Node 数量                      │
  │  ☐ 找出得分最低的 2-3 个查询 → 分析原因                    │
  └─────────────────────────────────────────────────────────┘

  Step 5: 混合使用发挥各种优势
  ┌─────────────────────────────────────────────────────────┐
  │  生产环境没有"唯一正确"的切分器:                            │
  │                                                         │
  │  · 结构文档 → 结构切分 (MarkdownNodeParser)               │
  │  · 无结构文档 → 语义切分 (SentenceSplitter + Semantic)     │
  │  · 代码 → CodeSplitter                                  │
  │  · 表格 → 不切分 / chunk_by_title                       │
  │  · 全局 → 元数据增强 (metadata继承+过滤+替换)              │
  │  · 所有文档 → 统一索引 → 统一查询引擎                      │
  │                                                         │
  │  核心原则: 让每种切分器做它最擅长的事                          │
  └─────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
```

**最终建议：**

> 1. **不要过度设计。** 先用 `SentenceSplitter(chunk_size=512, overlap=64)` 跑起来，有了真实的用户查询日志后，再用评估数据驱动调优。
> 2. **评估是迭代的。** 第一次评估往往不会完美，但每次评估都让你更接近最佳配置。
> 3. **混合策略是必然的。** 文档越多样，越不可能用一个切分器解决所有问题。接受这种多样性，用分发器模式（detect_doc_type → 选择切分器）管理复杂度。
> 4. **元数据是长期投资。** 在 Document 级别注入的 metadata（category/author/version/date）会在后续的检索过滤、路由分发、溯源的各个环节持续产生价值。
