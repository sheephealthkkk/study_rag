## 第四章：元数据建模与过滤 —— 从"找得近"到"找得对"

前三章解决了"怎么存"和"怎么找"的问题。但向量检索有一个根本局限：它只关心**语义相似度**，不关心**事实正确性**。

> 一个例子：用户问"2024年的加班费标准"。向量检索返回了一个高度相似的 Chunk——但它是 2022 年的旧版政策。语义完全匹配（都是关于加班费），但内容是错的。
>
> 元数据过滤解决的就是这个问题：**不仅"语义相近"，还要"条件正确"。**

### 4.1 元数据的价值：从语义检索到条件检索

```
═══════════════════════════════════════════════════════════════════════
              纯向量检索 vs 元数据增强的向量检索
═══════════════════════════════════════════════════════════════════════

  纯向量检索:
    Query → Embedding → 向量 → 找 Top-K 最相似的 Chunk
    "找意思最接近的"

    问题:
    · 用户问 "2024年的政策" → 可能返回 2022 年的 (语义也接近)
    · 用户问 "财务部的报销标准" → 可能返回 HR 的规定 (语义混淆)
    · 用户问 "V3.0版本的规定" → 可能返回 V2.0 的 (版本差异)

  元数据增强的向量检索:
    Query → Embedding → 向量
                      → 先过滤: category='财务制度' AND version='V3.0'
                      → 在过滤后的子集中找 Top-K
    "在满足条件的范围内找意思最接近的"

    效果:
    · 不会返回错误版本的文档
    · 不会返回错误类别的文档
    · 不会返回错误时间范围的文档
═══════════════════════════════════════════════════════════════════════
```

---

### 4.2 元数据建模：设计一个好的 metadata 结构

#### 4.2.1 核心原则

一个好的 metadata 设计应该覆盖三个维度：

```
═══════════════════════════════════════════════════════════════════════
              元数据建模的三个维度
═══════════════════════════════════════════════════════════════════════

  维度 1: 来源信息 (Provenance) —— "这段内容来自哪里?"
  ┌─────────────────────────────────────────────────────────┐
  │  · source_file: "考勤管理制度.pdf"                      │
  │  · page_number: 3                                      │
  │  · chunk_index: 15                                     │
  │  · document_id: "DOC-2024-001"                         │
  │                                                        │
  │  用途: 溯源、前端高亮定位                               │
  └─────────────────────────────────────────────────────────┘

  维度 2: 分类信息 (Taxonomy) —— "这段内容属于什么类别?"
  ┌─────────────────────────────────────────────────────────┐
  │  · category: "人事政策" | "财务制度" | "技术规范"       │
  │  · department: "HR" | "Finance" | "Engineering"         │
  │  · topic: "请假" | "加班" | "报销" | "入职"            │
  │  · audience: "全体员工" | "管理层" | "技术人员"         │
  │                                                        │
  │  用途: 按类别/部门/主题过滤、路由分发                    │
  └─────────────────────────────────────────────────────────┘

  维度 3: 时效信息 (Currency) —— "这段内容是什么时候的?"
  ┌─────────────────────────────────────────────────────────┐
  │  · version: "V3.0"                                     │
  │  · created_date: "2024-03-15"                          │
  │  · effective_date: "2024-04-01"                        │
  │  · expired_date: "2025-12-31"                          │
  │  · last_modified: "2025-01-20"                         │
  │  · status: "active" | "draft" | "archived"             │
  │                                                        │
  │  用途: 按版本/日期过滤、只检索"当前有效"的政策          │
  └─────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
```

#### 4.2.2 建模示例

以员工手册为例，每份文档的 metadata 可以设计为：

```python
metadata_example = {
    # ── 来源维度 ──
    "source": "星辰科技员工手册.md",
    "document_id": "DOC-HR-2024-001",
    "page_number": 5,

    # ── 分类维度 ──
    "category": "人事政策",
    "department": "HR",
    "topic": "请假制度",
    "subtopic": "事假",
    "doctype": "policy",          # policy / faq / guide / announcement

    # ── 时效维度 ──
    "version": "V3.0",
    "created_date": "2024-03-15",
    "effective_date": "2024-04-01",
    "status": "active",           # active / draft / archived
    "review_cycle": "annual",     # annual / quarterly / none
}
```

#### 4.2.3 不同文档类型的推荐 Metadata Schema

| 文档类型 | 必选字段 | 推荐字段 | 可选字段 |
|----------|----------|----------|----------|
| 企业制度/政策 | source, category, version, effective_date | department, status, author | tags, related_policies, supersedes |
| 技术文档 | source, product, version | component, api_version | deprecated_since, migration_guide |
| FAQ | source, category, topic | keywords, difficulty_level | related_questions |
| 会议纪要 | source, meeting_date, attendees | decisions, action_items | follow_up_date |
| 产品手册 | source, product, version | feature, platform | compatibility, prerequisites |

---

### 4.3 元数据过滤实战

#### 4.3.1 LlamaIndex 的过滤体系

LlamaIndex 提供了三层过滤能力：

| 层级 | 组件 | 作用 |
|------|------|------|
| 1. 简单过滤 | `MetadataFilters` + `MetadataFilter` | 单个条件或多个条件的 AND/OR |
| 2. 操作符过滤 | `FilterOperator` | EQ/GT/GTE/LT/LTE/IN/NIN/CONTAINS |
| 3. 嵌套过滤 | `FilterCondition` + 嵌套 `MetadataFilters` | 复杂逻辑：(A AND B) OR (C AND D) |

#### 4.3.2 单条件过滤

```python
from llama_index.core.vector_stores import MetadataFilter, MetadataFilters

# 只检索 "财务制度" 类别
filters = MetadataFilters(
    filters=[MetadataFilter(key="category", value="财务制度")]
)

query_engine = index.as_query_engine(filters=filters)
response = query_engine.query("报销标准是什么？")
# → 返回的答案只会来自 category="财务制度" 的 Chunk
```

#### 4.3.3 多条件 AND 过滤

```python
# 所有条件都必须满足
filters = MetadataFilters(
    filters=[
        MetadataFilter(key="category", value="人事政策"),
        MetadataFilter(key="version", value="V3.0"),
        MetadataFilter(key="status", value="active"),
    ],
    condition=FilterCondition.AND,  # ← AND 组合
)
```

#### 4.3.4 多条件 OR 过滤

```python
# 满足任一条件即可
filters = MetadataFilters(
    filters=[
        MetadataFilter(key="department", value="HR"),
        MetadataFilter(key="department", value="Finance"),
    ],
    condition=FilterCondition.OR,  # ← OR 组合
)
```

#### 4.3.5 操作符过滤

```python
from llama_index.core.vector_stores import FilterOperator

# 创建日期 > 2024-01-01 的 (大于)
filters = MetadataFilters(
    filters=[
        MetadataFilter(
            key="created_date",
            value="2024-01-01",
            operator=FilterOperator.GT,  # Greater Than
        ),
        MetadataFilter(key="status", value="active"),
    ],
    condition=FilterCondition.AND,
)

# 所有的 FilterOperator:
#   EQ        → == (等于, 默认)
#   GT        → >  (大于)
#   GTE       → >= (大于等于)
#   LT        → <  (小于)
#   LTE       → <= (小于等于)
#   NE        → != (不等于)
#   IN        → 值在列表中
#   NIN       → 值不在列表中
#   CONTAINS  → 字符串包含
#   TEXT_MATCH → 全文匹配
```

#### 4.3.6 嵌套复杂过滤

```python
# (category='人事政策' AND version='V3.0') OR (category='财务制度' AND version='V2.0')
filters = MetadataFilters(
    filters=[
        # 组1: 人事政策 V3.0
        MetadataFilters(
            filters=[
                MetadataFilter(key="category", value="人事政策"),
                MetadataFilter(key="version", value="V3.0"),
            ],
            condition=FilterCondition.AND,
        ),
        # 组2: 财务制度 V2.0
        MetadataFilters(
            filters=[
                MetadataFilter(key="category", value="财务制度"),
                MetadataFilter(key="version", value="V2.0"),
            ],
            condition=FilterCondition.AND,
        ),
    ],
    condition=FilterCondition.OR,  # 组1 或 组2
)
```

---

### 4.4 完整实战：元数据建模 → 注入 → 索引 → 过滤查询

```python
"""
═══════════════════════════════════════════════════════════════════════════
  元数据建模与过滤 —— 完整实战
═══════════════════════════════════════════════════════════════════════════

  流程:
    1. 设计 metadata schema (三个维度)
    2. 为每个 Document 注入 metadata
    3. 用 SentenceSplitter 切分 → metadata 自动继承到每个 Node
    4. 建 VectorStoreIndex
    5. 演示: 无过滤查询 vs 带过滤查询 vs 复杂过滤查询
"""
from llama_index.core import (
    Document, VectorStoreIndex, Settings,
)
from llama_index.core.node_parser import SentenceSplitter
from llama_index.core.vector_stores import (
    MetadataFilter, MetadataFilters, FilterOperator, FilterCondition,
)
from llama_index.embeddings.openai import OpenAIEmbedding

Settings.embed_model = OpenAIEmbedding(model="text-embedding-3-small", dimensions=512)


# ═══════════════════════════════════════════════════════════════
# Step 1: 创建带完整 metadata 的 Document 集合
# ═══════════════════════════════════════════════════════════════

documents = [
    # ── 人事政策 ──
    Document(
        text="""## 第三章 考勤与工时

### 3.1 工作时间
公司实行弹性工作制。核心工作时间为上午10:00至12:00，下午14:00至17:00。
弹性时段为上午8:00至10:00及下午17:00至20:00。午休时间为12:00至13:30。

### 3.2 打卡规定
公司使用企业微信进行打卡。每月补卡不超过3次。

### 3.3 迟到与旷工
核心工作时间内迟到30分钟以内不扣工资。迟到超过1小时按旷工半日处理。
累计旷工3日或连续旷工2日视为严重违纪。""",
        metadata={
            "title": "员工手册",
            "category": "人事政策",
            "department": "HR",
            "topic": "考勤",
            "version": "V3.0",
            "created_date": "2024-03-15",
            "status": "active",
            "doctype": "policy",
        }
    ),
    Document(
        text="""## 第四章 请假制度

### 4.1 事假
事假需提前1个工作日向部门主管申请。事假期间不发放工资。
每月事假累计不超过3天。紧急情况可事后补办手续。

### 4.2 病假
病假需在当日8:30前通知部门主管。连续超过2天须提供二级及以上医院证明。

### 4.3 年假
入职1-5年员工享有5天年假，5-10年10天，10年以上15天。
年假最小请假单位为半天。次年3月31日前未休完的自动清零。""",
        metadata={
            "title": "员工手册",
            "category": "人事政策",
            "department": "HR",
            "topic": "请假",
            "version": "V3.0",
            "created_date": "2024-03-15",
            "status": "active",
            "doctype": "policy",
        }
    ),
    # ── 财务制度 ──
    Document(
        text="""## 第八章 费用报销

### 8.1 报销范围
因公产生的交通费、住宿费、餐饮费可按标准报销。

### 8.2 住宿标准
一线城市不超过500元/天，二线城市不超过350元/天，其他城市不超过200元/天。

### 8.3 餐饮补贴
出差期间每日餐饮补贴80元。商务宴请需提前报备，实报实销。

### 8.4 报销流程
费用发生后30日内提交报销。单次超5000元需总经理审批。""",
        metadata={
            "title": "员工手册",
            "category": "财务制度",
            "department": "Finance",
            "topic": "报销",
            "version": "V2.1",
            "created_date": "2024-01-10",
            "status": "active",
            "doctype": "policy",
        }
    ),
    # ── 旧版政策 (已归档) ──
    Document(
        text="""## 第四章 请假制度 (旧版 V2.0)

### 4.1 事假
事假需提前3个工作日申请。事假期间不发放工资。""",
        metadata={
            "title": "员工手册",
            "category": "人事政策",
            "department": "HR",
            "topic": "请假",
            "version": "V2.0",
            "created_date": "2022-06-01",
            "status": "archived",    # ← 已归档! 不应被检索到
            "doctype": "policy",
        }
    ),
]

print("=" * 70)
print("  元数据建模示例")
print("=" * 70)
print(f"  共 {len(documents)} 个 Document:\n")
for i, doc in enumerate(documents):
    meta = doc.metadata
    print(f"  Doc[{i}]: {meta['topic']:<6} | {meta['category']:<8} | "
          f"V{meta['version']} | {meta['status']:<8} | "
          f"{meta['created_date']}")


# ═══════════════════════════════════════════════════════════════
# Step 2: 切分 (metadata 自动继承)
# ═══════════════════════════════════════════════════════════════

splitter = SentenceSplitter(chunk_size=300, chunk_overlap=40)
nodes = splitter.get_nodes_from_documents(documents)

# 验证 metadata 继承
sample = nodes[0]
print(f"\n  Metadata 继承验证:")
print(f"  第一个 Node 的 metadata: ")
for key in ["category", "version", "status", "topic"]:
    print(f"    {key}: {sample.metadata.get(key)}")
print(f"  (自动从父 Document 继承 ✓)")


# ═══════════════════════════════════════════════════════════════
# Step 3: 建索引
# ═══════════════════════════════════════════════════════════════

index = VectorStoreIndex(nodes)


# ═══════════════════════════════════════════════════════════════
# Step 4: 四种查询方式对比
# ═══════════════════════════════════════════════════════════════

print("\n" + "=" * 70)
print("  查询对比: 无过滤 vs 简单过滤 vs 操作符过滤 vs 复杂过滤")
print("=" * 70)

# ── 查询 A: 无过滤 ──────────────────────────────────────
query = "请假需要提前多久申请？"
engine_a = index.as_query_engine(similarity_top_k=3)
resp_a = engine_a.query(query)

print(f"\n  [A] 无过滤 —— 查询: {query}")
print(f"  回答: {resp_a}")
print(f"  来源:")
for node in resp_a.source_nodes:
    m = node.metadata
    print(f"    [{m.get('topic')}] V{m.get('version')} "
          f"status={m.get('status')} | score={node.score:.3f}")
print(f"  ⚠ 可能返回了旧版本(archived)或无关类别的内容")

# ── 查询 B: 简单过滤 (只要 active + V3.0) ────────────────
filters_b = MetadataFilters(
    filters=[
        MetadataFilter(key="status", value="active"),
        MetadataFilter(key="version", value="V3.0"),
    ]
)
engine_b = index.as_query_engine(similarity_top_k=3, filters=filters_b)
resp_b = engine_b.query(query)

print(f"\n  [B] 简单过滤 —— status='active' AND version='V3.0'")
print(f"  回答: {resp_b}")
print(f"  来源:")
for node in resp_b.source_nodes:
    m = node.metadata
    print(f"    [{m.get('topic')}] V{m.get('version')} "
          f"status={m.get('status')} | score={node.score:.3f}")

# ── 查询 C: 操作符过滤 (created_date > 2023-01-01) ───────
filters_c = MetadataFilters(
    filters=[
        MetadataFilter(key="status", value="active"),
        MetadataFilter(
            key="created_date",
            value="2023-01-01",
            operator=FilterOperator.GT,
        ),
    ]
)
engine_c = index.as_query_engine(similarity_top_k=3, filters=filters_c)
resp_c = engine_c.query(query)

print(f"\n  [C] 操作符过滤 —— status='active' AND created_date > '2023-01-01'")
print(f"  回答: {resp_c}")
print(f"  来源:")
for node in resp_c.source_nodes:
    m = node.metadata
    print(f"    [{m.get('topic')}] created={m.get('created_date')} "
          f"status={m.get('status')} | score={node.score:.3f}")

# ── 查询 D: 复杂过滤 (人事+财务 的 active) ───────────────
filters_d = MetadataFilters(
    filters=[
        MetadataFilters(
            filters=[
                MetadataFilter(key="category", value="人事政策"),
                MetadataFilter(key="status", value="active"),
            ],
            condition=FilterCondition.AND,
        ),
        MetadataFilters(
            filters=[
                MetadataFilter(key="category", value="财务制度"),
                MetadataFilter(key="status", value="active"),
            ],
            condition=FilterCondition.AND,
        ),
    ],
    condition=FilterCondition.OR,
)
engine_d = index.as_query_engine(similarity_top_k=3, filters=filters_d)
resp_d = engine_d.query("住宿标准和请假规定分别是什么？")

print(f"\n  [D] 复杂过滤 —— (cat='人事' AND active) OR (cat='财务' AND active)")
print(f"  回答: {resp_d}")
print(f"  来源:")
for node in resp_d.source_nodes:
    m = node.metadata
    print(f"    [{m.get('category')}] {m.get('topic')} "
          f"V{m.get('version')} | score={node.score:.3f}")

# ── 对比总结 ──────────────────────────────────────────────
print(f"\n  {'─'*60}")
print(f"  对比总结")
print(f"  {'─'*60}")
print(f"  A (无过滤):     可能返回旧版本/无关类别 → 答案可能不准确")
print(f"  B (简单过滤):   只返回 active + V3.0 → 精准命中")
print(f"  C (操作符过滤): 只返回 2023 年之后的 → 时效性保证")
print(f"  D (复杂过滤):   跨类别组合查询 → 灵活的条件逻辑")
```

---

### 4.5 元数据过滤的性能考量

```
═══════════════════════════════════════════════════════════════════════
              两种过滤策略: 前过滤 vs 后过滤
═══════════════════════════════════════════════════════════════════════

  前过滤 (Pre-filtering):
    先过滤 → 在符合条件的子集中做向量检索

    优势: 候选集小, 检索更快
    劣势: 如果过滤后候选集太小 (< top_k), 可能返回不相关的结果
    适用: 过滤条件能大幅缩小候选集时

    示例: 100万向量, 过滤后只剩 500 个 → 检索极快

  后过滤 (Post-filtering):
    先做向量检索(Top-K×N) → 在结果中过滤 → 返回 Top-K

    优势: 保证至少检索到候选集, 不会因为过滤太严而返回空
    劣势: 如果过滤掉太多结果, 返回数量 < top_k
    适用: 过滤条件比较宽松, 或需要保证召回量

    示例: 向量检索取 Top-50 → 过滤掉不符合条件的 → 取前 3 个

  ═══════════════════════════════════════════════════════════

  大多数向量数据库默认使用"前过滤"——在检索前就缩小候选集。
  Qdrant / Milvus / Weaviate 支持在 ANN 索引内直接处理过滤条件。

  如果有复杂的自定义过滤逻辑:
    → 取 Top-K × 3 的结果 → Python 侧做自定义过滤 → 取 Top-K
    → 这种"后过滤"方式最灵活, 但有额外延迟
═══════════════════════════════════════════════════════════════════════
```

---

### 4.6 最佳实践总结

| 实践 | 说明 |
|------|------|
| **在 Document 级别注入 metadata** | 一次设置, 所有子 Node 自动继承 —— 不用手动给每个 Chunk 加 |
| **metadata 字段值要稳定** | 用枚举值而非自由文本: "人事政策" 而非 "HR相关政策"、"hr政策"、"人事相关政策" |
| **时效字段用标准格式** | 日期用 "YYYY-MM-DD"，版本用 "V1.0"，便于操作符比较 |
| **status 字段是必需品** | active / draft / archived —— 避免检索到草稿或已废弃的内容 |
| **先无过滤验证，再加过滤优化** | 原型阶段不加过滤，验证语义检索效果后，逐步引入过滤条件 |
| **过滤条件要与用户意图对齐** | 如果用户说"最新的政策" → 过滤 + 排序 created_date DESC |
| | 如果用户说"HR 的规定" → 过滤 category='人事政策' |

```
  元数据建模的核心:
    → 设计 Schema 时想清楚"用户查询时可能用什么条件筛选"
    → 把这些条件设计为 metadata 字段
    → 在 Document 创建时一次性注入
    → Node 自动继承 → 过滤时零额外成本
```


---



---

