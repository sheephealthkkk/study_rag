## 十八、RAG 文档切分概述 —— 从策略到评估

文档切分（Chunking）是 RAG 管线中连接"文档解析"和"向量检索"的关键桥梁。切分的好坏直接决定了检索系统能找到什么、LLM 能读到什么。这一章我们从原理层面系统讲解切分的核心概念、影响因素和评估方法。

---

### 18.1 切分的重要性：四个维度

```
                    切分质量的四个影响维度

                          ┌──────────────┐
                          │   切分策略    │
                          │ chunk_size    │
                          │ chunk_overlap │
                          └──────┬───────┘
                                 │
           ┌─────────────────────┼─────────────────────┐
           ▼                     ▼                     ▼
    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
    │  检索准确性   │    │  上下文完整性  │    │  计算效率     │
    │  Retrieval   │    │   Context     │    │  Efficiency  │
    │  Accuracy    │    │  Completeness │    │              │
    └──────┬───────┘    └──────┬───────┘    └──────┬───────┘
           │                   │                   │
           └───────────────────┼───────────────────┘
                               ▼
                    ┌──────────────────┐
                    │  召回率与精确度   │
                    │  Recall &        │
                    │  Precision       │
                    └──────────────────┘
```

#### 维度一：检索准确性

切分粒度直接影响检索的"命中质量"。假设用户搜索"事假申请流程"：

```
文档原文：
  第一条 公司实行标准工时制...
  第二条 上班时间为9:00至18:00...
  第三条 事假需提前1个工作日申请，经部门主管审批后交HR备案...
  第四条 年假天数按工龄计算...

情况 A：chunk_size=100（细粒度切分）
  Chunk 0: "第一条 公司实行标准工时制...第二条 上班时间为9:00..."
  Chunk 1: "第三条 事假需提前1个工作日申请，经部门主管审批后交HR备案"
  Chunk 2: "第四条 年假天数按工龄计算..."

  检索 "事假申请流程" → Chunk 1 相似度 0.92 ✓ 精准命中！
  但 Chunk 1 只包含 1 条规定的完整信息

情况 B：chunk_size=500（粗粒度切分）
  Chunk 0: "第一条...第二条...第三条：事假需提前1个工作日申请，
            经部门主管审批后交HR备案...第四条：年假天数..."
  
  检索 "事假申请流程" → Chunk 0 相似度 0.65
  相似度被"第一条""第二条""第四条"这些无关内容稀释了
  但 Chunk 0 包含了更完整的上下文（前后的规定也在一起）
```

**结论：** 细粒度检索更精准（匹配信号强），但可能丢失上下文；粗粒度检索噪声大（匹配信号被稀释），但上下文更完整。:o::o::o::o:

---

#### 维度二：上下文完整性

LLM 理解一段信息需要足够的上下文。考虑以下场景：

```
Chunk 仅包含：
  "按 1.5 倍工资计算"

没有上下文时，LLM 不知道：
  - 什么情况下按 1.5 倍？ ← 前文 "工作日加班"
  - 和谁有关？           ← 前文 "加班费计算"
  - 有没有例外？         ← 后文 "法定节假日按3倍"

完整的上下文应该是：
  "9.2 加班费计算：工作日加班按1.5倍工资计算，
   休息日加班按2倍工资计算，法定节假日加班按3倍工资计算。"
```

**结论：** chunk_size 必须足够大以容纳完整的语义单元；:o::o::o:chunk_overlap 可以在相邻 chunk 之间建立"上下文纽带"。

---

#### 维度三：计算效率

| 切分粒度 | Chunk 数量 | Embedding 调用次数 | 索引大小 | 检索耗时 |
|----------|:---:|:---:|:---:|:---:|
| 粗粒度（1000 token/chunk） | 少 | 少 | 小 | 快 |
| 细粒度（200 token/chunk） | 多（5倍） | 多（5倍） | 大（5倍） | 慢（5倍） |
| 极细粒度（50 token/chunk） | 更多（20倍） | 更多（20倍） | 更大（20倍） | 更慢（20倍） |

**结论：** 切分过细会导致 chunk 数量爆炸，Embedding API 成本、存储成本和检索延迟都线性增长。100 万份文档如果选错 chunk_size，可能多花数千美元的 API 费用。

---

#### 维度四：召回率与精确度

这是衡量检索系统质量的**两个核心指标**，两者来自信息检索学科：

```
═══════════════════════════════════════════════════════════════════════
                    召回率 (Recall) 与 精确率 (Precision)
═══════════════════════════════════════════════════════════════════════

                        实际相关       实际不相关
                      ┌──────────┐  ┌──────────┐
    检索到的          │   TP     │  │   FP     │  → Precision = TP / (TP + FP)
   (系统返回的)       │  True    │  │  False   │     "返回的结果中有多少是真正相关的？"
                      │ Positive │  │ Positive │
                      ├──────────┤  ├──────────┤
    未检索到的        │   FN     │  │   TN     │
   (系统未返回的)     │  False   │  │  True    │
                      │ Negative │  │ Negative │
                      └──────────┘  └──────────┘
                            │
                            ▼
               Recall = TP / (TP + FN)
               "所有真正相关的文档中，有多少被检索到了？"

═══════════════════════════════════════════════════════════════════════

  通俗理解：

    召回率 (Recall)：
      知识库中有 10 个段落提到了"事假申请规定"。
      检索系统找回了其中 7 个。
      → Recall = 7/10 = 70%

      高召回率 = "该找到的都找到了"（查全）
      低召回率 = "漏掉了重要信息"（漏检）

    精确率 (Precision)：
      检索系统返回了 5 个段落。
      其中 4 个确实和"事假申请规定"相关，1 个是讲"年假"的（不相关）。
      → Precision = 4/5 = 80%

      高精确率 = "返回的都是相关的"（查准）
      低精确率 = "混入了无关内容"（噪声）

    理想状态：Recall=100%, Precision=100%
    现实情况：两者互为矛盾——提高一个通常会降低另一个
```

---

### 18.2 切分粒度对 Recall 和 Precision 的影响

切分粒度会同时影响召回率和精确率，但方向相反：:o::o::o::o::o:

```
═══════════════════════════════════════════════════════════════════════
              切分粒度 ←→ 召回率/精确率 的权衡关系
═══════════════════════════════════════════════════════════════════════

  细粒度切分 (小 chunk)：

    一段原文 "事假需提前1个工作日申请，病假应在当日8:30前通知主管"

    切分结果：
      Chunk A: "事假需提前1个工作日申请"      ← 30 token
      Chunk B: "病假应在当日8:30前通知主管"    ← 25 token

    检索 "请假规定"：
      → 返回 Chunk A (相似度 0.88) 和 Chunk B (相似度 0.85)
      → Recall ↑ (两个相关 chunk 都被找回了)
      → Precision ↑ (每个 chunk 只含单一主题，不会混入无关内容)
      → 上下文 ↓ (Chunk A 不知道 Chunk B 的存在，"事假"和"病假"的并列关系丢失)

  粗粒度切分 (大 chunk)：

    同上原文，切分结果：
      Chunk C: "事假需提前1个工作日申请，病假应在当日8:30前通知主管。
               年假按工龄计算：1-5年5天，5-10年10天，10年以上15天。

    检索 "请假规定"：
      → 返回 Chunk C (相似度 0.62)
      → Recall ↓ (只返回了1个 chunk，如果有其他相关的没在这个 chunk 里就漏了)
      → Precision ↓ (年假的内容混在同一个 chunk 里，对"请假规定"来说不是最相关的)
      → 上下文 ↑ (chunk 里同时有 事假+病假+年假 的信息)

═══════════════════════════════════════════════════════════════════════
                        权衡表
═══════════════════════════════════════════════════════════════════════

  指标         │  细粒度 (100-300t)  │  粗粒度 (800-2000t)
  ─────────────┼─────────────────────┼─────────────────────
  Recall       │  ↑ 高（更容易匹配）   │  ↓ 低（整体稀释）
  Precision    │  ↑ 高（单一主题）     │  ↓ 低（多主题混合）
  上下文完整性  │  ↓ 低（割裂）        │  ↑ 高（完整）
  计算成本     │  ↑ 高（chunk 多）     │  ↓ 低（chunk 少）
  LLM 理解     │  ↓ 差（碎片化）       │  ↑ 好（信息完整）
  检索速度     │  ↓ 慢（候选集大）     │  ↑ 快（候选集小）
  ─────────────┴─────────────────────┴─────────────────────

  没有"最佳粒度"，只有"最适合你场景的粒度"。
```

---

### 18.3 粗粒度+细粒度的双层切分策略:o::o::o::o::o::o::o::o::o::o::o::o::o::o::o::o::o:

实际生产环境不会死板地只用一个 chunk_size。业界的最佳实践是**双层切分**：先用粗粒度切出"候选段落"，再用细粒度切出"检索单元"：

```
═══════════════════════════════════════════════════════════════════════
                    双层切分策略（Coarse-to-Fine）
═══════════════════════════════════════════════════════════════════════

  Step 1: 粗粒度切分（如 chunk_size=1024, overlap=128）
  ┌─────────────────────────────────────────────────────────────────┐
  │                                                                 │
  │  原文：[A段][B段][C段][D段][E段][F段][G段][H段][I段][J段]       │
  │                                                                 │
  │  粗切结果：                                                      │
  │  ┌──────────────────────┐ ┌──────────────────────┐              │
  │  │ Big Chunk 0 (1024t)  │ │ Big Chunk 1 (1024t)  │  ...         │
  │  │ A段+B段+C段+D段      │ │ D段(overlap)+E+F+G   │              │
  │  └──────────┬───────────┘ └──────────────────────┘              │
  │             │                                                    │
  └─────────────┼────────────────────────────────────────────────────┘
                │
                ▼
  Step 2: 细粒度切分（如 chunk_size=256, overlap=32）
  ┌─────────────────────────────────────────────────────────────────┐
  │                                                                 │
  │  对每个 Big Chunk 内部再做细粒度切分：                            │
  │                                                                 │
  │  Big Chunk 0 内部：                                              │
  │  ┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐                      │
  │  │Small ││Small ││Small ││Small ││Small │    ← 检索用             │
  │  │Chunk ││Chunk ││Chunk ││Chunk ││Chunk │                      │
  │  │  0   ││  1   ││  2   ││  3   ││  4   │                      │
  │  └──┬───┘└──┬───┘└──┬───┘└──┬───┘└──┬───┘                      │
  │     │       │       │       │       │                            │
  │     └───────┴───────┴───┬───┴───────┘                            │
  │                         │                                        │
  │          每个 Small Chunk 都记录它所属的 Big Chunk               │
  │                         │                                        │
  └─────────────────────────┼────────────────────────────────────────┘
                            │
                            ▼
  Step 3: 检索时的工作流
  ┌─────────────────────────────────────────────────────────────────┐
  │                                                                 │
  │  Query → 用 Small Chunks 做检索（高 Recall + 高 Precision）      │
  │           │                                                      │
  │           ▼                                                      │
  │  找到最相关的 Small Chunk (如 Small Chunk 2, score=0.92)         │
  │           │                                                      │
  │           ▼                                                      │
  │  追溯 Small Chunk 2 所属的 Big Chunk 0                           │
  │           │                                                      │
  │           ▼                                                      │
  │  将整个 Big Chunk 0 作为上下文喂给 LLM                           │
  │  (LLM 看到的是完整上下文，而非碎片化的 Small Chunk)               │
  │                                                                 │
  └─────────────────────────────────────────────────────────────────┘

  这个双层策略同时解决了：
    ✓ 检索精准度 → Small Chunk 做匹配，单一主题，信号强
    ✓ 上下文完整性 → Big Chunk 做上下文，信息完整，LLM 理解好
    ✓ 计算效率   → 只对 Small Chunks 做向量检索（维度较小，速度快）
```

**LlamaIndex 内置了这种策略的两种实现：**:o::o::o::o:

| 实现 | 机制 |
|------|------|
| `SentenceWindowNodeParser` | 检索时用小 chunk，返回时自动扩展前后 N 个相邻句子 |
| `MetadataReplacementPostProcessor` | 用小 chunk 检索，返回时替换为其父级大 chunk |
| `AutoMergingRetriever` | 当一个小 chunk 匹中后，检查其相邻小 chunk 是否也被匹中，若多个小 chunk 来自同一个大 chunk 则合并返回 |

---

### 18.4 文档切分的基本流程:o::o::o:

一个完整的切分流线包含五个阶段：

```
═══════════════════════════════════════════════════════════════════════
                    文档切分五阶段流水线
═══════════════════════════════════════════════════════════════════════

  ┌─────────────────────────────────────────────────────────────┐
  │  阶段 1: 文档加载 (Document Loading)                        │
  │                                                            │
  │  做什么：将原始文件读入内存，转为统一的 Document 对象         │
  │  用什么：Unstructured / PyMuPDF / SimpleDirectoryReader     │
  │  输出：  Document {page_content: str, metadata: dict}       │
  │                                                            │
  │  关键考虑：                                                 │
  │    · 保留 metadata（文件名、页码、章节标题）                  │
  │    · metadata 在切分后需要随着 chunk 继承下去                │
  └────────────────────────┬────────────────────────────────────┘
                           │
                           ▼
  ┌─────────────────────────────────────────────────────────────┐
  │  阶段 2: 预处理 (Preprocessing)                             │
  │                                                            │
  │  做什么：清洗文本，为切分准备干净的输入                       │
  │  用什么：正则表达式 / pandas / BeautifulSoup / cv2          │
  │  具体操作（详见第十七章）：                                   │
  │    · 去除 HTML 标签、特殊符号、控制字符                       │
  │    · 合并多余空格/换行/制表符                                │
  │    · 统一全角半角、修正 OCR 错误                             │
  │    · 图像文档做二值化/降噪/倾斜矫正                          │
  │                                                            │
  │  为什么要在切分前做预处理：                                   │
  │    · 脏字符会影响 tokenizer 的计数——明明200个token的内容     │
  │      因为混入噪声被切成了300个token                           │
  │    · 多余换行会被当成"句子边界"，造成错误的切分点             │
  └────────────────────────┬────────────────────────────────────┘
                           │
                           ▼
  ┌─────────────────────────────────────────────────────────────┐
  │  阶段 3: 切分策略选择 (Strategy Selection)                   │
  │                                                            │
  │  根据文档特征和业务需求，选择最合适的切分策略：                │
  │                                                            │
  │  ┌──────────────────────────────────────────────────────┐  │
  │  │  策略              │ 原理              │ 适用场景       │  │
  │  ├────────────────────┼───────────────────┼───────────────┤  │
  │  │ 固定字符数切分       │ 按 N 个字符截断     │ 纯文本/无结构  │  │
  │  │ 固定 token 数切分    │ 按 N 个 token 截断  │ 需要精确控制   │  │
  │  │ 按句子切分          │ 在句号/问号/感叹号处 │ 叙述性文档     │  │
  │  │ 按段落切分          │ 在 \\n\\n 处        │ 结构清晰的文档  │  │
  │  │ 按标题层级切分       │ 在 # / ## / ### 处  │ Markdown/HTML  │  │
  │  │ 语义切分            │ Embedding 相似度阈值 │ 高质量需求     │  │
  │  │ 递归切分            │ 逐级降级分隔符       │ 通用/兜底      │  │
  │  │ 双层切分            │ 粗+细两步           │ 生产环境首选   │  │
  │  └──────────────────────────────────────────────────────┘  │
  │                                                            │
  │  选择依据：                                                 │
  │    · 文档类型（代码/散文/法律条文/聊天记录→切分方式不同）     │
  │    · 平均查询长度（短查询→细粒度，长查询→粗粒度）             │
  │    · Embedding 模型的最大输入长度                            │
  │    · LLM 的上下文窗口和生成质量                              │
  └────────────────────────┬────────────────────────────────────┘
                           │
                           ▼
  ┌─────────────────────────────────────────────────────────────┐
  │  阶段 4: 执行切分 (Execution)                                │
  │                                                            │
  │  做什么：按选定策略将 Document 拆分为 Node/Chunk 列表         │
  │                                                            │
  │  关键操作：                                                 │
  │    · 在语义边界处切分（不在词中间切断）                        │
  │    · 应用 chunk_overlap 实现重叠                              │
  │    · 保留 metadata（每个 chunk 继承原 Document 的元数据）     │
  │    · 记录 chunk 之间的关系（前一个/后一个/父级）              │
  │                                                            │
  │  具体实现：                                                 │
  │    · LangChain: RecursiveCharacterTextSplitter,              │
  │                 TokenTextSplitter, SemanticChunker           │
  │    · LlamaIndex: SentenceSplitter, TokenTextSplitter,       │
  │                  SemanticSplitterNodeParser,                │
  │                  SentenceWindowNodeParser                   │
  │    · 手写: split_text() 递归降级切分（第12/13章）            │
  └────────────────────────┬────────────────────────────────────┘
                           │
                           ▼
  ┌─────────────────────────────────────────────────────────────┐
  │  阶段 5: 后处理 (Post-processing)                            │
  │                                                            │
  │  做什么：对切分结果做最后的校验和优化                          │
  │                                                            │
  │  常见操作：                                                 │
  │    · 过短 chunk 合并（如只有 5 个 token 的片段→合并到前一个）│
  │    · 过长 chunk 再切（超 Embedding 模型输入限制的再切）      │
  │    · 去重（相邻 chunk 内容高度重复的去除）                    │
  │    · metadata 补充（补充 chunk_index、parent_id 等）         │
  │    · token 计数校验（可选，确保不超限）                       │
  │    · 质量过滤（全是数字/符号的无意义 chunk 丢弃）             │
  └─────────────────────────────────────────────────────────────┘
```

### 18.5 各阶段详解

#### 阶段 1：文档加载 —— 元数据保留是关键:o:

```python
# 加载时必须保留 metadata，切分后需要继承
doc = load_document("员工手册.pdf")
# metadata = {"source": "员工手册.pdf", "page": 3, "chapter": "请假制度"}
```

#### 阶段 2：预处理 —— 清洗决定切分质量:o:

切分器依赖换行符（`\n`）、句号（`。`）等分隔符判断切分点。如果文本中混入了 HTML 的 `<br>` 标签、多个连续空格、OCR 引入的 `\x00` 控制字符，切分器会在错误的位置切分。

这就是第十七章放在第十八章前面的原因——清洗是切分的前置条件。

#### 阶段 3：切分策略选择 —— 根据:o:文档类型决定

```
不同文档类型的推荐切分策略：

  Markdown / 结构化文档：
    → 按标题层级切分（# ## ### 作为切分锚点）
    → 每个 ## 段落 + 其下属文本 = 1 个 chunk
    优点：语义单元自然形成，不需要调整 chunk_size

  PDF / 非结构化文档：
    → 递归字符切分 (chunk_size=512, overlap=64)
    → 先按段落切，再按句子切，最后按字符切
    优点：通用性强，不需要预知文档结构

  代码文件：
    → 按函数/类边界切分
    → 每个 function / class = 1 个 chunk
    优点：不切断函数体，retrieval 时语义完整

  聊天记录 / 对话：
    → 按轮次切分（每轮 Q+A = 1 个 chunk）
    优点：Q 和 A 在同一 chunk 内，检索时意图和答案在一起

  法律 / 合同文档：
    → 按条款编号切分（第一条/第二条/第三条...）
    优点：每一条款是独立的语义单元
```

#### 阶段 4：执行切分 —— overlay 的核心作用

```
chunk_overlap 保证了关键信息不落在边界上：

  Chunk 1: "员工请假应提前申请。事假需提前1个工作日申"
                                                 ↑ 被截断
  Chunk 2: (overlap) "事假需提前1个工作日申请，病假应在8:30前通知"

  overlap 覆盖了被截断的那部分，使"事假"信息在 Chunk 2 中完整出现。
```

#### 阶段 5：后处理 —— 最后的质量兜底:o::o:

```python
def postprocess_chunks(chunks: List[Document], 
                       min_length: int = 20,
                       max_tokens: int = 8192) -> List[Document]:
    """切分后处理"""
    result = []
    for i, chunk in enumerate(chunks):
        # 1. 过短 → 合并到前一个
        if len(chunk.page_content) < min_length and result:
            result[-1].page_content += "\n" + chunk.page_content
            continue
        # 2. 纯符号/数字（无意义）→ 丢弃
        text = chunk.page_content.strip()
        if re.match(r'^[\d\s\W]+$', text):
            continue
        # 3. 补充元数据
        chunk.metadata["chunk_index"] = str(i)
        chunk.metadata["chunk_count"] = str(len(chunks))
        result.append(chunk)
    return result
```

---

### 18.6 切分效果评估

怎么知道你的切分策略好不好？以下是一个多维评估框架：

```
═══════════════════════════════════════════════════════════════════════
                    切分质量评估框架
═══════════════════════════════════════════════════════════════════════

  ┌─────────────────────────────────────────────────────────────┐
  │  1. 结构完整性评估                                           │
  │                                                            │
  │  指标：                                                     │
  │    · 语义单元断裂率 = 被截断的句/段落 / 总句/段落数          │
  │      理想值：< 5%                                           │
  │    · 表格完整率 = 没有被切散的表格 / 总表格数                │
  │      理想值：100%                                           │
  │                                                            │
  │  检查方法：                                                 │
  │    · 抽样检查 N 个 chunk，看是否有在不完整句子处切断的       │
  │    · 检查是否有 "前一个chunk的末尾 + 后一个chunk的开头"       │
  │      拼起来才是一句完整的话                                  │
  └─────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────┐
  │  2. 长度分布评估                                             │
  │                                                            │
  │  指标：                                                     │
  │    · 平均 chunk 长度（应该接近 chunk_size 的 60-80%）        │
  │    · chunk 长度的标准差（越小越均匀）                         │
  │    · 过长 chunk 占比（>1.5×chunk_size 的比例，理想 < 5%）    │
  │    · 过短 chunk 占比（<50 token 的比例，理想 < 10%）         │
  │                                                            │
  │  可视化方法：                                               │
  │    · 绘制 chunk 长度分布的直方图                             │
  │    · 好的分布：集中在目标值附近、无长尾                       │
  └─────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────┐
  │  3. 检索效果评估                                             │
  │                                                            │
  │  准备：10-20 条标准查询，人工标注每条查询"应该返回的段落"     │
  │                                                            │
  │  指标：                                                     │
  │    · Recall@K  = 前K个结果中命中的人工标注 / 人工标注总数     │
  │    · MRR (Mean Reciprocal Rank) = 第一个正确答案的排名倒数    │
  │    · NDCG@K (Normalized Discounted Cumulative Gain)         │
  │                                                            │
  │  对比方法：                                                 │
  │    · 用不同的 chunk_size (200/400/600/800) 各跑一轮         │
  │    · 比较各轮次的 Recall@5 和 MRR                            │
  │    · 选 Recall@5 最高的 chunk_size 作为生产配置              │
  └─────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────┐
  │  4. LLM 生成质量评估                                          │
  │                                                            │
  │  指标：                                                     │
  │    · Faithfulness (忠实度) = 回答中有多少内容可以直接引用到    │
  │      检索到的 chunk 中。理想值：> 0.90                       │
  │    · Answer Relevance (回答相关性) = 回答是否直接解决了用户问题│
  │    · Context Relevance (上下文相关性) = 检索到的 chunk 中      │
  │      有多大比例是真正需要的。理想值：> 0.80                   │
  │                                                            │
  │  实践方法：                                                 │
  │    · 用 LlamaIndex 的 FaithfulnessEvaluator 自动评估         │
  │    · 用 RAGAS 框架做系统级评估                               │
  └─────────────────────────────────────────────────────────────┘
```

### 18.7 总结：切分策略选择速查

```
═══════════════════════════════════════════════════════════════════════
                    切分策略选择速查表
═══════════════════════════════════════════════════════════════════════

  你的场景                      →  推荐切分策略

  快速原型、不确定参数            → chunk_size=512, overlap=64, 递归切分
                                  (最通用的起点，跑起来再调)

  追求高 Recall (少漏检)         → 减小 chunk_size (200-300) + 增大 top_k
                                  + 双层切分（Small检索→Big做上下文）

  追求高 Precision (少噪声)      → 增大 chunk_size (800-1000) + Rerank精排
                                  + 语义切分（在话题边界处切）

  长篇叙述文档 (论文/报告)        → 按章节/标题层级切分 + 中等 chunk_size (512)
                                  + 大的 overlap (128) 保持叙述连贯性

  短 FAQ / 问答对                → 一对一保留 (1个QA对 = 1个chunk)
                                  不要切分，FAQ 的自然边界就是最佳边界

  代码仓库                        → 按函数/类边界切分
                                  不要按字符数硬切（会切断函数体）

  混合文档库 (各种格式都有)        → 按文档类型分类 → 各自选最佳策略
                                  (Markdown→标题切, PDF→递归切, 代码→函数切)
═══════════════════════════════════════════════════════════════════════
```


---

## 十九、大厂面试题深度讲解

### 19.1 切分基础与核心概念类

#### Q1：chunk_size 和 chunk_overlap 分别影响 RAG 的哪些方面？如何为你的场景选择合适的值？（字节跳动 / 腾讯 高频基础题）

**面试官考察点：** 这是 Chunking 最基础的面试题。80% 的候选人会回答"chunk_size 越大上下文越完整，越小检索越精准"。面试官想听的是——你能不能用具体的数值和场景讲清楚这个 trade-off，而不是背概念。

**回答框架（从四个维度切入，每个维度给出定量认知）：**

**维度一：检索精度——chunk_size 直接影响"信号噪声比"**

```
chunk_size=200（细粒度）：
  用户搜"事假申请流程"
  → Chunk "第三条 事假需提前1个工作日申请，经部门主管审批" 
  → 相似度 0.92 ✓ 精准命中
  → 信号噪声比高：chunk 内 100% 内容都与查询相关

chunk_size=800（粗粒度）：
  同样的查询
  → Chunk "第一条...第二条...第三条 事假需提前1个工作日申请...
          第四条 年假按工龄计算...第五条 婚假..."
  → 相似度 0.58
  → 信号噪声比低：chunk 内只有 20% 内容与查询相关
  → 其余 80% 的"其他条款"向量稀释了"事假"的语义信号
```

**维度二：上下文完整性——LLM 需要"足够"但不"冗余"的上下文**

```
chunk_size=100：
  LLM 看到的："...按 1.5 倍工资计算"
  LLM 困惑：什么情况下按 1.5 倍？和谁有关？
  
chunk_size=400：
  LLM 看到的："工作日加班按 1.5 倍工资计算，休息日加班按 2 倍，
             法定节假日按 3 倍。"
  LLM 理解：这是加班费的分级计算规则 ✓
  
关键认知：chunk_size 不是越大越好。太大了会稀释语义；太小了会丢失上下文。
          "刚好容纳一个完整的语义单元"才是最佳粒度。
```

**维度三：计算成本——chunk 数量 = Embedding 调用次数 = 成本**:o::o:

```
同一份 10 万 token 的文档：

chunk_size=200 → ~625 个 chunk → 625 次 Embedding 调用
chunk_size=512 → ~244 个 chunk → 244 次 Embedding 调用（60% 成本节省）
chunk_size=1024 → ~122 个 chunk → 122 次 Embedding 调用（80% 成本节省）

对于 10 万份文档的知识库：
  差距 = (625 - 122) × 10万 ≈ 5000 万次 Embedding 调用差异
  按 OpenAI text-embedding-3-small 定价 → 数千美元的差额
```

**维度四：chunk_overlap——防止语义"落在边界上"**

```
无 overlap：
  Chunk A: "...工作日加班按 1.5 倍工资"  
  Chunk B: "计算，休息日加班按 2 倍..."
  → "1.5 倍工资计算" 被从中间劈开！两个 chunk 都没有完整语义。

有 overlap (64 token)：
  Chunk A: "...工作日加班按 1.5 倍工资计算，休"
  Chunk B: (overlap) "...加班按 1.5 倍工资计算，休息日加班按 2 倍..."
  → Chunk B 中有完整的 "1.5 倍工资计算" 语义 ✓
```

**实战选值指南：**

| 场景 | chunk_size | overlap | 理由 |
|------|:---:|:---:|------|
| 通用起步 | 512 | 64 (12.5%) | 业界使用最广泛的"甜点值" |
| FAQ/短问答 | 按 QA 对，不固定 | 0 | QA 自成一体的自然边界 |
| 法律条款 | 256-384 | 32 | 每条条款独立且简短 |
| 技术文档 | 512-768 | 96 | 代码块+说明文字，需要稍大 |
| 长篇叙述 | 768-1024 | 128 | 需要更大的上下文保持连贯 |
| 多跳推理 | 256 (检索) + 1024 (生成) | — | 双层切分策略 |

**面试官追问："overlap 设多大合适？10% 还是 20%？"**:o:

"一般设为 chunk_size 的 10-15%。太小了覆盖不够（关键信息可能仍落在边界），太大了浪费存储和计算（相邻 chunk 内容高度重复）。64/512≈12.5% 是广泛验证的经验值，但最终还是要看你的文档特征——如果你的文档中关键信息通常以短句形式出现（如法律条款），overlap 可以小一些；如果关键信息通常以长句出现（如技术规范），overlap 可以大一些。"

---

#### Q2：什么是"语义单元断裂"？为什么它是切分中最大的坑？（阿里巴巴 / 百度）

**面试官考察点：** 考察是否真正关注过"切分错误"的具体表现。能举出具体例子说明你确实排查过线上 bad case。

**回答思路（从现象到根因，再到解法）：**

**定义：** 语义单元断裂是指——一个需要完整阅读才能理解的语义段，被切分器在非语义边界处截断，导致信息分散在两个或多个 chunk 中，每个 chunk 单独看都无法完整回答问题。

**三类典型断裂场景：**

**断裂类型一：句子截断**

```
原文："工作日加班按 1.5 倍工资计算，休息日加班按 2 倍工资计算。"

切分后：
  Chunk A: "工作日加班按 1.5 倍工资"
  Chunk B: "计算，休息日加班按 2 倍工资计算。"
  
后果：用户搜"工作日加班费"，Chunk A 相似度高但信息不完整（"按 1.5 倍工资"后面缺动词）。
      LLM 看到 Chunk A 知道"1.5 倍工资"但不知道是"计算"还是"发放"还是什么。
```

**断裂类型二：表格拆散**:o:

```
原文表格：
  | 项目 | 公司缴纳 | 个人缴纳 |
  | 养老 | 16%     | 8%      |
  | 医疗 | 9.8%    | 2%      |

切分后（按 512 token 硬切）：
  Chunk A: "| 项目 | 公司缴纳 | 个人缴纳 |\n| 养老 | 16% | 8% |"
  Chunk B: "| 医疗 | 9.8% | 2% |"
  
后果：Chunk B 丢失了表头（项目/公司缴纳/个人缴纳），LLM 看到的是三个孤立的数字。
      它不知道"9.8%"和"2%"分别代表什么。
```

**断裂类型三：前后依赖关系断裂**

```
原文：
  "如遇以下情况之一，员工可申请病假：（1）因病需就医治疗；
  （2）因病需住院治疗；（3）因病需居家休养。"
  
切分后：
  Chunk A: "如遇以下情况之一，员工可申请病假："
  Chunk B: "（1）因病需就医治疗；（2）因病需住院治疗；（3）因病需居家休养。"
  
后果：Chunk A 和 Chunk B 各自检索分数都可能不高。
      因为 Chunk A 设定了语境但没有具体内容，Chunk B 有具体内容但丢失了"这是关于病假的规定"的语境。
      两者分开后，语义价值都降低了。
```

**系统工程解法（三层防护）：**

| 层级 | 手段 | 解决的问题 |
|------|------|-----------|
| **切分策略** | 递归切分（优先在段落/句子边界切） | 减少句子截断 |
| **Overlap** | 相邻 chunk 重叠 10-15%:o: | 关键信息落在边界时，overlap 区间有完整版 |
| **双层切分** | 细粒度检索 + 粗粒度生成:o: | 检索用小 chunk（精准），生成时扩展为大 chunk（完整） |

**面试官追问："有没有绝对避免断裂的方法？"**

"坦白说，没有。任何自动切分策略都会有边界——因为语言本身的语义边界不是规则化的，同一个句子既可以是独立的语义单元，也可以是更大语义单元的一部分。完全消除断裂的唯一方法是不切分——但那就等价于不用 RAG。

所以追求的不是'零断裂'，而是'断裂的负面影响可控'——用 overlap 覆盖边界风险，用双层切分保证生成时有完整上下文。这两者组合后，即使 chunk 层面有断裂，LLM 最终看到的上下文仍然是完整的。"

---

### 19.2 切分策略类

#### Q3：递归字符切分（RecursiveCharacterTextSplitter）的原理是什么？为什么它是最通用的切分策略？（所有大厂通用）

**面试官考察点：** LangChain 中最常用的切分器，考察是否理解其"逐级降级"的设计思想。

**回答思路（从问题到解法，再到参数调优）：**

**为什么需要递归切分——"一刀切"的问题：**

```
假设 chunk_size=512，一段文本如下：

  "第一条...（150字符）\n\n第二条...（200字符）\n\n第三条...（400字符）\n\n第四条...（300字符）"

固定字符切分器（简单截断）：
  → 在 512 字符处截断
  → 恰好把"第三条"从中间劈开："第三条...（前 162 字符）" / "（后 238 字符）\n\n第四条..."
  → 语义单元 = 断裂 ✗

递归切分器：
  → 首先尝试在 \n\n（段落边界）处切分
  → "第一条...\n\n" = 150 < 512 ✓ 独立成块
  → "第二条...\n\n" = 200 < 512 ✓ 独立成块
  → "第三条...\n\n" = 400 < 512 ✓ 独立成块
  → 没有 chunk 被截断 ✓
```

**递归切分的核心算法——"分隔符逐级降级"：**

```python
# LangChain RecursiveCharacterTextSplitter 的内置分隔符优先级
DEFAULT_SEPARATORS = [
    "\n\n",     # 第 1 优先级：段落边界（最强语义边界）
    "\n",       # 第 2 优先级：换行（句子边界）
    "。",       # 第 3 优先级：中文句号
    ". ",       # 第 4 优先级：英文句号
    " ",        # 第 5 优先级：空格（词边界）
    "",         # 第 6 优先级：字符边界（无路可退）
]

# 算法逻辑：
# chunk_size=512 时：
#   1. 尝试用 "\n\n" 切分 → 如果所有片段都 ≤ 512 → 完成
#   2. 某片段 > 512 → 对该片段用 "\n" 再切分
#   3. 仍 > 512 → 用 "。" 再切分
#   ...
#   最终降级到 ""（逐字符切分）——保证任何文本都能切
```

**递归切分为什么是最通用的起点——三个理由：**

1. **不需要预知文档结构。** 标题切分需要 Markdown 的 `#` 标记，函数切分需要 AST 解析。递归切分对任何纯文本都有合理的结果——它利用的是自然语言的通用特征（段落/句子/词边界）。

2. **"尽量不破坏语义"的贪心策略。** 优先在最强语义边界处切——先尝试段落、再尝试句子、再尝试词、最后是字符。每一步都试图找到"对语义伤害最小的切分点"。

3. **作为基线后，针对性优化。** 先用递归切分跑通，观察哪些文档类型的切分效果不好 → 对那类文档切换到专用切分策略（标题切分/语义切分/双层切分）。递归切分是最佳的"起步基线"。

**参数调优的三个关键：**

| 参数 | 建议 | 原因 |
|------|------|------|
| `chunk_size` | 从 512 起步 | 512 是文本 Embedding 模型最常见的训练长度，也是 LLM 生成质量的最佳区间 |
| `chunk_overlap` | chunk_size 的 10-15% | 覆盖语义边界但不过度重复 |
| `separators` | 中文加 `"。"` `"；"` 到优先级表 | 默认分隔符偏英文，中文文档需要自定义分隔符序列 |

**面试官追问："递归切分能处理 Markdown 吗？"**

"可以，但不是最优。递归切分会用 `\n\n` 和 `\n` 切分 Markdown，但它不理解 `#` 标题和 ` ``` ` 代码块的语义。例如一个代码块中被 `\n\n` 分隔的两段代码会被错误地切成两个 chunk——而代码块应该是一个整体。对 Markdown 文档，更优的做法是用 MarkdownHeaderTextSplitter 按标题层级切分，保证每个 `##` 段落 + 其下属内容为一个 chunk。递归切分只在对结构不敏感的场景中作为兜底。"

---

#### Q4：语义切分（Semantic Chunking）的原理是什么？它和传统的固定长度切分有什么区别？（腾讯 / 阿里巴巴 中高频题）

**面试官考察点：** 是否了解 2024 年的切分新趋势——用 Embedding 做自适应切分。

**回答思路（先讲传统切分的根本缺陷，再讲语义切分如何解决）：**

**传统切分的根本缺陷——"盲人切蛋糕"：**

```
传统切分（固定长度/递归切分）：
  不看内容，只看长度。"到 512 字符了，切！"

  问题：它不知道"这里是否是一个话题的结束"。
  
  一段文本：
    "第三章 休假制度\n\n3.1 年假\n\n正式员工每年享有5天带薪年假..."
    
  固定长度在 512 字符处切 → 可能恰好把 "3.1 年假" 和它的内容切到两个 chunk
  但它没有办法知道——它只是数到了 512 就切了。
```

**语义切分的核心思想——"在话题变化处切分"：**

```
语义切分的算法流程：

Step 1：将文档切分为句子
  Doc → [句子1, 句子2, 句子3, ..., 句子N]

Step 2：计算相邻句子的语义相似度
  句子1 vs 句子2 → 相似度 0.92（都在讲年假）
  句子2 vs 句子3 → 相似度 0.88（都在讲年假）
  句子3 vs 句子4 → 相似度 0.45（从年假换到了病假！← 话题转换点）
  句子4 vs 句子5 → 相似度 0.91（都在讲病假）

Step 3：在相似度"断层"处切分
  相似度阈值 = 0.70
  句子3 vs 句子4 = 0.45 < 0.70 → 在此切分！

Step 4：聚合
  句子1-3 → Chunk A（关于年假的完整讨论）
  句子4-N → Chunk B（关于病假的完整讨论）

结果：每个 chunk 都是"一个连贯话题"，不会把年假和病假混在同一 chunk 中。
```

**语义切分 vs 传统切分的核心区别：**

| 维度 | 固定长度 / 递归切分 | 语义切分 |
|------|-------------------|----------|
| **切分依据** | 字符数 / token 数 | 语义相似度"断层" |
| **chunk 大小** | 固定（接近 chunk_size） | 不固定（随话题边界变化） |
| **优点** | 简单、可预测、速度快 | chunk 内部语义高度一致 |
| **缺点** | 可能在话题中间截断 | 需要 Embedding 计算、chunk 大小不均匀 |
| **适用** | 通用场景、快速起步 | 多主题混合文档、高质量需求 |

**语义切分的代价（面试官喜欢追问这个）：**

```
成本：
  N 个句子 → 需要计算 N-1 次相邻句子相似度
  → 每次相似度 = 2 次 Embedding + 1 次余弦相似度
  → N=100 时，需要 99 × 2 = 198 次 Embedding 调用
  → 传统切分：0 次 Embedding 调用

  对于大规模文档库，语义切分的 Embedding 开销是显著的。
  因此语义切分更多用于中小规模的高质量场景。

速度：
  语义切分比递归切分慢 10-100 倍（取决于句子数量和 Embedding API 延迟）。
```

**最佳实践：** "语义切分不是替代递归切分，而是在递归切分上的增强。先用递归切分做粗切（保证 chunk 不超限），再在 chunk 内部检测语义断层做细切（保证话题一致性）。两者组合使用。"

---

#### Q5：双层切分策略（Coarse-to-Fine）是如何工作的？它解决了什么传统单层切分解决不了的问题？（字节跳动 / 华为 高频）

**面试官考察点：** 是否理解"检索粒度"和"生成粒度"应该不同这个核心洞察。

**回答思路（先说单层切分的根本矛盾，再说双层如何解耦）：**

**单层切分的根本矛盾——"一把尺子量两件事"：**

```
单层切分只有一个 chunk_size 参数，但它要同时满足两个互相矛盾的目标：

  目标 1（检索）：chunk 越小越好
    → 内容单一、主题聚焦 → 与 query 的相似度匹配更精准
    → 不会被无关内容"稀释"语义信号

  目标 2（生成）：chunk 越大越好
    → 上下文完整 → LLM 能理解信息的全貌
    → 不会看到"按 1.5 倍工资计算"而不知道前提条件

  矛盾：你无法用一个 chunk_size 同时满足"越小越好"和"越大越好"。
```

**双层切分的核心思想——解耦：**

```
Layer 1（检索层/小 Chunk）：专门服务"检索"
  chunk_size = 200-300 token，按句子/段落边界切分
  做 Embedding → 存入向量库
  目标：高 Recall + 高 Precision（检索精准度优先）
  → 不负责给 LLM 看

Layer 2（生成层/大 Chunk）：专门服务"生成"
  chunk_size = 800-1200 token，对小 Chunk 做上下文扩展
  不参与向量检索（不需要 Embedding）
  目标：上下文完整（LLM 理解优先）
  → 不负责检索召回

工作流：
  Query → 在小 Chunk 索引中检索 → 命中 Top-K 小 Chunk
  → 通过 metadata 追溯每个小 Chunk 所属的大 Chunk
  → 将对应的大 Chunk（而非小 Chunk）注入 LLM
  → LLM 看到的是完整上下文 ✓
```

**LlamaIndex 双层切分的三种内置实现：**这是双层的实现，

| 实现 | 小 Chunk | 大 Chunk | 映射方式 |
|------|----------|----------|----------|
| **SentenceWindowNodeParser** | 1-2 个句子（~50-100 token） | 小 Chunk + 前后各 5 句 | metadata 中的 PREVIOUS/NEXT 关系 |
| **MetadataReplacementPostProcessor** | 小粒度 chunk（~256 token） | 其父级大 chunk（~1024 token） | 检索后用父 Node 替换子 Node |
| **AutoMergingRetriever** | 子 chunk（~256 token） | 父 chunk（~1024 token） | 子 chunk 命中比例 > 阈值 → 合并为父 chunk |

**面试官追问："双层切分增加了多少存储成本？"**

:o::o::o::o::o::o::o::o::o::o::o::o::o::o::o::o:

"大 Chunk 不需要额外的 Embedding 向量存储:o:——它们不参与检索，只需要存储文本本身。所以增加的存储成本主要是文本存储（对象存储 或 向量数据库中一行字段），相比每次 Embedding 的成本几乎可以忽略。以 SentenceWindowNodeParser 为例，小 Chunk 是 100 token，生成上下文是 500 token，增加的文本存储是 5 倍——但文本存储成本远低于向量存储成本（向量是 1536 维 float32 = 6KB/chunk，文本 500 token ≈ 2KB/chunk）。所以双层切分的额外成本主要是文本存储层面，通常只增加 20-30% 的总存储，但能带来显著的生成质量提升。"

---

### 19.3 不同文档类型的切分策略类

#### Q6：代码、聊天记录、法律条款这三种文档的切分策略分别应该怎么设计？（字节跳动 / 腾讯 综合应用题）

**面试官考察点：** 能否根据文档的语义结构特征，选择"最匹配"的切分方式。而不是所有文档都 `chunk_size=512`。

**回答思路（对三种文档类型分别分析）：**

**类型一：代码——按函数/类边界切分**

```
代码的核心特征：语义单元 = 函数/类，而非字符数。

为什么不能按字符数切：
  一段代码中"def calculate_overtime(hours, rate):"的高度语义
  集中在前几行（函数签名+核心逻辑），后续可能是异常处理/日志等辅助代码。
  
  按 512 字符硬切 → 函数签名在 Chunk A、核心逻辑在 Chunk B
  → 用户搜"加班费计算" → 两个 chunk 各自分数都不高

正确做法：按函数/类边界切分
  · Python: 用 AST 解析（ast.parse），识别 FunctionDef/ClassDef
  · JavaScript/TS: 用 tree-sitter 解析
  · 通用方案: 用正则匹配缩进为 0 的 `def / class / function / const` 行作为切分边界
  → 每个函数 = 1 个 chunk，函数签名为 chunk 的 metadata["title"]

特殊情况：
  · 超大函数（> 2000 token）→ 降级为按内部逻辑块切分（循环/条件判断边界）
  · 微小函数（< 20 token）→ 合并相邻的小函数
```

**类型二：聊天记录——按轮次切分**

```
聊天记录的核心特征：Q 和 A 必须在同一个 chunk 内。

为什么不能按字符数切：
  客服对话：
    [Q1] 用户："年假怎么申请？" 
    [A1] 客服："正式员工每年享有5天带薪年假，请提前..."
    [Q2] 用户："需要什么材料？"
    [A2] 客服："需要填写请假申请表，并经部门主管..."

  按 512 字符切 → Q1+A1 的前半在 Chunk A，A1 的后半+Q2+A2 在 Chunk B
  → "年假怎么申请" 这个问题的**完整答案**被切到两个 Chunk 里
  → 更糟的是，Chunk B 里 Q2 和 A1 残片混淆在一起

正确做法：按轮次切分
  · 每条"用户+客服"的完整交互 = 1 个 chunk（Question + Answer 绑在一起）
  · Chunk metadata 中标记 turn_id、timestamp、user_id
  · 检索时 Q 和 A 一起返回，LLM 看到完整的问答上下文

高级技巧：
  · 多轮上下文的摘要链：每 3-5 轮生成一个摘要，存入下一个 chunk 的 metadata
    作为该轮对话的"上文摘要"
```

**类型三：法律条款——按条款编号切分**

```
法律文档的核心特征：每条"第X条/第X款"是独立的、自洽的语义单元。

为什么不能按字符数切：
  "第一条 公司实行标准工时制...第二条 上班时间为9:00至18:00...
   第三条 事假需提前1个工作日申请..."

  按 512 字符切 → 可能切在"第三条"中间
  → 一条法律条款被劈成两半，法律效力无法从半个条款中判断

正确做法：按条款编号切分
  · 用正则识别条款边界：r'第[一二三四五六七八九十百零\d]+条'
  · 每条条款 = 1 个 chunk
  · Chunk metadata：条款编号、章节标题、生效日期、适用范围
  · 条款间不需要 overlap（每条条款是自洽的，不需要上下文也能理解）

特殊情况：
  · 引用型条款（"参照第三条第2款执行"）→ 在 metadata 中标记引用的条款编号
    辅助跨 chunk 的多跳检索
```

**三者的对比总结：**

| 文档类型 | 语义单元 | 切分边界标记 | 是否需要 overlap | chunk 大小特征 |
|----------|----------|-------------|:---:|------|
| 代码 | 函数/类 | `def`/`class`/`function` | 否 | 不均匀（短函数 50t，长类 2000t） |
| 聊天记录 | QA 轮次 | 用户/客服角色切换 | 否 | 中等均匀（通常 200-800 token/轮） |
| 法律条款 | 条款项 | `第X条`/`第X款` | 否 | 较均匀（通常 100-300 token/条） |

**关键论点：** "这三类文档其实说明了一个共同原则——**找到文档的自然语义单元作为切分边界，而不是用字符数硬切**。代码的自然单元是函数，聊天的自然单元是 QA 对，法律的自然是条款。当你尊重文档自身的信息结构时，很多切分问题不需要复杂的参数调优——边界本来就在那里。"

---

### 19.4 切分评估与调优类

#### Q7：你怎么评估一个切分策略的好坏？有什么指标和方法？（美团 / 阿里巴巴 工程方法论题）

**面试官考察点：** 评估是工程能力的标志。能提出量化指标说明你不只是"感觉这个切分挺好的"。

**回答框架（四层评估体系）：**

**第一层：结构完整性评估（不依赖检索，只看切分结果本身）**:o:

| 指标 | 计算方式 | 理想值 | 说明 |
|------|----------|:---:|------|
| **语义单元断裂率** | 被截断的句子数 / 总句子数 | < 5% | 抽样检查 chunk 边界是否在完整句子处 |
| **表格完整率** | 完整表格数 / 总表格数 | 100% | 检查是否有表格被切成两半 |
| **标题-内容分离率** | 标题与内容在不同 chunk 的标题数 / 总标题数 | < 3% | 检查 "## 标题" 和它下面的正文是否在同一 chunk |

```
实战做法：抽样 50 个 chunk，人工检查每个 chunk 的开头和结尾：
  - 开头是否是一个完整的句子（不是从半句话开始）？
  - 结尾是否是一个完整的句子（不是在半句话处截断）？
  - 如果有表格，表格是否完整（没有被截断）？
  
统计断裂率 = 有问题的 chunk 数 / 50
```

**第二层：长度分布评估（统计学方法）**:o:

```python
# 绘制 chunk 长度的直方图
import matplotlib.pyplot as plt

chunk_lengths = [len(chunk.split()) for chunk in chunks]

# 好的分布特征：
# 1. 均值接近 chunk_size 的 60-80%（因为按语义边界切往往比目标值稍小）
# 2. 标准差小（chunk 大小均匀，避免"超大 chunk"和"超小 chunk"混在一起）
# 3. 无严重长尾（> 1.5× chunk_size 的 chunk 占比 < 5%）
# 4. 极少碎片（< 50 token 的 chunk 占比 < 10%）

# 如果分布图中出现"双峰"或严重长尾 →
#   说明切分器在某些位置使用了降级策略（如降到了逐字符切分）
#   → 需要调整参数或换切分策略
```

**第三层：检索效果评估（最核心）**:o:

```
准备评估集：
  10-20 条标准查询 + 人工标注每条查询"正确答案在哪些段落中"
  （不需要很多，但要覆盖不同类型的查询：事实型/对比型/总结型）

指标：
  Recall@K = 前 K 个检索结果中包含正确段落的比例
    如 Recall@5 = 0.85 → 85% 的查询在前 5 个结果中找到了正确答案

  MRR = Mean Reciprocal Rank = 第一个正确答案排名的倒数的均值
    如 第 1 名命中 → 1/1 = 1.0；第 3 名 → 1/3 = 0.33
    MRR 越高 → 正确答案排名越靠前

A/B 测试方法：
  用不同 chunk_size（200/400/512/600/800）各跑一轮
  比较各轮次的 Recall@5 和 MRR
  → 选 Recall@5 最高的 chunk_size 作为生产配置
```

**第四层：LLM 生成质量评估（最终指标）**

```
用 RAGAS 框架做端到端评估：

  Faithfulness（忠实度）：
    回答中有多少陈述可以在检索到的 chunk 中找到原文支持
    理想值 > 0.90
    
    低 → 可能原因：检索遗漏了关键信息（chunk 太小 / 检索不准）

  Context Precision（上下文精度）：
    检索到的 chunk 中有多少比例是真正相关的
    理想值 > 0.80

    低 → 可能原因：chunk 太大混入了无关内容（噪声）

  Context Recall（上下文召回）：
    参考答案所需信息在检索结果中的覆盖程度

    低 → 可能原因：chunk 太小信息不完整，或检索遗漏
```

**调优实战流程：**

```
Step 1：从 baseline 开始
  chunk_size=512, overlap=64, 递归切分

Step 2：跑评估
  抽样 20 条 query → 计算 Recall@5 + RAGAS Faithfulness

Step 3：看 bad case
  如果 Faithfulness 低 → 检查是否 chunk 太小导致信息不完整
    → 增大 chunk_size（512→768）或启用双层切分

  如果 Context Precision 低 → 检查是否 chunk 太大混入噪声
    → 减小 chunk_size（512→384）或加 Rerank

Step 4：迭代
  调整参数 → 重跑评估 → 对比指标 → 选择最优配置

整个流程可能在半天内完成（取决于 Embedding API 调用速度）。
```

---

#### Q8：端到端讲一下——从一份新的文档集到确定最优切分策略，你会怎么做？（华为 / 腾讯 综合性开放题）

**面试官考察点：** 这是考察完整工作流的综合性开放题。能从"无策略"到"最优策略"讲清楚每一个决策节点的考量。

**回答框架（分四步走）：**

**Step 1——了解文档（半天）**

```
抽样 20-30 份文档，回答三个问题：
  ① 文档是什么类型？
     Markdown？PDF？代码？聊天记录？混合？
     → 决定用哪个"切分策略族"
  
  ② 文档的"自然语义单元"是什么？
     法律条文 → 每一条款
     技术博客 → 每个 ## 章节
     代码仓库 → 每个函数/类
     → 决定切分边界应该对齐什么
  
  ③ 文档有多长？分布如何？
     平均长度？中位数长度？最长/最短？
     → 决定是否需要多级切分（大文档先按章节切，再按段落切）
```

**Step 2——确定基线（1 天）**

```
基线策略：
  · 按文档的自然语义单元切分（不硬设 chunk_size）
  · 如果文档没有明显结构 → 使用递归切分 chunk_size=512, overlap=64
  · 如果文档有明显结构（Markdown 标题/法律条款/QA对）→ 按结构切分

跑一遍基线，统计：
  · 总 chunk 数
  · chunk 长度分布（检查是否有过长/过短的异常）
  · Semantic Unit Breakage Rate（抽样 50 chunk 人工检查）
```

**Step 3——A/B 调优（1-2 天）**

```
准备评估 query 集（10-20 条）：
  · 从线上日志中抽样真实用户 query（最理想）
  · 或自己编写覆盖不同意图的 query（事实型/对比型/总结型）
  · 为每条 query 标注正确答案在哪些段落中

调优参数：
  round 1：只调 chunk_size（256 / 384 / 512 / 768 / 1024）
  → 对比 Recall@5 + MRR → 确定最优 chunk_size

  round 2：固定最优 chunk_size，调 overlap（32 / 64 / 96 / 128）
  → 对比 Semantic Unit Breakage Rate → 确定最优 overlap

  round 3：对 Recall 低的 query 类型 → 考虑是否启用双层切分
  例如：总结型 query 在 chunk_size=512 时 Faithfulness 低
       → 对这些 query 路径启用双层切分（检索用 256，生成用 1024）
```

**Step 4——上线与监控**

```
上线配置：
  · 90% 的查询走 baseline 配置（在 Step 3 中确定的最优 chunk_size）
  · 10% 的特定查询类型走增强配置（双层切分/专用切分策略）

上线后监控：
  · 在线指标：用户点赞率、点踩率
  · 切分异常告警：过短 chunk 占比突增（> 20%）→ 可能文档格式变化
  · 每周回顾 10 个 bad case → 调优切分策略
```

**关键话术（加分）：** "最优切分策略不是一次性找出来的，而是在线上运行中持续优化的。基线 → A/B 调优 → 上线 → Bad Case 驱动迭代，这是一个闭环。很多团队花大量时间做离线调优，结果上线后发现真实用户的 query 和离线评估 query 分布完全不一样——所以我会在拿到最优配置后尽快上线，用真实用户反馈做最终校准。"

---

### 19.5 面试高频知识点速查

#### 一句话答案系列

| 问题 | 一句话答案 |
|------|-----------|
| chunk_size 的最优值？ | 没有绝对最优，但 512 token 是业界的"甜点值"——在检索精度和上下文完整性之间平衡最好 |
| chunk_overlap 的作用？ | 防止关键信息被截断在 chunk 边界上——让相邻 chunk 有内容重叠 |
| 语义单元断裂是什么？ | 一个完整的语义段被切分器在中部截断，分散在两个 chunk 中，各自单独看都不完整 |
| 递归切分的核心思想？ | 从强语义边界到弱语义边界逐级降级（段落→句子→词→字符），尽量在语义自然处切分 |
| 语义切分的核心思想？ | 计算相邻句子的 Embedding 相似度，在相似度"断层"处切分——每个 chunk 内部话题一致 |
| 双层切分的核心思想？ | 解耦检索粒度和生成粒度——小 chunk 做检索（精准），大 chunk 做生成（完整） |
| 代码为什么不能按字符数切？ | 代码的语义单元是函数/类，按字符截断会切断函数体，导致签名和逻辑体分离 |
| 聊天记录应该怎么切？ | 按 QA 轮次切分，保证问题和答案在同一个 chunk 内 |
| 法律条款应该怎么切？ | 按条款编号（第X条/第X款）切分，每条条款是自洽的语义单元 |
| 如何评估切分质量？ | 四层：结构完整性（断裂率）→ 长度分布（直方图）→ 检索效果（Recall@K/MRR）→ 生成质量（RAGAS） |

#### 必知切分策略速查表

| 策略 | 原理 | 优点 | 缺点 | 适用场景 |
|------|------|------|------|----------|
| **固定字符切分** | 按 N 字符硬截断 | 最简单、最快 | 经常截断句子和表格 | 仅快速原型 |
| **递归切分** | 分隔符逐级降级切分 | 通用性强、不需要预知结构 | 不理解内容语义 | 通用起步基线 |
| **语义切分** | Embedding 相似度断层检测 | chunk 内部话题高度一致 | 慢、成本高、chunk 大小不均 | 高质量需求 |
| **按标题切分** | 按 `#`/`##` 层级切分 | 语义单元天然形成 | 仅适用 Markdown/HTML | 结构化文档 |
| **按句子切分** | 在句号/问号处切 | 保证每个 chunk 是完整句子 | 大小可能很不均匀 | 叙述性文档 |
| **双层切分** | 细粒度检索+粗粒度生成 | 同时兼顾精准和完整 | 存储增加 20-30% | 生产环境首选 |
| **按函数切分** | 按 `def`/`class` 边界切 | 保护函数完整性 | 仅适用代码 | 代码仓库 |
| **按轮次切分** | 按 QA 对切分 | 保护对话上下文 | 仅适用对话 | 聊天记录/客服 |

---



