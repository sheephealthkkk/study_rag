## 十八、RAG 文档切分概述 —— 从策略到评估

文档切分（Chunking）是 RAG 管线中连接"文档解析"和"向量检索"的关键桥梁。切分的好坏直接决定了检索系统能找到什么、LLM 能读到什么。这一章我们从原理层面系统讲解切分的核心概念、影响因素和评估方法。

---

### 18.1 切分的重要性：四个维度

```
                    切分质量的四个影响维度

                          ┌──────────────┐
                          │   切分策略    │
                          │ chunk_size    │
                          │ chunk_overlap │
                          └──────┬───────┘
                                 │
           ┌─────────────────────┼─────────────────────┐
           ▼                     ▼                     ▼
    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
    │  检索准确性   │    │  上下文完整性  │    │  计算效率     │
    │  Retrieval   │    │   Context     │    │  Efficiency  │
    │  Accuracy    │    │  Completeness │    │              │
    └──────┬───────┘    └──────┬───────┘    └──────┬───────┘
           │                   │                   │
           └───────────────────┼───────────────────┘
                               ▼
                    ┌──────────────────┐
                    │  召回率与精确度   │
                    │  Recall &        │
                    │  Precision       │
                    └──────────────────┘
```

#### 维度一：检索准确性

切分粒度直接影响检索的"命中质量"。假设用户搜索"事假申请流程"：

```
文档原文：
  第一条 公司实行标准工时制...
  第二条 上班时间为9:00至18:00...
  第三条 事假需提前1个工作日申请，经部门主管审批后交HR备案...
  第四条 年假天数按工龄计算...

情况 A：chunk_size=100（细粒度切分）
  Chunk 0: "第一条 公司实行标准工时制...第二条 上班时间为9:00..."
  Chunk 1: "第三条 事假需提前1个工作日申请，经部门主管审批后交HR备案"
  Chunk 2: "第四条 年假天数按工龄计算..."

  检索 "事假申请流程" → Chunk 1 相似度 0.92 ✓ 精准命中！
  但 Chunk 1 只包含 1 条规定的完整信息

情况 B：chunk_size=500（粗粒度切分）
  Chunk 0: "第一条...第二条...第三条：事假需提前1个工作日申请，
            经部门主管审批后交HR备案...第四条：年假天数..."
  
  检索 "事假申请流程" → Chunk 0 相似度 0.65
  相似度被"第一条""第二条""第四条"这些无关内容稀释了
  但 Chunk 0 包含了更完整的上下文（前后的规定也在一起）
```

**结论：** 细粒度检索更精准（匹配信号强），但可能丢失上下文；粗粒度检索噪声大（匹配信号被稀释），但上下文更完整。

---

#### 维度二：上下文完整性

LLM 理解一段信息需要足够的上下文。考虑以下场景：

```
Chunk 仅包含：
  "按 1.5 倍工资计算"

没有上下文时，LLM 不知道：
  - 什么情况下按 1.5 倍？ ← 前文 "工作日加班"
  - 和谁有关？           ← 前文 "加班费计算"
  - 有没有例外？         ← 后文 "法定节假日按3倍"

完整的上下文应该是：
  "9.2 加班费计算：工作日加班按1.5倍工资计算，
   休息日加班按2倍工资计算，法定节假日加班按3倍工资计算。"
```

**结论：** chunk_size 必须足够大以容纳完整的语义单元；chunk_overlap 可以在相邻 chunk 之间建立"上下文纽带"。

---

#### 维度三：计算效率

| 切分粒度 | Chunk 数量 | Embedding 调用次数 | 索引大小 | 检索耗时 |
|----------|:---:|:---:|:---:|:---:|
| 粗粒度（1000 token/chunk） | 少 | 少 | 小 | 快 |
| 细粒度（200 token/chunk） | 多（5倍） | 多（5倍） | 大（5倍） | 慢（5倍） |
| 极细粒度（50 token/chunk） | 更多（20倍） | 更多（20倍） | 更大（20倍） | 更慢（20倍） |

**结论：** 切分过细会导致 chunk 数量爆炸，Embedding API 成本、存储成本和检索延迟都线性增长。100 万份文档如果选错 chunk_size，可能多花数千美元的 API 费用。

---

#### 维度四：召回率与精确度

这是衡量检索系统质量的**两个核心指标**，两者来自信息检索学科：

```
═══════════════════════════════════════════════════════════════════════
                    召回率 (Recall) 与 精确率 (Precision)
═══════════════════════════════════════════════════════════════════════

                        实际相关       实际不相关
                      ┌──────────┐  ┌──────────┐
    检索到的          │   TP     │  │   FP     │  → Precision = TP / (TP + FP)
   (系统返回的)       │  True    │  │  False   │     "返回的结果中有多少是真正相关的？"
                      │ Positive │  │ Positive │
                      ├──────────┤  ├──────────┤
    未检索到的        │   FN     │  │   TN     │
   (系统未返回的)     │  False   │  │  True    │
                      │ Negative │  │ Negative │
                      └──────────┘  └──────────┘
                            │
                            ▼
               Recall = TP / (TP + FN)
               "所有真正相关的文档中，有多少被检索到了？"

═══════════════════════════════════════════════════════════════════════

  通俗理解：

    召回率 (Recall)：
      知识库中有 10 个段落提到了"事假申请规定"。
      检索系统找回了其中 7 个。
      → Recall = 7/10 = 70%

      高召回率 = "该找到的都找到了"（查全）
      低召回率 = "漏掉了重要信息"（漏检）

    精确率 (Precision)：
      检索系统返回了 5 个段落。
      其中 4 个确实和"事假申请规定"相关，1 个是讲"年假"的（不相关）。
      → Precision = 4/5 = 80%

      高精确率 = "返回的都是相关的"（查准）
      低精确率 = "混入了无关内容"（噪声）

    理想状态：Recall=100%, Precision=100%
    现实情况：两者互为矛盾——提高一个通常会降低另一个
```

---

### 18.2 切分粒度对 Recall 和 Precision 的影响

切分粒度会同时影响召回率和精确率，但方向相反：

```
═══════════════════════════════════════════════════════════════════════
              切分粒度 ←→ 召回率/精确率 的权衡关系
═══════════════════════════════════════════════════════════════════════

  细粒度切分 (小 chunk)：

    一段原文 "事假需提前1个工作日申请，病假应在当日8:30前通知主管"

    切分结果：
      Chunk A: "事假需提前1个工作日申请"      ← 30 token
      Chunk B: "病假应在当日8:30前通知主管"    ← 25 token

    检索 "请假规定"：
      → 返回 Chunk A (相似度 0.88) 和 Chunk B (相似度 0.85)
      → Recall ↑ (两个相关 chunk 都被找回了)
      → Precision ↑ (每个 chunk 只含单一主题，不会混入无关内容)
      → 上下文 ↓ (Chunk A 不知道 Chunk B 的存在，"事假"和"病假"的并列关系丢失)

  粗粒度切分 (大 chunk)：

    同上原文，切分结果：
      Chunk C: "事假需提前1个工作日申请，病假应在当日8:30前通知主管。
               年假按工龄计算：1-5年5天，5-10年10天，10年以上15天。

    检索 "请假规定"：
      → 返回 Chunk C (相似度 0.62)
      → Recall ↓ (只返回了1个 chunk，如果有其他相关的没在这个 chunk 里就漏了)
      → Precision ↓ (年假的内容混在同一个 chunk 里，对"请假规定"来说不是最相关的)
      → 上下文 ↑ (chunk 里同时有 事假+病假+年假 的信息)

═══════════════════════════════════════════════════════════════════════
                        权衡表
═══════════════════════════════════════════════════════════════════════

  指标         │  细粒度 (100-300t)  │  粗粒度 (800-2000t)
  ─────────────┼─────────────────────┼─────────────────────
  Recall       │  ↑ 高（更容易匹配）   │  ↓ 低（整体稀释）
  Precision    │  ↑ 高（单一主题）     │  ↓ 低（多主题混合）
  上下文完整性  │  ↓ 低（割裂）        │  ↑ 高（完整）
  计算成本     │  ↑ 高（chunk 多）     │  ↓ 低（chunk 少）
  LLM 理解     │  ↓ 差（碎片化）       │  ↑ 好（信息完整）
  检索速度     │  ↓ 慢（候选集大）     │  ↑ 快（候选集小）
  ─────────────┴─────────────────────┴─────────────────────

  没有"最佳粒度"，只有"最适合你场景的粒度"。
```

---

### 18.3 粗粒度+细粒度的双层切分策略

实际生产环境不会死板地只用一个 chunk_size。业界的最佳实践是**双层切分**：先用粗粒度切出"候选段落"，再用细粒度切出"检索单元"：

```
═══════════════════════════════════════════════════════════════════════
                    双层切分策略（Coarse-to-Fine）
═══════════════════════════════════════════════════════════════════════

  Step 1: 粗粒度切分（如 chunk_size=1024, overlap=128）
  ┌─────────────────────────────────────────────────────────────────┐
  │                                                                 │
  │  原文：[A段][B段][C段][D段][E段][F段][G段][H段][I段][J段]       │
  │                                                                 │
  │  粗切结果：                                                      │
  │  ┌──────────────────────┐ ┌──────────────────────┐              │
  │  │ Big Chunk 0 (1024t)  │ │ Big Chunk 1 (1024t)  │  ...         │
  │  │ A段+B段+C段+D段      │ │ D段(overlap)+E+F+G   │              │
  │  └──────────┬───────────┘ └──────────────────────┘              │
  │             │                                                    │
  └─────────────┼────────────────────────────────────────────────────┘
                │
                ▼
  Step 2: 细粒度切分（如 chunk_size=256, overlap=32）
  ┌─────────────────────────────────────────────────────────────────┐
  │                                                                 │
  │  对每个 Big Chunk 内部再做细粒度切分：                            │
  │                                                                 │
  │  Big Chunk 0 内部：                                              │
  │  ┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐                      │
  │  │Small ││Small ││Small ││Small ││Small │    ← 检索用             │
  │  │Chunk ││Chunk ││Chunk ││Chunk ││Chunk │                      │
  │  │  0   ││  1   ││  2   ││  3   ││  4   │                      │
  │  └──┬───┘└──┬───┘└──┬───┘└──┬───┘└──┬───┘                      │
  │     │       │       │       │       │                            │
  │     └───────┴───────┴───┬───┴───────┘                            │
  │                         │                                        │
  │          每个 Small Chunk 都记录它所属的 Big Chunk               │
  │                         │                                        │
  └─────────────────────────┼────────────────────────────────────────┘
                            │
                            ▼
  Step 3: 检索时的工作流
  ┌─────────────────────────────────────────────────────────────────┐
  │                                                                 │
  │  Query → 用 Small Chunks 做检索（高 Recall + 高 Precision）      │
  │           │                                                      │
  │           ▼                                                      │
  │  找到最相关的 Small Chunk (如 Small Chunk 2, score=0.92)         │
  │           │                                                      │
  │           ▼                                                      │
  │  追溯 Small Chunk 2 所属的 Big Chunk 0                           │
  │           │                                                      │
  │           ▼                                                      │
  │  将整个 Big Chunk 0 作为上下文喂给 LLM                           │
  │  (LLM 看到的是完整上下文，而非碎片化的 Small Chunk)               │
  │                                                                 │
  └─────────────────────────────────────────────────────────────────┘

  这个双层策略同时解决了：
    ✓ 检索精准度 → Small Chunk 做匹配，单一主题，信号强
    ✓ 上下文完整性 → Big Chunk 做上下文，信息完整，LLM 理解好
    ✓ 计算效率   → 只对 Small Chunks 做向量检索（维度较小，速度快）
```

**LlamaIndex 内置了这种策略的两种实现：**

| 实现 | 机制 |
|------|------|
| `SentenceWindowNodeParser` | 检索时用小 chunk，返回时自动扩展前后 N 个相邻句子 |
| `MetadataReplacementPostProcessor` | 用小 chunk 检索，返回时替换为其父级大 chunk |
| `AutoMergingRetriever` | 当一个小 chunk 匹中后，检查其相邻小 chunk 是否也被匹中，若多个小 chunk 来自同一个大 chunk 则合并返回 |

---

### 18.4 文档切分的基本流程

一个完整的切分流线包含五个阶段：

```
═══════════════════════════════════════════════════════════════════════
                    文档切分五阶段流水线
═══════════════════════════════════════════════════════════════════════

  ┌─────────────────────────────────────────────────────────────┐
  │  阶段 1: 文档加载 (Document Loading)                        │
  │                                                            │
  │  做什么：将原始文件读入内存，转为统一的 Document 对象         │
  │  用什么：Unstructured / PyMuPDF / SimpleDirectoryReader     │
  │  输出：  Document {page_content: str, metadata: dict}       │
  │                                                            │
  │  关键考虑：                                                 │
  │    · 保留 metadata（文件名、页码、章节标题）                  │
  │    · metadata 在切分后需要随着 chunk 继承下去                │
  └────────────────────────┬────────────────────────────────────┘
                           │
                           ▼
  ┌─────────────────────────────────────────────────────────────┐
  │  阶段 2: 预处理 (Preprocessing)                             │
  │                                                            │
  │  做什么：清洗文本，为切分准备干净的输入                       │
  │  用什么：正则表达式 / pandas / BeautifulSoup / cv2          │
  │  具体操作（详见第十七章）：                                   │
  │    · 去除 HTML 标签、特殊符号、控制字符                       │
  │    · 合并多余空格/换行/制表符                                │
  │    · 统一全角半角、修正 OCR 错误                             │
  │    · 图像文档做二值化/降噪/倾斜矫正                          │
  │                                                            │
  │  为什么要在切分前做预处理：                                   │
  │    · 脏字符会影响 tokenizer 的计数——明明200个token的内容     │
  │      因为混入噪声被切成了300个token                           │
  │    · 多余换行会被当成"句子边界"，造成错误的切分点             │
  └────────────────────────┬────────────────────────────────────┘
                           │
                           ▼
  ┌─────────────────────────────────────────────────────────────┐
  │  阶段 3: 切分策略选择 (Strategy Selection)                   │
  │                                                            │
  │  根据文档特征和业务需求，选择最合适的切分策略：                │
  │                                                            │
  │  ┌──────────────────────────────────────────────────────┐  │
  │  │  策略              │ 原理              │ 适用场景       │  │
  │  ├────────────────────┼───────────────────┼───────────────┤  │
  │  │ 固定字符数切分       │ 按 N 个字符截断     │ 纯文本/无结构  │  │
  │  │ 固定 token 数切分    │ 按 N 个 token 截断  │ 需要精确控制   │  │
  │  │ 按句子切分          │ 在句号/问号/感叹号处 │ 叙述性文档     │  │
  │  │ 按段落切分          │ 在 \\n\\n 处        │ 结构清晰的文档  │  │
  │  │ 按标题层级切分       │ 在 # / ## / ### 处  │ Markdown/HTML  │  │
  │  │ 语义切分            │ Embedding 相似度阈值 │ 高质量需求     │  │
  │  │ 递归切分            │ 逐级降级分隔符       │ 通用/兜底      │  │
  │  │ 双层切分            │ 粗+细两步           │ 生产环境首选   │  │
  │  └──────────────────────────────────────────────────────┘  │
  │                                                            │
  │  选择依据：                                                 │
  │    · 文档类型（代码/散文/法律条文/聊天记录→切分方式不同）     │
  │    · 平均查询长度（短查询→细粒度，长查询→粗粒度）             │
  │    · Embedding 模型的最大输入长度                            │
  │    · LLM 的上下文窗口和生成质量                              │
  └────────────────────────┬────────────────────────────────────┘
                           │
                           ▼
  ┌─────────────────────────────────────────────────────────────┐
  │  阶段 4: 执行切分 (Execution)                                │
  │                                                            │
  │  做什么：按选定策略将 Document 拆分为 Node/Chunk 列表         │
  │                                                            │
  │  关键操作：                                                 │
  │    · 在语义边界处切分（不在词中间切断）                        │
  │    · 应用 chunk_overlap 实现重叠                              │
  │    · 保留 metadata（每个 chunk 继承原 Document 的元数据）     │
  │    · 记录 chunk 之间的关系（前一个/后一个/父级）              │
  │                                                            │
  │  具体实现：                                                 │
  │    · LangChain: RecursiveCharacterTextSplitter,              │
  │                 TokenTextSplitter, SemanticChunker           │
  │    · LlamaIndex: SentenceSplitter, TokenTextSplitter,       │
  │                  SemanticSplitterNodeParser,                │
  │                  SentenceWindowNodeParser                   │
  │    · 手写: split_text() 递归降级切分（第12/13章）            │
  └────────────────────────┬────────────────────────────────────┘
                           │
                           ▼
  ┌─────────────────────────────────────────────────────────────┐
  │  阶段 5: 后处理 (Post-processing)                            │
  │                                                            │
  │  做什么：对切分结果做最后的校验和优化                          │
  │                                                            │
  │  常见操作：                                                 │
  │    · 过短 chunk 合并（如只有 5 个 token 的片段→合并到前一个）│
  │    · 过长 chunk 再切（超 Embedding 模型输入限制的再切）      │
  │    · 去重（相邻 chunk 内容高度重复的去除）                    │
  │    · metadata 补充（补充 chunk_index、parent_id 等）         │
  │    · token 计数校验（可选，确保不超限）                       │
  │    · 质量过滤（全是数字/符号的无意义 chunk 丢弃）             │
  └─────────────────────────────────────────────────────────────┘
```

### 18.5 各阶段详解

#### 阶段 1：文档加载 —— 元数据保留是关键

```python
# 加载时必须保留 metadata，切分后需要继承
doc = load_document("员工手册.pdf")
# metadata = {"source": "员工手册.pdf", "page": 3, "chapter": "请假制度"}
```

#### 阶段 2：预处理 —— 清洗决定切分质量

切分器依赖换行符（`\n`）、句号（`。`）等分隔符判断切分点。如果文本中混入了 HTML 的 `<br>` 标签、多个连续空格、OCR 引入的 `\x00` 控制字符，切分器会在错误的位置切分。

这就是第十七章放在第十八章前面的原因——清洗是切分的前置条件。

#### 阶段 3：切分策略选择 —— 根据文档类型决定

```
不同文档类型的推荐切分策略：

  Markdown / 结构化文档：
    → 按标题层级切分（# ## ### 作为切分锚点）
    → 每个 ## 段落 + 其下属文本 = 1 个 chunk
    优点：语义单元自然形成，不需要调整 chunk_size

  PDF / 非结构化文档：
    → 递归字符切分 (chunk_size=512, overlap=64)
    → 先按段落切，再按句子切，最后按字符切
    优点：通用性强，不需要预知文档结构

  代码文件：
    → 按函数/类边界切分
    → 每个 function / class = 1 个 chunk
    优点：不切断函数体，retrieval 时语义完整

  聊天记录 / 对话：
    → 按轮次切分（每轮 Q+A = 1 个 chunk）
    优点：Q 和 A 在同一 chunk 内，检索时意图和答案在一起

  法律 / 合同文档：
    → 按条款编号切分（第一条/第二条/第三条...）
    优点：每一条款是独立的语义单元
```

#### 阶段 4：执行切分 —— overlay 的核心作用

```
chunk_overlap 保证了关键信息不落在边界上：

  Chunk 1: "员工请假应提前申请。事假需提前1个工作日申"
                                                 ↑ 被截断
  Chunk 2: (overlap) "事假需提前1个工作日申请，病假应在8:30前通知"

  overlap 覆盖了被截断的那部分，使"事假"信息在 Chunk 2 中完整出现。
```

#### 阶段 5：后处理 —— 最后的质量兜底

```python
def postprocess_chunks(chunks: List[Document], 
                       min_length: int = 20,
                       max_tokens: int = 8192) -> List[Document]:
    """切分后处理"""
    result = []
    for i, chunk in enumerate(chunks):
        # 1. 过短 → 合并到前一个
        if len(chunk.page_content) < min_length and result:
            result[-1].page_content += "\n" + chunk.page_content
            continue
        # 2. 纯符号/数字（无意义）→ 丢弃
        text = chunk.page_content.strip()
        if re.match(r'^[\d\s\W]+$', text):
            continue
        # 3. 补充元数据
        chunk.metadata["chunk_index"] = str(i)
        chunk.metadata["chunk_count"] = str(len(chunks))
        result.append(chunk)
    return result
```

---

### 18.6 切分效果评估

怎么知道你的切分策略好不好？以下是一个多维评估框架：

```
═══════════════════════════════════════════════════════════════════════
                    切分质量评估框架
═══════════════════════════════════════════════════════════════════════

  ┌─────────────────────────────────────────────────────────────┐
  │  1. 结构完整性评估                                           │
  │                                                            │
  │  指标：                                                     │
  │    · 语义单元断裂率 = 被截断的句/段落 / 总句/段落数          │
  │      理想值：< 5%                                           │
  │    · 表格完整率 = 没有被切散的表格 / 总表格数                │
  │      理想值：100%                                           │
  │                                                            │
  │  检查方法：                                                 │
  │    · 抽样检查 N 个 chunk，看是否有在不完整句子处切断的       │
  │    · 检查是否有 "前一个chunk的末尾 + 后一个chunk的开头"       │
  │      拼起来才是一句完整的话                                  │
  └─────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────┐
  │  2. 长度分布评估                                             │
  │                                                            │
  │  指标：                                                     │
  │    · 平均 chunk 长度（应该接近 chunk_size 的 60-80%）        │
  │    · chunk 长度的标准差（越小越均匀）                         │
  │    · 过长 chunk 占比（>1.5×chunk_size 的比例，理想 < 5%）    │
  │    · 过短 chunk 占比（<50 token 的比例，理想 < 10%）         │
  │                                                            │
  │  可视化方法：                                               │
  │    · 绘制 chunk 长度分布的直方图                             │
  │    · 好的分布：集中在目标值附近、无长尾                       │
  └─────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────┐
  │  3. 检索效果评估                                             │
  │                                                            │
  │  准备：10-20 条标准查询，人工标注每条查询"应该返回的段落"     │
  │                                                            │
  │  指标：                                                     │
  │    · Recall@K  = 前K个结果中命中的人工标注 / 人工标注总数     │
  │    · MRR (Mean Reciprocal Rank) = 第一个正确答案的排名倒数    │
  │    · NDCG@K (Normalized Discounted Cumulative Gain)         │
  │                                                            │
  │  对比方法：                                                 │
  │    · 用不同的 chunk_size (200/400/600/800) 各跑一轮         │
  │    · 比较各轮次的 Recall@5 和 MRR                            │
  │    · 选 Recall@5 最高的 chunk_size 作为生产配置              │
  └─────────────────────────────────────────────────────────────┘

  ┌─────────────────────────────────────────────────────────────┐
  │  4. LLM 生成质量评估                                          │
  │                                                            │
  │  指标：                                                     │
  │    · Faithfulness (忠实度) = 回答中有多少内容可以直接引用到    │
  │      检索到的 chunk 中。理想值：> 0.90                       │
  │    · Answer Relevance (回答相关性) = 回答是否直接解决了用户问题│
  │    · Context Relevance (上下文相关性) = 检索到的 chunk 中      │
  │      有多大比例是真正需要的。理想值：> 0.80                   │
  │                                                            │
  │  实践方法：                                                 │
  │    · 用 LlamaIndex 的 FaithfulnessEvaluator 自动评估         │
  │    · 用 RAGAS 框架做系统级评估                               │
  └─────────────────────────────────────────────────────────────┘
```

### 18.7 总结：切分策略选择速查

```
═══════════════════════════════════════════════════════════════════════
                    切分策略选择速查表
═══════════════════════════════════════════════════════════════════════

  你的场景                      →  推荐切分策略

  快速原型、不确定参数            → chunk_size=512, overlap=64, 递归切分
                                  (最通用的起点，跑起来再调)

  追求高 Recall (少漏检)         → 减小 chunk_size (200-300) + 增大 top_k
                                  + 双层切分（Small检索→Big做上下文）

  追求高 Precision (少噪声)      → 增大 chunk_size (800-1000) + Rerank精排
                                  + 语义切分（在话题边界处切）

  长篇叙述文档 (论文/报告)        → 按章节/标题层级切分 + 中等 chunk_size (512)
                                  + 大的 overlap (128) 保持叙述连贯性

  短 FAQ / 问答对                → 一对一保留 (1个QA对 = 1个chunk)
                                  不要切分，FAQ 的自然边界就是最佳边界

  代码仓库                        → 按函数/类边界切分
                                  不要按字符数硬切（会切断函数体）

  混合文档库 (各种格式都有)        → 按文档类型分类 → 各自选最佳策略
                                  (Markdown→标题切, PDF→递归切, 代码→函数切)
═══════════════════════════════════════════════════════════════════════
```


---

