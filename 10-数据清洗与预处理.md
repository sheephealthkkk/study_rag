## 十七、数据清洗与预处理 —— RAG 的第一道质量关口

完整代码见 `data_cleaning_demo.py`。

### 17.1 为什么数据清洗对 RAG 至关重要

数据清洗发生在"文档解析"之后、"切分与向量化"之前。它的目标是：

```
原始解析结果（含噪声）  →  清洗  →  干净文本 → 切分器 → Embedding → 向量库
                         ↑
                    这一章聚焦的环节
```

**三大目标：**

| 目标 | 说明 | 不清洗的后果 |
|------|------|-------------|
| **去除噪音和无关数据** | HTML标签、特殊符号、控制字符、页眉页脚、注释 | 噪声词污染向量空间，检索精度下降 |
| **标准化数据格式** | 合并多余空格/换行/制表符，统一全角半角 | Tokenizer 把多余空白切成独立 token，破坏语义 |
| **提高切分边界准确性** | 干净的文本让 token 计数准确，chunk 边界对齐语义 | chunk 在不该断开的地方断开，信息碎片化 |

用一个直观的例子说明清洗前后的差异：

```
清洗前（从网页抓取的 FAQ）：
  "<div class='faq'><b>Q:</b> 请问  事假 需要   提前多久  申请？</div>"
   → Tokenizer 会把 <div, class=, 'faq', >, <b>, 等都当成独立 token
   → 向量化时，HTML 标签的权重和正文一样高

清洗后：
  "Q: 请问 事假 需要 提前多久 申请？"
   → 每个 token 都是有意义的内容词
   → 向量化时，语义集中在"事假 提前 申请"这些实词上
```

---

### 17.2 文本清洗 —— pandas 批量处理

#### 17.2.1 为什么用 pandas

企业 RAG 中，数据往往以 CSV/Excel 形式存在（FAQ 列表、产品库、知识条目）。pandas 的 `.apply()` 是向量化操作，比 `for` 循环遍历每行更高效，且清洗结果可以直接导出为干净的 CSV 或喂给后续管线。

#### 17.2.2 完整的 11 步清洗流水线

```python
def clean_text(text: str) -> str:
    # Step 1: 移除 HTML/XML 标签
    #   <div>, <b>, </b> 等 → 对 RAG 无意义
    text = re.sub(r'<[^>]*>', '', text)

    # Step 2: 移除控制字符
    #   \x00-\x1f（除了 \n \t）是二进制/OCR 产生的不可见字符
    text = re.sub(r'[\x00-\x08\x0b\x0c\x0e-\x1f]', '', text)

    # Step 3: 移除零宽字符
    #   \u200b（零宽空格）肉眼不可见但影响 tokenizer
    text = re.sub(r'[\u200b\u200c\u200d\u200e\u200f\ufeff]', '', text)

    # Step 4: 将不间断空格（\u00a0）和全角空格（\u3000）替换为普通空格
    text = text.replace('\u00a0', ' ').replace('\u3000', ' ')

    # Step 5: 移除 URL（可选，取决于场景是否需要保留链接信息）
    text = re.sub(r'https?://\S+|www\.\S+', '', text)

    # Step 6: 合并连续换行符 → 1 个 \n
    text = re.sub(r'[\r\n]+', '\n', text)

    # Step 7: 合并连续空格/制表符 → 1 个空格
    text = re.sub(r'[ \t]+', ' ', text)

    # Step 8: 去掉 \n 前后的空格
    text = re.sub(r' *\n *', '\n', text)

    # Step 9: 全角数字和字母 → 半角（中文文档常见问题）
    text = text.translate(str.maketrans(
        '０１２３...', '0123...'))

    # Step 10: 合并连续重复标点
    text = re.sub(r'，{2,}', '，', text)
    text = re.sub(r'。{2,}', '。', text)

    # Step 11: 去除首尾空白，去除纯空白行
    lines = [l.strip() for l in text.split('\n') if l.strip()]
    return '\n'.join(lines)
```

#### 17.2.3 清洗效果对比

| 原始文本 | 清洗后 |
|----------|--------|
| `<div class='faq'><b>Q:</b> 请问  事假 需要   提前多久  申请？</div>` | `Q: 请问 事假 需要 提前多久 申请？` |
| `A: 事假需提前\u00a0\u00a01个工作日\u00a0申请。\n\n\n（紧急情况可事后补办）\r\n\t\t` | `A: 事假需提前 1个工作日 申请。\n（紧急情况可事后补办）` |
| `年假天数：入职１－５年享有５天年假，，，入职５－１０年享有１０天年假。。。` | `年假天数：入职1-5年享有5天年假，入职5-10年享有10天年假。` |

**核心收益：** Tokenizer 现在只处理有意义的内容词，向量空间的"语义信噪比"显著提升。

---

### 17.3 图像文档预处理 —— cv2 实战

图像预处理是 OCR 之前的必要步骤。扫描件、拍照文档直接送 OCR 的准确率通常只有 60-80%，经过预处理可提升到 90%+。

#### 17.3.1 二值化（Binarization）

**是什么：** 将彩色/灰度图像（0-255 灰阶）转换为纯黑白（只有 0 和 255）。

**为什么需要：** OCR 引擎（Tesseract/PaddleOCR）的工作方式是检测"暗色像素组成的笔画形状"。灰度过渡区域的像素介于黑白之间，OCR 难以判断它们"属于文字"还是"属于背景"，造成笔画粘连或断裂。

**两种方法对比：**

| 方法 | 原理 | 适用场景 |
|------|------|----------|
| **固定阈值** `cv2.threshold(img, 127, 255, THRESH_BINARY)` | 所有像素 > 127 → 255(白)，≤127 → 0(黑) | 光照均匀的扫描件 |
| **自适应阈值** `cv2.adaptiveThreshold(img, 255, ADAPTIVE_THRESH_GAUSSIAN_C, THRESH_BINARY, 11, 2)` | 每个像素的阈值 = 周围 11×11 区域高斯加权均值 - 2 | 光照不均匀的拍照文档 |

```python
# 固定阈值
_, binary_fixed = cv2.threshold(img, 127, 255, cv2.THRESH_BINARY)

# 自适应阈值（推荐用于拍照文档）
binary_adaptive = cv2.adaptiveThreshold(
    img, 255, cv2.ADAPTIVE_THRESH_GAUSSIAN_C,
    cv2.THRESH_BINARY, 11, 2)
#                       ↑         ↑
#                  邻域大小(奇数)  减去常数
#     值越大 → 阈值区域越大 → 更"粗放"的二值化
#     值越小 → 阈值区域越小 → 更"精细"但可能引入噪声
```

#### 17.3.2 降噪（Denoising）

**是什么：** 去除扫描件上的噪点（灰尘颗粒、传感器噪点、复印斑点）。

**为什么需要：** 一个噪点就是一个孤立的黑像素。OCR 会把它们识别为句号（`.`）、逗号（`,`）或无意义的文字碎片——导致"事假.需提前"这样的断词错误。

**两种方法对比：**

| 方法 | 原理 | 优缺点 |
|------|------|--------|
| **中值滤波** `cv2.medianBlur(img, 3)` | 每个像素 = 周围 3×3 区域的中位数 | 保留边缘（文字笔画不模糊），适合去椒盐噪点 |
| **高斯滤波** `cv2.GaussianBlur(img, (3,3), 0)` | 每个像素 = 周围 3×3 区域的加权平均 | 噪点去除更彻底，但文字边缘会被轻微模糊 |

```python
# 中值滤波（推荐首选，保留文字边缘）
denoised = cv2.medianBlur(binary_img, 3)

# 高斯滤波 + 重新二值化（噪声严重时使用）
blurred = cv2.GaussianBlur(binary_img, (3, 3), 0)
_, denoised = cv2.threshold(blurred, 127, 255, cv2.THRESH_BINARY)
```

#### 17.3.3 倾斜矫正（Deskewing）

**是什么：** 检测文档在扫描/拍照时的倾斜角度，旋转回水平。

**为什么需要：** Tesseract 对倾斜 > 5° 的文字识别率急剧下降。倾斜的文字在 OCR 的"行切分"步骤中，上一行的下半部分和下一行的上半部分会混在一起，造成行间串字。

**实现原理（四步）：**

```
Step 1: Canny 边缘检测 → 找出所有笔画的边缘
Step 2: 霍夫变换 (HoughLines) → 从边缘中检测出直线
Step 3: 统计所有直线的角度分布 → 取中位数角度
Step 4: 仿射变换 (warpAffine) → 旋转 -angle 回到水平
```

```python
def deskew_image(image, max_angle=15.0):
    # Step 1: Canny 边缘检测
    edges = cv2.Canny(image, 50, 150)

    # Step 2: 霍夫变换检测直线
    lines = cv2.HoughLines(edges, 1, np.pi/180, 200)

    # Step 3: 收集角度
    angles = []
    for line in lines:
        angle = np.rad2deg(line[0][1])
        # 矫正到 [-45, 45] 范围
        if 45 < angle < 135: angle -= 90
        if abs(angle) < max_angle:
            angles.append(angle)

    median_angle = np.median(angles)

    # Step 4: 旋转矫正
    h, w = image.shape
    M = cv2.getRotationMatrix2D((w//2, h//2), median_angle, 1.0)
    return cv2.warpAffine(image, M, (w, h),
                          flags=cv2.INTER_CUBIC,    # 三次插值
                          borderValue=(255, 255, 255))
```

**预处理流水线的执行顺序很重要：**

```
正确顺序：灰度化 → 二值化 → 降噪 → 倾斜矫正 → 送入 OCR
错误顺序：先倾斜矫正再做二值化 → 旋转产生的插值引入了新灰度 → 二值化不准
```

---

### 17.4 代码块清洗

**为什么需要专门清洗代码：**

开发者文档、技术博客中的代码块含有大量注释、文档字符串、不一致缩进。这些噪声在向量检索时稀释语义："# TODO: fix this" 的语义权重和函数签名 `def calculate_overtime_pay` 一样高——但用户搜"加班费计算"时，前者毫无价值。

**清洗策略（八步）：**

| 步骤 | 目标 | 正则/方法 |
|------|------|-----------|
| 1 | 去除单行注释 | `re.sub(r'^\s*#.*$', '', code, MULTILINE)` |
| 2 | 去除文档字符串 | `re.sub(r'""".*?"""', '', code, DOTALL)` |
| 3 | 去除行尾注释 | `re.sub(r'  # .*$', '', code, MULTILINE)` |
| 4 | Tab → 4空格 | `code.replace('\t', '    ')` |
| 5 | 去除多余空行 | `re.sub(r'\n\s*\n', '\n', code)` |
| 6 | 去除行尾空格 | `re.sub(r'[ \t]+$', '', code, MULTILINE)` |
| 7 | 去除调试语句 | `re.sub(r'^\s*print\(.*\).*$', '', code, MULTILINE)` |
| 8 | 去除首尾空白 | `code.strip()` |

**清洗效果：**

```
清洗前（32行，576字符）：
  # 这是一个计算加班费的函数
  # TODO: 后续需要支持调休计算
  def calculate_overtime(hours, rate, is_holiday=False):
      """计算加班费 Args: ..."""
      # 基础倍数
      multiplier = 1.5
      if is_holiday:    # 节假日加班按 3 倍
          multiplier = 3.0
      print("计算完成")  # 调试用的，后面删掉
      return total

清洗后（7行，195字符）：
  def calculate_overtime(hours, rate, is_holiday=False):
      multiplier = 1.5
      if is_holiday:        # 节假日加班按 3 倍
          multiplier = 3.0
      total = hours * rate * multiplier
      return total
```

**关键原则：保留函数签名和核心逻辑，去除注释、文档字符串、调试语句。** 这些才是检索时真正需要的语义信息。`def calculate_overtime` 在向量空间中比 `# TODO: fix this` 的语义匹配度高得多。

---

### 17.5 混合文档清洗管线

#### 17.5.1 架构：分发器模式（又一次出现）

```python
# 文档类型 → 清洗器的映射表
CLEANER_REGISTRY = {
    "text":    clean_text_doc,      # 纯文本 → 11步正则清洗
    "html":    clean_html_doc,      # HTML → 去标签+脚本+样式 → 文本清洗
    "code":    clean_code_doc,      # 代码 → 去注释+文档字符串+统一缩进
    "tabular": clean_tabular_doc,   # 表格 → 保留行列结构+基本清洗
    "office":  clean_text_doc,      # Office → Unstructured解析后 → 文本清洗
    "pdf":     clean_text_doc,      # PDF → Unstructured解析后 → 文本清洗
    "image":   clean_unknown_doc,   # 图像 → 需OCR前置（见17.3）
}

def clean_document(content: str, doc_type: str) -> str:
    cleaner = CLEANER_REGISTRY.get(doc_type, clean_unknown_doc)
    return cleaner(content)
```

#### 17.5.2 完整流水线

```
  📄 原始文件 → [detect_doc_type] → 识别类型
                                        │
                   ┌─────────────────────┼──────────────────┐
                   ▼                     ▼                   ▼
               "text"                "html"               "code"
           clean_text_doc()     clean_html_doc()     clean_code_doc()
         (11步正则清洗)      (去标签+脚本+样式)     (去注释+格式化)
                   │                     │                   │
                   └─────────────────────┼──────────────────┘
                                         ▼
                                  干净的文本 → 切分器 → Embedding → 索引
```

---

### 17.6 清洗时机与位置

```
                   RAG 管线中清洗的最佳位置

  Step 1: 原始文档
     │
     ▼
  Step 2: 文档解析 (Unstructured / PyMuPDF / ...)
     │     注意：有些清洗可以与解析同时进行
     │     Unstructured 的 partition() 已经内置了页眉页脚去除
     │
     ▼
  Step 3: ★ 清洗与预处理 ★ ← 本章的内容，最佳位置
     │     · 文本清洗（去HTML标签、特殊符号、空白符统一）
     │     · 图像预处理（二值化、降噪、倾斜矫正 → 提高OCR）
     │     · 代码清洗（去注释、统一缩进）
     │
     ▼
  Step 4: 切分 (Splitter / Chunker)
     │     干净的文本 → Token 计数准确 → chunk 边界准确
     │
     ▼
  Step 5: Embedding → Vector Store → 检索 → LLM
            干净向量 → 语义信号强 → 检索精度高 → 回答质量好
```

**一个重要的工程经验：** 不要在解析前清洗（原始二进制文件不可直接"清洗"），也不要太晚（向量化后数据已固化为向量，无法清洗）。**解析后、切分前是清洗的最佳时机。**


---

## 十八、大厂面试题深度讲解

### 18.1 清洗基础与原理类

#### Q1：为什么数据清洗被称为 RAG 的"第一道质量关口"？清洗不到位会引发哪些连锁问题？（字节跳动 / 腾讯 高频）

**面试官考察点：** 是否理解"垃圾进、垃圾出"在整个 RAG 链路上的传导机制。很多候选人只关注检索和生成优化，忽视数据质量是基础。

**回答思路（用一个具体例子串联清洗不足的连锁反应）：**

**核心观点：清洗是 RAG 链路的第一环，错误在此产生后不会被后续环节修复，只会被放大。**

**连锁反应链——以一个 HTML 网页的清洗不足为例：**

```
原始文本（未充分清洗）：
  "<div class='faq'><b>Q:</b> 请问  事假 需要   提前多久  申请？</div>"

↓ 阶段一：Tokenizer 污染
  Tokenizer 把 <div, class=, 'faq', >, <b>, Q:, </b> 都当成独立 token
  → "提前多久申请" 这段核心语义被 HTML 标签 token 稀释
  → Token 序列中，50% 是噪声 token

↓ 阶段二：Embedding 向量漂移
  Embedding 模型对所有 token 做加权池化
  → HTML 标签 token 也参与了语义向量的计算
  → "事假申请"的向量被 <div> 和 class= 的向量"拉偏"
  → 最终生成的向量在语义空间中偏离了"请假流程"的正确位置

↓ 阶段三：检索失准
  用户问"怎么申请年假？"（语义正确）
  → 检索到的向量包含了 HTML 噪声，与 query 的向量距离变大
  → 本该排名 Top-1 的片段掉到 Top-15（漏召）

↓ 阶段四：LLM 生成幻觉
  检索没命中正确的片段 → LLM 没有正确的参考资料
  → LLM 基于自己的"记忆"编造了一个答案（幻觉）

根因追溯：不是检索策略的问题，不是 Embedding 模型的问题，不是 LLM 的问题
          → 是清洗阶段没有去掉 HTML 标签
```

**三个维度的量化影响：**

| 维度 | 清洗不足的表现 | 定量影响 |
|------|---------------|----------|
| **Token 效率** | 噪声 token 占用上下文窗口 | 有效信息占比可能从 90% 降到 50%，相当于浪费了一半的上下文预算 |
| **检索精度** | 语义向量被噪声"稀释" | Recall@5 可能下降 15-30%（取决于噪声比例） |
| **切分质量** | 多余空白导致 chunk 边界不在语义边界 | 句子被截断的概率上升，chunk 语义完整性下降 |

**面试官追问（高压）："清洗真的那么重要吗？Embedding 模型不是有鲁棒性吗？"**

"Embedding 模型确实有一定鲁棒性——它不会被几个 HTML 标签完全搞垮。但问题是真实的 RAG 场景中，这不是'几个标签'的问题——是从网页抓取的上万条 FAQ 中每条都带着 `<div>` `<span>` `<script>` 内联样式，从 OCR 结果中每条都有控制字符和零宽空格，从 Chat 日志中每条都有 `@mention` 和 emoji 短码。噪声是**系统性、大规模的**，不是偶发的。当噪声占比超过 30%，任何 Embedding 模型的鲁棒性都会被击穿。

更重要的是，这是一个**投入产出比极高**的优化——写 10 行正则清洗代码的成本，比调检索参数、换 Embedding 模型、做 Rerank 都低得多。清洗是 ROI 最高的 RAG 优化，没有之一。"

---

#### Q2：数据清洗在 RAG 管线中的最佳位置是哪里？为什么不能太早也不能太晚？（阿里巴巴 / 美团）

**面试官考察点：** 对 RAG 管线各阶段职责的理解，考察"为什么在这个位置做这件事"的系统思维。

**回答思路（从管线的数据形态变化切入）：**

**RAG 管线中数据形态的演变：**

```
阶段                    数据形态              能否清洗？
─────────────────────────────────────────────────────────
① 原始文件             二进制字节流           ✗ 不可（二进制无法做文本清洗）
② 文档解析后           结构化文本             ✓ 最佳位置
③ 切分后               Chunk 列表             △ 可以但效率低
④ Embedding 后         浮点数向量             ✗ 不可（已固化为向量）
⑤ 检索结果             相关 Node 列表         ✗ 太晚了（脏数据已入库）
⑥ LLM 生成后           自然语言文本           ✗ 太晚了（影响已造成）
```

**为什么解析后、切分前是最佳位置——三个理由：**

**理由 1——数据形态最适合清洗：** 解析后的数据是纯文本（不再有二进制/格式包袱），但尚未被切分为碎片。在整段文本上做清洗（如合并空白行、去除重复内容）比在切分后的碎片上做更有效——你可以在整段中检测某行是否"每页都出现"（页眉页脚），但在 chunk 碎片中做不了这个统计。

**理由 2——为切分提供干净的输入：** 切分器依赖 token 计数来决定在哪里"下刀"。如果文本中有大量多余空格和特殊字符，切分器会把它们算作有效 token——最终 chunk 的实际有效内容比预期少，因为一部分"预算"被噪声 token 占用了。清洗后再切分 = 切分器的 chunk_size 设定和实际语义单元对齐。

**理由 3——不破坏结构信息：** 切分后再清洗有个风险——chunk 之间是通过 `PREVIOUS/NEXT` 关系连接的，如果在切分后做清洗导致了文本内容变化（如去除了某行），chunk 之间的语义连续性可能被破坏。

**面试官追问："那能不能在解析的同时做清洗？比如 Unstructured 自带页眉页脚去除？"**

"部分可以，但不应该完全依赖。Unstructured 的页眉页脚去除是基于统计重复检测，效果不错但不够灵活——它去除的是它认为的噪声，但你可能想保留某些它判定为噪声的内容（比如某些文档的页眉就是章节标题）。最佳实践是：**解析工具做第一轮粗清洗（去页眉页脚、去明显噪声），你写的清洗管线做第二轮精细清洗（业务规则、格式标准化）。两层过滤，各司其职。**"

---

### 18.2 文本清洗技术细节类

#### Q3：中文文档清洗有哪些特殊问题？零宽字符和全角半角转换到底有多重要？（百度 / 腾讯 中文 NLP 面试高频）

**面试官考察点：** 中文 RAG 的差异性挑战。国内面试必考的中文特殊性。

**回答思路（先讲三个中文特有的坑，再深入每个）：**

**坑 1——零宽字符（Zerowidth Characters）："看不见的杀手"**

```
问题：零宽字符在视觉上完全不可见，但对 NLP 管线的破坏是毁灭性的。

常见零宽字符及来源：
  U+200B 零宽空格    ← 从网页、微信复制时自动插入
  U+200C 零宽非连接符  ← 从 PDF 提取时的编码转换残留
  U+200D 零宽连接符    ← Emoji 组合（如 👨‍👩‍👧）的底层编码
  U+FEFF BOM 标记     ← UTF-8 文件头标记（有时留在正文中）

实际影响：
  文本："请\u200b事\u200b假需\u200b要提\u200b前申\u200b请"
  显示："请事假需要提前申请"（看起来正常！）
  Tokenizer 实际看到的："请" "\u200b" "事" "\u200b" "假" "\u200b" ...
  → 一个 8 字的句子被切成 15 个 token
  → "提前申请" 的语义向量完全被零宽字符的向量稀释
  → 用户搜 "提前申请"，检索相似度显著低于预期
```

**坑 2——全角半角混排：**

```
问题：中文文档中全角（全宽）和半角（半宽）字符经常混用。

  "入职１－５年享有５天年假，入职５－１０年享有１０天年假"
   ↑    ↑ ↑   ↑         ↑      ↑   ↑  ↑           ↑
  半角  全角              半角   半角  全角           全角

  全角数字"５"（U+FF15）和半角数字"5"（U+0035）是不同的 Unicode 码点
  → Tokenizer 会把它们当成不同的 token！
  → Embedding 向量也不在同一位置
  → 用户搜"5天年假"（半角）搜不到"５天年假"（全角）的内容

  这不是理论问题——很多企业文档是从 Word/WPS 导出的，
  中文输入法默认产生全角标点，但数字经常半角/全角混用。
```

**坑 3——中文标点符号的特殊性：**

```
中文标点问题：
  - 中文引号 " " vs 英文引号 " "
  - 中文冒号 ： vs 英文冒号 :
  - 中文括号 （） vs 英文括号 ()
  - 连续重复标点：用户可能输入"年假，，，怎么申请？？？"
  
  这些在英文 NLP 中不存在或很少见，但中文文档中是常态。

解决方案：
  step 1: 统一全角数字/字母 → 半角（0xFF10-0xFF19 → 0x30-0x39）
  step 2: 统一全角标点 → 半角（可选，取决于你的 tokenizer 如何处理中文标点）
  step 3: 合并连续重复标点（，，，→ ，）
  step 4: 移除零宽字符（一行正则解决）
```

**面试官追问："全角转半角会不会影响中文标点？中文句号。转成英文句号.合适吗？"**

"这是个好问题，说明你实际做过中文清洗。答案是——**只转数字和字母，不转标点**。具体来说：
- 全角数字 ０-９ → 半角 0-9 ✓
- 全角字母 Ａ-Ｚ ａ-ｚ → 半角 A-Z a-z ✓
- 全角标点 ，。！？——保留不转 ✗

因为中文句号 `。` 转成 `.` 后，在中文文本中反而奇怪，且中文标点携带了句子边界信息（句号 = 句子结束），对切分器有辅助作用。精细的做法是维护一个映射表，只转数字和字母范围内的字符。"

---

#### Q4：11 步文本清洗流水线中，每一步的设计理由是什么？如果只能保留 3 步，你会保留哪 3 步？（字节跳动 / 拼多多）

**面试官考察点：** 这道题考察对清洗优先级的判断——不是所有清洗步骤都同等重要，要能区分"必须做"和"锦上添花"。

**回答思路（先说每步的理由，再做优先级排序）：**

**11 步清洗的理由分类：**

| 分类 | 步骤 | 理由 |
|------|------|------|
| **结构噪声** | ① 去 HTML 标签 | `<div>` `<b>` 等对 RAG 无语义，占 token |
| **不可见污染** | ② 去控制字符 | 二进制/OCR残留的不可见字符干扰 tokenizer |
| **不可见污染** | ③ 去零宽字符 | 肉眼不可见但 tokenizer 会单独切分 |
| **空白标准化** | ④ 统一空格 | `\u00a0`(不间断空格)和`\u3000`(全角空格)不是标准空格 |
| **内容噪声** | ⑤ 去 URL | URL 字符串对文档问答语义几乎无贡献 |
| **空白标准化** | ⑥ 合并换行 | 连续 5 个 `\n` → 1 个，避免切分器误判段落边界 |
| **空白标准化** | ⑦ 合并空格 | 连续空格占用 token 预算但无信息增益 |
| **空白标准化** | ⑧ 换行前后去空格 | ` \n ` → `\n`，避免换行符被空格"包裹" |
| **中文特性** | ⑨ 全角转半角 | 全角数字/字母与半角是不同的 Unicode 码点 |
| **中文特性** | ⑩ 合并重复标点 | ，，，→ ，（中文常见但英文不存在） |
| **收尾** | ⑪ 去首尾空白+空行 | 确保输出是紧凑的文本块 |

**如果只能保留 3 步——优先级排序：**

```
Top 1：① 去 HTML 标签 + ②③ 不可见字符（合并为"去所有非语义内容"）
  理由：这是最大类的噪声来源。HTML 标签和控制字符直接毒化 token 序列。
  影响面：网页抓取、PDF 提取、OCR 输出都有。

Top 2：⑦ 合并空格 + ⑥ 合并换行（合并为"空白标准化"）
  理由：多余空白直接浪费 token 预算。一个 512 token 的 chunk，
  如果 20% 是多余空白，有效内容只有 ~400 token。
  影响面：几乎所有非规范化的文本来源。

Top 3：⑨ 全角转半角（中文场景）或 ⑩ 合并重复标点
  理由：这是中文特有的问题，英文场景不存在。
  如果纯英文场景，Top 3 替换为 ⑤ 去 URL。
```

**关键论点（加分项）：** "这个优先级排序不是固定的——它取决于你的数据源。数据源是网页爬虫，Top 1 是去 HTML。数据源是 PDF 提取，Top 1 是去页眉页脚。数据源是 OCR 输出，Top 1 是去控制字符和 OCR 碎片。**清洗优先级由你的数据噪声特征决定，不由通用规则决定。** 所以我在接手新项目时，第一件事不是写清洗规则，而是随机抽 100 条数据，肉眼分析噪声类型和分布。"

---

### 18.3 图像预处理类

#### Q5：为什么 OCR 之前必须做图像预处理？预处理流水线的正确顺序是什么？（百度 OCR 团队 / 阿里 高频）

**面试官考察点：** 图像预处理步骤之间的依赖关系——顺序错了一切白做。考察工程细节。

**回答思路（先讲为什么必须预处理，再讲顺序为什么重要）：**

**为什么必须预处理——OCR 引擎的"脆弱性"：**

Tesseract 和 PaddleOCR 的工作机制是"检测暗色像素组成的笔画形状"。任何干扰这个判断的因素——光照不均、噪点、倾斜——都会导致：
- 光照不均 → 同一页上部分文字太淡，OCR 检测不到
- 噪点 → 被误识为句号、逗号或无意义碎片
- 倾斜 > 5° → 行切分时上行的下半部和下行的上半部混在一起

量化数据：未预处理的拍照文档送入 Tesseract，准确率通常 60-80%。经过灰度化→二值化→降噪→倾斜矫正后，可提升到 90%+。

**预处理流水线的正确顺序及每一步的依赖关系：**

```
步骤 1：灰度化（GrayScale）
  RGB 三通道 → 单通道灰度
  理由：后续步骤都基于灰度图操作，减少计算量。
  依赖：无

步骤 2：二值化（Binarization）
  灰度图 (0-255) → 纯黑白 (0 或 255)
  理由：消除灰度过渡像素，让文字和背景有明确的边界。
  依赖：需要灰度图作为输入
  关键选择：
    固定阈值 → 光照均匀的扫描件
    自适应阈值 → 光照不均匀的拍照文档

步骤 3：降噪（Denoising）
  去除孤立的黑白像素噪点
  理由：二值化会放大噪点（灰度过渡区的像素被"强制"归入黑或白）。
        必须先二值化再降噪，否则噪点在灰度中不明显，无法有效去除。
  依赖：需要二值化后的图像
  关键选择：
    中值滤波 (medianBlur) → 保留文字边缘，首选
    高斯滤波 (GaussianBlur) → 噪点严重时

步骤 4：倾斜矫正（Deskewing）
  检测文字行的倾斜角度 → 逆向旋转
  理由：倾斜的文字在 OCR 的行切分阶段造成严重的行间串字。
  依赖：最好是降噪后的干净图像（噪点会产生"假边缘"干扰角度检测）
```

**为什么顺序不能颠倒——两个反例：**

```
❌ 错误顺序：先倾斜矫正 → 再二值化
  问题：旋转操作会产生插值像素（非整像素坐标映射到整像素需要插值）。
        插值引入了新的灰度值（介于 0-255 之间）。
        再做二值化时，这些"新灰度"会造成额外的笔画变形。
  
  正确做法：先二值化（像素只有 0 和 255），再旋转。
  旋转的插值也只产生 0 和 255，不会引入歧义灰度。

❌ 错误顺序：先降噪 → 再二值化
  问题：灰度图中的噪点对比度低，降噪算法难以区分"淡灰色噪点"
        和"淡灰色文字笔画"。要么降不干净，要么把文字也抹掉了。
  
  正确做法：先二值化→噪点和文字的对比度被拉到最大→降噪算法清楚判断。
```

**面试官追问："自适应阈值的参数 11 和 2 怎么调？"（考察调参经验）**

"两个参数：
- 邻域大小（blockSize，奇数）：控制'每个像素参考多大邻域来决定自己的阈值'。值越大 → 阈值变化越平缓 → 倾向把大片区域判为同色；值越小 → 阈值变化越敏感 → 文字边缘更清晰但也更容易把背景噪声识别为文字。11 是常用的起点值。
- 减去常数（C）：邻域加权均值减去 C 作为该像素的阈值。C 越大 → 阈值越低 → 更多像素被判为白色（背景）→ 文字可能变细或断裂。C 越小 → 阈值越高 → 更多像素被判为黑色（文字）→ 文字更粗但背景噪声也可能被误判为文字。2 是一个保守的默认值。

实际调法：找一张代表性的文档，从 (11, 2) 开始，肉眼检查文字笔画是否完整、背景是否有噪点，微调直到平衡。"

---

#### Q6：倾斜矫正（Deskewing）的算法原理是什么？为什么要用中位数角度而不是平均角度？（腾讯 / 华为 算法细节题）

**面试官考察点：** 算法实现细节——不只是会用 `cv2.HoughLines`，而是理解每一步的设计理由。

**回答思路（四步讲清算法 + 中位数 vs 平均数的关键选择）：**

**倾斜矫正的四步算法：**

```
Step 1：Canny 边缘检测
  目标：找出图像中所有"像素值剧烈变化"的边界
  原理：计算每个像素的梯度（亮度变化率），保留梯度最大的像素
  参数：低阈值 50，高阈值 150
  为什么要做：霍夫变换需要的是"边缘像素"的坐标，而非整张图的每个像素。
              边缘检测减少了 99% 的计算量。

Step 2：霍夫变换检测直线
  目标：从边缘像素中找出直线
  原理：每个边缘像素 (x, y) → 在霍夫空间中映射为一条曲线。
        多条曲线在霍夫空间中交于同一点 → 这些边缘像素共线（组成一条直线）。
  参数：ρ 精度 = 1 像素，θ 精度 = π/180（1度），阈值 = 200 票
  输出：每条检测到的直线 → (ρ, θ) → 直线的法线角度就是 θ

Step 3：收集角度并引用中位数
  从所有检测到的直线的 θ 中提取角度
  角度归一化到 [-45°, 45°]（排除垂直线，垂直线对应的是文本行而非倾斜方向）
  过滤异常值（|angle| > max_angle=15° 的丢弃）
  → 取中位数

Step 4：仿射变换旋转
  用 OpenCV 的 getRotationMatrix2D + warpAffine 做图像旋转
  旋转中心 = 图像中心
  旋转角度 = -median_angle（负号 = 反向旋转，矫正回水平）
  插值方法 = INTER_CUBIC（三次插值，保持文字笔画质量）
```

**为什么要用中位数而不是平均角度——这是这道题的核心考点：**

```
假设我们检测到 100 条直线的角度分布：

  真实文字行角度：-3.2°（绝大多数直线）
  噪声直线角度：+25°, -42°, +18°, -37°, +29°（少数误检线）

平均值 = (-3.2×95 + 25 - 42 + 18 - 37 + 29) / 100
       = (-304 - 7) / 100
       ≈ -3.11°
  → 被噪声拉偏了约 0.1°，虽然不算严重

但如果噪声更强：
  误检线：+89°, -87°, +75°（几条"垂直"误检线）
  平均值 = (-3.2×95 + 89 - 87 + 75) / 100
         = (-304 + 77) / 100
         ≈ -2.27°
  → 被拉偏了近 1°！矫正会不准确。

中位数：
  排序后取第 50 个值 = -3.2°
  → 完全不受极端值影响 ← 中位数对离群值的鲁棒性
```

**结论：** 对于角度估计这种存在大量离群值（误检的非文字直线、装订线、图表边框线）的场景，**中位数天然比平均值鲁棒**。这是统计学中"用中位数估计集中趋势比均值更稳健"的经典应用。

**面试官追问："如果文档本身没有明显的直线特征怎么办？比如全是弯曲的手写文字？"**

"两个方案：
1. 用形态学操作（`cv2.morphologyEx`）先把弯曲的文字行"拉直"——通过腐蚀+膨胀操作把文字的笔画连成近似直线的连通区域，再送霍夫变换。
2. 用基于投影的方法替代霍夫变换——把图像在不同角度下做水平投影（统计每行黑色像素数），文字行对齐时投影的"峰"最尖锐（方差最大），搜索使方差最大的角度。这种方法对弯曲文字更鲁棒，但计算量更大（需要枚举角度做多次投影）。"

---

### 18.4 代码清洗与混合文档类

#### Q7：为什么 RAG 中代码需要专门的清洗策略？代码清洗和普通文本清洗有什么不同？（字节跳动 / 腾讯 代码 RAG 场景）

**面试官考察点：** 是否理解代码作为一种"特殊文本"在 RAG 中的不同处理方式。

**回答思路（先说代码为什么特殊，再说清洗策略的差异）：**

**代码文本的三个特殊性：**

**特殊性 1——语义密度极不均匀：**

```
普通文本：
  "事假需提前1个工作日申请。" 
  → 几乎每个词都参与语义，语义密度均匀

代码文本：
  """
  # 这是一个计算加班费的函数                          ← 注释：对检索几乎无意义
  # TODO: 后续需要支持调休计算                         ← TODO：对检索无意义
  def calculate_overtime(hours, rate, is_holiday=False):  ← 函数签名：检索的核心目标
      \"\"\"计算加班费 Args: hours: 加班时长 \"\"\"     ← 文档字符串：部分有用
      print("开始计算")                               ← 调试语句：对检索无意义
      multiplier = 1.5                                ← 核心逻辑：有用
  """
  
  语义密度的分布：90% 的检索价值集中在 10% 的代码行上（函数签名、核心逻辑）。
  注释、TODO、文档字符串、调试语句占据了大量 token，但对"用户搜这个函数"
  的匹配几乎没有帮助。
```

**特殊性 2——缩进是语义的一部分：**

```
普通文本：多余空格是噪声，合并即可。
代码文本：缩进表示代码块层级（Python 尤甚）。
  → 不能用"合并所有空格"的策略
  → 需要统一缩进风格（Tab → 4空格），而非去除缩进
```

**特殊性 3——注释分两种，处理方式不同：**

| 注释类型 | 示例 | 对 RAG 的价值 | 处理方式 |
|----------|------|:---:|------|
| 解释性注释 | `# 节假日加班按3倍工资` | 高（解释了业务逻辑） | 保留 |
| 元注释 | `# TODO: fix this` `# FIXME` | 零（纯开发标记） | 去除 |
| 版权声明 | `# Copyright 2025 xxx` | 零 | 去除 |
| 行内解释 | `multiplier = 1.5  # 基础倍数` | 中（可保留可丢弃） | 保留 |

**代码清洗 vs 普通文本清洗的差异：**

| 维度 | 普通文本 | 代码 |
|------|----------|------|
| 去注释 | 不适用 | 去除元注释和 TODO，保留解释性注释 |
| 去文档字符串 | 不适用 | Python 的 `"""..."""` 通常去除（大多是 API 说明，非业务逻辑） |
| 空白处理 | 合并连续空格/换行 | Tab→4空格，保留缩进结构，只去多余空行 |
| 去调试语句 | 不适用 | `print()`、`console.log()`、`System.out.println()` |
| Token 预算分配 | 均匀分配 | 向函数签名和核心逻辑倾斜 |
| 切分策略 | 按语义段落切 | 按函数/类边界切（保持函数完整） |

**面试官追问："去掉了注释，用户搜'节假日加班费怎么算'时，代码里的 `# 节假日加班按 3 倍工资` 这个注释不是刚好能匹配吗？去掉会不会有损召回？"**

"这个追问很精准。确实存在你说的这种情况——注释中包含了有价值的业务信息。这正是'代码清洗需要在激进和保守之间找平衡'的体现。

我的做法是分层清洗：
1. 确定性噪声先删——TODO/FIXME/HACK/print 调试语句（这些几乎不可能有价值）
2. 文档字符串和解释性注释——**做摘要而非删除**。把它提取出来做成代码块的 metadata description，参与检索但不占主文本的 token 预算
3. 版权声明和长段注释头——用规则去除（'Copyright'、'All rights reserved'、'Created by' 等模式）

这样做的好处是——有价值的注释信息仍然参与检索（作为 metadata），但不会稀释函数签名本身的向量表示。"

---

#### Q8：混合文档清洗管线的分发器模式（Dispatcher Pattern）是如何工作的？如果文档类型检测错了怎么办？（美团 / 拼多多 工程实践题）

**面试官考察点：** 分发器模式在 Unstructured 中已经出现过，这里考察的是"能否迁移这个设计模式到不同场景"。

**回答思路（先说模式，再说容错）：**

**分发器模式的核心——"类型检测 + 路由 + 专用处理器"：**

```python
# 不推荐：if-else 瀑布
def clean(content, doc_type):
    if doc_type == "text":
        return clean_text(content)
    elif doc_type == "html":
        return clean_html(content)
    elif doc_type == "code":
        return clean_code(content)
    # ... 每新增一种类型就要改这个函数（违反开闭原则）

# 推荐：注册表（Registry）模式
CLEANER_REGISTRY = {
    "text":    clean_text_doc,
    "html":    clean_html_doc,
    "code":    clean_code_doc,
    "tabular": clean_tabular_doc,
}

def clean_document(content, doc_type):
    cleaner = CLEANER_REGISTRY.get(doc_type, clean_unknown_doc)
    return cleaner(content)
```

**这种模式的三层价值：**

1. **开闭原则：** 新增文档类型 → 只需在 CLEANER_REGISTRY 中加一行映射，不改动主流程代码
2. **可测试性：** 每种清洗器独立函数，可以独立做单元测试
3. **可组合性：** 复杂文档可以用多个清洗器组合处理（如 HTML → clean_html 去标签 → clean_text 标准化空白）

**文档类型检测错了怎么办——三层防护：**

```
第一层：明确的兜底处理器
  CLEANER_REGISTRY.get(doc_type, clean_unknown_doc)
  → 未知类型走 clean_unknown_doc（保守清洗：只做空白标准化和去控制字符）

第二层：清洗器的鲁棒性
  一个好清洗器应该"对不该处理的文本不造成伤害"
  例如：
    clean_html_doc 对纯文本 → 去 HTML 标签正则匹配不到 → 文本原样通过
    clean_code_doc 对纯文本 → 去注释正则匹配不到 → 文本原样通过
  
  这意味着：类型检测错了"漏洗"只会导致清洗不彻底（残留一些噪声），
  不会导致"错洗"（把正确内容删掉了）。
  
  "漏洗"比"误洗"好一万倍——漏洗最多检索质量下降 10%，
  误洗可能把关键内容永久删除（索引中消失）。

第三层：质量评分 + 反馈修正
  解析+清洗后的文本 → 质量评分（文字覆盖率、非法字符比例、语义连贯性）
  低分文档 → 标记为"需人工处理" → 重新判定类型 → 重新清洗
  
  人工不需要处理所有文档，只需要处理自动化评分低于阈值的那一小部分。
```

**面试官追问："如果文本和代码混在一起呢？比如 Markdown 文档中嵌入了代码块？"**

"这是分发器模式的一个升级——从'文档级分发'到'段落级分发'：

```python
def clean_mixed_document(content):
    segments = []
    for segment in split_by_block_type(content):
        if segment.type == "code_block":
            segments.append(clean_code(segment.text))
        elif segment.type == "markdown":
            segments.append(clean_markdown(segment.text))
        else:
            segments.append(clean_text(segment.text))
    return '\n'.join(segments)
```

Markdown 的 ` ``` ` 标记天然提供了代码块边界。先按块类型切分，再对每个块用对应的清洗器——这是分发器模式在更细粒度上的应用。核心思想不变：**在能区分类型的最细粒度上做分支处理，但保持每个清洗器的独立性。**"

---

### 18.5 面试高频知识点速查

#### 一句话答案系列

| 问题 | 一句话答案 |
|------|-----------|
| 清洗为什么是 RAG 第一道质量关口？ | 清洗不足的错误不会被后续环节修复——tokenizer 污染 → Embedding 漂移 → 检索失准 → LLM 幻觉，层层放大 |
| 清洗的最佳时机？ | 解析后、切分前——此时数据是纯文本但尚未碎片化，且为切分器提供干净的 token 计数依据 |
| 零宽字符为什么危险？ | 肉眼不可见但 tokenizer 会单独切分，把"提前申请"切成"提\u200b前\u200b申\u200b请"，语义向量完全被稀释 |
| 全角半角转换要注意什么？ | 只转数字和字母，不转标点——中文句号。转成.反而丢失句子边界信息 |
| 11 步清洗保留 3 步？ | 去 HTML/控制字符 > 空白标准化 > 全角转半角（中文）/ 去 URL（英文） |
| 二值化为什么要用自适应阈值？ | 拍照文档光照不均匀，同一页上"暗处的白色背景"和"亮处的浅色文字"灰度值可能重叠，固定阈值一刀切必然出错 |
| 为什么先二值化再降噪？ | 二值化把灰度噪声和灰度文字拉到最大对比度，降噪算法才能精确区分 |
| 倾斜矫正为什么用中位数？ | 角度分布有大量离群值（误检的非文字直线），中位数对离群值鲁棒，平均值会被拉偏 |
| 代码清洗和文本清洗的核心区别？ | 语义密度极不均匀（90% 价值在 10% 代码行），缩进是语义而非噪声，注释要分层处理 |
| 分发器模式的好处？ | 开闭原则（新增类型不修改主流程）+ 可测试（独立测试每种清洗器）+ 兜底安全（未知类型走保守清洗） |

#### 必知清洗手段速查表

| 手段 | 目标 | 优先级 | 适用场景 |
|------|------|:---:|----------|
| **去 HTML 标签** | 移除 `<div>` `<b>` 等标记 | P0 | 网页抓取、HTML 转存 |
| **去控制字符** | 移除 `\x00-\x1f` 不可见字符 | P0 | OCR 输出、PDF 提取 |
| **去零宽字符** | 移除 `\u200b` 等零宽字符 | P0 | 微信/网页复制文本 |
| **空白标准化** | 合并多余空格/换行/制表符 | P0 | 所有数据源 |
| **全角转半角** | 数字和字母全角→半角 | P1 | 中文文档 |
| **合并重复标点** | ，，，→ ， | P1 | 中文 UGC 内容 |
| **去 URL** | 移除链接字符串 | P2 | 非链接保留场景 |
| **二值化** | 灰度→纯黑白 | P0(图像) | 扫描件/拍照文档 |
| **降噪** | 去除椒盐噪点 | P1(图像) | 旧文档扫描件 |
| **倾斜矫正** | 旋转回水平 | P1(图像) | 拍照文档/歪斜扫描件 |
| **去代码注释** | 去除 TODO/调试语句 | P1(代码) | 代码仓库 RAG |
| **统一缩进** | Tab→4空格 | P2(代码) | Python 代码库 |

---



